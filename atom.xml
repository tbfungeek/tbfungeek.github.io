<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>Edgar&#39;s Blog</title>
  <icon>https://www.gravatar.com/avatar/7fe200595858ba396fefc40e38f25683</icon>
  <subtitle>等待执剑少年修炼归来！</subtitle>
  <link href="/atom.xml" rel="self"/>
  
  <link href="http://yoursite.com/"/>
  <updated>2020-07-05T09:33:18.000Z</updated>
  <id>http://yoursite.com/</id>
  
  <author>
    <name>Edgar</name>
    <email>tbfungeek@163.com</email>
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title>Photoshop基本操作总结</title>
    <link href="http://yoursite.com/2020/07/04/Photoshop%E5%9F%BA%E6%9C%AC%E6%93%8D%E4%BD%9C%E6%80%BB%E7%BB%93/"/>
    <id>http://yoursite.com/2020/07/04/Photoshop基本操作总结/</id>
    <published>2020-07-04T09:57:00.000Z</published>
    <updated>2020-07-05T09:33:18.000Z</updated>
    
    <content type="html"><![CDATA[<h4>一. <strong><strong>图层操作</strong></strong></h4><ol><li>选择图层，隐藏显示图层，锁定，解锁，改变图层顺序，移动图层</li><li>锁定像素，新建图层，图层分组，更改图层透明度</li><li>图层筛选</li><li>图层合并，图层混合，添加图层蒙版，增加图层填充<img src="/2020/07/04/Photoshop基本操作总结/0001.png" alt></li><li>特殊图层：文字图层，形状图层，智能对象图层，调整对象图层，剪切图层,特殊图层转换为普通图层使用栅格化来转换</li><li>设置图层样式：双击图层面板的某个图层，可以弹出图层样式编辑弹窗，可以通过这种方式来添加图层阴影等样式<img src="/2020/07/04/Photoshop基本操作总结/0002.png" alt></li><li>正常情况下调整层影响的是所有它之下的层，如果要指定某个层，将某个调整层和要应用的层放在一起，返回按下alt键盘，将光标移到两个层之间，这时候会出现一个向下箭头的提示，点击就可以了，还有一种方式是将两个层组合成组，然后光标选定组，这时候默认模式为穿透，选择正常即可。</li><li>调整对象图层：亮度对比度调节图层：一般发灰的照片调整对比度，发暗的照片调整亮度。色阶调节图层：<a href="https://zhuanlan.zhihu.com/p/64420683" target="_blank" rel="noopener">调整之“色阶”</a>颜色曲线调整曝光度调节图层自然饱和度调节图层色相/饱和度调节色彩平衡调节黑白调节照片滤镜通道混合器反相色调分离阈值可选颜色渐变映射</li></ol><h4>二. <strong><strong>对象选择操作</strong></strong></h4><ol><li>矩形/椭圆选择工具，可以调整抗锯齿和羽化两个参数来修改选择效果。</li><li>使用魔棒工具来选中相似颜色的对象。这里最关键的有两个：“容差值”，容差值越小越精确但是选中的区域越小。“连续”：默认情况下只会选中连续的块，对于某些由于隔离导致而没选中的可以先取消连续后重新选择。</li><li>快速选择工具：很好用的一个选择工具能够快速得选择对象，比魔棒要精确</li><li>对象选择区域：这个速度有点慢，但是还是蛮精确的，适合于边缘比较清晰的场合。</li><li>套索工具，多边形套索，磁性套索，这些一般用于选择细节部分，按下Shift选择的区域会追加到已经选择的区域，按下Alt会从已经选择的区域减去。</li><li>使用选择菜单栏里面的颜色范围和焦点区域也可以进行选择。</li><li>边缘调整可以调整存在软边的情况：在使用某种工具初步选择完毕后可以使用选择并遮住来细节调整。</li><li>查看选中区域使用Q快捷键，要改变选区显示的方式可以双击工具栏底部倒数第二个按钮，对颜色透明度进行修改。反向选择可以使用Command + Shift + I 快捷键。Command + D取消选择</li><li>使用Command + T键可以切换到自由变换模式，按下Shift 可以锁住宽高不变。</li><li>可以对对象进行：操控变换，透视变换，缩放，旋转，斜切，扭曲，透视，变形/拆分变形，水平垂直翻转，都在编辑菜单拦里面。</li><li>使用图层蒙版进行抠图：使用任意一个选区工具对对象进行初略选择，然后点击图层面板下面的剪切蒙层，就会新建一个剪切蒙层，这时候可以按下alt点击图层面板上的蒙层就会切换显示模式，这时候使用黑色画笔将要去掉的地方绘制，使用白色画笔在要恢复的地方绘制。</li><li>通过钢笔工具绘制轮廓，然后制作几何蒙层。</li></ol><h4>三. <strong><strong>蒙版操作</strong></strong></h4><ol><li>使用图层蒙版进行抠图：使用任意一个选区工具对对象进行初略选择，然后点击图层面板下面的剪切蒙层，就会新建一个剪切蒙层，这时候可以按下alt点击图层面板上的蒙层就会切换显示模式，这时候使用黑色画笔将要去掉的地方绘制，使用白色画笔在要恢复的地方绘制。</li><li>几何蒙层：使用多边形或者其他形状绘制工具绘制出几何形状，然后在面板上新建一个剪切蒙层，和图层蒙层的区别是它的外部是灰色的不是黑色的。</li></ol><h4>四. <strong><strong>智能对象操作</strong></strong></h4><ol><li>在正常工作过程中一般会将图片等对象转换为智能对象，这样缩放的时候不会损失画质，并且所有的操作都是无损的。将对象转换为智能对象可以通过：图层 --&gt; 智能对象 --&gt; 转换为智能对象。或者选中图层，右击选择转换为智能对象。</li><li>除了图片还可以将矢量工具转换为智能对象。还可以通过文件 --&gt; 置入链接智能对象，将外部对象导入到项目中。这样的好处是只要链接的智能对象修改了，所有导入的都会随之改变。这种情况在保存的时候注意使用文件 --&gt; 打包 将这些链接也打包进来。</li></ol><h4>五. <strong><strong>绘图操作</strong></strong></h4><ol><li>几个很重要的快捷键需要记住：</li></ol><ul><li>B 调出画笔</li><li>[] 调大调小画笔</li><li>Control + Alt + 横向拖动 改变大小，纵向拖动改变硬度。</li><li>数字键盘输入改变透明度</li></ul><ol start="2"><li>画笔设置有两个重要面板：画笔面板，画笔，还有顶部的画笔设置工具栏</li><li>自定义画笔：绘制形状，选中形状，点击面板右上角的更多按钮，弹出对话框中选中新建画笔预设。编辑画笔。</li><li>PS中的矢量工具有：基本的形状绘制，直接选择和路径选择工具，钢笔工具。</li></ol><h4>六. <strong><strong>文字操作</strong></strong></h4><ol><li>文字操作主要有两个主面板：字符面板和段落面板，以及字符样式面板和段落样式面板。</li><li>路径文字</li><li>将文字作为图片蒙版：将图片拖到文字上方，按住Alt点击图片和文字的中间即可。</li><li>文字景深制作：将要放在文字上方的通过剪切蒙版扣出来，底下放一个一样的图片，将文字放置于二者中间。<img src="/2020/07/04/Photoshop基本操作总结/0003.png" alt></li></ol><h4>七. <strong><strong>图片操作</strong></strong></h4><ol><li><p>图片修复工具：</p><p>在使用修复工具的时候有几步操作是必须的：新建图层，点击运用到所有层，有画笔的还可以选择打开画笔压感。最简单的是通过污点修复工具和修复工具来修复，修复工具需要按下alt + 点击鼠标选择采样点。后续的修复都是基于这些采样点来修复的。仿章工具它的特点是直接将采样点复制到对应的区域，没有做进一步的融合处理。还可以调出仿章工具面板，点击一个仿章源，在一个页面选择仿章采样点，切换到另一个页面就可以使用仿章工具，将形状复制过去了。</p></li><li><p>智能滤镜：将对象转换为智能对象，然后在智能对象之上添加滤镜效果。</p></li><li><p>内容感知功能：内容感知包括内容感知移动（位于工具栏），内容感知缩放，内容识别填充，修补工具。内容感知移动/扩展的时候可以将原来的使用算法识别的内容填充，然后将新的内容通过算法融入到目标环境中。内容感知缩放和内容感知填充也是同样道理。修补工具算是一个比较实用的工具，可以将选定的区域，使用目标区域来替换。</p></li><li><p>细节微调工具：使用减淡，加深，和海绵工具可以对图像明度，暗度及饱和度细节调整。</p></li></ol><h4>八. <strong><strong>钢笔操作</strong></strong></h4><ol><li>在PS中钢笔工具有两种用途，一种用于绘制路径，一种用于绘制图形，绘制图形的钢笔工具有描边和填充颜色，以及线条的设置，绘制路径的则没有这些配置，一般用于选择轮廓等用途。</li><li>在顶点点击不拖动会在两个顶点之间生成一条直线。</li><li>将钢笔放在在已经绘制的路径上点击会增加锚点，如果将钢笔工具放在已经绘制的顶点上则会删除锚点。</li><li>在绘制到第一个顶点，可以闭合后将会在钢笔的旁边出现一个圆形小圆圈</li><li>在绘制完成后可以按下alt键，这时候光标会显示一个箭头，这时候沿着绘制路径的方向可以将尖角改成圆角。</li><li>要改变锚点的位置可以通过直接选择工具来移动锚点。在绘制过程中如果需要改变位置可以在未抬起鼠标之前通过按下space移动。</li><li>在绘制圆角之后要绘制一个直角可以通过alt + 点击切换到直角模式。</li><li>要移动句柄可以通过按下Command + 点击句柄 移动</li><li>绘制完成后在AI中可以使用简化工具对路径进行简化：对象 --&gt; 路径 --&gt; 简化</li><li>选中后可以作为选区或者矢量蒙版，这些都可以通过右击在菜单中选中。</li><li>沿着路径填充描边：选择路径模式，右击路径，在弹出的菜单中选择填充描边。然后选择使用哪种工具填充。</li></ol><h4>总结</h4><p>PS 中的关键词：图像，变换（对应编辑菜单），选择，滤镜，图层</p><p><img src="/2020/07/04/Photoshop基本操作总结/0004.jpg" alt></p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h4&gt;一. &lt;strong&gt;&lt;strong&gt;图层操作&lt;/strong&gt;&lt;/strong&gt;&lt;/h4&gt;
&lt;ol&gt;
&lt;li&gt;选择图层，隐藏显示图层，锁定，解锁，改变图层顺序，移动图层&lt;/li&gt;
&lt;li&gt;锁定像素，新建图层，图层分组，更改图层透明度&lt;/li&gt;
&lt;li&gt;图层筛选&lt;/li&gt;
      
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>Adobe Illustrator 技巧总结</title>
    <link href="http://yoursite.com/2020/07/02/Adobe-Illustrator-%E5%AD%A6%E4%B9%A0%E8%AE%B0%E5%BD%95/"/>
    <id>http://yoursite.com/2020/07/02/Adobe-Illustrator-学习记录/</id>
    <published>2020-07-02T15:16:56.000Z</published>
    <updated>2020-07-04T09:34:33.000Z</updated>
    
    <content type="html"><![CDATA[<h4>一 <strong><strong>画板操作</strong></strong>：</h4><p>一个文件可以保存多个画板，按下如下按钮：<img src="/2020/07/02/Adobe-Illustrator-学习记录/0001.png" alt> 或者 Shift + O 可以激活画板编辑模式：这时候选中一个画板顶部会出现一个编辑工具拦可以对画板板式，宽高，方向等进行编辑：<img src="/2020/07/02/Adobe-Illustrator-学习记录/0002.png" alt></p><p>画板的常用操作：</p><ol><li>在新建文档的时候批量新建多个画板</li><li>画板全部重新排列</li><li>画板重命名</li><li>画板尺寸修改</li></ol><h4>二 <strong><strong>导航操作</strong></strong>：</h4><ol><li>Z 切换到放大镜模式 按下Alt键会切换到缩小模式</li><li>Command + 0 会将选中的放大到居于工作区合适的位置</li><li>Space 点击鼠标拖动会拖动整个画布</li><li>点击界面的左上角可以将多个文档同时打开，在这种模式可以很方便得将一个文档的对象，拖动到另一个文档。<img src="/2020/07/02/Adobe-Illustrator-学习记录/0003.png" alt></li></ol><h4>三 <strong><strong>首选项设置</strong></strong>:</h4><ol><li>打开首选项设置 Command + K</li><li>键盘增量设置，每次按下键盘会移动一个键盘增量，按下Shift每次按下键盘会移动10个键盘增量。</li><li>单位设置</li></ol><h4>四 <strong><strong>基本形状操作</strong></strong>:</h4><p>AI中有如下的基本形状：<img src="/2020/07/02/Adobe-Illustrator-学习记录/0004.png" alt></p><p>相关快捷操作：</p><ol><li>M 矩形</li><li>L 椭圆形</li><li>画出多边形和星型的时候按下上下方向键盘会增加和减少边数</li><li>使用Shift 画出正方形和圆形</li><li>如果按下后发现放置的位置需要修改，可以按下Space键盘拖动形状到目标位置</li><li>Shift + M 快速切换描边色和填充色</li><li>D 快速恢复到默认状态</li><li>快速改变形状大小，快速设置圆角，快速旋转</li><li>形状生成器 快捷键(Shift + M) 正常情况下是将对应的块合并，如果按下Alt后滑动，表示挖去。</li><li>使用Shaper Shift + N 来画形状</li></ol><p>技能清单：</p><ol><li>画出基本形状，多边形和星型可以快速增加和减少边数，画出正方形和圆形</li><li>修改填充色和描边色，快速切换填充色和描边色，快速恢复到默认状态</li><li>修改描边的粗细，边角，虚线，箭头</li><li>偏移路径</li><li>基本形状变换：包括位置，尺寸，旋转角度，圆角，这里涉及到了变换面板<img src="/2020/07/02/Adobe-Illustrator-学习记录/0005.png" alt></li><li>打开缩放圆角，缩放描边和效果</li><li>AI也支持其他绘图工具的布尔运算，但是区别是在未确认是最终操作的时候需要按下Alt 避免自动扩展<img src="/2020/07/02/Adobe-Illustrator-学习记录/0006.png" alt></li><li>除了这些工具外还有一个非常强大的工具-形状生成器 快捷键(Shift + M) 正常情况下是将对应的块合并，如果按下Alt后滑动，表示挖去。</li><li>使用扩展</li><li>使用外观面板添加多个填充，多个描边的形状，</li></ol><h4>五 <strong><strong>整理对象操作</strong></strong>： 对齐，分组，放置到新图层，改变对象在层中的层级位置</h4><ol><li>对于某些已经确定的物体可以将其锁定起来，快捷键为Command + 2</li><li>使用魔板工具可以快速选中相似颜色的</li><li>套索工具可以选中物体的锚点进行操作。</li><li>最常用的两个选择工具，号称小黑和小白的V,A 组合。</li><li>Command + A 选择全部画板的全部对象, Command + Alt + A 选择当前画板的全部对象，Command + Shift + A 取消选择</li><li>使用Command + G 对对象进行编组操作，Command + Shift + G 取消编组</li><li>在图层面板新建一个图层使用Command + F 黏贴到最前面，然后改变对应层的层次结构</li><li>使用Command + []可以改变对象在图层中的层级关系</li><li>在工作中可以打开对齐面板来操作物体间的对齐操作，在对齐过程中如果需要辅助线，可以从标尺上拖动到某个位置就可以在当前位置添加一条辅助线。</li></ol><h4>六 <strong><strong>颜色操作</strong></strong>：</h4><p>AI中的着色工具包括：</p><ol><li>色板：负责选择颜色</li><li>颜色：负责某个颜色细调</li><li>颜色参考和Adobe Color Theme: 用于选择颜色</li><li>渐变工具：调整渐变</li><li>通过外观面板fx修改形状外观。</li><li>吸管工具 I</li><li>使用重新着色工具对某个对象进行调整颜色，一般用于没有灵感的情况下，尝试配色使用。<img src="/2020/07/02/Adobe-Illustrator-学习记录/0009.png" alt></li></ol><h4>七 <strong><strong>绘图操作</strong></strong>：</h4><ol><li><p>N 铅笔工具</p></li><li><p>B 画笔工具</p></li><li><p>Shift B 斑点画笔工具</p></li><li><p>铅笔，画笔设置项：保真度，平滑度，保持编辑状态，在一定距离的时候自动闭合，在某个距离继续画下去的时候会自动从上一次位置继续。</p></li><li><p>上色方法：5.1 使用斑点画笔工具，它的特点是可以将多次画的一块联合起来，并且可以使用[]来调整画笔宽度5.2 使用实时上色工具进行上色</p></li><li><p>其他绘图辅助工具：6.1 路径平滑工具：抹去线条的尖锐不平滑的地方6.2 连接工具：将对应的断开的线连接起来，然后使用平滑工具，锚点工具来编辑6.3 剪刀工具：用于断开路径6.4 橡皮擦工具：用于擦除内容6.5 美工刀工具：用于切割形状7 绘图模式：7.1 正常绘图：在物体上一层绘图7.2 背面绘图：在物体的下一层绘图7.3 内部绘图：在物体轮廓内部绘图</p></li><li><p>钢笔工具：手动生成路径使用P快捷键调出钢笔工具。点击中间锚点会打断对称，直线只要点击不要拖动，而曲线需要拖动，起点和终点一个在高峰/低谷的一边，一个在另一边。如果不确定可以按着space键移动。画完可以通过A 锚点工具来调整，+ 和删除锚点来修改，对于转折点可以使用Command点击拖动句柄来调整handler的方向，不要一味地在尖角的地方点终止，结束 command 点击屏幕</p></li><li><p>其他路径绘图工具除了使用 钢笔工具还可以使用曲率工具和重塑工具来绘制边界曲率工具： 先勾勒出轮廓，然后在调整轮廓的曲率，点击锚点可以在平滑和尖锐模式下切换重塑工具： 在钢笔绘制过程中按下alt切换到重塑工具，按下shift能够切换到半圆型，可以用这种方式画八卦弧线工具： x 切换方向   上下键 调整幅度弧线工具还有一个很好用的功能就是画下一个弧线的时候按住～移动会形成一个头发类似的东西。</p></li><li><p>变换工具变形工具旋转扭曲工具缩融工具<br>膨胀工具晶格工具扇贝工具褶皱工具自由变换操控变形旋转,镜像封套扭曲 可以作用于对象，或者文本：可以用变形，网格，或者顶部对象作为封套。工具的具体路径在: 对象 --&gt; 封套扭曲</p></li><li><p>同心圆形状画单个 —-&gt; command + G —-&gt; 效果 —-&gt; 扭曲变换—&gt; 变换 —-&gt; 点击变换图案</p></li></ol><h4>八 <strong><strong>文字操作</strong></strong>：</h4><ol><li>字体的常用编辑项目都在字体面板中：<img src="/2020/07/02/Adobe-Illustrator-学习记录/0007.png" alt></li><li>字距微调 alt + 左右键  alt + 上下 行距微调，选择字 shift + alt 微调基线</li><li>可以使用工具栏的修饰文字工具来旋转字体以及缩放文字大小</li><li>使用工具栏中的网格工具来对文字进行变形操作，变形的按钮将会出现在右上角顶部。还可以使用 “对象 —&gt; 封套扭曲 -&gt; 用顶层对象” 注意形状要位于上层</li><li>使用文字样式和段落样式来管理全局的样式，以便统一。</li><li>文字路径：画路径，切换到文字模式，贴近路径会出现提示，输入文字即可。点击选择工具会出现三个光标开始,结束,中间，开始和结束可以移动文本在path上的位置，中间是整体移动，还可以将字符移到路径上面和路径下面。在形状外部和内部填充文字：在外部填充文字需要按下alt 然后点击形状轮廓<img src="/2020/07/02/Adobe-Illustrator-学习记录/0008.png" alt>在内部填充只需要点击形状轮廓即可<img src="/2020/07/02/Adobe-Illustrator-学习记录/0009.png" alt></li><li>可以设计3D文字:在效果 --&gt; 3D中调出来调整面板</li><li>添加文字轮廓：通过apearance 选中文本，新增填充，移动顺序从上到下 填充，字符 轮廓。修改轮廓大小</li><li>使用文字作为剪切蒙版：command + 7</li><li>可以设置文本环绕: 对象 -&gt; 文本环绕</li></ol><h4>九 <strong><strong>剪切蒙版</strong></strong> (和Sketch 不同AI剪切蒙版的模版在上面，内容在下面)：</h4><ol><li>形状剪切蒙版 :对象-&gt;剪切蒙版 或者Command + 7  上面作为蒙版，下面作为内容</li><li>透明度剪切蒙版 :渐变，黑的减去，白的留下底部 在透明度面板里面设置</li></ol><h4>十 <strong><strong>符号/图案操作</strong></strong></h4><p>在我的理解中：图形样式，字符样式，段落样式以及符号都是用于管理整个项目使得项目具有统一的设计规范。</p><ol><li>符号：可以将某个对象，拖动到符号面板，建立动态连接，如果后续其他地方还需要这个符号，就直接从符号面板拖动。如果要修改可以通过点击符号面板中的符号统一修改，这样文档中的所有符号都同时修改。符号编辑工具：<img src="/2020/07/02/Adobe-Illustrator-学习记录/0011.png" alt></li><li>图案：利用多个图像构成一个图案，之后可以添加到色版中作为填充使用。创建图案方法: 对象 -&gt; 图案 -&gt; 建立。 图案编辑可以使用图案面板。</li></ol><h4>十一 <strong><strong>其他高级操作</strong></strong></h4><ol><li>数据可视化表格操作</li><li>渐变网格</li><li>绘制透视视图</li><li>混合对象</li></ol><h4><strong><strong>总结</strong></strong></h4><p>AI 中最关键的几个关键字：形状（路径），绘图，笔刷，颜色，文本</p><p><img src="/2020/07/02/Adobe-Illustrator-学习记录/00012.jpg" alt></p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h4&gt;一 &lt;strong&gt;&lt;strong&gt;画板操作&lt;/strong&gt;&lt;/strong&gt;：&lt;/h4&gt;
&lt;p&gt;一个文件可以保存多个画板，按下如下按钮：&lt;img src=&quot;/2020/07/02/Adobe-Illustrator-学习记录/0001.png&quot; alt&gt; 或者 Shi
      
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>Sketch的技巧总结</title>
    <link href="http://yoursite.com/2020/07/01/Sketch%E7%9A%84%E6%8A%80%E5%B7%A7%E6%80%BB%E7%BB%93/"/>
    <id>http://yoursite.com/2020/07/01/Sketch的技巧总结/</id>
    <published>2020-07-01T14:42:19.000Z</published>
    <updated>2020-07-01T17:08:04.000Z</updated>
    
    <content type="html"><![CDATA[<h3>常用快捷键：</h3><table><thead><tr><th>快捷键</th><th>介绍</th></tr></thead><tbody><tr><td>A</td><td>插入画板</td></tr><tr><td>R</td><td>插入矩形</td></tr><tr><td>U</td><td>插入圆角矩形</td></tr><tr><td>O</td><td>插入圆形</td></tr><tr><td>V</td><td>矢量工具</td></tr><tr><td>T</td><td>插入文本</td></tr><tr><td>P</td><td>画笔</td></tr><tr><td>S</td><td>切片 (Export group content only 只到处组的内容，不包含背景/trim transparent pixel,会删除多余的透明像素)</td></tr><tr><td>B</td><td>是否显示边界</td></tr><tr><td>F</td><td>是否填充</td></tr><tr><td>Tab</td><td>在组件列表中上下移动</td></tr><tr><td>Command + Alt + C/V</td><td>复制黏贴样式</td></tr><tr><td>Command + C/V</td><td>复制黏贴对象</td></tr><tr><td>Alt + 拖动</td><td>复制</td></tr><tr><td>Command + D</td><td>就地复制</td></tr><tr><td>Command + Alt + F</td><td>查找并替换颜色</td></tr><tr><td>Command + R</td><td>重命名组件</td></tr><tr><td>Control  + C</td><td>选取颜色</td></tr><tr><td>Command + Shift  H</td><td>隐藏</td></tr><tr><td>Command + Shift  L</td><td>锁定</td></tr><tr><td>Command + G</td><td>成组</td></tr><tr><td>Command + Shift + G</td><td>分组</td></tr><tr><td>Command + 2</td><td>放大选中的</td></tr><tr><td>Command + 4</td><td>放大当前的画板</td></tr><tr><td>Command + Control + F</td><td>进入全屏</td></tr><tr><td>Command + 单击</td><td>直接选择</td></tr><tr><td>按住空格键 + 移动</td><td>移动画布</td></tr><tr><td>Command + Shift + D</td><td>切换数据</td></tr><tr><td>Command + Shift + E</td><td>导出</td></tr><tr><td>按住 option 不放，鼠标放到另一个元素上</td><td>查看间距</td></tr><tr><td>Command-K</td><td>图层矢量缩放</td></tr><tr><td>Command + Alt +/-</td><td>对字体进行缩放</td></tr><tr><td>Command + Shift { }</td><td>字体对齐</td></tr><tr><td>Command + -</td><td>缩放</td></tr><tr><td>Command + []</td><td>层级上下更改</td></tr></tbody></table><h3>常见技巧：</h3><ul><li><p><strong><strong>加减乘除</strong></strong>做界面的时候经常会出现这样的情况，要把一个图形均分三等分、四等分的，或者说加上30px、40px，这时候要手动去计算，然后再去输入，很浪费时间成本，而且对于数学不好的人来说，还容易算错。其实 sketch 自带就有一些快捷方法，在右侧尺寸大小的面板处，可以直接在尺寸后输入「+」「-」「*」「/」，后面跟上数字，输完之后确定，就可以得到想要的计算结果了。</p></li><li><p><strong><strong>不透明度设置</strong></strong>当要改变一个元素的不透明度的时候，没有必要到右侧的参数面板来修改，可以直接按数字来调整，比如你想把不透明度改到65%，那么选中这个元素后，直接按数字65就好了，不满意还可以重新输入新的数字，当不透明度是整数的时候，可以直接输入数字1234567890，就不用输「80」「90」了。</p></li><li><p><strong><strong>移动微调</strong></strong>按住 shift 不放，选择「上下左右」可以快速移动10px</p></li><li><p><strong><strong>鼠标、Shift、Option实现快速修改</strong></strong></p><p>主界面中的右侧的属性栏中可以单击属性值以快速递增或递减该值（注意要把鼠标放在文字的上下方位内，出现左右箭头才可以快速修改）。如果按住 Shift键，Sketch 将相应地将值递增或递减10倍，按住 Option键会将其增加/减少十分之一。</p></li><li><p><strong><strong>输入方式更改大小和圆角</strong></strong></p><p>支持简单数学公式，支持样式单位L（中心点左对齐）R（中心点居右）C（中心点居中）M（中心点居中）T（中心点置顶）B（中心点底部对齐），支持圆角快速自定义（格式是X;Y;N;M，顺序是左上-右上-右下-左下），能帮助快速精确调整，是不是很高级？</p></li><li><p><strong><strong>图层对齐</strong></strong>图层对齐也是经常要用到的操作。直接与对象对齐的话，Sketch 会将图层对齐到所有选定对象的最外层。但要将图层与特定对象对齐，怎么操作呢？比如想让下图中两个元素以正方形为参照物进行居中，那应该先使用图层列表中的锁定按钮或使用 Shift-Command-L  锁定该图层，然后再执行对齐操作即可。</p></li><li><p><strong><strong>快速定位到上一级层级</strong></strong>在很多图层未整理，处于比较混乱的情况下，按 command+鼠标左键只能快速的选到某个具体图层，但是接着你又在想，如果我还能一键快速选到图层所在的组，那该多好。方法是在选择具体图层后，按键盘上的 esc 键，能快速选择图层所在的上一级，可能是组，也可以是更上一级的组，甚至能快速选到画板，是不是很方便？选择后如果要移动就按 option+command，还能避免误操作。</p></li></ul><h3>技能清单：</h3><ul><li><p>形状/文本/图片编辑位置设置(X,Y,W,H,旋转角度，镜像，圆角)填充（渐变，色彩，图片），边框（颜色，边框位置，宽度，结束点，衔接部位，虚线样式），阴影，内阴影，模糊缩放，变换，旋转，横向翻转，纵向翻转，布局约束文本路径</p></li><li><p>布尔运算/蒙版/钢笔工具/路径形状蒙版，透明蒙版关闭路径，反向路径，裁剪路径，旋转复制，偏移，文本路径[蒙版] 下层负责显示形状，上层负责显示内容,一旦添加蒙版，上层的所有都会受到影响，可以使用ignore underling mark layer,添加蒙版可以选中底部的决定形状的图形，然后点击mark with selected shape 或者工具栏的mask图标</p></li><li><p>切图/标注/导出</p></li><li><p>图层/编组/对齐/分布/Make Grid/Copy Rotate</p></li><li><p>Flaten/Outline</p></li><li><p>Text Style/Layer Style/Symbols 复用样式Symbol 嵌套Symbol: 创建最外框，将最外框打包成组，将组创建成Symbol，点击Symbol 进入Symbols 界面。在里面添加某个控件，将这个控件创建成Symbol,在Symbol中选中新建的Symbol(这里不要选错了),Command + D复制一份。修改属性，比如下面的bg和avator</p></li></ul><h3>较好的文章</h3><ul><li><a href="https://zhuanlan.zhihu.com/p/69358464" target="_blank" rel="noopener">Sketch自带的Data(数据)功能怎么用？Data可智能填充图片、文本数据 - 知乎</a></li><li><a href="https://www.uihacker.com/sketch-data/" target="_blank" rel="noopener">怎么快速给界面填充图文数据？Sketch数据源轻松解决 - UI黑客</a></li><li><a href="https://www.uisdc.com/symbol-build-component-library" target="_blank" rel="noopener">Sketch 进阶教程！利用Symbol 建立一套设计规范组件库？ | 优设网 - UISDC</a></li><li><a href="https://www.jianshu.com/p/c5e796b55044" target="_blank" rel="noopener">如何用Sketch中Symbol/Styled Text制作设计规范 - 简书</a></li><li><a href="https://zhuanlan.zhihu.com/p/29220802" target="_blank" rel="noopener">「技法」五步学会用 Sketch 的功能 shared text 搭建字体样式库 - 知乎</a></li><li><a href="https://www.uisdc.com/symbol-build-component-library" target="_blank" rel="noopener">Sketch 进阶教程！利用Symbol 建立一套设计规范组件库？ | 优设网 - UISDC</a></li><li><a href="https://www.uisdc.com/use-symbol-in-sketch" target="_blank" rel="noopener">这样使用Sketch的Symbol功能，能极大提高你的工作效率！ | 优设网 - UISDC</a></li><li><a href="https://www.uibaba.com/article/1033.html" target="_blank" rel="noopener">干货：帮你从零开始学会用Sketch做设计规范（一） - UI巴巴</a></li><li><a href="https://www.yuque.com/jingwhale/tool/syd5oz" target="_blank" rel="noopener">建立一套设计规范组件库 · 语雀</a></li><li><a href="https://blog.envylabs.com/symbol-in-a-symbol-building-design-systems-in-sketch-30b8d167506e" target="_blank" rel="noopener">Symbol in a Symbol: Building Design Systems in Sketch</a></li><li><a href="http://beforweb.com/node/943" target="_blank" rel="noopener">使用Sketch Libraries构建组件库/设计体系</a></li><li><a href="https://www.shejidaren.com/sketch-zhujian-sheji.html" target="_blank" rel="noopener">学会组件UI设计，5分钟快速修改100多个界面 | 设计达人</a></li><li><a href="https://medium.com/uwillx/%E5%88%A9%E7%94%A8sketch%E6%96%BC%E8%A8%AD%E8%A8%88%E5%B7%A5%E4%BD%9C%E6%B5%81%E7%A8%8B-b98664419454" target="_blank" rel="noopener">利用Sketch於設計流程 - uwillx - Medium</a></li><li><a href="https://medium.com/sketch-app-sources/text-styles-mastery-with-sketch-52-dc00c7fe1aa6" target="_blank" rel="noopener">Text Styles Mastery with Sketch 52+ - Design + Sketch - Medium</a></li></ul>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h3&gt;常用快捷键：&lt;/h3&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;快捷键&lt;/th&gt;
&lt;th&gt;介绍&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;A&lt;/td&gt;
&lt;td&gt;插入画板&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;R&lt;/td&gt;
&lt;td
      
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>叨叨 叨叨 by 叨叨</title>
    <link href="http://yoursite.com/2020/03/15/%E5%8F%A8%E5%8F%A8-%E5%8F%A8%E5%8F%A8-by-%E5%8F%A8%E5%8F%A8/"/>
    <id>http://yoursite.com/2020/03/15/叨叨-叨叨-by-叨叨/</id>
    <published>2020-03-15T06:50:52.000Z</published>
    <updated>2020-03-15T07:15:34.000Z</updated>
    
    <content type="html"><![CDATA[<ul><li><p>每个踏上征途的行者，在行路过程中未必见得是享受，也未必能够真正能够享受沿途的美景，但是可以确定的是每向前一步，远方的诱惑就多一分，这对每个行者而言是最致命的诱惑。</p></li><li><p>每一年都会对自己说不要太着急，太焦虑，</p></li><li><p>Dont limit your self.</p></li></ul>]]></content>
    
    <summary type="html">
    
      
      
        &lt;ul&gt;
&lt;li&gt;
&lt;p&gt;每个踏上征途的行者，在行路过程中未必见得是享受，也未必能够真正能够享受沿途的美景，但是可以确定的是每向前一步，远方的诱惑就多一分，这对每个行者而言是最致命的诱惑。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;每一年都会对自己说不要太着急，太焦虑，&lt;/p&gt;
&lt;/l
      
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>iOS 系列相关总结博客目录列表</title>
    <link href="http://yoursite.com/2020/02/08/iOS-%E7%9B%B8%E5%85%B3%E6%80%BB%E7%BB%93%E5%8D%9A%E5%AE%A2%E5%88%97%E8%A1%A8/"/>
    <id>http://yoursite.com/2020/02/08/iOS-相关总结博客列表/</id>
    <published>2020-02-07T16:33:59.000Z</published>
    <updated>2020-02-09T04:04:29.000Z</updated>
    
    <content type="html"><![CDATA[<p>1.<strong><strong>Objective C 相关</strong></strong></p><ul><li><a href>Objective C 代码规范</a></li><li><a href>iOS中的宏用法简单总结</a></li><li><a href>iOS 内存管理总结</a></li><li><a href>iOS Autorelease Pool 总结</a></li><li><a href>iOS 分类扩展与协议总结</a></li><li><a href>iOS Block 总结</a></li><li><a href>iOS Runloop 总结</a></li><li><a href>iOS KVC KVO 总结</a></li><li><a href>iOS 学习记录[一] 图解生命周期</a></li><li><a href>iOS-Runtime源码解析之Runtime较好的文章推荐</a></li><li><a href>iOS Runtime源码解析Mach O 结构</a></li><li><a href>iOS Runtime源码解析之App启动</a></li><li><a href>iOS Runtime源码解析之dyld</a></li><li><a href>iOS Runtime源码解析重要的数据结构介绍</a></li><li><a href>iOS Runtime源码解析 Runtime初始化</a></li><li><a href>iOS Runtime源码解析消息处理</a></li><li><a href>iOS Runtime源码解析initialize load __attribute__总结</a></li><li><a href>iOS Runtime应用之Method Swizzling</a></li><li><a href>iOS Runtime应用之多继承</a></li><li><a href>iOS Runtime应用之Runtime API分类</a></li><li><a href>图解iOS系列之Runtime（一）MachO 介绍</a></li><li><a href>图解iOS系列之Runtime（二）基本数据结构</a></li><li><a href>图解iOS系列之Runtime（三）应用启动到main 过程的操作</a></li><li><a href>图解iOS系列之Runtime（四）发送消息流程</a></li><li><a href>图解iOS系列之Runtime（五) 对象创建，关联对象 自动释放池</a></li></ul><p>2.<strong><strong>iOS基础相关</strong></strong></p><ul><li><a href>iOS 生命周期</a></li><li><a href>iOS 渲染系统工作原理介绍</a></li><li><a href>iOS 渲染框架Core Graphic</a></li><li><a href>iOS 布局总结</a></li><li><a href>iOS 动画总结</a></li><li><a href>iOS 事件及手势处理流程</a></li><li><a href>iOS KeyChain 总结</a></li><li><a href>iOS 多线程总结 [一] - 基本概念 GCD NSOperation</a></li><li><a href>iOS 多线程总结 [二] - 线程安全</a></li><li><a href>XCode 快捷键</a></li><li><a href>使用LLDB来辅助调试iOS程序</a></li><li><a href>Cocopods 使用</a></li><li><a href>iOS 推送总结</a></li><li><a href>iOS 支付内购</a></li><li><a href>iOS 上架流程</a></li></ul><p>3.<strong><strong>iOS进阶相关</strong></strong></p><ul><li><a href>iOS 性能优化总结</a></li><li><a href>iOS 动态化方案总结</a></li><li><a href>iOS 跨平台方案总结</a></li><li><a href>iOS 组件化总结</a></li></ul><p>4.<strong><strong>iOS 源码分析系列</strong></strong></p><ul><li><a href>图解iOS之ReactiveObjc 部分重要概念及操作简图 一</a></li><li><a href>图解iOS之ReactiveObjc 部分重要概念及操作简图 二</a></li><li><a href>[转]RAC 知识点汇总 总结得非常全</a></li><li><a href>JSPatch源码分析</a></li><li><a href>Aspect源码解析</a></li><li><a href>图解iOS之GVUserDefaults</a></li><li><a href>JLRoute源码解析</a></li><li><a href>iOS 基于JLRoute 改造的路由</a></li><li><a href>图解移动端日志系统</a></li><li><a href>YYCache 源码解析</a></li><li><a href>ijkplayer 源码分析</a></li><li><a href>图解加解密</a></li><li><a href>加解密技术概述</a></li><li><a href>基于AutoLayout的开源布局库Masonry源码解析</a></li><li><a href>在项目中使用Yoga 布局引擎</a></li><li><a href>YYText 使用详解</a></li><li><a href>Texture 一 简要介绍</a></li><li><a href>Texture 二 基本使用Node及布局</a></li><li><a href>Texture 五 Texture源码解析</a></li><li><a href>Texture 六 Texture 的其他用法</a></li><li><a href>YYDispatchQueuePool 源码解析</a></li><li><a href>YYModel 源码解析</a></li><li><a href>MLeaksFinder 源码解析</a></li><li><a href>PromiseKit 的使用</a></li><li><a href>BlocksKit 的使用</a></li><li><a href>FBRetainCycleDetector FBAllocationTracker 源码解析</a></li><li><a href>日志库CocoaLumberjack 源码解析</a></li><li><a href>SDWebImage 源码解析</a></li><li><a href>YTKNetWork源码解析</a></li><li><a href>AFNetWorking源码解析</a></li><li><a href>iOS开源库之SRWebSocket源码解析</a></li><li><a href>YYAsyncLayer 源码解析</a></li><li><a href>Mars源码分析介绍</a></li><li><a href>MJRefresh 使用及源码分析</a></li><li><a href>IGKListKit 使用及源码分析</a></li><li><a href>iOS开源库源码解析组件化BeeHive</a></li><li><a href>iOS开源库之objection 源码解析</a></li><li><a href>iOS开源库之fishhook 概述</a></li><li><a href>iOS开源库之coobjc使用</a></li></ul><p>5.<strong><strong>音视频相关</strong></strong></p><ul><li><a href>iOS 音视频专题目录篇</a></li><li><a href>iOS 直播技术总结[一] 图解iOS移动端直播技术框架</a></li><li><a href>iOS 直播技术总结[二] 图解CDN架构</a></li><li><a href>iOS 直播技术总结 [三] 图解 AV Foundation初步结构与音视频基础知识</a></li><li><a href>iOS 直播技术总结 [四] 图解 音视频数据采集</a></li><li><a href>iOS 直播技术总结 五 音视频数据处理 [使用Core Image]</a></li><li><a href>iOS 直播技术总结 六 音视频数据处理 [使用GPUImage2]</a></li><li><a href>iOS 直播技术总结 七 第一阶段总图</a></li></ul><p>6.<strong><strong>iOS 设计相关</strong></strong></p><ul><li><a href>iOS 设计规范整理</a></li></ul><p>7.<strong><strong>其他</strong></strong></p><ul><li><a href>工作生活之余，别忘了给自己点乐趣 ^v^</a></li></ul>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;1.&lt;strong&gt;&lt;strong&gt;Objective C 相关&lt;/strong&gt;&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href&gt;Objective C 代码规范&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href&gt;iOS中的宏用法简单总结&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;
      
    
    </summary>
    
      <category term="博客分类目录" scheme="http://yoursite.com/categories/%E5%8D%9A%E5%AE%A2%E5%88%86%E7%B1%BB%E7%9B%AE%E5%BD%95/"/>
    
    
      <category term="博客分类目录" scheme="http://yoursite.com/tags/%E5%8D%9A%E5%AE%A2%E5%88%86%E7%B1%BB%E7%9B%AE%E5%BD%95/"/>
    
  </entry>
  
  <entry>
    <title>iOS Runtime应用之Runtime API分类</title>
    <link href="http://yoursite.com/2020/02/06/iOS-Runtime%E5%BA%94%E7%94%A8%E4%B9%8B%E5%B8%A6%E5%A4%A7%E5%AE%B6%E7%86%9F%E6%82%89Runtime-API/"/>
    <id>http://yoursite.com/2020/02/06/iOS-Runtime应用之带大家熟悉Runtime-API/</id>
    <published>2020-02-06T03:44:53.000Z</published>
    <updated>2020-02-16T13:16:17.000Z</updated>
    
    <content type="html"><![CDATA[<p>本打算这个系列结束的，但是由于疫情影响在家的这几天有空闲时间，想着把这块也整理下，今天这篇文章打算带大家熟悉下Runtime API.其这部分和Runtime重要的数据结构紧密相关，通过<strong><strong>objc/runtime.h</strong></strong>无非就是获得类，对象，Method,SEL,IMP,Ivar,Property,Protocal相关类以及对应的属性。这块还是蛮有规律的，最早接触Runtime还是蛮抗拒的，后面才慢慢觉得它的强大。废话不多说这篇博客主要分两大部分，一部分介绍上面提到的这些数据结构相关的数据，另一部分介绍十分重要的类型编码和属性编码。搞定这两部分估计大家在使用上就会游刃有余了，但是还是建立在理解Runtime各个环节的基础上。</p><p>这里推荐大家用<strong><strong>Cooci</strong></strong>可编译苹果官方源码objc来写demo这样可以比较方便得调试整个过程，这也是最近才发现的一个很棒的开源库，之前研究底层代码的时候要是早点发现这个就不会那么纠结了。好东西不私藏。<a href="https://github.com/LGCooci/objc4_debug" target="_blank" rel="noopener">https://github.com/LGCooci/objc4_debug</a>:</p><p>在开始之前建议大家通读下我的《iOS Runtime源码分析系列》的博客，当然在这篇博客介绍的时候还会简要提一下相关内容。好了我们开始我们的API之旅：</p><p>首先是类相关的，这些都以****class_****开头：</p><h6><strong><strong>类相关：</strong></strong></h6><p><figure class="highlight oxygene"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="comment">//获取类名</span></span><br><span class="line">OBJC_EXPORT <span class="keyword">const</span> char * _Nonnull class_getName(<span class="keyword">Class</span> _Nullable cls) </span><br><span class="line"></span><br><span class="line"><span class="comment">//获取isa</span></span><br><span class="line">OBJC_EXPORT <span class="keyword">Class</span> _Nullable object_getClass(id _Nullable obj) </span><br><span class="line"></span><br><span class="line"><span class="comment">//获取父类</span></span><br><span class="line">OBJC_EXPORT <span class="keyword">Class</span> _Nullable class_getSuperclass(<span class="keyword">Class</span> _Nullable cls) </span><br><span class="line"></span><br><span class="line">获取实例变量的信息</span><br><span class="line"><span class="number">1</span>.实例变量是指变量不是属性.每个属性都有对应的实例变量，比如下面例子中包含属性name,那么它对应的实例变量为_name</span><br><span class="line"><span class="number">2</span>.这个方法可以获取属性的变量,也可以获取私有变量</span><br><span class="line"><span class="number">3</span>.如果没有找到对应的实例变量，就会返回<span class="keyword">nil</span></span><br><span class="line"><span class="number">4</span>.返回的列表不包含父类的实例变量</span><br><span class="line">Ivar class_getClassVariable(<span class="keyword">Class</span> cls, <span class="keyword">const</span> char *name)</span><br><span class="line"></span><br><span class="line">获取属性:</span><br><span class="line">这个方法只能获得属性，不能获得变量</span><br><span class="line">objc_property_t class_getProperty(<span class="keyword">Class</span> cls, <span class="keyword">const</span> char *name)</span><br><span class="line"></span><br><span class="line">获取实例方法信息</span><br><span class="line">OBJC_EXPORT <span class="function"><span class="keyword">Method</span> _<span class="title">Nullable</span> <span class="title">class_getInstanceMethod</span><span class="params">(<span class="keyword">Class</span> _Nullable cls, SEL _Nonnull name)</span>;</span></span><br><span class="line"></span><br><span class="line">获取类方法信息</span><br><span class="line">OBJC_EXPORT <span class="function"><span class="keyword">Method</span> _<span class="title">Nullable</span> <span class="title">class_getClassMethod</span><span class="params">(<span class="keyword">Class</span> _Nullable cls, SEL _Nonnull name)</span></span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function">获取<span class="title">SEL</span>对应的实现体，也就是<span class="title">IMP</span></span></span><br><span class="line"><span class="function"><span class="title">IMP</span> <span class="title">class_getMethodImplementation</span><span class="params">(<span class="keyword">Class</span> cls, SEL name)</span></span></span><br></pre></td></tr></table></figure></p><p>通过这部分接口可以获得类名，父类，实例变量，属性，实例方法，类方法和IMP,注意这里只是获取单个的并不是全部的数据。如果要获得全部的就需要class_copyXXXX了。</p><p>获取数据列表类：</p><p><figure class="highlight sqf"><table><tr><td class="code"><pre><span class="line"><span class="comment">//获取实例变量列表</span></span><br><span class="line"><span class="comment">//1.获取所有[==私有变量==]和属性对应的变量</span></span><br><span class="line"><span class="comment">//2.获取的私有变量的名和定义的名一样</span></span><br><span class="line"><span class="comment">//3.获取的属性的名前面都添加了[下划线]</span></span><br><span class="line"><span class="comment">//4.注意这里[不包括父类的实例变量]</span></span><br><span class="line">OBJC_EXPORT Ivar <span class="variable">_Nonnull</span> * <span class="variable">_Nullable</span> class_copyIvarList(Class <span class="variable">_Nullable</span> cls, unsigned int * <span class="variable">_Nullable</span> outCount);</span><br><span class="line"></span><br><span class="line"><span class="comment">//获取方法列表</span></span><br><span class="line"><span class="comment">//1.获取所有实例方法,[==不包含静态方法==]</span></span><br><span class="line"><span class="comment">//2.不获取[父类的方法]</span></span><br><span class="line"><span class="comment">//3.[隐式的get set 方法也能获取到]</span></span><br><span class="line"><span class="comment">//4.[==可以获取分类和动态添加的方法==]。</span></span><br><span class="line">OBJC_EXPORT Method <span class="variable">_Nonnull</span> * <span class="variable">_Nullable</span> class_copyMethodList(Class <span class="variable">_Nullable</span> cls, unsigned int * <span class="variable">_Nullable</span> outCount) </span><br><span class="line"></span><br><span class="line"><span class="comment">//获取协议列表</span></span><br><span class="line"><span class="comment">//不能获取分类实现的协议</span></span><br><span class="line">OBJC_EXPORT Protocol * <span class="variable">__unsafe_unretained</span> <span class="variable">_Nonnull</span> * <span class="variable">_Nullable</span> </span><br><span class="line">class_copyProtocolList(Class <span class="variable">_Nullable</span> cls, unsigned int * <span class="variable">_Nullable</span> outCount)</span><br><span class="line"></span><br><span class="line"><span class="comment">//获取属性列表</span></span><br><span class="line"><span class="comment">//1.获取的属性名不自动添加下划线</span></span><br><span class="line"><span class="comment">//2.[不能获取Category添加的属性]。</span></span><br><span class="line">OBJC_EXPORT objc_property_t <span class="variable">_Nonnull</span> * <span class="variable">_Nullable</span></span><br><span class="line">class_copyPropertyList(Class <span class="variable">_Nullable</span> cls, unsigned int * <span class="variable">_Nullable</span> outCount)</span><br></pre></td></tr></table></figure></p><p>这些方法都需要注意最后调用free释放掉存放copy来的空间</p><p><figure class="highlight sqf"><table><tr><td class="code"><pre><span class="line">添加方法</span><br><span class="line">OBJC_EXPORT BOOL</span><br><span class="line">class_addMethod(Class <span class="variable">_Nullable</span> cls, SEL <span class="variable">_Nonnull</span> <span class="built_in">name</span>, IMP <span class="variable">_Nonnull</span> imp, </span><br><span class="line">                const char * <span class="variable">_Nullable</span> types) </span><br><span class="line"></span><br><span class="line">添加实例变量</span><br><span class="line"><span class="comment">//1.只能给动态创建的类添加变量也就是用 objc_allocateClassPair 创建的类</span></span><br><span class="line"><span class="comment">//2.添加变量只能在函数 objc_allocateClassPair 和 class_getInstanceVariable 之间添加才有效</span></span><br><span class="line">OBJC_EXPORT BOOL</span><br><span class="line">class_addIvar(Class <span class="variable">_Nullable</span> cls, const char * <span class="variable">_Nonnull</span> <span class="built_in">name</span>, size_t <span class="built_in">size</span>, </span><br><span class="line">              uint8_t alignment, const char * <span class="variable">_Nullable</span> types) </span><br><span class="line"></span><br><span class="line">添加协议</span><br><span class="line">OBJC_EXPORT BOOL</span><br><span class="line">class_addProtocol(Class <span class="variable">_Nullable</span> cls, Protocol * <span class="variable">_Nonnull</span> protocol) </span><br><span class="line"></span><br><span class="line"><span class="comment">//添加属性</span></span><br><span class="line"><span class="comment">//1. 添加属性不用在objc_registerClassPair之前,因为添加属性其实就是添加变量的set 和 get方法而已</span></span><br><span class="line"><span class="comment">//2. 添加的属性和变量不能用kvc设置值和取值</span></span><br><span class="line">OBJC_EXPORT BOOL</span><br><span class="line">class_addProperty(Class <span class="variable">_Nullable</span> cls, const char * <span class="variable">_Nonnull</span> <span class="built_in">name</span>,</span><br><span class="line">                  const objc_property_attribute_t * <span class="variable">_Nullable</span> attributes,</span><br><span class="line">                  unsigned int attributeCount)</span><br></pre></td></tr></table></figure></p><p><figure class="highlight sqf"><table><tr><td class="code"><pre><span class="line">替换方法</span><br><span class="line">OBJC_EXPORT IMP <span class="variable">_Nullable</span></span><br><span class="line">class_replaceMethod(Class <span class="variable">_Nullable</span> cls, SEL <span class="variable">_Nonnull</span> <span class="built_in">name</span>, IMP <span class="variable">_Nonnull</span> imp, const char * <span class="variable">_Nullable</span> types) </span><br><span class="line"></span><br><span class="line">替换属性</span><br><span class="line">OBJC_EXPORT void</span><br><span class="line">class_replaceProperty(Class <span class="variable">_Nullable</span> cls, const char * <span class="variable">_Nonnull</span> <span class="built_in">name</span>,</span><br><span class="line">                      const objc_property_attribute_t * <span class="variable">_Nullable</span> attributes,</span><br><span class="line">                      unsigned int attributeCount)</span><br></pre></td></tr></table></figure></p><p><figure class="highlight stylus"><table><tr><td class="code"><pre><span class="line">创建实例</span><br><span class="line">OBJC_EXPORT id _Nullable</span><br><span class="line"><span class="function"><span class="title">class_createInstance</span><span class="params">(Class _Nullable cls, size_t extraBytes)</span></span></span><br><span class="line"></span><br><span class="line">遵循协议</span><br><span class="line">OBJC_EXPORT BOOL</span><br><span class="line"><span class="function"><span class="title">class_conformsToProtocol</span><span class="params">(Class _Nullable cls, Protocol * _Nullable protocol)</span></span> </span><br><span class="line"></span><br><span class="line">判断是否响应某个SEL</span><br><span class="line">OBJC_EXPORT BOOL</span><br><span class="line"><span class="function"><span class="title">class_respondsToSelector</span><span class="params">(Class _Nullable cls, SEL _Nonnull sel)</span></span></span><br></pre></td></tr></table></figure></p><p>下面是简要的实例：</p><p><figure class="highlight objectivec"><table><tr><td class="code"><pre><span class="line"><span class="meta">#import <span class="meta-string">&lt;Foundation/Foundation.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="built_in">NS_ASSUME_NONNULL_BEGIN</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">@interface</span> <span class="title">IDLTestObject</span> : <span class="title">NSObject</span> </span>&#123;</span><br><span class="line">    <span class="built_in">NSString</span> *privateInstanceVar;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@property</span>(<span class="keyword">nonatomic</span>, <span class="keyword">strong</span>, <span class="keyword">readwrite</span>) <span class="built_in">NSString</span> *name;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@property</span>(<span class="keyword">nonatomic</span>, <span class="keyword">assign</span>, <span class="keyword">readwrite</span>) <span class="built_in">NSInteger</span> age;</span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)testInstanceMethod;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">NS_ASSUME_NONNULL_END</span></span><br></pre></td></tr></table></figure></p><p><figure class="highlight objectivec"><table><tr><td class="code"><pre><span class="line"><span class="meta">#import <span class="meta-string">"IDLTestObject.h"</span></span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">@implementation</span> <span class="title">IDLTestObject</span></span></span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)testInstanceMethod &#123;</span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@"run testInstanceMethod"</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">+ (<span class="keyword">void</span>)testClassMethod &#123;</span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@"testclassMethod"</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br></pre></td></tr></table></figure></p><p><figure class="highlight objectivec"><table><tr><td class="code"><pre><span class="line"><span class="comment">//获取类名</span></span><br><span class="line"><span class="keyword">const</span> <span class="keyword">char</span> *className = class_getName([IDLTestObject <span class="keyword">class</span>]);</span><br><span class="line"><span class="built_in">NSLog</span>(<span class="string">@"className : %@"</span>,[<span class="built_in">NSString</span> stringWithUTF8String:className]);</span><br><span class="line"></span><br><span class="line"><span class="comment">//获取父类</span></span><br><span class="line">Class superClass = class_getSuperclass([IDLTestObject <span class="keyword">class</span>]);</span><br><span class="line"><span class="built_in">NSLog</span>(<span class="string">@"superClass : %@"</span>, <span class="built_in">NSStringFromClass</span>(superClass));</span><br><span class="line"></span><br><span class="line"><span class="comment">//获取实例对象的大小</span></span><br><span class="line">size_t instanceSize = class_getInstanceSize([IDLTestObject <span class="keyword">class</span>]);</span><br><span class="line"><span class="built_in">NSLog</span>(<span class="string">@"instanceSize = %zu"</span>,instanceSize);</span><br><span class="line"></span><br><span class="line"><span class="comment">//获取实例变量信息 如果没有找到会返回nil</span></span><br><span class="line">Ivar nameVar = class_getInstanceVariable([IDLTestObject <span class="keyword">class</span>], [<span class="string">@"_name"</span> UTF8String]);</span><br><span class="line"><span class="built_in">NSLog</span>(<span class="string">@"InstanceVariable :%@"</span>,[<span class="built_in">NSString</span> stringWithUTF8String:ivar_getName(nameVar)]);</span><br><span class="line"><span class="built_in">NSLog</span>(<span class="string">@"Instance Variable TypeEncoding:%@"</span>,[<span class="built_in">NSString</span> stringWithUTF8String:ivar_getTypeEncoding(nameVar)]);</span><br><span class="line"><span class="built_in">NSLog</span>(<span class="string">@"Instance Variable Offset:%td"</span>,ivar_getOffset(nameVar));</span><br><span class="line"></span><br><span class="line"><span class="comment">//获取实例变量列表</span></span><br><span class="line"><span class="keyword">unsigned</span> <span class="keyword">int</span> outCount = <span class="number">0</span>;</span><br><span class="line">Ivar *varList = class_copyIvarList([IDLTestObject <span class="keyword">class</span>], &amp;outCount);</span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">unsigned</span> <span class="keyword">int</span> index = <span class="number">0</span> ;index &lt; outCount; index++) &#123;</span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@"var %u = %@"</span>,index,[<span class="built_in">NSString</span> stringWithUTF8String:ivar_getName(varList[index])]);</span><br><span class="line">&#125;</span><br><span class="line">free(varList);</span><br><span class="line"></span><br><span class="line"><span class="comment">//获取对象属性</span></span><br><span class="line">objc_property_t property = class_getProperty([IDLTestObject <span class="keyword">class</span>], [<span class="string">@"age"</span> UTF8String]);</span><br><span class="line"><span class="built_in">NSLog</span>(<span class="string">@"Instance Property Name :%@"</span>,[<span class="built_in">NSString</span> stringWithUTF8String:property_getName(property)]);</span><br><span class="line"><span class="built_in">NSLog</span>(<span class="string">@"Instance Property Attribute:%@"</span>,[<span class="built_in">NSString</span> stringWithUTF8String:property_getAttributes(property)]);</span><br><span class="line"></span><br><span class="line"><span class="comment">//获取实例方法</span></span><br><span class="line">Method method = class_getInstanceMethod([IDLTestObject <span class="keyword">class</span>], <span class="keyword">@selector</span>(testInstanceMethod));</span><br><span class="line"><span class="built_in">NSLog</span>(<span class="string">@"get InstanceMethod %@"</span>,<span class="built_in">NSStringFromSelector</span>(method_getName(method)));</span><br><span class="line"></span><br><span class="line"><span class="comment">//获取方法列表</span></span><br><span class="line">Method *methodList = class_copyMethodList([IDLTestObject <span class="keyword">class</span>], &amp;outCount);</span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">unsigned</span> <span class="keyword">int</span> index = <span class="number">0</span>; index &lt; outCount; index++) &#123;</span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@"method %u = %@"</span>,index,<span class="built_in">NSStringFromSelector</span>(method_getName(methodList[index])));</span><br><span class="line">&#125;</span><br><span class="line">free(methodList);</span><br><span class="line"></span><br><span class="line"><span class="comment">//获取类方法</span></span><br><span class="line">Method classMethod = class_getClassMethod([IDLTestObject <span class="keyword">class</span>], <span class="keyword">@selector</span>(testClassMethod));</span><br><span class="line"><span class="built_in">NSLog</span>(<span class="string">@"get classMethod %@"</span>,<span class="built_in">NSStringFromSelector</span>(method_getName(classMethod)));</span><br><span class="line"></span><br><span class="line">IMP testMethod = class_getMethodImplementation([IDLTestObject <span class="keyword">class</span>], <span class="keyword">@selector</span>(testInstanceMethod));</span><br><span class="line">testMethod();</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">//给某个类添加方法</span></span><br><span class="line">class_addMethod([IDLTestObject <span class="keyword">class</span>],</span><br><span class="line">                <span class="keyword">@selector</span>(addedMethod),</span><br><span class="line">                (IMP)addedMethodIMP,</span><br><span class="line">                <span class="string">"v@:"</span>);</span><br><span class="line"></span><br><span class="line">[[IDLTestObject new] performSelector:<span class="keyword">@selector</span>(addedMethod)];</span><br><span class="line"></span><br><span class="line">objc_property_attribute_t type = &#123; <span class="string">"T"</span>, <span class="string">"@\"NSString\""</span> &#125;;  <span class="comment">//T：类型</span></span><br><span class="line">objc_property_attribute_t ownership = &#123; <span class="string">"C"</span>, <span class="string">""</span> &#125;;          <span class="comment">// C：copy</span></span><br><span class="line">objc_property_attribute_t <span class="keyword">nonatomic</span> = &#123; <span class="string">"N"</span>, <span class="string">""</span> &#125;;          <span class="comment">// N：nonatomic</span></span><br><span class="line">objc_property_attribute_t ivar  = &#123; <span class="string">"V"</span>, <span class="string">"_addedMethod"</span> &#125;;  <span class="comment">//V： 实例变量变量名</span></span><br><span class="line">objc_property_attribute_t attributs[] = &#123; type, ownership,<span class="keyword">nonatomic</span>, ivar &#125;;</span><br><span class="line">class_addProperty([IDLTestObject <span class="keyword">class</span>], <span class="string">"addedMethod"</span>, attributs, <span class="number">4</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">//输出添加的成员变量</span></span><br><span class="line"><span class="keyword">unsigned</span> <span class="keyword">int</span> count;</span><br><span class="line">objc_property_t *propertyList = class_copyPropertyList([IDLTestObject <span class="keyword">class</span>], &amp;count);</span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">unsigned</span> <span class="keyword">int</span> i = <span class="number">0</span>; i&lt; count; i++) &#123;</span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">char</span> *name = property_getName(propertyList[i]);</span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@"属性%@ 属性信息%@"</span>,[<span class="built_in">NSString</span> stringWithUTF8String:name],</span><br><span class="line">            [<span class="built_in">NSString</span> stringWithUTF8String:property_getAttributes(propertyList[i])]);</span><br><span class="line">&#125;</span><br><span class="line">free(propertyList);</span><br><span class="line"></span><br><span class="line"><span class="comment">//替换方法</span></span><br><span class="line">class_replaceMethod([IDLTestObject <span class="keyword">class</span>],</span><br><span class="line">                    <span class="keyword">@selector</span>(testInstanceMethod),</span><br><span class="line">                    (IMP)addedMethodIMP,</span><br><span class="line">                    <span class="string">"v@:"</span>);</span><br><span class="line">[[[IDLTestObject alloc] init] testInstanceMethod];</span><br><span class="line"></span><br><span class="line"><span class="comment">//创建实例</span></span><br><span class="line">IDLTestObject *testClass = class_createInstance([IDLTestObject <span class="keyword">class</span>], <span class="number">0</span>);</span><br><span class="line">[testClass performSelector:<span class="keyword">@selector</span>(testInstanceMethod)];</span><br><span class="line"></span><br><span class="line">Class DynamicClass = objc_allocateClassPair([<span class="built_in">NSObject</span> <span class="keyword">class</span>], <span class="string">"DynamicClass"</span>, <span class="number">0</span>);</span><br><span class="line">class_addIvar(DynamicClass, <span class="string">"_attribute0"</span>, <span class="keyword">sizeof</span>(<span class="built_in">NSString</span> *), log(<span class="keyword">sizeof</span>(<span class="built_in">NSString</span> *)), <span class="string">"i"</span>);</span><br><span class="line">Ivar ivar = class_getInstanceVariable(DynamicClass, <span class="string">"_attribute0"</span>);</span><br><span class="line"><span class="built_in">NSLog</span>(<span class="string">@"DynamicClass var:%@"</span>,[<span class="built_in">NSString</span> stringWithUTF8String:ivar_getName(ivar)]);</span><br><span class="line">objc_registerClassPair(DynamicClass);</span><br></pre></td></tr></table></figure></p><p>输出结果如下：</p><p><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="number">2020</span><span class="number">-02</span><span class="number">-08</span> <span class="number">19</span><span class="string">:05:51.294372+0800</span> <span class="string">objc-debug[43702:387877]</span> <span class="attr">className :</span> <span class="string">IDLTestObject</span></span><br><span class="line"><span class="number">2020</span><span class="number">-02</span><span class="number">-08</span> <span class="number">19</span><span class="string">:05:51.296220+0800</span> <span class="string">objc-debug[43702:387877]</span> <span class="attr">superClass :</span> <span class="string">NSObject</span></span><br><span class="line"><span class="number">2020</span><span class="number">-02</span><span class="number">-08</span> <span class="number">19</span><span class="string">:05:51.297028+0800</span> <span class="string">objc-debug[43702:387877]</span> <span class="string">instanceSize</span> <span class="string">=</span> <span class="number">32</span></span><br><span class="line"><span class="number">2020</span><span class="number">-02</span><span class="number">-08</span> <span class="number">19</span><span class="string">:05:51.299444+0800</span> <span class="string">objc-debug[43702:387877]</span> <span class="string">InstanceVariable</span> <span class="string">:_name</span></span><br><span class="line"><span class="number">2020</span><span class="number">-02</span><span class="number">-08</span> <span class="number">19</span><span class="string">:05:51.306411+0800</span> <span class="string">objc-debug[43702:387877]</span> <span class="string">Instance</span> <span class="string">Variable</span> <span class="string">TypeEncoding:@"NSString"</span></span><br><span class="line"><span class="number">2020</span><span class="number">-02</span><span class="number">-08</span> <span class="number">19</span><span class="string">:05:51.306766+0800</span> <span class="string">objc-debug[43702:387877]</span> <span class="string">Instance</span> <span class="string">Variable</span> <span class="string">Offset:16</span></span><br><span class="line"><span class="number">2020</span><span class="number">-02</span><span class="number">-08</span> <span class="number">19</span><span class="string">:05:51.307020+0800</span> <span class="string">objc-debug[43702:387877]</span> <span class="string">var</span> <span class="number">0</span> <span class="string">=</span> <span class="string">privateInstanceVar</span></span><br><span class="line"><span class="number">2020</span><span class="number">-02</span><span class="number">-08</span> <span class="number">19</span><span class="string">:05:51.307322+0800</span> <span class="string">objc-debug[43702:387877]</span> <span class="string">var</span> <span class="number">1</span> <span class="string">=</span> <span class="string">_name</span></span><br><span class="line"><span class="number">2020</span><span class="number">-02</span><span class="number">-08</span> <span class="number">19</span><span class="string">:05:51.307559+0800</span> <span class="string">objc-debug[43702:387877]</span> <span class="string">var</span> <span class="number">2</span> <span class="string">=</span> <span class="string">_age</span></span><br><span class="line"><span class="number">2020</span><span class="number">-02</span><span class="number">-08</span> <span class="number">19</span><span class="string">:05:51.308250+0800</span> <span class="string">objc-debug[43702:387877]</span> <span class="string">Instance</span> <span class="string">Property</span> <span class="string">Name</span> <span class="string">:age</span></span><br><span class="line"><span class="number">2020</span><span class="number">-02</span><span class="number">-08</span> <span class="number">19</span><span class="string">:05:51.308553+0800</span> <span class="string">objc-debug[43702:387877]</span> <span class="string">Instance</span> <span class="string">Property</span> <span class="string">Attribute:Tq,N,V_age</span></span><br><span class="line"><span class="number">2020</span><span class="number">-02</span><span class="number">-08</span> <span class="number">19</span><span class="string">:05:51.308878+0800</span> <span class="string">objc-debug[43702:387877]</span> <span class="string">get</span> <span class="string">InstanceMethod</span> <span class="string">testInstanceMethod</span></span><br><span class="line"><span class="number">2020</span><span class="number">-02</span><span class="number">-08</span> <span class="number">19</span><span class="string">:05:51.309351+0800</span> <span class="string">objc-debug[43702:387877]</span> <span class="string">method</span> <span class="number">0</span> <span class="string">=</span> <span class="string">testInstanceMethod</span></span><br><span class="line"><span class="number">2020</span><span class="number">-02</span><span class="number">-08</span> <span class="number">19</span><span class="string">:05:51.309624+0800</span> <span class="string">objc-debug[43702:387877]</span> <span class="string">method</span> <span class="number">1</span> <span class="string">=</span> <span class="string">.cxx_destruct</span></span><br><span class="line"><span class="number">2020</span><span class="number">-02</span><span class="number">-08</span> <span class="number">19</span><span class="string">:05:51.334783+0800</span> <span class="string">objc-debug[43702:387877]</span> <span class="string">method</span> <span class="number">2</span> <span class="string">=</span> <span class="string">name</span></span><br><span class="line"><span class="number">2020</span><span class="number">-02</span><span class="number">-08</span> <span class="number">19</span><span class="string">:05:51.335325+0800</span> <span class="string">objc-debug[43702:387877]</span> <span class="string">method</span> <span class="number">3</span> <span class="string">=</span> <span class="attr">setName:</span></span><br><span class="line"><span class="number">2020</span><span class="number">-02</span><span class="number">-08</span> <span class="number">19</span><span class="string">:05:51.335856+0800</span> <span class="string">objc-debug[43702:387877]</span> <span class="string">method</span> <span class="number">4</span> <span class="string">=</span> <span class="string">age</span></span><br><span class="line"><span class="number">2020</span><span class="number">-02</span><span class="number">-08</span> <span class="number">19</span><span class="string">:05:51.336313+0800</span> <span class="string">objc-debug[43702:387877]</span> <span class="string">method</span> <span class="number">5</span> <span class="string">=</span> <span class="attr">setAge:</span></span><br><span class="line"><span class="number">2020</span><span class="number">-02</span><span class="number">-08</span> <span class="number">19</span><span class="string">:05:51.336745+0800</span> <span class="string">objc-debug[43702:387877]</span> <span class="string">get</span> <span class="string">classMethod</span> <span class="string">testClassMethod</span></span><br><span class="line"><span class="number">2020</span><span class="number">-02</span><span class="number">-08</span> <span class="number">19</span><span class="string">:05:51.336909+0800</span> <span class="string">objc-debug[43702:387877]</span> <span class="string">run</span> <span class="string">testInstanceMethod</span></span><br><span class="line"><span class="number">2020</span><span class="number">-02</span><span class="number">-08</span> <span class="number">19</span><span class="string">:05:51.340473+0800</span> <span class="string">objc-debug[43702:387877]</span> <span class="string">run</span> <span class="string">addedMethodIMP</span></span><br><span class="line"><span class="number">2020</span><span class="number">-02</span><span class="number">-08</span> <span class="number">19</span><span class="string">:05:51.351977+0800</span> <span class="string">objc-debug[43702:387877]</span> <span class="string">属性addedMethod</span> <span class="string">属性信息T@"NSString",C,N,V_addedMethod</span></span><br><span class="line"><span class="number">2020</span><span class="number">-02</span><span class="number">-08</span> <span class="number">19</span><span class="string">:05:51.352879+0800</span> <span class="string">objc-debug[43702:387877]</span> <span class="string">属性name</span> <span class="string">属性信息T@"NSString",&amp;,N,V_name</span></span><br><span class="line"><span class="number">2020</span><span class="number">-02</span><span class="number">-08</span> <span class="number">19</span><span class="string">:05:51.353429+0800</span> <span class="string">objc-debug[43702:387877]</span> <span class="string">属性age</span> <span class="string">属性信息Tq,N,V_age</span></span><br><span class="line"><span class="number">2020</span><span class="number">-02</span><span class="number">-08</span> <span class="number">19</span><span class="string">:05:51.353906+0800</span> <span class="string">objc-debug[43702:387877]</span> <span class="string">run</span> <span class="string">addedMethodIMP</span></span><br><span class="line"><span class="number">2020</span><span class="number">-02</span><span class="number">-08</span> <span class="number">19</span><span class="string">:05:51.354370+0800</span> <span class="string">objc-debug[43702:387877]</span> <span class="string">run</span> <span class="string">addedMethodIMP</span></span><br><span class="line"><span class="number">2020</span><span class="number">-02</span><span class="number">-08</span> <span class="number">19</span><span class="string">:05:51.354378+0800</span> <span class="string">objc-debug[71958:585781]</span> <span class="string">DynamicClass</span> <span class="string">var:_attribute0</span></span><br></pre></td></tr></table></figure></p><h6><strong><strong>Ivar实例变量相关：</strong></strong></h6><p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">//获取变量名</span></span><br><span class="line">OBJC_EXPORT <span class="keyword">const</span> <span class="keyword">char</span> * _Nullable</span><br><span class="line">ivar_getName(Ivar _Nonnull v) </span><br><span class="line"></span><br><span class="line"><span class="comment">//获取变量类型描述</span></span><br><span class="line">OBJC_EXPORT <span class="keyword">const</span> <span class="keyword">char</span> * _Nullable</span><br><span class="line">ivar_getTypeEncoding(Ivar _Nonnull v) </span><br><span class="line"></span><br><span class="line"><span class="comment">//获取变量基地址偏移量</span></span><br><span class="line">OBJC_EXPORT <span class="keyword">ptrdiff_t</span></span><br><span class="line">ivar_getOffset(Ivar _Nonnull v)</span><br></pre></td></tr></table></figure></p><p>上面的三个方法基本上对应于IVar的数据结构的各个字段，后面会单独介绍TypeEncoding和属性编码。</p><h6><strong><strong>Property属性相关：</strong></strong></h6><p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">//获取属性名</span></span><br><span class="line">OBJC_EXPORT <span class="keyword">const</span> <span class="keyword">char</span> * _Nonnull</span><br><span class="line">property_getName(<span class="keyword">objc_property_t</span> _Nonnull property) </span><br><span class="line"></span><br><span class="line"><span class="comment">//获取属性编码</span></span><br><span class="line">OBJC_EXPORT <span class="keyword">const</span> <span class="keyword">char</span> * _Nullable</span><br><span class="line">property_getAttributes(<span class="keyword">objc_property_t</span> _Nonnull property) </span><br><span class="line"></span><br><span class="line"><span class="comment">//获取属性列表返回的objc_property_attribute_t的结构如下：</span></span><br><span class="line"><span class="comment">//typedef struct &#123;</span></span><br><span class="line"><span class="comment">//        const char *name;</span></span><br><span class="line"><span class="comment">//        const char *value;</span></span><br><span class="line"><span class="comment">//&#125; objc_property_attribute_t;</span></span><br><span class="line">OBJC_EXPORT <span class="keyword">objc_property_attribute_t</span> * _Nullable</span><br><span class="line">property_copyAttributeList(<span class="keyword">objc_property_t</span> _Nonnull property,</span><br><span class="line">                           <span class="keyword">unsigned</span> <span class="keyword">int</span> * _Nullable outCount)</span><br><span class="line"></span><br><span class="line"><span class="comment">//获取属性的属性值</span></span><br><span class="line">OBJC_EXPORT <span class="keyword">char</span> * _Nullable</span><br><span class="line">property_copyAttributeValue(<span class="keyword">objc_property_t</span> _Nonnull property,</span><br><span class="line">                            <span class="keyword">const</span> <span class="keyword">char</span> * _Nonnull attributeName)</span><br></pre></td></tr></table></figure></p><h6><strong><strong>Method方法相关：</strong></strong></h6><p><figure class="highlight oxygene"><table><tr><td class="code"><pre><span class="line"><span class="comment">//获得方法名</span></span><br><span class="line">OBJC_EXPORT SEL _Nonnull</span><br><span class="line">method_getName(<span class="function"><span class="keyword">Method</span> _<span class="title">Nonnull</span> <span class="title">m</span>)</span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function">//获得方法的实现</span></span><br><span class="line"><span class="function"><span class="title">OBJC_EXPORT</span> <span class="title">IMP</span> _<span class="title">Nonnull</span></span></span><br><span class="line"><span class="function"><span class="title">method_getImplementation</span><span class="params">(<span class="keyword">Method</span> _Nonnull m)</span> </span></span><br><span class="line"><span class="function">    <span class="title">OBJC_AVAILABLE</span><span class="params">(10.5, 2.0, 9.0, 1.0, 2.0)</span>;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">//获得参数和返回值的类型编码</span></span><br><span class="line">OBJC_EXPORT <span class="keyword">const</span> char * _Nullable</span><br><span class="line">method_getTypeEncoding(<span class="function"><span class="keyword">Method</span> _<span class="title">Nonnull</span> <span class="title">m</span>) </span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function">//获得参数数量</span></span><br><span class="line"><span class="function"><span class="title">OBJC_EXPORT</span> <span class="title">unsigned</span> <span class="title">int</span></span></span><br><span class="line"><span class="function"><span class="title">method_getNumberOfArguments</span><span class="params">(<span class="keyword">Method</span> _Nonnull m)</span></span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function">//获得返回类型</span></span><br><span class="line"><span class="function"><span class="title">OBJC_EXPORT</span> <span class="title">char</span> * _<span class="title">Nonnull</span></span></span><br><span class="line"><span class="function"><span class="title">method_copyReturnType</span><span class="params">(<span class="keyword">Method</span> _Nonnull m)</span> </span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function">//获得参数类型</span></span><br><span class="line"><span class="function"><span class="title">OBJC_EXPORT</span> <span class="title">char</span> * _<span class="title">Nullable</span></span></span><br><span class="line"><span class="function"><span class="title">method_copyArgumentType</span><span class="params">(<span class="keyword">Method</span> _Nonnull m, unsigned int <span class="keyword">index</span>)</span> </span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function">//获得返回类型，指定存储空间</span></span><br><span class="line"><span class="function"><span class="title">OBJC_EXPORT</span> <span class="title">void</span></span></span><br><span class="line"><span class="function"><span class="title">method_getReturnType</span><span class="params">(<span class="keyword">Method</span> _Nonnull m, char * _Nonnull dst, size_t dst_len)</span> </span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function">//获得参数类型，指定存储空间</span></span><br><span class="line"><span class="function"><span class="title">OBJC_EXPORT</span> <span class="title">void</span></span></span><br><span class="line"><span class="function"><span class="title">method_getArgumentType</span><span class="params">(<span class="keyword">Method</span> _Nonnull m, unsigned int <span class="keyword">index</span>, </span></span></span><br><span class="line"><span class="function"><span class="params">                       char * _Nullable dst, size_t dst_len)</span> </span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function">//获得方法描述</span></span><br><span class="line"><span class="function"><span class="title">OBJC_EXPORT</span> <span class="title">struct</span> <span class="title">objc_method_description</span> * _<span class="title">Nonnull</span></span></span><br><span class="line"><span class="function"><span class="title">method_getDescription</span><span class="params">(<span class="keyword">Method</span> _Nonnull m)</span> </span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function">设置方法实现</span></span><br><span class="line"><span class="function"><span class="title">OBJC_EXPORT</span> <span class="title">IMP</span> _<span class="title">Nonnull</span></span></span><br><span class="line"><span class="function"><span class="title">method_setImplementation</span><span class="params">(<span class="keyword">Method</span> _Nonnull m, IMP _Nonnull imp)</span> </span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function">//交换两个方法的实现</span></span><br><span class="line"><span class="function"><span class="title">OBJC_EXPORT</span> <span class="title">void</span></span></span><br><span class="line"><span class="function"><span class="title">method_exchangeImplementations</span><span class="params">(<span class="keyword">Method</span> _Nonnull m1, <span class="keyword">Method</span> _Nonnull m2)</span></span></span><br></pre></td></tr></table></figure></p><p>之前博客已经介绍过，方法主要有方法名SEL,包含方法参数，返回值类型的编码，以及实现体IMP.这里最重要的是类型编码在后面统一介绍。这里还增加了可以分开获取方法参数和返回值类型编码的方法，以及方法参数数量。</p><h6><strong><strong>SEL相关：</strong></strong></h6><p><figure class="highlight sqf"><table><tr><td class="code"><pre><span class="line"><span class="comment">//返回SEL名字</span></span><br><span class="line">OBJC_EXPORT const char * <span class="variable">_Nonnull</span> sel_getName(SEL <span class="variable">_Nonnull</span> sel)</span><br><span class="line"></span><br><span class="line"><span class="comment">//在系统中注册一个方法，将方法名映射到一个选择器，并返回这个选择器</span></span><br><span class="line">OBJC_EXPORT SEL <span class="variable">_Nonnull</span> sel_registerName(const char * <span class="variable">_Nonnull</span> <span class="built_in">str</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment">//对比方法选择器</span></span><br><span class="line">OBJC_EXPORT BOOL sel_isEqual(SEL <span class="variable">_Nonnull</span> lhs, SEL <span class="variable">_Nonnull</span> rhs)</span><br></pre></td></tr></table></figure></p><h6><strong><strong>协议Protocal相关：</strong></strong></h6><p><figure class="highlight sqf"><table><tr><td class="code"><pre><span class="line"><span class="comment">//返回一个指定的协议</span></span><br><span class="line">OBJC_EXPORT Protocol * <span class="variable">_Nullable</span> objc_getProtocol(const char * <span class="variable">_Nonnull</span> <span class="built_in">name</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">//返回运行时所有的协议</span></span><br><span class="line">OBJC_EXPORT Protocol * <span class="variable">__unsafe_unretained</span> <span class="variable">_Nonnull</span> * <span class="variable">_Nullable</span></span><br><span class="line">objc_copyProtocolList(unsigned int * <span class="variable">_Nullable</span> outCount);</span><br><span class="line"></span><br><span class="line"><span class="comment">//返回某个协议是否遵循另一个协议</span></span><br><span class="line">OBJC_EXPORT BOOL</span><br><span class="line">protocol_conformsToProtocol(Protocol * <span class="variable">_Nullable</span> proto,</span><br><span class="line">                            Protocol * <span class="variable">_Nullable</span> other)</span><br><span class="line"></span><br><span class="line"><span class="comment">//返回协议是否相等</span></span><br><span class="line">OBJC_EXPORT BOOL</span><br><span class="line">protocol_isEqual(Protocol * <span class="variable">_Nullable</span> proto, Protocol * <span class="variable">_Nullable</span> other)</span><br><span class="line"></span><br><span class="line"><span class="comment">//返回协议名</span></span><br><span class="line">OBJC_EXPORT const char * <span class="variable">_Nonnull</span></span><br><span class="line">protocol_getName(Protocol * <span class="variable">_Nonnull</span> proto)</span><br><span class="line"></span><br><span class="line"><span class="comment">//返回协议的某个方法描述</span></span><br><span class="line">OBJC_EXPORT struct objc_method_description</span><br><span class="line">protocol_getMethodDescription(Protocol * <span class="variable">_Nonnull</span> proto, SEL <span class="variable">_Nonnull</span> aSel,</span><br><span class="line">                              BOOL isRequiredMethod, BOOL isInstanceMethod)</span><br><span class="line"></span><br><span class="line"><span class="comment">//返回协议的所有方法描述</span></span><br><span class="line">OBJC_EXPORT struct objc_method_description * <span class="variable">_Nullable</span></span><br><span class="line">protocol_copyMethodDescriptionList(Protocol * <span class="variable">_Nonnull</span> proto,</span><br><span class="line">                                   BOOL isRequiredMethod,</span><br><span class="line">                                   BOOL isInstanceMethod,</span><br><span class="line">                                   unsigned int * <span class="variable">_Nullable</span> outCount)</span><br><span class="line"></span><br><span class="line"><span class="comment">//返回协议指定的属性</span></span><br><span class="line">OBJC_EXPORT objc_property_t <span class="variable">_Nullable</span></span><br><span class="line">protocol_getProperty(Protocol * <span class="variable">_Nonnull</span> proto,</span><br><span class="line">                     const char * <span class="variable">_Nonnull</span> <span class="built_in">name</span>,</span><br><span class="line">                     BOOL isRequiredProperty, BOOL isInstanceProperty)</span><br><span class="line"></span><br><span class="line"><span class="comment">//返回协议的所有属性</span></span><br><span class="line">OBJC_EXPORT objc_property_t <span class="variable">_Nonnull</span> * <span class="variable">_Nullable</span></span><br><span class="line">protocol_copyPropertyList(Protocol * <span class="variable">_Nonnull</span> proto,</span><br><span class="line">                          unsigned int * <span class="variable">_Nullable</span> outCount)</span><br><span class="line"></span><br><span class="line">OBJC_EXPORT objc_property_t <span class="variable">_Nonnull</span> * <span class="variable">_Nullable</span></span><br><span class="line">protocol_copyPropertyList2(Protocol * <span class="variable">_Nonnull</span> proto,</span><br><span class="line">                           unsigned int * <span class="variable">_Nullable</span> outCount,</span><br><span class="line">                           BOOL isRequiredProperty, BOOL isInstanceProperty)</span><br><span class="line"><span class="comment">//返回某个协议遵循的协议列表</span></span><br><span class="line">OBJC_EXPORT Protocol * <span class="variable">__unsafe_unretained</span> <span class="variable">_Nonnull</span> * <span class="variable">_Nullable</span></span><br><span class="line">protocol_copyProtocolList(Protocol * <span class="variable">_Nonnull</span> proto,</span><br><span class="line">                          unsigned int * <span class="variable">_Nullable</span> outCount)</span><br></pre></td></tr></table></figure></p><p>动态创建协议</p><p><figure class="highlight objectivec"><table><tr><td class="code"><pre><span class="line"><span class="comment">//创建协议</span></span><br><span class="line">OBJC_EXPORT Protocol * _Nullable</span><br><span class="line">objc_allocateProtocol(<span class="keyword">const</span> <span class="keyword">char</span> * _Nonnull name) </span><br><span class="line"></span><br><span class="line"><span class="comment">//向Runtime注册协议</span></span><br><span class="line">OBJC_EXPORT <span class="keyword">void</span></span><br><span class="line">objc_registerProtocol(Protocol * _Nonnull proto) </span><br><span class="line">    OBJC_AVAILABLE(<span class="number">10.7</span>, <span class="number">4.3</span>, <span class="number">9.0</span>, <span class="number">1.0</span>, <span class="number">2.0</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">//向协议添加方法描述</span></span><br><span class="line">OBJC_EXPORT <span class="keyword">void</span></span><br><span class="line">protocol_addMethodDescription(Protocol * _Nonnull proto, SEL _Nonnull name,</span><br><span class="line">                              <span class="keyword">const</span> <span class="keyword">char</span> * _Nullable types,</span><br><span class="line">                              <span class="built_in">BOOL</span> isRequiredMethod, <span class="built_in">BOOL</span> isInstanceMethod) </span><br><span class="line"></span><br><span class="line"><span class="comment">//添加协议</span></span><br><span class="line">OBJC_EXPORT <span class="keyword">void</span></span><br><span class="line">protocol_addProtocol(Protocol * _Nonnull proto, Protocol * _Nonnull addition) </span><br><span class="line">    OBJC_AVAILABLE(<span class="number">10.7</span>, <span class="number">4.3</span>, <span class="number">9.0</span>, <span class="number">1.0</span>, <span class="number">2.0</span>);</span><br><span class="line"></span><br><span class="line">给协议添加属性</span><br><span class="line">OBJC_EXPORT <span class="keyword">void</span></span><br><span class="line">protocol_addProperty(Protocol * _Nonnull proto, <span class="keyword">const</span> <span class="keyword">char</span> * _Nonnull name,</span><br><span class="line">                     <span class="keyword">const</span> objc_property_attribute_t * _Nullable attributes,</span><br><span class="line">                     <span class="keyword">unsigned</span> <span class="keyword">int</span> attributeCount,</span><br><span class="line">                     <span class="built_in">BOOL</span> isRequiredProperty, <span class="built_in">BOOL</span> isInstanceProperty)</span><br></pre></td></tr></table></figure></p><h6>**** 类型编码与属性编码 ****</h6><p>编译器将每个方法的返回值和参数类型编码为一个字符串，并将其与方法的selector关联在一起。同时每个属性的类型也进行了编码，下面是这两部分的官方文档，供大家查阅使用。</p><ul><li><p><a href="https://developer.apple.com/library/archive/documentation/Cocoa/Conceptual/ObjCRuntimeGuide/Articles/ocrtTypeEncodings.html" target="_blank" rel="noopener">Type Encodings</a></p></li><li><p><a href="https://developer.apple.com/library/archive/documentation/Cocoa/Conceptual/ObjCRuntimeGuide/Articles/ocrtPropertyIntrospection.html" target="_blank" rel="noopener">Property Type and Functions</a></p></li><li><p><strong><strong>类型编码</strong></strong>：</p></li></ul><p>我们给出一个例子,下面的例子会将方法的返回值以及参数编码打印出来：</p><p>这个方法如下所示：<figure class="highlight groovy"><table><tr><td class="code"><pre><span class="line">+ (IDLReturnType *)<span class="string">testTypeEncoding:</span>(NSString *)strValue <span class="string">intValue:</span>(NSInteger)intValue &#123;</span><br><span class="line">    <span class="keyword">return</span> [IDLReturnType <span class="keyword">new</span>];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p><figure class="highlight lisp"><table><tr><td class="code"><pre><span class="line">Method testMethods = class_getClassMethod([IDLTestObject class], @selector(<span class="name">testTypeEncoding</span><span class="symbol">:intValue</span>:))<span class="comment">;</span></span><br><span class="line">NSLog(@<span class="string">"Type Encoding : %@"</span>,[NSString stringWithUTF8String<span class="symbol">:method_getTypeEncoding</span>(<span class="name">testMethods</span>)])<span class="comment">;</span></span><br><span class="line">char *returnType = method_copyReturnType(<span class="name">testMethods</span>)<span class="comment">;</span></span><br><span class="line">NSLog(@<span class="string">"Return Type: %@"</span>,[NSString stringWithUTF8String<span class="symbol">:returnType</span>])<span class="comment">;</span></span><br><span class="line">free(<span class="name">returnType</span>)<span class="comment">;</span></span><br><span class="line"></span><br><span class="line">NSInteger numberOfArguments = method_getNumberOfArguments(<span class="name">testMethods</span>)<span class="comment">;</span></span><br><span class="line">char *argument = NULL<span class="comment">;</span></span><br><span class="line">for (<span class="name">unsigned</span> int index = <span class="number">0</span><span class="comment">; index &lt; numberOfArguments; index++) &#123;</span></span><br><span class="line">    argument = method_copyArgumentType(<span class="name">testMethods</span>, index)<span class="comment">;</span></span><br><span class="line">    NSLog(@<span class="string">"Index %d Return Type: %@"</span>, index,[NSString stringWithUTF8String<span class="symbol">:argument</span>])<span class="comment">;</span></span><br><span class="line">&#125;</span><br><span class="line">free(<span class="name">argument</span>)<span class="comment">;</span></span><br></pre></td></tr></table></figure></p><p>输出内容为：<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="number">2020</span><span class="number">-02</span><span class="number">-09</span> <span class="number">10</span><span class="string">:21:22.231254+0800</span> <span class="string">objc-debug[84523:1305673]</span> <span class="attr">Type Encoding :</span> <span class="string">@32@0:8@16q24</span></span><br><span class="line"><span class="number">2020</span><span class="number">-02</span><span class="number">-09</span> <span class="number">10</span><span class="string">:21:22.231343+0800</span> <span class="string">objc-debug[84523:1305673]</span> <span class="attr">Return Type:</span> <span class="string">@</span></span><br><span class="line"><span class="number">2020</span><span class="number">-02</span><span class="number">-09</span> <span class="number">10</span><span class="string">:21:22.231425+0800</span> <span class="string">objc-debug[84523:1305673]</span> <span class="attr">Index 0 Return Type:</span> <span class="string">@</span></span><br><span class="line"><span class="number">2020</span><span class="number">-02</span><span class="number">-09</span> <span class="number">10</span><span class="string">:21:22.231503+0800</span> <span class="string">objc-debug[84523:1305673]</span> <span class="attr">Index 1 Return Type: :</span></span><br><span class="line"><span class="number">2020</span><span class="number">-02</span><span class="number">-09</span> <span class="number">10</span><span class="string">:21:22.231579+0800</span> <span class="string">objc-debug[84523:1305673]</span> <span class="attr">Index 2 Return Type:</span> <span class="string">@</span></span><br><span class="line"><span class="number">2020</span><span class="number">-02</span><span class="number">-09</span> <span class="number">10</span><span class="string">:21:22.231653+0800</span> <span class="string">objc-debug[84523:1305673]</span> <span class="attr">Index 3 Return Type:</span> <span class="string">q</span></span><br></pre></td></tr></table></figure></p><p>我们看****@32@0:8@16q24****这实际上就是方法的类型编码。我们先看下类型编码表：</p><p><img src="/2020/02/06/iOS-Runtime应用之带大家熟悉Runtime-API/00001.png" alt>这里每个部分都包含两位，前面一个表示编码，后面表示这个数据对应的偏移量，返回的类型数据会将返回值放在最后面，所以****@32@0:8@16q24****对应部分的意义如下：</p><p><figure class="highlight less"><table><tr><td class="code"><pre><span class="line"><span class="variable">@32</span> 返回值编码为@，也就是id类型，所处的偏移量为<span class="number">32</span>。也就是q24的后面</span><br><span class="line"><span class="variable">@0</span>  第一个参数的编码为@，也就是id类型，所处的偏移量为<span class="number">0</span>，由于第一个参数恒为方法的接收者，所以是id类型。</span><br><span class="line">:<span class="number">8</span>  第二个参数的编码为: 也就是SEL类型，所处的偏移量为<span class="number">8</span>，由于第二个参数恒为_cmd所以是SEL类型。</span><br><span class="line"><span class="variable">@16</span> 第三个参数的编码为@ 也就是id类型，所处的偏移量为<span class="number">16</span>，上面第三个参数为NSString*</span><br><span class="line">q24 第四个参数的编码为q 也就是long long类型，由于用的是<span class="number">64</span>位机器所以对得上。</span><br></pre></td></tr></table></figure></p><p>Runtime为了我们方便，为我们提供了@encode编译器指令来获取它，当给定一个类型时，@encode返回这个类型的字符串编码，如下所示：</p><p><figure class="highlight less"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">NSLog</span>(@<span class="string">"char     : %s, %lu"</span>, <span class="variable">@encode</span>(char), sizeof(char));</span><br><span class="line"><span class="selector-tag">NSLog</span>(@<span class="string">"short    : %s, %lu"</span>, <span class="variable">@encode</span>(short), sizeof(short));</span><br><span class="line"><span class="selector-tag">NSLog</span>(@<span class="string">"int      : %s, %lu"</span>, <span class="variable">@encode</span>(int), sizeof(int));</span><br><span class="line"><span class="selector-tag">NSLog</span>(@<span class="string">"long     : %s, %lu"</span>, <span class="variable">@encode</span>(long), sizeof(long));</span><br><span class="line"><span class="selector-tag">NSLog</span>(@<span class="string">"long long: %s, %lu"</span>, <span class="variable">@encode</span>(long long), sizeof(long long));</span><br><span class="line"><span class="selector-tag">NSLog</span>(@<span class="string">"float    : %s, %lu"</span>, <span class="variable">@encode</span>(float), sizeof(float));</span><br><span class="line"><span class="selector-tag">NSLog</span>(@<span class="string">"double   : %s, %lu"</span>, <span class="variable">@encode</span>(double), sizeof(double));</span><br><span class="line"><span class="selector-tag">NSLog</span>(@<span class="string">"NSInteger: %s, %lu"</span>, <span class="variable">@encode</span>(NSInteger), sizeof(NSInteger));</span><br><span class="line"><span class="selector-tag">NSLog</span>(@<span class="string">"CGFloat  : %s, %lu"</span>, <span class="variable">@encode</span>(CGFloat), sizeof(CGFloat));</span><br><span class="line"><span class="selector-tag">NSLog</span>(@<span class="string">"int32_t  : %s, %lu"</span>, <span class="variable">@encode</span>(int32_t), sizeof(int32_t));</span><br><span class="line"><span class="selector-tag">NSLog</span>(@<span class="string">"int64_t  : %s, %lu"</span>, <span class="variable">@encode</span>(int64_t), sizeof(int64_t))</span><br></pre></td></tr></table></figure></p><p><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="number">2020</span><span class="number">-02</span><span class="number">-09</span> <span class="number">11</span><span class="string">:04:28.116484+0800</span> <span class="string">objc-debug[91476:1363542]</span> <span class="attr">char     :</span> <span class="string">c,</span> <span class="number">1</span></span><br><span class="line"><span class="number">2020</span><span class="number">-02</span><span class="number">-09</span> <span class="number">11</span><span class="string">:04:28.116991+0800</span> <span class="string">objc-debug[91476:1363542]</span> <span class="attr">short    :</span> <span class="string">s,</span> <span class="number">2</span></span><br><span class="line"><span class="number">2020</span><span class="number">-02</span><span class="number">-09</span> <span class="number">11</span><span class="string">:04:28.117745+0800</span> <span class="string">objc-debug[91476:1363542]</span> <span class="attr">int      :</span> <span class="string">i,</span> <span class="number">4</span></span><br><span class="line"><span class="number">2020</span><span class="number">-02</span><span class="number">-09</span> <span class="number">11</span><span class="string">:04:28.118195+0800</span> <span class="string">objc-debug[91476:1363542]</span> <span class="attr">long     :</span> <span class="string">q,</span> <span class="number">8</span></span><br><span class="line"><span class="number">2020</span><span class="number">-02</span><span class="number">-09</span> <span class="number">11</span><span class="string">:04:28.118943+0800</span> <span class="string">objc-debug[91476:1363542]</span> <span class="attr">long long:</span> <span class="string">q,</span> <span class="number">8</span></span><br><span class="line"><span class="number">2020</span><span class="number">-02</span><span class="number">-09</span> <span class="number">11</span><span class="string">:04:28.121076+0800</span> <span class="string">objc-debug[91476:1363542]</span> <span class="attr">float    :</span> <span class="string">f,</span> <span class="number">4</span></span><br><span class="line"><span class="number">2020</span><span class="number">-02</span><span class="number">-09</span> <span class="number">11</span><span class="string">:04:28.123510+0800</span> <span class="string">objc-debug[91476:1363542]</span> <span class="attr">double   :</span> <span class="string">d,</span> <span class="number">8</span></span><br><span class="line"><span class="number">2020</span><span class="number">-02</span><span class="number">-09</span> <span class="number">11</span><span class="string">:04:28.124016+0800</span> <span class="string">objc-debug[91476:1363542]</span> <span class="attr">NSInteger:</span> <span class="string">q,</span> <span class="number">8</span></span><br><span class="line"><span class="number">2020</span><span class="number">-02</span><span class="number">-09</span> <span class="number">11</span><span class="string">:04:28.124132+0800</span> <span class="string">objc-debug[91476:1363542]</span> <span class="attr">CGFloat  :</span> <span class="string">d,</span> <span class="number">8</span></span><br><span class="line"><span class="number">2020</span><span class="number">-02</span><span class="number">-09</span> <span class="number">11</span><span class="string">:04:28.124211+0800</span> <span class="string">objc-debug[91476:1363542]</span> <span class="attr">int32_t  :</span> <span class="string">i,</span> <span class="number">4</span></span><br><span class="line"><span class="number">2020</span><span class="number">-02</span><span class="number">-09</span> <span class="number">11</span><span class="string">:04:28.124283+0800</span> <span class="string">objc-debug[91476:1363542]</span> <span class="attr">int64_t  :</span> <span class="string">q,</span> <span class="number">8</span></span><br></pre></td></tr></table></figure></p><p>一般我们会使用：</p><p><figure class="highlight lisp"><table><tr><td class="code"><pre><span class="line">method_copyArgumentType(<span class="name">testMethods</span>, index)<span class="comment">;</span></span><br></pre></td></tr></table></figure></p><p>这种方式，避免涉及到偏移量等数据，直接获取参数类型。</p><ul><li><strong><strong>属性编码</strong></strong>：</li></ul><p>前面提到一个例子：</p><p><figure class="highlight crmsh"><table><tr><td class="code"><pre><span class="line">objc_property_t <span class="keyword">property</span><span class="title"> </span>= class_getProperty([IDLTestObject class], [@<span class="string">"age"</span> UTF8String]);</span><br><span class="line">NSLog(@<span class="string">"Instance Property Name :%@"</span>,[NSString stringWithUTF8String:property_getName(<span class="keyword">property</span><span class="title"></span>)]);</span><br><span class="line">NSLog(@<span class="string">"Instance Property Attribute:%@"</span>,[NSString stringWithUTF8String:property_getAttributes(<span class="keyword">property</span><span class="title"></span>)]);</span><br></pre></td></tr></table></figure></p><p>age属性的声明如下所示：<figure class="highlight objectivec"><table><tr><td class="code"><pre><span class="line"><span class="keyword">@property</span>(<span class="keyword">nonatomic</span>, <span class="keyword">copy</span>, <span class="keyword">readonly</span>,<span class="keyword">getter</span>=ageValue,<span class="keyword">setter</span>=setAageValue:) <span class="built_in">NSString</span> *age;</span><br></pre></td></tr></table></figure></p><p>输出结果为：</p><p><figure class="highlight routeros"><table><tr><td class="code"><pre><span class="line">2020-02-09 11:23:38.356033+0800 objc-<span class="builtin-name">debug</span>[94653:1391447]<span class="built_in"> Instance </span>Property Attribute:T@<span class="string">"NSString"</span>,R,C,N,GageValue,SsetAageValue:,V_age</span><br></pre></td></tr></table></figure></p><p><img src="/2020/02/06/iOS-Runtime应用之带大家熟悉Runtime-API/00002.png" alt></p><p>我们对照上表给大家分析：属性编码是通过逗号进行分隔开的，<strong><strong>T@&quot;NSString&quot;,R,C,N,GageValue,SsetAageValue:,V_age</strong></strong> 表示的意义如下：</p><p><figure class="highlight asciidoc"><table><tr><td class="code"><pre><span class="line"><span class="bullet">* </span>T@"NSString" 属性类型为NSString</span><br><span class="line"><span class="bullet">* </span>R 属性类型为ReadOnly类型</span><br><span class="line"><span class="bullet">* </span>C 属性存储属性为Copy</span><br><span class="line"><span class="bullet">* </span>N 属性为nonatomic</span><br><span class="line"><span class="bullet">* </span>GageValue 属性指定getter方法为ageValue</span><br><span class="line"><span class="bullet">* </span>SsetAageValue: 属性指定setter方法为setAageValue:</span><br><span class="line"><span class="bullet">* </span>V<span class="emphasis">_age 属性对应的实例变量为_</span>age</span><br></pre></td></tr></table></figure></p><p>对于类型编码和属性编码大致就这些。大家可以通过下面的表格熟悉下属性编码：</p><p><img src="/2020/02/06/iOS-Runtime应用之带大家熟悉Runtime-API/00003.png" alt></p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;本打算这个系列结束的，但是由于疫情影响在家的这几天有空闲时间，想着把这块也整理下，今天这篇文章打算带大家熟悉下Runtime API.其这部分和Runtime重要的数据结构紧密相关，通过&lt;strong&gt;&lt;strong&gt;objc/runtime.h&lt;/strong&gt;&lt;/str
      
    
    </summary>
    
      <category term="Objective C 相关" scheme="http://yoursite.com/categories/Objective-C-%E7%9B%B8%E5%85%B3/"/>
    
    
      <category term="Objective C 相关" scheme="http://yoursite.com/tags/Objective-C-%E7%9B%B8%E5%85%B3/"/>
    
  </entry>
  
  <entry>
    <title>iOS Runtime应用之多继承</title>
    <link href="http://yoursite.com/2020/02/05/iOS-Runtime%E5%BA%94%E7%94%A8%E4%B9%8B%E5%A4%9A%E7%BB%A7%E6%89%BF/"/>
    <id>http://yoursite.com/2020/02/05/iOS-Runtime应用之多继承/</id>
    <published>2020-02-04T16:33:56.000Z</published>
    <updated>2020-02-04T17:52:10.000Z</updated>
    
    <content type="html"><![CDATA[<p>下面是模拟IDLPainterDeveloper继承IDLDeveloper以及IDLPainter的例子，直接上代码吧：</p><p><figure class="highlight objectivec"><table><tr><td class="code"><pre><span class="line"><span class="meta">#import <span class="meta-string">&lt;Foundation/Foundation.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="built_in">NS_ASSUME_NONNULL_BEGIN</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">@interface</span> <span class="title">IDLDeveloper</span> : <span class="title">NSObject</span></span></span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)coding;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">NS_ASSUME_NONNULL_END</span></span><br></pre></td></tr></table></figure></p><p><figure class="highlight objectivec"><table><tr><td class="code"><pre><span class="line"><span class="meta">#import <span class="meta-string">"IDLDeveloper.h"</span></span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">@implementation</span> <span class="title">IDLDeveloper</span></span></span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)coding &#123;</span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@"I Can coding!"</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br></pre></td></tr></table></figure></p><p><figure class="highlight objectivec"><table><tr><td class="code"><pre><span class="line"><span class="meta">#import <span class="meta-string">&lt;Foundation/Foundation.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="built_in">NS_ASSUME_NONNULL_BEGIN</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">@interface</span> <span class="title">IDLPainter</span> : <span class="title">NSObject</span></span></span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)drawing;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">NS_ASSUME_NONNULL_END</span></span><br></pre></td></tr></table></figure></p><p><figure class="highlight objectivec"><table><tr><td class="code"><pre><span class="line"><span class="meta">#import <span class="meta-string">"IDLPainter.h"</span></span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">@implementation</span> <span class="title">IDLPainter</span></span></span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)drawing &#123;</span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@"I Can Drawing!"</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br></pre></td></tr></table></figure></p><p><figure class="highlight objectivec"><table><tr><td class="code"><pre><span class="line"><span class="built_in">NS_ASSUME_NONNULL_BEGIN</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">@interface</span> <span class="title">IDLPainterDeveloper</span> : <span class="title">NSProxy</span></span></span><br><span class="line"></span><br><span class="line">+ (<span class="keyword">instancetype</span>)createPerson;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">NS_ASSUME_NONNULL_END</span></span><br></pre></td></tr></table></figure></p><p><figure class="highlight objectivec"><table><tr><td class="code"><pre><span class="line"><span class="meta">#import <span class="meta-string">"IDLPainterDeveloper.h"</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">//super</span></span><br><span class="line"><span class="meta">#import <span class="meta-string">"IDLPainter.h"</span></span></span><br><span class="line"><span class="meta">#import <span class="meta-string">"IDLDeveloper.h"</span></span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">@interface</span> <span class="title">IDLPainterDeveloper</span> ()</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">@property</span>(<span class="keyword">nonatomic</span>, <span class="keyword">strong</span>, <span class="keyword">readwrite</span>) <span class="built_in">NSMutableDictionary</span> *methodDictionary;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">@implementation</span> <span class="title">IDLPainterDeveloper</span></span></span><br><span class="line"></span><br><span class="line">+ (<span class="keyword">instancetype</span>)createPerson &#123;</span><br><span class="line">    <span class="keyword">return</span> [[IDLPainterDeveloper alloc] init];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (<span class="keyword">instancetype</span>)init &#123;</span><br><span class="line">    </span><br><span class="line">    IDLPainter   *painter   = [[IDLPainter alloc] init];</span><br><span class="line">    IDLDeveloper *developer = [[IDLDeveloper alloc] init];</span><br><span class="line">    </span><br><span class="line">    [<span class="keyword">self</span> inheriteMethodsFromSuperTarget:painter];</span><br><span class="line">    [<span class="keyword">self</span> inheriteMethodsFromSuperTarget:developer];</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">self</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)inheriteMethodsFromSuperTarget:(<span class="keyword">id</span>)target&#123;</span><br><span class="line">    <span class="keyword">if</span>(!target) <span class="keyword">return</span>;</span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">int</span> numberOfMethods = <span class="number">0</span>;</span><br><span class="line">    Method *methodList = class_copyMethodList([target <span class="keyword">class</span>], &amp;numberOfMethods);</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; numberOfMethods; i++) &#123;</span><br><span class="line">        SEL sel = method_getName(methodList[i]);</span><br><span class="line">        <span class="keyword">const</span> <span class="keyword">char</span> *methodName = sel_getName(sel);</span><br><span class="line">        [<span class="keyword">self</span>.methodDictionary setObject:target forKey:[<span class="built_in">NSString</span> stringWithUTF8String:methodName]];</span><br><span class="line">    &#125;</span><br><span class="line">    free(methodList);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)forwardInvocation:(<span class="built_in">NSInvocation</span> *)invocation&#123;</span><br><span class="line">    SEL sel = invocation.selector;</span><br><span class="line">    <span class="built_in">NSString</span> *methodName = <span class="built_in">NSStringFromSelector</span>(sel);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">id</span> target = <span class="keyword">self</span>.methodDictionary[methodName];</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span> (target &amp;&amp; [target respondsToSelector:sel]) &#123;</span><br><span class="line">        [invocation invokeWithTarget:target];</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        [<span class="keyword">super</span> forwardInvocation:invocation];</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (<span class="built_in">NSMethodSignature</span> *)methodSignatureForSelector:(SEL)sel&#123;</span><br><span class="line">    <span class="built_in">NSString</span> *methodName = <span class="built_in">NSStringFromSelector</span>(sel);</span><br><span class="line">    <span class="keyword">id</span> target = <span class="keyword">self</span>.methodDictionary[methodName];</span><br><span class="line">    <span class="keyword">if</span> (target &amp;&amp; [target respondsToSelector:sel]) &#123;</span><br><span class="line">        <span class="keyword">return</span> [target methodSignatureForSelector:sel];</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> [<span class="keyword">super</span> methodSignatureForSelector:sel];</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (<span class="built_in">NSMutableDictionary</span> *)methodDictionary &#123;</span><br><span class="line">    <span class="keyword">if</span>(!_methodDictionary) &#123;</span><br><span class="line">        _methodDictionary = [[<span class="built_in">NSMutableDictionary</span> alloc] init];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> _methodDictionary;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br></pre></td></tr></table></figure></p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;下面是模拟IDLPainterDeveloper继承IDLDeveloper以及IDLPainter的例子，直接上代码吧：&lt;/p&gt;
&lt;p&gt;&lt;figure class=&quot;highlight objectivec&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;code&quot;&gt;&lt;pr
      
    
    </summary>
    
      <category term="Objective C 相关" scheme="http://yoursite.com/categories/Objective-C-%E7%9B%B8%E5%85%B3/"/>
    
    
      <category term="Objective C 相关" scheme="http://yoursite.com/tags/Objective-C-%E7%9B%B8%E5%85%B3/"/>
    
  </entry>
  
  <entry>
    <title>iOS Runtime应用总结</title>
    <link href="http://yoursite.com/2020/02/02/iOS-Runtime%E5%BA%94%E7%94%A8%E6%80%BB%E7%BB%93/"/>
    <id>http://yoursite.com/2020/02/02/iOS-Runtime应用总结/</id>
    <published>2020-02-01T23:52:46.000Z</published>
    <updated>2020-02-09T12:56:04.000Z</updated>
    
    <content type="html"><![CDATA[<p>Runtime 用处蛮多的但是归结起来有如下几个：</p><h5><strong><strong>1. 使用Runtime获得对象的属性，方法，协议等信息：</strong></strong></h5><p><figure class="highlight asciidoc"><table><tr><td class="code"><pre><span class="line"><span class="bullet">* </span>实现NSCoding的自动归档和解档</span><br><span class="line"><span class="bullet">* </span>实现字典的模型和自动转换</span><br></pre></td></tr></table></figure></p><p>这个大家可以阅读YYModel,MJExtension这些开源库，从中学习这部分的用法。</p><p>下面给出简化版的代码来方便大家理解Runtime在这方面的应用：</p><ul><li><strong><strong>实现NSCoding的自动归档和解档</strong></strong></li></ul><p>这个比较简单，就是通过class_copyIvarList获取到类到各个实例变量，然后通过KVC方式获取或者设置当前的属性，这里需要注意的是YYModel中针对某些不支持KVC的类型通过向属性的setter/getter发送消息的方式来替换KVC.目前几乎所有的Objective-C对象都能使用KVC，但是一些纯Swift类和结构体是不支持KVC的。或许YYModel出于这方面考虑使用了通过发送消息的方式来取值和设值。</p><p><figure class="highlight tex"><table><tr><td class="code"><pre><span class="line">#ifndef CommonHeader_h</span><br><span class="line">#define CommonHeader_h</span><br><span class="line"></span><br><span class="line">#define INIT_WITH_CODER                                                 <span class="tag">\</span></span><br><span class="line">- (instancetype)initWithCoder:(NSCoder *)coder &#123;                        <span class="tag">\</span></span><br><span class="line">    if(self = [super init]) &#123;                                           <span class="tag">\</span></span><br><span class="line">        unsigned int outCount = 0;                                      <span class="tag">\</span></span><br><span class="line">        Ivar *varList = class_copyIvarList([self class], &amp;outCount);    <span class="tag">\</span></span><br><span class="line">        for (unsigned int index = 0; index &lt; outCount; index++) &#123;       <span class="tag">\</span></span><br><span class="line">            const char *var = ivar_getName(varList[index]);             <span class="tag">\</span></span><br><span class="line">            if(!var || !strlen(var)) continue;                          <span class="tag">\</span></span><br><span class="line">            NSString *varName = [NSString stringWithUTF8String:var];    <span class="tag">\</span></span><br><span class="line">            id value = [coder decodeObjectForKey:varName];              <span class="tag">\</span></span><br><span class="line">            if(value) &#123;                                                 <span class="tag">\</span></span><br><span class="line">                [self setValue:value forKey:varName];                   <span class="tag">\</span></span><br><span class="line">            &#125;                                                           <span class="tag">\</span></span><br><span class="line">        &#125;                                                               <span class="tag">\</span></span><br><span class="line">        free(varList);                                                  <span class="tag">\</span></span><br><span class="line">    &#125;                                                                   <span class="tag">\</span></span><br><span class="line">    return self;                                                        <span class="tag">\</span></span><br><span class="line">&#125;                                                                       <span class="tag">\</span></span><br><span class="line"></span><br><span class="line">#define ENCODE_WITH_CODER                                               <span class="tag">\</span></span><br><span class="line">- (void)encodeWithCoder:(NSCoder *)coder &#123;                              <span class="tag">\</span></span><br><span class="line">    unsigned int outCount = 0;                                          <span class="tag">\</span></span><br><span class="line">    Ivar *varList = class_copyIvarList([self class], &amp;outCount);        <span class="tag">\</span></span><br><span class="line">    for (unsigned int index = 0; index &lt; outCount; index++) &#123;           <span class="tag">\</span></span><br><span class="line">        const char *var = ivar_getName(varList[index]);                 <span class="tag">\</span></span><br><span class="line">        if(!var || !strlen(var)) continue;                              <span class="tag">\</span></span><br><span class="line">        NSString *varName = [NSString stringWithUTF8String:var];        <span class="tag">\</span></span><br><span class="line">        id value = [self valueForKey:varName];                          <span class="tag">\</span></span><br><span class="line">        [coder encodeObject:value forKey:varName];                      <span class="tag">\</span></span><br><span class="line">    &#125;                                                                   <span class="tag">\</span></span><br><span class="line">    free(varList);                                                      <span class="tag">\</span></span><br><span class="line">&#125;                                                                       <span class="tag">\</span></span><br><span class="line"></span><br><span class="line">#endif /* CommonHeader_h */</span><br></pre></td></tr></table></figure></p><p><figure class="highlight objectivec"><table><tr><td class="code"><pre><span class="line"><span class="meta">#import <span class="meta-string">&lt;Foundation/Foundation.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="built_in">NS_ASSUME_NONNULL_BEGIN</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">@interface</span> <span class="title">IDLTopic</span> : <span class="title">NSObject</span>&lt;<span class="title">NSCoding</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">@property</span>(<span class="keyword">nonatomic</span>, <span class="keyword">strong</span>) <span class="built_in">NSString</span> *topicName;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@property</span>(<span class="keyword">nonatomic</span>, <span class="keyword">assign</span>) <span class="built_in">NSInteger</span> topicRankStar;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">@interface</span> <span class="title">IDLUser</span> : <span class="title">NSObject</span>&lt;<span class="title">NSCoding</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">@property</span>(<span class="keyword">nonatomic</span>, <span class="keyword">strong</span>) <span class="built_in">NSString</span> *userName;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@property</span>(<span class="keyword">nonatomic</span>, <span class="keyword">assign</span>) <span class="built_in">NSInteger</span> userAge;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@property</span>(<span class="keyword">nonatomic</span>, <span class="keyword">strong</span>) <span class="built_in">NSString</span> *localAddress;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@property</span>(<span class="keyword">nonatomic</span>, <span class="keyword">strong</span>) <span class="built_in">NSString</span> *country;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@property</span>(<span class="keyword">nonatomic</span>, <span class="keyword">strong</span>) <span class="built_in">NSArray</span>&lt;IDLTopic *&gt; *topics;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">NS_ASSUME_NONNULL_END</span></span><br></pre></td></tr></table></figure></p><p><figure class="highlight objectivec"><table><tr><td class="code"><pre><span class="line"><span class="meta">#import <span class="meta-string">"IDLUser.h"</span></span></span><br><span class="line"><span class="meta">#import <span class="meta-string">&lt;objc/runtime.h&gt;</span></span></span><br><span class="line"><span class="meta">#import <span class="meta-string">"CommonHeader.h"</span></span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">@implementation</span> <span class="title">IDLTopic</span></span></span><br><span class="line"></span><br><span class="line">INIT_WITH_CODER</span><br><span class="line"></span><br><span class="line">ENCODE_WITH_CODER</span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">@implementation</span> <span class="title">IDLUser</span></span></span><br><span class="line"></span><br><span class="line">+ (<span class="built_in">NSDictionary</span> *)arrayElementModelTypeMap &#123;</span><br><span class="line">    <span class="keyword">return</span> @&#123;<span class="string">@"topics"</span>:[IDLTopic <span class="keyword">class</span>]&#125;;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">INIT_WITH_CODER</span><br><span class="line"></span><br><span class="line">ENCODE_WITH_CODER</span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br></pre></td></tr></table></figure></p><ul><li><strong><strong>字典转模型：</strong></strong>这部分的思路就是通过class_copyIvarList获取Model的各个属性，然后去掉属性名字下的下划线，作为查询字典的key。取出值通过KVC设置到model，这里有两个比较关键的问题，当model属性为字典或者自定义类型的时候需要怎么处理，当model属性为数组的时候怎么处理。大家看下面代码:</li></ul><p><figure class="highlight objectivec"><table><tr><td class="code"><pre><span class="line"><span class="built_in">NS_ASSUME_NONNULL_BEGIN</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">@interface</span> <span class="title">IDLTopic</span> : <span class="title">NSObject</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">@property</span>(<span class="keyword">nonatomic</span>, <span class="keyword">strong</span>) <span class="built_in">NSString</span> *topicName;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@property</span>(<span class="keyword">nonatomic</span>, <span class="keyword">assign</span>) <span class="built_in">NSInteger</span> topicRankStar;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">@interface</span> <span class="title">IDLUser</span> : <span class="title">NSObject</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">@property</span>(<span class="keyword">nonatomic</span>, <span class="keyword">strong</span>) <span class="built_in">NSString</span> *userName;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@property</span>(<span class="keyword">nonatomic</span>, <span class="keyword">assign</span>) <span class="built_in">NSInteger</span> userAge;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@property</span>(<span class="keyword">nonatomic</span>, <span class="keyword">strong</span>) <span class="built_in">NSString</span> *localAddress;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@property</span>(<span class="keyword">nonatomic</span>, <span class="keyword">strong</span>) <span class="built_in">NSString</span> *country;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@property</span>(<span class="keyword">nonatomic</span>, <span class="keyword">strong</span>) <span class="built_in">NSArray</span>&lt;IDLTopic *&gt; *topics;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">NS_ASSUME_NONNULL_END</span></span><br></pre></td></tr></table></figure></p><p><figure class="highlight objectivec"><table><tr><td class="code"><pre><span class="line"><span class="meta">#import <span class="meta-string">"IDLUser.h"</span></span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">@implementation</span> <span class="title">IDLTopic</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">@implementation</span> <span class="title">IDLUser</span></span></span><br><span class="line"></span><br><span class="line">+ (<span class="built_in">NSDictionary</span> *)arrayElementModelTypeMap &#123;</span><br><span class="line">    <span class="keyword">return</span> @&#123;<span class="string">@"topics"</span>:[IDLTopic <span class="keyword">class</span>]&#125;;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br></pre></td></tr></table></figure></p><p><figure class="highlight objectivec"><table><tr><td class="code"><pre><span class="line">+ (<span class="keyword">instancetype</span>)idl_modelWithDic:(<span class="built_in">NSDictionary</span> *)dictionary &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span>(!dictionary || ![dictionary count]) <span class="keyword">return</span> <span class="literal">nil</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">id</span> objc = [[<span class="keyword">self</span> alloc] init];</span><br><span class="line"></span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">int</span> outCount = <span class="number">0</span>;</span><br><span class="line">    <span class="comment">//没有参数的情况</span></span><br><span class="line">    Ivar *varList = class_copyIvarList([<span class="keyword">self</span> <span class="keyword">class</span>], &amp;outCount);</span><br><span class="line">    <span class="keyword">if</span>(!outCount) &#123;</span><br><span class="line">        free(varList);</span><br><span class="line">        <span class="keyword">return</span> objc;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">unsigned</span> <span class="keyword">int</span> index = <span class="number">0</span>; index &lt; outCount; index++) &#123;</span><br><span class="line"></span><br><span class="line">        <span class="comment">//获取到属性名称</span></span><br><span class="line">        <span class="built_in">NSString</span> *varName = [<span class="built_in">NSString</span> stringWithUTF8String:ivar_getName(varList[index])];</span><br><span class="line">        <span class="keyword">if</span>(!varName || ![varName length]) <span class="keyword">continue</span>;</span><br><span class="line">        <span class="comment">//去掉下划线_</span></span><br><span class="line">        varName = [varName substringFromIndex:<span class="number">1</span>];</span><br><span class="line">        <span class="comment">//去掉@ 和 “ 之后的实例变量类型 获取到的时候类型为@"NSString"这种</span></span><br><span class="line">        <span class="built_in">NSString</span> *varType = [<span class="built_in">NSString</span> stringWithUTF8String:ivar_getTypeEncoding(varList[index])];</span><br><span class="line">        varType = [varType stringByReplacingOccurrencesOfString:<span class="string">@"\""</span> withString:<span class="string">@""</span>];</span><br><span class="line">        varType = [varType stringByReplacingOccurrencesOfString:<span class="string">@"@"</span> withString:<span class="string">@""</span>];</span><br><span class="line"></span><br><span class="line">        <span class="comment">//取得变量值</span></span><br><span class="line">        <span class="keyword">id</span> varValue = dictionary[varName];</span><br><span class="line"></span><br><span class="line">        <span class="comment">//自定义类型处理</span></span><br><span class="line">        <span class="keyword">if</span>([varValue isKindOfClass:[<span class="built_in">NSDictionary</span> <span class="keyword">class</span>]] || ![varType hasPrefix:<span class="string">@"NS"</span>]) &#123;</span><br><span class="line">            <span class="comment">//拿到自定义类型 递归生成 自定义的类型属性</span></span><br><span class="line">            Class modelClass = <span class="built_in">NSClassFromString</span>(varType);</span><br><span class="line">            <span class="keyword">if</span>(modelClass) &#123;</span><br><span class="line">                varValue = [modelClass idl_modelWithDic:varValue];</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">//实例变量为数组类型</span></span><br><span class="line">        <span class="keyword">if</span>([varValue isKindOfClass:[<span class="built_in">NSArray</span> <span class="keyword">class</span>]] || [varValue isKindOfClass:[<span class="built_in">NSMutableArray</span> <span class="keyword">class</span>]]) &#123;</span><br><span class="line">            <span class="comment">//获取映射的模型类型</span></span><br><span class="line">            <span class="keyword">if</span>([<span class="keyword">self</span> respondsToSelector:<span class="keyword">@selector</span>(arrayElementModelTypeMap)]) &#123;</span><br><span class="line">                <span class="comment">//拿到元素类型</span></span><br><span class="line">                <span class="built_in">NSDictionary</span> *arrayElementModelTypeMap = [<span class="keyword">self</span> performSelector:<span class="keyword">@selector</span>(arrayElementModelTypeMap)];</span><br><span class="line">                Class elementModelType = arrayElementModelTypeMap[varName];</span><br><span class="line"></span><br><span class="line">                <span class="built_in">NSMutableArray</span> *array = [<span class="built_in">NSMutableArray</span> new];</span><br><span class="line">                <span class="keyword">for</span> (<span class="built_in">NSDictionary</span> *dict <span class="keyword">in</span> varValue<span class="comment">/*这个为字典数组*/</span>) &#123;</span><br><span class="line">                    <span class="keyword">id</span> elementValue = [elementModelType idl_modelWithDic:dict];</span><br><span class="line">                    [array addObject:elementValue];</span><br><span class="line">                &#125;</span><br><span class="line">                varValue = array;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">//赋值</span></span><br><span class="line">        <span class="keyword">if</span>(varValue) &#123;</span><br><span class="line">            [objc setValue:varValue forKey:varName];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    free(varList);</span><br><span class="line">    <span class="keyword">return</span> objc;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><h5><strong><strong>2. 通过Runtime动态创建一个类，给类增加方法，协议，属性等信息，调用私有方法：</strong></strong></h5><ul><li><strong><strong>在方法决议阶段给类添加方法</strong></strong></li></ul><p><figure class="highlight less"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">void</span> <span class="selector-tag">speak</span>() &#123;</span><br><span class="line">    <span class="selector-tag">NSLog</span>(@<span class="string">"Hello World!"</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">@<span class="selector-tag">implementation</span> <span class="selector-tag">IDLUser</span></span><br><span class="line"></span><br><span class="line">+ (BOOL)<span class="selector-tag">resolveInstanceMethod</span><span class="selector-pseudo">:(SEL)sel</span> &#123;</span><br><span class="line">    <span class="selector-tag">if</span>([NSStringFromSelector(sel) <span class="attribute">isEqualToString</span>:@<span class="string">"speak"</span>]) &#123;</span><br><span class="line">        <span class="selector-tag">class_addMethod</span>([self class], <span class="variable">@selector</span>(speak), (IMP)speak, <span class="string">"v@:"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="selector-tag">return</span> <span class="selector-attr">[super resolveInstanceMethod:sel]</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">@<span class="selector-tag">end</span></span><br><span class="line"></span><br><span class="line"><span class="selector-attr">[user performSelector:@selector(speak)]</span>;</span><br></pre></td></tr></table></figure></p><ul><li><strong><strong>调用私有方法</strong></strong></li></ul><p><figure class="highlight objectivec"><table><tr><td class="code"><pre><span class="line"><span class="built_in">UIView</span> *maskView = [[<span class="built_in">UIView</span> alloc] initWithFrame:<span class="built_in">CGRectMake</span>(<span class="number">0</span>, <span class="number">0</span>, <span class="number">200</span>, <span class="number">200</span>)];</span><br><span class="line">maskView.backgroundColor = [<span class="built_in">UIColor</span> redColor];</span><br><span class="line">SEL selector = <span class="built_in">NSSelectorFromString</span>(<span class="string">@"setMaskView:"</span>);</span><br><span class="line">[<span class="keyword">self</span>.view performSelector:selector withObject:maskView];</span><br></pre></td></tr></table></figure></p><h5><strong><strong>3. 使用Method Swizzling交换方法实现</strong></strong></h5><p>Method Swizzling的主要用于改变已经存在的selector 的实现，我们知道每个方法都和一个selector以及IMP相对应。但是iOS的Runtime 提供了一种改变这种对应关系的方法，它就是Method Swizzling，直接上代码,这个例子来自 nshipster <a href="https://nshipster.com/method-swizzling/" target="_blank" rel="noopener">Method Swizzling</a>.</p><p>Method Swizzling 大体都一个模子：</p><p><figure class="highlight kotlin"><table><tr><td class="code"><pre><span class="line"><span class="meta">@implementation</span> IDLViewController (IDLMethodSwizzling)</span><br><span class="line"></span><br><span class="line">+ (void)load &#123;</span><br><span class="line">    static dispatch_once_t onceToken;</span><br><span class="line">    dispatch_once(&amp;onceToken, ^&#123;</span><br><span class="line"></span><br><span class="line">        SEL originalSEL = <span class="meta">@selector(viewWillAppear:)</span>;</span><br><span class="line">        SEL swizzlingSEL = <span class="meta">@selector(idl_viewWillAppear:)</span>;</span><br><span class="line"></span><br><span class="line">        Class <span class="class"><span class="keyword">class</span> = [<span class="title">self</span> <span class="title">class</span>];</span></span><br><span class="line">        Method originalMethod = class_getInstanceMethod(<span class="class"><span class="keyword">class</span>, <span class="type">originalSEL);</span></span></span><br><span class="line">        Method swizzlingMethod = class_getInstanceMethod(<span class="class"><span class="keyword">class</span>, <span class="type">swizzlingSEL);</span></span></span><br><span class="line"></span><br><span class="line">        <span class="comment">// 如果要交换类方法可以使用下面代码替换上面的</span></span><br><span class="line">        <span class="comment">// Class class = object_getClass((id)self);</span></span><br><span class="line">        <span class="comment">// Method originalMethod = class_getClassMethod(class, originalSelector);</span></span><br><span class="line">        <span class="comment">// Method swizzledMethod = class_getClassMethod(class, swizzledSelector);</span></span><br><span class="line"></span><br><span class="line">        BOOL didAddedSuccess = class_addMethod(<span class="class"><span class="keyword">class</span>, <span class="type">originalSEL</span>,<span class="type"></span></span></span><br><span class="line">                                                method_getImplementation(swizzlingMethod),</span><br><span class="line">                                                method_getTypeEncoding(swizzlingMethod));</span><br><span class="line">        <span class="keyword">if</span>(didAddedSuccess) &#123;</span><br><span class="line">            class_replaceMethod(<span class="class"><span class="keyword">class</span>, <span class="type">swizzlingSEL</span>,<span class="type"></span></span></span><br><span class="line">                                method_getImplementation(originalMethod),</span><br><span class="line">                                method_getTypeEncoding(originalMethod));</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            method_exchangeImplementations(originalMethod, swizzlingMethod);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">- (void)idl_viewWillAppear:(BOOL)animated &#123;</span><br><span class="line">    [self idl_viewWillAppear:animated];</span><br><span class="line">    NSLog(@<span class="string">"========&gt;"</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>之所以放在load方法，主要有两点原因：</p><ol><li>load方法的时机在镜像加载的时候，这时候可以保证在任何逻辑开始之前就交换好了。</li><li>load方法可以避免分类中的Method Swizzling 覆盖主类的Method Swizzling。</li></ol><p>这里在最开始的时候会先试探得添加<strong><strong>originalSEL</strong></strong>对应的方法，如果我们类中还没有实现这个方法那么就会通过class_addMethod进行添加，这时候didAddedSuccess返回YES, 由于已经添加了originalSEL 指向swizzlingMethod的IMP,接下来就是通过class_replaceMethod，将swizzlingSEL指向originalMethod的IMP，从而完成方法的交换。如果类中实现了originalSEL，那么didAddedSuccess返回NO。由于swizzlingSEL和swizzlingMethod的IMP也已经存在了，所以只要交换下这两个SEL的实现就可以了。</p><p><figure class="highlight less"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">-</span> (void)<span class="selector-tag">idl_viewWillAppear</span><span class="selector-pseudo">:(BOOL)animated</span> &#123;</span><br><span class="line">    <span class="selector-attr">[self idl_viewWillAppear:animated]</span>;</span><br><span class="line">    <span class="selector-tag">NSLog</span>(@<span class="string">"========&gt;"</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>看到上面代码，大家估计会觉得不对吧，实际上是这样的，经过Method Swizzling，一旦系统向UIViewController 发送 viewWillAppear:消息，就会执行****- (void)idl_viewWillAppear:(BOOL)animated****的IMP。也就是：</p><p><figure class="highlight clojure"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    [self idl_viewWillAppear:animated]<span class="comment">;</span></span><br><span class="line">    NSLog(@<span class="string">"========&gt;"</span>)<span class="comment">;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>执行到****[self idl_viewWillAppear:animated]**** 就会执行原来的viewWillAppear: IMP实现。所以这个是没有问题的。</p><p>上面只是一个简单的例子，如果大家对Method Swizzling感兴趣，可以在GitHub上搜索 <a href="https://github.com/search?q=method+swizzling&amp;type=Repositories" target="_blank" rel="noopener">Method Swizzling</a>。 其中<a href="https://github.com/rentzsch/jrswizzle" target="_blank" rel="noopener">jrswizzle</a>,<a href="https://github.com/rabovik/RSSwizzle" target="_blank" rel="noopener">RSSwizzle</a>大家可以了解下。实际项目中没有用过。有机会看下源码的实现再给大家分析下整个实现。无非就是兼容性好一点，但是实际项目中上面应该够用了。</p><h5><strong><strong>4. 使用消息转发机制实现多继承，热修复，消息分发器，路由：</strong></strong></h5><p>这部分大家可以看JSPatch,ASpect 这两个开源库。多继承见下一篇博客</p><h5><strong><strong>5. 使用关联对象给Catogry添加属性</strong></strong></h5><p><figure class="highlight less"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">-</span> (NSString *)<span class="selector-tag">catogiesName</span> &#123;</span><br><span class="line">    <span class="selector-tag">return</span> <span class="selector-tag">objc_getAssociatedObject</span>(self, _cmd);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="selector-tag">-</span> (void)<span class="selector-tag">setCatogiesName</span><span class="selector-pseudo">:(NSString</span> *)<span class="selector-tag">catogiesName</span> &#123;</span><br><span class="line">    <span class="selector-tag">objc_setAssociatedObject</span>(self, <span class="variable">@selector</span>(catogiesName), catogiesName, OBJC_ASSOCIATION_RETAIN_NONATOMIC);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>关于Runtime的API最主要的都集中在****&lt;objc/runtime.h&gt;<strong><strong>,</strong></strong>&lt;objc/message.h&gt;****这两个文件中大家可以在使用的时候进行查看。</p><p>如果大家想要更进一步学习Runtime 可以在Github搜索 <a href="https://github.com/search?l=Objective-C&amp;q=runtime&amp;type=Repositories" target="_blank" rel="noopener">Runtime</a></p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;Runtime 用处蛮多的但是归结起来有如下几个：&lt;/p&gt;
&lt;h5&gt;&lt;strong&gt;&lt;strong&gt;1. 使用Runtime获得对象的属性，方法，协议等信息：&lt;/strong&gt;&lt;/strong&gt;&lt;/h5&gt;
&lt;p&gt;&lt;figure class=&quot;highlight asciid
      
    
    </summary>
    
      <category term="Objective C 相关" scheme="http://yoursite.com/categories/Objective-C-%E7%9B%B8%E5%85%B3/"/>
    
    
      <category term="Objective C 相关" scheme="http://yoursite.com/tags/Objective-C-%E7%9B%B8%E5%85%B3/"/>
    
  </entry>
  
  <entry>
    <title>iOS Runtime源码解析initialize load __attribute__总结</title>
    <link href="http://yoursite.com/2020/02/02/iOS-Runtime%E6%BA%90%E7%A0%81%E8%A7%A3%E6%9E%90initialize-load-attribute-%E6%80%BB%E7%BB%93/"/>
    <id>http://yoursite.com/2020/02/02/iOS-Runtime源码解析initialize-load-attribute-总结/</id>
    <published>2020-02-01T23:52:30.000Z</published>
    <updated>2020-02-04T17:52:10.000Z</updated>
    
    <content type="html"><![CDATA[<p>经过前面几篇论文关于Runtime部分已经介绍差不多了，这篇算是一个总结性的文章吧，其实+initialize 和 +load 已经在之前的几篇博客中都做了十分详细的介绍，这里放在一起无非就是对比一下，增加下记忆，所以这篇博客不会写得很长很复杂，好了言归正传：</p><h6><strong><strong>+initialize</strong></strong></h6><ul><li><p>initialize的特点是惰性调用方式，它的调用时机是在对某个对象发送第一条消息的时候，也就是说如果我们不对某个对象发送消息，那么这个对象的initialize是不会被调用的。</p></li><li><p>在分类中也可以实现initialize方法，只不过分类中的initialize方法会覆盖主类中的initialize方法，如果在多个分类中同时实现了initialize方法，那么只有最后被编译的那个分类的initialize方法会被调用。它会强制先向父类先发送 +initialize。</p></li><li><p>与 load 不同，initialize 方法调用时，所有的类都已经加载到了内存中。</p></li><li><p>其作用也非常局限，一般我们只会在 initialize 方法中进行一些常量的初始化。</p></li></ul><p><figure class="highlight actionscript"><table><tr><td class="code"><pre><span class="line">+ (<span class="keyword">void</span>)initialize &#123;</span><br><span class="line">    <span class="keyword">if</span> (self == [FLAnimatedImage <span class="class"><span class="keyword">class</span>]) </span>&#123;</span><br><span class="line">        <span class="comment">//......</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><h6><strong><strong>+load</strong></strong></h6><p>+load 方法相对于 +initialize 来说更为常见它是我们开发过程中可以接触到的调用时机最靠前的方法，和initialize不同的是它的调用<strong><strong>不是惰性的</strong></strong>，在主函数运行之前，load 方法就会被调用，并且它只会在程序调用期间调用一次，最重要的是如果在类与分类中都实现了 load 方法，它们都会被调用，不像+initialize 在分类中实现的方法会被覆盖，但是在使用load方法的时候需要注意，由于load方法的运行时间过早，所以可能不是一个理想的环境，因为它不能保证某些类可能需要在在其它类之前加载，但是在这个时间点，所有的 framework 都已经加载到了运行时中，所以调用 framework 中的方法都是安全的，同时需要注意的是重载Class 的 +load 方法时不能调父类super。</p><p><strong><strong>load / initialize 对比</strong></strong></p><table><thead><tr><th>方法</th><th>load</th><th>initialize</th></tr></thead><tbody><tr><td>是否惰性</td><td>否，都会被调用，并且只会被调用一次</td><td>是，只有在第一次向某个对象发送消息的时候调用</td></tr><tr><td>调用时机</td><td>main之前</td><td>main之后，第一次向某个对象发送消息的时候</td></tr><tr><td>在分类中的行为</td><td>在分类中和在主类中都会被调用，不会发生覆盖，并且先父类，再主类，再父类分类，主类分类</td><td>分类中的会覆盖主类中的initialize，所有分类中只有最后编译的那个生效，会强制先向父类先发送 +initialize</td></tr><tr><td>在调用的时候是否全部类已经加载完毕</td><td>否</td><td>是</td></tr></tbody></table><h6><strong><strong><strong>attribute</strong></strong></strong></h6><p>关于__attribute__有很多很好的文档，大家如果看了还不大明白的话可以在下面文档中找答案，这里只想列出在开发过程中比较常用的几个：</p><ul><li><strong><strong><strong>attribute</strong>((objc_requires_super))</strong></strong></li></ul><p>有时候我们自己编写的框架需要给别人用的时候，某个方法在使用的时候必须先调用父类的方法，一种做法是写注释，但是如果使用者不看注释就gg了，所以比较好的方式是使用__attribute__((objc_requires_super)) 如果我们没有调用父类的这个方法编译器就会发出警告。</p><ul><li><strong><strong><strong>attribute</strong>((unavailable(&quot;not available&quot;)))</strong></strong></li></ul><p>告诉编译器方法不可用，如果使用了就会编译失败，提示错误。比如自定义了初始化方法，为了防止别人调用init初始化，就可以使用它来提示错误，这个在开发中也很常见，但是一般写成：</p><p><figure class="highlight elm"><table><tr><td class="code"><pre><span class="line">- (instance<span class="keyword">type</span>)init <span class="type">NS_UNAVAILABLE</span>;</span><br></pre></td></tr></table></figure></p><ul><li><strong><strong><strong>attribute</strong>((deprecated(&quot;该方法已经废弃&quot;)))</strong></strong></li></ul><p>用于在编译时提示方法过时：</p><p><figure class="highlight d"><table><tr><td class="code"><pre><span class="line">- (<span class="keyword">void</span>)deprecatedMethod:(NSString *)<span class="built_in">string</span> __attribute__((<span class="keyword">deprecated</span>(<span class="string">"该方法已经废弃，请使用xxx方法"</span>)));</span><br><span class="line">- (<span class="keyword">void</span>)deprecatedMethod DEPRECATED_ATTRIBUTE;</span><br></pre></td></tr></table></figure></p><ul><li><strong><strong><strong>attribute</strong>((cleanup))</strong></strong></li></ul><p>声明到一个变量或者block上，当这个变量作用域结束时，调用指定的一个函数. 这个个人在时机工作中用得不多，大家可以看sunnyxx的<a href="http://blog.sunnyxx.com/2014/09/15/objc-attribute-cleanup/" target="_blank" rel="noopener">黑魔法__attribute__((cleanup))</a>这篇博客。</p><ul><li><strong><strong><strong>attribute</strong>((overloadable))</strong></strong></li></ul><p>这个个人在时机工作中也用得不多，它主要用于C语言函数,可以定义若干个函数名相同，但参数不同的方法，调用时编译器会自动根据参数选择函数原型.</p><p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">__attribute__((overloadable)) <span class="function"><span class="keyword">void</span> <span class="title">print</span><span class="params">(NSString *<span class="built_in">string</span>)</span></span>&#123;</span><br><span class="line">    NSLog(@<span class="string">"%@"</span>,<span class="built_in">string</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">__attribute__((overloadable)) <span class="function"><span class="keyword">void</span> <span class="title">print</span><span class="params">(<span class="keyword">int</span> num)</span></span>&#123;</span><br><span class="line">    NSLog(@<span class="string">"%d"</span>,num);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><ul><li><strong><strong><strong>attribute</strong>((objc_runtime_name(&quot;NSObject&quot;)))</strong></strong></li></ul><p>这个用于将类或协议的名字在编译时指定成另一个，这个没用过。</p><p><figure class="highlight elixir"><table><tr><td class="code"><pre><span class="line">__attribute__((objc_runtime_name(<span class="string">"NSObject"</span>)))</span><br><span class="line"><span class="variable">@interface</span> IDLObject <span class="symbol">:NSObject</span></span><br></pre></td></tr></table></figure></p><ul><li><strong><strong><strong>attribute</strong>((objc_subclassing_restricted))</strong></strong></li></ul><p>这个用于禁止被继承的时候使用，很少用到。</p><ul><li><strong><strong><strong>attribute</strong>((constructor)) / <strong>attribute</strong>((destructor))</strong></strong></li></ul><p>最后介绍的是我们开发过程中经常用到的两个，在用之前需要和load区分开来，我们在将到load方法的时候有提到它的不足之处是它调用的时候某些类可能还没有被加载完成，所以不能在load方法中使用我们自己的类，这样很可能类都没被加载起来。那么__attribute__((constructor))的好处是什么呢？在__attribute__((constructor))标志的方法执行的时候，所有 Class 都已经加载完成，所以可以使用任意的类，并且它可以单独放在一个文件中，不用挂载在一个 Class 中，它和load的方法一样都是在main还未执行的时候调用。如果有多个 constructor 并且想按照指定顺序执行的话，可以写成 <strong>attribute</strong>((constructor(101)))这种形式，1 ~ 100 为系统保留，所以一般以101 为开始，里面的数字越小优先级越高。</p><h6><strong><strong>[推荐文档]</strong></strong></h6><ul><li><a href="https://blog.sunnyxx.com/2016/05/14/clang-attributes/" target="_blank" rel="noopener">Clang Attributes 黑魔法小记</a></li><li><a href="https://blog.twitter.com/engineering/en_us/a/2014/attribute-directives-in-objective-c.html" target="_blank" rel="noopener"><strong>attribute</strong> directives in Objective-C</a></li><li><a href="https://nshipster.com/__attribute__/" target="_blank" rel="noopener">nshipster <strong>attribute</strong></a></li><li><a href="https://gcc.gnu.org/onlinedocs/gcc-4.0.0/gcc/Function-Attributes.html" target="_blank" rel="noopener">官方文档</a></li><li><a href="https://www.jianshu.com/p/29eb7b5c8b2d" target="_blank" rel="noopener"><strong>attribute</strong> 总结</a></li></ul>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;经过前面几篇论文关于Runtime部分已经介绍差不多了，这篇算是一个总结性的文章吧，其实+initialize 和 +load 已经在之前的几篇博客中都做了十分详细的介绍，这里放在一起无非就是对比一下，增加下记忆，所以这篇博客不会写得很长很复杂，好了言归正传：&lt;/p&gt;
&lt;h
      
    
    </summary>
    
      <category term="Objective C 相关" scheme="http://yoursite.com/categories/Objective-C-%E7%9B%B8%E5%85%B3/"/>
    
    
      <category term="Objective C 相关" scheme="http://yoursite.com/tags/Objective-C-%E7%9B%B8%E5%85%B3/"/>
    
  </entry>
  
  <entry>
    <title>iOS Runtime源码解析消息处理</title>
    <link href="http://yoursite.com/2020/02/02/iOS-Runtime%E6%BA%90%E7%A0%81%E8%A7%A3%E6%9E%90%E6%B6%88%E6%81%AF%E5%A4%84%E7%90%86/"/>
    <id>http://yoursite.com/2020/02/02/iOS-Runtime源码解析消息处理/</id>
    <published>2020-02-01T23:49:24.000Z</published>
    <updated>2020-02-04T17:52:10.000Z</updated>
    
    <content type="html"><![CDATA[<p>我们知道Objective C 是一门动态语言，它是在编译器和Runtime共同协作下完成的，编译时期决定了向哪个对象发送哪个消息，但是这个对象在收到具体消息的时候如何处理是由Runtime决定的,Runtime对消息的处理包括了消息的发送和消息的转发。我们接下来就来看下这部分处理：</p><p>在Objective C代码编译期间当遇到一个方法调用的时候，编译器会产生一个对<strong><strong>objc_msgSend</strong></strong>，<strong><strong>objc_msgSend_stret</strong></strong>，<strong><strong>objc_msgSendSuper</strong></strong>，<strong><strong>objc_msgSendSuper_stret</strong></strong>的函数调用</p><ul><li>如果调用的是[super xxxx] 那么将会调用objc_msgSendSuper objc_msgSendSuper_stret 也就是有带super的函数</li><li>如果调用的是[obj xxxxx] 那么将会调用objc_msgSend objc_msgSend_stret</li><li>如果返回的值是带有结构的将会调用 objc_msgSendSuper_stret objc_msgSend_stret 也就是带有stret的。</li></ul><p>为了简单起见我们这里只研究<strong><strong>objc_msgSend</strong></strong>的情况：</p><p><figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">OBJC_EXPORT</span> <span class="selector-tag">id</span> _<span class="selector-tag">Nullable</span> <span class="selector-tag">objc_msgSend</span>(<span class="selector-tag">id</span> _<span class="selector-tag">Nullable</span> <span class="selector-tag">self</span>, <span class="selector-tag">SEL</span> _<span class="selector-tag">Nonnull</span> <span class="selector-tag">op</span>, ...) <span class="selector-tag">OBJC_AVAILABLE</span>(10<span class="selector-class">.0</span>, 2<span class="selector-class">.0</span>, 9<span class="selector-class">.0</span>, 1<span class="selector-class">.0</span>, 2<span class="selector-class">.0</span>);</span><br></pre></td></tr></table></figure></p><p>这里的第一个参数为消息的接收对象，第二个参数为SEL，可以看成是一个消息。</p><p>objc_msgSend底层代码是通过汇编实现的：</p><p><figure class="highlight gams"><table><tr><td class="code"><pre><span class="line">    ENTRY _objc_msgSend</span><br><span class="line">    UNWIND _objc_msgSend, NoFrame</span><br><span class="line">    <span class="comment">//判空 如果检测方法的接受者是nil，那么系统会自动clean并且return。</span></span><br><span class="line">    NilTest<span class="built-in">NORMAL</span></span><br><span class="line">    <span class="comment">//可以快速地获取到对象的 isa 指针地址放到 r11 寄存器</span></span><br><span class="line">    GetIsaFast <span class="built-in">NORMAL</span>           <span class="comment">// r10 = self-&gt;isa</span></span><br><span class="line">    <span class="comment">//在缓存中查找IMP 如果查找成功直接调用 IMP</span></span><br><span class="line">    CacheLookup <span class="built-in">NORMAL</span>, CALL    <span class="comment">// calls IMP on success</span></span><br><span class="line">    <span class="comment">//检查是否返回值为空</span></span><br><span class="line">    NilTestReturnZero <span class="built-in">NORMAL</span></span><br><span class="line"></span><br><span class="line">    GetIsaSupport <span class="built-in">NORMAL</span></span><br><span class="line"></span><br><span class="line"><span class="comment">//查找缓存失败，在方法列表中查找</span></span><br><span class="line">LCacheMiss:</span><br><span class="line">    <span class="comment">// isa still in r10</span></span><br><span class="line">    jmp__objc_msgSend_uncached</span><br><span class="line">    END_ENTRY _objc_msgSend</span><br></pre></td></tr></table></figure></p><p>上面的汇编代码会通过isa找到消息接收者的objc_class，然后通过CacheLookup汇编代码在objc_class 方法缓存中查找，是否有和当前SEL匹配的方法缓存，如果有直接调用对应的IMP,如果没有则走LCacheMiss分支，它会跳转到__objc_msgSend_uncached中：</p><p><figure class="highlight sqf"><table><tr><td class="code"><pre><span class="line">STATIC_ENTRY <span class="variable">__objc_msgSend_uncached</span></span><br><span class="line">UNWIND <span class="variable">__objc_msgSend_uncached</span>, FrameWithNoSaves</span><br><span class="line"></span><br><span class="line"><span class="comment">// THIS IS NOT A CALLABLE C FUNCTION</span></span><br><span class="line"><span class="comment">// Out-of-band r10 is the searched class</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 在方法表中查找对应的IMP</span></span><br><span class="line"><span class="comment">// r10 is already the class to search</span></span><br><span class="line">MethodTableLookup NORMAL<span class="comment">// r11 = IMP</span></span><br><span class="line"><span class="comment">// 跳到对应的IMP去执行</span></span><br><span class="line">jmp*%r11<span class="comment">// goto *imp</span></span><br><span class="line"></span><br><span class="line">END_ENTRY <span class="variable">__objc_msgSend_uncached</span></span><br></pre></td></tr></table></figure></p><p>在__objc_msgSend_uncached会调用MethodTableLookup从方法列表中查找：</p><p><figure class="highlight mel"><table><tr><td class="code"><pre><span class="line">.macro MethodTableLookup</span><br><span class="line">    <span class="comment">//......</span></span><br><span class="line"></span><br><span class="line">.<span class="keyword">if</span> $0 == NORMAL</span><br><span class="line">    <span class="comment">// receiver already in a1</span></span><br><span class="line">    <span class="comment">// selector already in a2</span></span><br><span class="line">.<span class="keyword">else</span></span><br><span class="line">    movq    %a2, %a1</span><br><span class="line">    movq    %a3, %a2</span><br><span class="line">.endif</span><br><span class="line">    movq    %r10, %a3</span><br><span class="line">    <span class="comment">// 调用__class_lookupMethodAndLoadCache3</span></span><br><span class="line">    call    __class_lookupMethodAndLoadCache3</span><br><span class="line"></span><br><span class="line">    <span class="comment">// IMP is now in %rax</span></span><br><span class="line">    movq    %rax, %r11</span><br><span class="line"></span><br><span class="line"><span class="comment">//......</span></span><br><span class="line"></span><br><span class="line">.endmacro</span><br></pre></td></tr></table></figure></p><p>这里会调用****__class_lookupMethodAndLoadCache3**** 进行查找。</p><p><figure class="highlight objectivec"><table><tr><td class="code"><pre><span class="line">IMP _class_lookupMethodAndLoadCache3(<span class="keyword">id</span> obj, SEL sel, Class cls)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">return</span> lookUpImpOrForward(cls, sel, obj, </span><br><span class="line">                              <span class="literal">YES</span><span class="comment">/*initialize*/</span>, <span class="literal">NO</span><span class="comment">/*cache*/</span>, <span class="literal">YES</span><span class="comment">/*resolver*/</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>_class_lookupMethodAndLoadCache3 是专门提供给汇编调用的不需要查找方法缓存，直接查找objc_class方法列表的方法。所以这里调用<strong><strong>lookUpImpOrForward</strong></strong>时候cache传的是NO.</p><p><figure class="highlight hsp"><table><tr><td class="code"><pre><span class="line">IMP lookUpImpOrForward(Class <span class="keyword">cls</span>, SEL sel, id inst, </span><br><span class="line">                       bool initialize, bool cache, bool resolver)</span><br><span class="line">&#123;</span><br><span class="line">    IMP imp = nil<span class="comment">;</span></span><br><span class="line">    bool triedResolver = NO<span class="comment">;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">//确保进入的时候是没锁住的</span></span><br><span class="line">    runtimeLock.assertUnlocked()<span class="comment">; //这个是加一个读写锁，保证线程安全。</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">//优化缓存查找传入的参数cache 用于表示是否找到cache的布尔量，</span></span><br><span class="line">    <span class="comment">//从_class_lookupMethodAndLoadCache3进来的是在缓存中已经找过并且没找到的情景,这时候cache为NO</span></span><br><span class="line">    <span class="keyword">if</span> (cache) &#123;</span><br><span class="line">        <span class="comment">//如果传入的是YES，那么就会调用cache_getImp方法去找到缓存里面的IMP,注意cache_getImp是通过汇编实现的，cache_getImp会把找到的IMP放在r11中</span></span><br><span class="line">        imp = cache_getImp(<span class="keyword">cls</span>, sel)<span class="comment">;</span></span><br><span class="line">        <span class="keyword">if</span> (imp) <span class="keyword">return</span> imp<span class="comment">;</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//锁住读写锁</span></span><br><span class="line">    runtimeLock.lock()<span class="comment">;</span></span><br><span class="line">    <span class="comment">//.......</span></span><br><span class="line">    <span class="comment">//如果已经初始化会在objc_class对应的标识位设置为true</span></span><br><span class="line">    <span class="keyword">if</span> (!<span class="keyword">cls</span>-&gt;isRealized()) &#123;</span><br><span class="line">        <span class="comment">//实例化类结构</span></span><br><span class="line">        realizeClass(<span class="keyword">cls</span>)<span class="comment">;</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// +initialize就是在这个阶段调用的</span></span><br><span class="line">    <span class="keyword">if</span> (initialize  &amp;&amp;  !<span class="keyword">cls</span>-&gt;isInitialized()) &#123;</span><br><span class="line">        runtimeLock.unlock()<span class="comment">;</span></span><br><span class="line">        <span class="comment">//_class_initialize是类初始化的过程。它会发送一个initialize消息给当前类</span></span><br><span class="line">        _class_initialize (_class_getNonMetaClass(<span class="keyword">cls</span>, inst))<span class="comment">;</span></span><br><span class="line">        runtimeLock.lock()<span class="comment">;</span></span><br><span class="line">        <span class="comment">//如果我们发送的sel就是initialize那么这里的_class_initialize会发送一次+initialize，后续还会发送一次+initialize，但是这种情况很少见</span></span><br><span class="line">        <span class="comment">// If sel == initialize, _class_initialize will send +initialize and </span></span><br><span class="line">        <span class="comment">// then the messenger will send +initialize again after this </span></span><br><span class="line">        <span class="comment">// procedure finishes. Of course, if this is not being called </span></span><br><span class="line">        <span class="comment">// from the messenger then it won't happen. 2778172</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    </span><br><span class="line"> retry:    </span><br><span class="line">    runtimeLock.assertLocked()<span class="comment">;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 从缓存中查找方法实现</span></span><br><span class="line">    imp = cache_getImp(<span class="keyword">cls</span>, sel)<span class="comment">;</span></span><br><span class="line">    <span class="keyword">if</span> (imp) <span class="keyword">goto</span> done<span class="comment">;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">//尝试在本类的方法列表中查找</span></span><br><span class="line">    &#123;</span><br><span class="line">        Method meth = getMethodNoSuper_nolock(<span class="keyword">cls</span>, sel)<span class="comment">;</span></span><br><span class="line">        <span class="keyword">if</span> (meth) &#123;</span><br><span class="line">            <span class="comment">//找到的情况下添加到缓存并返回方法实现</span></span><br><span class="line">            log_and_fill_cache(<span class="keyword">cls</span>, meth-&gt;imp, sel, inst, <span class="keyword">cls</span>)<span class="comment">;</span></span><br><span class="line">            imp = meth-&gt;imp<span class="comment">;</span></span><br><span class="line">            <span class="keyword">goto</span> done<span class="comment">;</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//尝试在父类的缓存和方法列表中查找</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">//如果以上尝试都失败了，接下来就会循环尝试父类的缓存和方法列表。一直找到NSObject为止。因为NSObject的superclass为nil，才跳出循环。</span></span><br><span class="line">        unsigned attempts = unreasonableClassCount()<span class="comment">;</span></span><br><span class="line">        <span class="keyword">for</span> (Class curClass = <span class="keyword">cls</span>-&gt;superclass<span class="comment">;</span></span><br><span class="line">             curClass != nil<span class="comment">;</span></span><br><span class="line">             curClass = curClass-&gt;superclass)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="comment">// Halt if there is a cycle in the superclass chain.</span></span><br><span class="line">            <span class="keyword">if</span> (--attempts == <span class="number">0</span>) &#123;</span><br><span class="line">                _objc_fatal(<span class="string">"Memory corruption in class list."</span>)<span class="comment">;</span></span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// Superclass cache.</span></span><br><span class="line">            imp = cache_getImp(curClass, sel)<span class="comment">;</span></span><br><span class="line">            <span class="keyword">if</span> (imp) &#123;</span><br><span class="line">                <span class="keyword">if</span> (imp != (IMP)_objc_msgForward_impcache) &#123;</span><br><span class="line">                    <span class="comment">// Found the method in a superclass. Cache it in this class.</span></span><br><span class="line">                    log_and_fill_cache(<span class="keyword">cls</span>, imp, sel, inst, curClass)<span class="comment">;</span></span><br><span class="line">                    <span class="keyword">goto</span> done<span class="comment">;</span></span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">else</span> &#123;</span><br><span class="line">                    <span class="comment">// Found a forward:: entry in a superclass.</span></span><br><span class="line">                    <span class="comment">// Stop searching, but don't cache yet; call method </span></span><br><span class="line">                    <span class="comment">// resolver for this class first.</span></span><br><span class="line">                    <span class="keyword">break</span><span class="comment">;</span></span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// Superclass method list.</span></span><br><span class="line">            Method meth = getMethodNoSuper_nolock(curClass, sel)<span class="comment">;</span></span><br><span class="line">            <span class="keyword">if</span> (meth) &#123;</span><br><span class="line">                log_and_fill_cache(<span class="keyword">cls</span>, meth-&gt;imp, sel, inst, curClass)<span class="comment">;</span></span><br><span class="line">                imp = meth-&gt;imp<span class="comment">;</span></span><br><span class="line">                <span class="keyword">goto</span> done<span class="comment">;</span></span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//没有找到实现方法，尝试寻找方法的解决者</span></span><br><span class="line">    <span class="comment">// No implementation found. Try method resolver once.</span></span><br><span class="line">    <span class="keyword">if</span> (resolver  &amp;&amp;  !triedResolver) &#123;</span><br><span class="line">        runtimeLock.unlock()<span class="comment">;</span></span><br><span class="line">        <span class="comment">//如果父类找到NSObject还没有找到，那么就会开始尝试_class_resolveMethod方法。</span></span><br><span class="line">        <span class="comment">//注意，这些需要打开读锁，因为开发者可能会在这里动态增加方法实现，所以不需要缓存结果。</span></span><br><span class="line">        <span class="comment">//此处虽然锁被打开，可能会出现线程问题，所以在执行完_class_resolveMethod方法之后，会goto retry，重新执行一遍之前查找的过程。</span></span><br><span class="line">        <span class="comment">// try [nonMetaClass resolveClassMethod:sel]</span></span><br><span class="line">        <span class="comment">// and [cls resolveInstanceMethod:sel]</span></span><br><span class="line">        _class_resolveMethod(<span class="keyword">cls</span>, sel, inst)<span class="comment">;</span></span><br><span class="line">        runtimeLock.lock()<span class="comment">;</span></span><br><span class="line">        <span class="comment">//寻找用户指定的方法的解决者</span></span><br><span class="line">        <span class="comment">// Don't cache the result; we don't hold the lock so it may have </span></span><br><span class="line">        <span class="comment">// changed already. Re-do the search from scratch instead.</span></span><br><span class="line">        triedResolver = YES<span class="comment">;</span></span><br><span class="line">        <span class="comment">//重新查找</span></span><br><span class="line">        <span class="keyword">goto</span> retry<span class="comment">;</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// No implementation found, and method resolver didn't help. </span></span><br><span class="line">    <span class="comment">// Use forwarding.</span></span><br><span class="line">    <span class="comment">// 消息转发</span></span><br><span class="line">    imp = (IMP)_objc_msgForward_impcache<span class="comment">;</span></span><br><span class="line">    <span class="comment">//---&gt; __objc_msgForward_impcache --&gt; __objc_msgForward --&gt; __objc_forward_handler --&gt; objc_defaultForwardHandler</span></span><br><span class="line">    <span class="comment">//在cache_fill中还会去调用cache_fill_nolock函数，如果缓存中的内容大于容量的 3/4就会扩充缓存，使缓存的大小翻倍。找到第一个空的 bucket_t，以 (SEL, IMP)的形式填充进去。</span></span><br><span class="line">    cache_fill(<span class="keyword">cls</span>, sel, imp, inst)<span class="comment">;</span></span><br><span class="line"> done:</span><br><span class="line">    runtimeLock.unlock()<span class="comment">;</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> imp<span class="comment">;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>lookUpImpOrForward 会先根据实际情况看要不要从缓存中查找，然后再从objc_class的方法列表中查找，如果还没有找到再从父类的缓存和方法列表中找，一直找到NSObject如果发现还没有，那么就调用_class_resolveMethod，这时候_class_resolveMethod可能会通过代码为该类增加方法实现，所以在_class_resolveMethod结束之后还需要通过goto retry走一遍上面流程。上面的resolve过程有些地方称为动态方法决议，下面给出一个简单例子：</p><p><figure class="highlight less"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">void</span> <span class="selector-tag">customMethodIMP</span>(id self, SEL _cmd) &#123;</span><br><span class="line">    <span class="selector-tag">NSLog</span>(<span class="string">"customMethodIMP running"</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">@<span class="selector-tag">implementation</span> <span class="selector-tag">IDLResolveTestClass</span></span><br><span class="line"></span><br><span class="line">+ (BOOL)<span class="selector-tag">resolveInstanceMethod</span><span class="selector-pseudo">:(SEL)aSEL</span> &#123;</span><br><span class="line">    <span class="selector-tag">if</span> (aSEL == <span class="variable">@selector</span>(unimplementSelector)) &#123;</span><br><span class="line">          <span class="selector-tag">class_addMethod</span>([self class], aSEL, (IMP) customMethodIMP, <span class="string">"v@:"</span>);</span><br><span class="line">          <span class="selector-tag">return</span> <span class="selector-tag">YES</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="selector-tag">return</span> <span class="selector-attr">[super resolveInstanceMethod:aSEL]</span>;</span><br><span class="line">&#125;</span><br><span class="line">@<span class="selector-tag">end</span></span><br></pre></td></tr></table></figure></p><p>上面例子中会判断如果向当前对象发送unimplementSelector消息的时候，resolveInstanceMethod会通过class_addMethod动态完类中添加unimplementSelector的IMP <strong><strong>customMethodIMP</strong></strong>，并返回YES,由于前面介绍了这个流程走完还会走一遍goto retry，所以就会在goto retry中找到customMethodIMP并执行。</p><p>如果方法决议还没有找到就准备走消息转发机制了，也就是：</p><p><figure class="highlight ini"><table><tr><td class="code"><pre><span class="line"><span class="attr">imp</span> = (IMP)_objc_msgForward_impcache<span class="comment">;</span></span><br></pre></td></tr></table></figure></p><p>先结合下面这张图有个大致的认知下，后面会整理个比较全面的流程图：</p><p><img src="/2020/02/02/iOS-Runtime源码解析消息处理/000000003.png" alt></p><p>这里同时需要注意一点：初始化方法+initialize就是在这个阶段调用的。也就是在第一次往某个对象发送消息的时候如果没有初始化就会调用+initialize。</p><p>前面我们了解到缓存查找cache_getImp是汇编实现的，我们接着来看下类方法列表的查找是怎样的：</p><p><figure class="highlight elixir"><table><tr><td class="code"><pre><span class="line">static method_t *</span><br><span class="line">getMethodNoSuper_nolock(Class cls, SEL sel)</span><br><span class="line">&#123;</span><br><span class="line">    runtimeLock.assertLocked();</span><br><span class="line"></span><br><span class="line">    assert(cls-&gt;isRealized());</span><br><span class="line">    /<span class="regexp">/在getMethodNoSuper_nolock方法中，会遍历一次methodList链表，从begin一直遍历到end。遍历过程中会调用search_method_list函数。</span></span><br><span class="line"><span class="regexp">    for (auto mlists = cls-&gt;data()-&gt;methods.beginLists(), </span></span><br><span class="line"><span class="regexp">              end = cls-&gt;data()-&gt;methods.endLists(); </span></span><br><span class="line"><span class="regexp">         mlists != end;</span></span><br><span class="line"><span class="regexp">         ++mlists)</span></span><br><span class="line"><span class="regexp">    &#123;</span></span><br><span class="line"><span class="regexp">        method_t *m = search_method_list(*mlists, sel);</span></span><br><span class="line"><span class="regexp">        if (m) return m;</span></span><br><span class="line"><span class="regexp">    &#125;</span></span><br><span class="line"><span class="regexp"></span></span><br><span class="line"><span class="regexp">    return nil;</span></span><br><span class="line"><span class="regexp">&#125;</span></span><br></pre></td></tr></table></figure></p><p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">method_t</span> *<span class="title">search_method_list</span><span class="params">(<span class="keyword">const</span> <span class="keyword">method_list_t</span> *mlist, SEL sel)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> methodListIsFixedUp = mlist-&gt;isFixedUp();</span><br><span class="line">    <span class="keyword">int</span> methodListHasExpectedSize = mlist-&gt;entsize() == <span class="keyword">sizeof</span>(<span class="keyword">method_t</span>);</span><br><span class="line">    <span class="comment">//在search_method_list函数中，会去判断当前methodList是否有序，如果有序，会调用findMethodInSortedMethodList方法，这个方法里面的实现是一个二分搜索</span></span><br><span class="line">    <span class="comment">//如果非有序，就调用线性的傻瓜式遍历搜索。</span></span><br><span class="line">    <span class="keyword">if</span> (__builtin_expect(methodListIsFixedUp &amp;&amp; methodListHasExpectedSize, <span class="number">1</span>)) &#123;</span><br><span class="line">        <span class="keyword">return</span> findMethodInSortedMethodList(sel, mlist);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="comment">// Linear search of unsorted method list</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">auto</span>&amp; meth : *mlist) &#123;</span><br><span class="line">            <span class="keyword">if</span> (meth.name == sel) <span class="keyword">return</span> &amp;meth;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//.....</span></span><br><span class="line">    <span class="keyword">return</span> nil;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>这里需要注意的是search_method_list方法，它会去判断当前methodList是否有序，如果有序，会调用findMethodInSortedMethodList方法，这个方法里面的实现是一个二分搜索，如果是无序的，就调用线性的傻瓜式遍历搜索。</p><p>消息查找已经大致介绍完了，如果这时候没有找到方法，就会进入下个阶段<strong><strong>消息转发阶段</strong></strong>了，消息转发顾名思义就是在当前对象不能处理当前消息的情况下将消息转给能够处理的对象进行处理。</p><p>我们紧接着上面_objc_msgForward_impcache来讲：</p><p><figure class="highlight sqf"><table><tr><td class="code"><pre><span class="line">STATIC_ENTRY <span class="variable">__objc_msgForward_impcache</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// No stret specialization.</span></span><br><span class="line">b<span class="variable">__objc_msgForward</span></span><br><span class="line"></span><br><span class="line">END_ENTRY <span class="variable">__objc_msgForward_impcache</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">ENTRY <span class="variable">__objc_msgForward</span></span><br><span class="line"></span><br><span class="line">adrpx17, <span class="variable">__objc_forward_handler</span>@PAGE</span><br><span class="line">ldrp17, [x17, <span class="variable">__objc_forward_handler</span>@PAGEOFF]</span><br><span class="line">TailCallFunctionPointer x17</span><br><span class="line"></span><br><span class="line">END_ENTRY <span class="variable">__objc_msgForward</span></span><br></pre></td></tr></table></figure></p><p>__objc_msgForward_impcache 内部很简单就是调用了__objc_msgForward，而在__objc_msgForward中也只是简单得转调了__objc_forward_handler。但是期间还有一系列的调用，我们没有办法看到源码，这部分大家可以看下杨萧玉的 <a href="http://yulingtianxia.com/blog/2016/06/15/Objective-C-Message-Sending-and-Forwarding/" target="_blank" rel="noopener">Objective-C 消息发送与转发机制原理</a>,这里为避免复杂化我们直接介绍结论：</p><p>在我们通过动态方法决议给类添加缺失的方法无效的情况下，Runtime就会继续调用****-(id)forwardingTargetForSelector:(SEL)aSelector****，这个方法会根据传入的selector，返回一个备用的target，这个消息会被转发给它进行处理，如果****-(id)forwardingTargetForSelector:(SEL)aSelector**** 返回nil 或者self，就会进入下一步。运行时系统首先会调用****-(NSMethodSignature <em>)methodSignatureForSelector:(SEL)aSelector</em><strong><em>方法来获得记录了方法的参数和返回值的信息的方法签名。如果</em></strong><em>methodSignatureForSelector</em>*** 返回的是nil, 运行时系统会抛出<strong><strong>unrecognized selector exception</strong></strong> ，程序到这里就结束了否则会继续调用 <em><strong><em>-(void)forwardInvocation:(NSInvocation <em>)anInvocation</em></em></strong>。我们这里要看下</em><strong><em>NSInvocation</em></strong>*有哪些功能，才能知道forwardInvocation 可以做哪些工作，NSInvocation定义如下：</p><p><figure class="highlight objectivec"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">@interface</span> <span class="title">NSInvocation</span> : <span class="title">NSObject</span></span></span><br><span class="line"></span><br><span class="line">+ (<span class="built_in">NSInvocation</span> *)invocationWithMethodSignature:(<span class="built_in">NSMethodSignature</span> *)sig;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@property</span> (<span class="keyword">readonly</span>, <span class="keyword">retain</span>) <span class="built_in">NSMethodSignature</span> *methodSignature;</span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)retainArguments;</span><br><span class="line"><span class="keyword">@property</span> (<span class="keyword">readonly</span>) <span class="built_in">BOOL</span> argumentsRetained;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@property</span> (<span class="keyword">nullable</span>, <span class="keyword">assign</span>) <span class="keyword">id</span> target;</span><br><span class="line"><span class="keyword">@property</span> SEL selector;</span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)getReturnValue:(<span class="keyword">void</span> *)retLoc;</span><br><span class="line">- (<span class="keyword">void</span>)setReturnValue:(<span class="keyword">void</span> *)retLoc;</span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)getArgument:(<span class="keyword">void</span> *)argumentLocation atIndex:(<span class="built_in">NSInteger</span>)idx;</span><br><span class="line">- (<span class="keyword">void</span>)setArgument:(<span class="keyword">void</span> *)argumentLocation atIndex:(<span class="built_in">NSInteger</span>)idx;</span><br><span class="line"></span><br><span class="line">- (<span class="keyword">void</span>)invoke;</span><br><span class="line">- (<span class="keyword">void</span>)invokeWithTarget:(<span class="keyword">id</span>)target;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br></pre></td></tr></table></figure></p><p>这里最关键的属性包括<strong><strong>methodSignature</strong></strong>，<strong><strong>target</strong></strong>，<strong><strong>selector</strong></strong>，方法部分主要可以分成三类，参数设置获取部分，返回值设置获取部分，以及触发部分，触发可以通过invokeWithTarget交给某个具体目标对象执行。</p><p>接着我们再来看下NSMethodSignature：</p><p><figure class="highlight objectivec"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">@interface</span> <span class="title">NSMethodSignature</span> : <span class="title">NSObject</span></span></span><br><span class="line"></span><br><span class="line">+ (<span class="keyword">nullable</span> <span class="built_in">NSMethodSignature</span> *)signatureWithObjCTypes:(<span class="keyword">const</span> <span class="keyword">char</span> *)types;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@property</span> (<span class="keyword">readonly</span>) <span class="built_in">NSUInteger</span> numberOfArguments;</span><br><span class="line">- (<span class="keyword">const</span> <span class="keyword">char</span> *)getArgumentTypeAtIndex:(<span class="built_in">NSUInteger</span>)idx <span class="built_in">NS_RETURNS_INNER_POINTER</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@property</span> (<span class="keyword">readonly</span>) <span class="built_in">NSUInteger</span> frameLength;</span><br><span class="line"></span><br><span class="line">- (<span class="built_in">BOOL</span>)isOneway;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@property</span> (<span class="keyword">readonly</span>) <span class="keyword">const</span> <span class="keyword">char</span> *methodReturnType <span class="built_in">NS_RETURNS_INNER_POINTER</span>;</span><br><span class="line"><span class="keyword">@property</span> (<span class="keyword">readonly</span>) <span class="built_in">NSUInteger</span> methodReturnLength;</span><br><span class="line"></span><br><span class="line"><span class="keyword">@end</span></span><br></pre></td></tr></table></figure></p><p>它主要包括了方法参数和返回值的信息获取。最后以一个流程图来做个总结：</p><p><img src="/2020/02/02/iOS-Runtime源码解析消息处理/000000001.png" alt></p><p>整个过程可以分成如下三个阶段：</p><ol><li>消息查找</li></ol><p>这个阶段开始之前会进行一系列的检查，比如检查当前selector是否是忽略的selector，检查target是否为空。然后查找当前对象的缓存是否有当前的要查找的对象，如果没有则进入方法列表的查找阶段，这个阶段会先在当前对象的方法列表中查找是否有待查找的方法，如果没有则顺着继承链往上找，先找父类的方法缓存，再找方法列表。一直找到NSObject都没找到的时候，就进入消息动态决议阶段。</p><ol start="2"><li>消息动态决议</li></ol><p>消息动态决议实际上是通过resolveInstanceMethod针对当前的消息，添加一个补充的IMP,然后再重新走消息查找的流程。如果还没找到则进入消息转发流程。</p><ol start="3"><li>消息转发</li></ol><p>消息转发可以分成两种，第一种通过forwardingTargetForSelector它不对消息参数和返回值类型做处理单纯转发给某个对象，这种比较简单，但是灵活性不如后者。</p><p>第二种通过forwardInvocation它可以通过methodSignatureForSelector改写方法签名，并在forwardInvocation中对参数数值进行修改，然后再调用invokeWithTarget将消息转发给某个特定的对象，在拿到返回值的时候还可以对返回值进行处理，可以说这是最灵活的一种方式了，JSPatch以及Aspect都是基于这种方式实现的。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;我们知道Objective C 是一门动态语言，它是在编译器和Runtime共同协作下完成的，编译时期决定了向哪个对象发送哪个消息，但是这个对象在收到具体消息的时候如何处理是由Runtime决定的,Runtime对消息的处理包括了消息的发送和消息的转发。我们接下来就来看下这
      
    
    </summary>
    
      <category term="Objective C 相关" scheme="http://yoursite.com/categories/Objective-C-%E7%9B%B8%E5%85%B3/"/>
    
    
      <category term="Objective C 相关" scheme="http://yoursite.com/tags/Objective-C-%E7%9B%B8%E5%85%B3/"/>
    
  </entry>
  
  <entry>
    <title>iOS Runtime源码解析 Runtime初始化</title>
    <link href="http://yoursite.com/2020/02/02/iOS-Runtime%E6%BA%90%E7%A0%81%E8%A7%A3%E6%9E%90-Runtime%E5%88%9D%E5%A7%8B%E5%8C%96/"/>
    <id>http://yoursite.com/2020/02/02/iOS-Runtime源码解析-Runtime初始化/</id>
    <published>2020-02-01T23:49:23.000Z</published>
    <updated>2020-02-04T17:52:10.000Z</updated>
    
    <content type="html"><![CDATA[<p>前面我们介绍了Mach O的结构，App的启动，dyld的加载，以及通过dyld将镜像加载进来，经过rebase/Bind处理后，找到main入口，以及Runtime相关的数据结构，有了前面的一系列铺垫这里介绍Runtime初始化就显得相对轻松了，这篇博客我们从《iOS Runtime源码解析之dyld》结尾处接着介绍，《iOS Runtime源码解析之dyld》中介绍了从dyld入口__dyld_start作为起点，到找到并跳转到主函数入口期间的一系列工作：</p><p><figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line"><span class="bullet">1. </span>将主程序初始化为imageLoader</span><br><span class="line"><span class="bullet">2. </span>加载共享库到内存</span><br><span class="line"><span class="bullet">3. </span>加载插入的动态库</span><br><span class="line"><span class="bullet">4. </span>链接主程序,链接插入库</span><br><span class="line"><span class="bullet">5. </span>初始化主程序，插入库</span><br><span class="line"><span class="bullet">6. </span>寻找主程序入口点</span><br></pre></td></tr></table></figure></p><p>这篇博客和《iOS Runtime源码解析之dyld》的衔接点就在于第5步初始化主程序，插入库这一步，在这个阶段，会调用使用attribute((constructor) 进行修饰的方法,其中有个十分重要的动态库初始化方法，<strong><strong>libSystem_initializer</strong></strong>：</p><p><figure class="highlight delphi"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> __attribute__((<span class="function"><span class="keyword">constructor</span>)) </span></span><br><span class="line"><span class="function"><span class="title">void</span> <span class="title">libSystem_initializer</span><span class="params">(int argc, <span class="keyword">const</span> char* argv[], <span class="keyword">const</span> char* envp[], <span class="keyword">const</span> char* apple[], <span class="keyword">const</span> struct ProgramVars* vars)</span> <span class="comment">&#123;</span></span></span><br><span class="line"><span class="function"><span class="comment"></span></span></span><br><span class="line"><span class="function"><span class="comment">    //......</span></span></span><br><span class="line"><span class="function"><span class="comment"></span></span></span><br><span class="line"><span class="function"><span class="comment">    _libkernel_init(libkernel_funcs);</span></span></span><br><span class="line"><span class="function"><span class="comment"></span></span></span><br><span class="line"><span class="function"><span class="comment">    bootstrap_init();</span></span></span><br><span class="line"><span class="function"><span class="comment">    mach_init();</span></span></span><br><span class="line"><span class="function"><span class="comment">    pthread_init();</span></span></span><br><span class="line"><span class="function"><span class="comment">    __libc_init(vars, libSystem_atfork_prepare, libSystem_atfork_parent, libSystem_atfork_child, apple);</span></span></span><br><span class="line"><span class="function"><span class="comment">    __keymgr_initializer();</span></span></span><br><span class="line"><span class="function"><span class="comment">    _dyld_initializer();</span></span></span><br><span class="line"><span class="function"><span class="comment">    libdispatch_init();</span></span></span><br><span class="line"><span class="function"><span class="comment">    _libxpc_initializer();</span></span></span><br><span class="line"><span class="function"><span class="comment"></span></span></span><br><span class="line"><span class="function"><span class="comment">    __stack_logging_early_finished();</span></span></span><br><span class="line"><span class="function"><span class="comment">    //.......</span></span></span><br><span class="line"><span class="function"><span class="comment">    errno = 0;</span></span></span><br><span class="line"><span class="function"><span class="comment">&#125;</span></span></span><br></pre></td></tr></table></figure></p><p>在libSystem.dylib的初始化方法libSystem_initializer中初始化了多了dylib库,比如:liblaunch.dylib,libc.a,libdispatch.a等等，这里最关键的是****<a href="https://github.com/nickhutchinson/libdispatch" target="_blank" rel="noopener">libdispatch_init</a>****:</p><p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">libdispatch_init</span><span class="params">(<span class="keyword">void</span>)</span> </span>&#123;</span><br><span class="line">    <span class="comment">//......</span></span><br><span class="line">    _os_object_init();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>在libdispatch_init的最后部分会调用****_os_object_init****：</p><p><figure class="highlight aspectj"><table><tr><td class="code"><pre><span class="line"><span class="keyword">void</span></span><br><span class="line">_os_object_init(<span class="keyword">void</span>)</span><br><span class="line">&#123;</span><br><span class="line"><span class="function"><span class="keyword">return</span> <span class="title">_objc_init</span><span class="params">()</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p><strong><strong>_os_object_init</strong></strong>方法只有一行代码就是转调_objc_init，这就是我们十分关注的Runtime 初始化的入口。</p><p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">void</span> _objc_init(<span class="keyword">void</span>) &#123;</span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">bool</span> initialized = <span class="literal">false</span>;</span><br><span class="line">    <span class="keyword">if</span> (initialized) <span class="keyword">return</span>;</span><br><span class="line">    initialized = <span class="literal">true</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// fixme defer initialization until an objc-using image is found?</span></span><br><span class="line">    environ_init();</span><br><span class="line">    tls_init();</span><br><span class="line">    static_init();</span><br><span class="line">    lock_init();</span><br><span class="line">    exception_init();</span><br><span class="line"></span><br><span class="line">    <span class="comment">//_dyld_objc_notify_register(&amp;map_2_images, load_images, unmap_image);</span></span><br><span class="line">    <span class="comment">//向dyld注册了回调函数,当imagemap到内存中,当初始化完成image时和卸载image的时候都会回调注册者</span></span><br><span class="line">    _dyld_objc_notify_register(&amp;map_images, load_images, unmap_image);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p><strong><strong>_objc_init</strong></strong>向dyld注册了map_images，load_images，unmap_image三个关键的回调函数,各个关键阶段节点常量定义如下：</p><p><figure class="highlight crystal"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">enum</span> <span class="title">dyld_image_states</span></span></span><br><span class="line">&#123;</span><br><span class="line">    dyld_image_state_mapped                 = <span class="number">10</span>,       <span class="regexp">//</span> No batch notification <span class="keyword">for</span> this</span><br><span class="line">    dyld_image_state_dependents_mapped      = <span class="number">20</span>,       <span class="regexp">//</span> Only batch notification <span class="keyword">for</span> this</span><br><span class="line">    dyld_image_state_rebased                = <span class="number">30</span>,</span><br><span class="line">    dyld_image_state_bound                  = <span class="number">40</span>,</span><br><span class="line">    dyld_image_state_dependents_initialized = <span class="number">45</span>,       <span class="regexp">//</span> Only single notification <span class="keyword">for</span> this</span><br><span class="line">    dyld_image_state_initialized            = <span class="number">50</span>,</span><br><span class="line">    dyld_image_state_terminated             = <span class="number">60</span>        /<span class="regexp">/ Only single notification for this</span></span><br><span class="line"><span class="regexp">&#125;;</span></span><br></pre></td></tr></table></figure></p><p>我们看下****_dyld_objc_notify_register****这个方法的注释，通过注释我们可以了解到，在runtime中可以通过这个方法可以向dyld注册用于处理镜像完成映射，取消映射和初始化之后的处理方法。</p><p><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">//</span><br><span class="line">// Note: only for <span class="keyword">use</span> <span class="keyword">by</span> objc runtime</span><br><span class="line">// <span class="keyword">Register</span> handlers <span class="keyword">to</span> be called <span class="keyword">when</span> objc images <span class="keyword">are</span> mapped, unmapped, <span class="keyword">and</span> initialized.</span><br><span class="line">// Dyld will <span class="keyword">call</span> back the <span class="string">"mapped"</span> <span class="keyword">function</span> <span class="keyword">with</span> an <span class="built_in">array</span> <span class="keyword">of</span> images that contain an objc-image-info section.</span><br><span class="line">// Those images that <span class="keyword">are</span> dylibs will have the <span class="keyword">ref</span>-counts automatically bumped, so objc will <span class="keyword">no</span> longer need <span class="keyword">to</span></span><br><span class="line">// <span class="keyword">call</span> dlopen() <span class="keyword">on</span> them <span class="keyword">to</span> <span class="keyword">keep</span> them <span class="keyword">from</span> being unloaded.  During the <span class="keyword">call</span> <span class="keyword">to</span> _dyld_objc_notify_register(),</span><br><span class="line">// dyld will <span class="keyword">call</span> the <span class="string">"mapped"</span> <span class="keyword">function</span> <span class="keyword">with</span> already loaded objc images.  During <span class="keyword">any</span> later dlopen() <span class="keyword">call</span>,</span><br><span class="line">// dyld will also <span class="keyword">call</span> the <span class="string">"mapped"</span> function.  Dyld will <span class="keyword">call</span> the <span class="string">"init"</span> <span class="keyword">function</span> <span class="keyword">when</span> dyld would be called</span><br><span class="line">// initializers <span class="keyword">in</span> that image.  This <span class="keyword">is</span> <span class="keyword">when</span> objc calls <span class="keyword">any</span> +<span class="keyword">load</span> methods <span class="keyword">in</span> that image.</span><br><span class="line">// 通过这个方法可以向dyld注册用于处理镜像完成映射，取消映射和初始化之后的处理方法。</span><br><span class="line"></span><br><span class="line"><span class="built_in">void</span> _dyld_objc_notify_register(_dyld_objc_notify_mapped    mapped,</span><br><span class="line">                                _dyld_objc_notify_init      init,</span><br><span class="line">                                _dyld_objc_notify_unmapped  unmapped);</span><br></pre></td></tr></table></figure></p><p>我们重点关注下****_dyld_objc_notify_mapped<strong><strong>以及</strong></strong>_dyld_objc_notify_init****，</p><ul><li><strong><strong>_dyld_objc_notify_mapped</strong></strong></li></ul><p><strong><strong>map_images</strong></strong>会在主程序以及其他库加载进来后调用<strong><strong>ImageLoader::setMapped</strong></strong>发出通知，调用map_images：</p><p><figure class="highlight verilog"><table><tr><td class="code"><pre><span class="line"><span class="keyword">void</span> ImageLoader::setMapped(<span class="keyword">const</span> LinkContext&amp; <span class="keyword">context</span>)</span><br><span class="line">&#123;</span><br><span class="line">    fState = dyld_image_state_mapped;</span><br><span class="line">    <span class="keyword">context</span><span class="variable">.notifySingle</span>(dyld_image_state_mapped, <span class="keyword">this</span>);  <span class="comment">// note: can throw exception</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>而<strong><strong>ImageLoader::setMapped</strong></strong>会在instantiateMainExecutable实例话主程序，instantiateFromFile实例化镜像的时候被调用：</p><ul><li><strong><strong>_dyld_objc_notify_init</strong></strong></li></ul><p>会在<strong><strong>ImageLoader::runInitializers</strong></strong> 方法中被调用，而<strong><strong>ImageLoader::runInitializers</strong></strong> 则会在 <strong><strong>initializeMainExecutable</strong></strong>中调用。</p><p>也即是说在实例化主程序或者其他dylib库的时候都会发出_dyld_objc_notify_mapped通知，这时候会调用runtime 的 map_images方法进行后续处理，在这些初始化完成的时候会发出_dyld_objc_notify_init通知调用load_images方法进行后续处理。</p><p>我们紧接着来看下<strong><strong>map_images</strong></strong>以及<strong><strong>load_images</strong></strong>的处理，其实在介绍分类的时候已经介绍过了map_images这里为保证整个文章的完整性再过一遍这部分内容：</p><ul><li><h5><strong><strong>map_images</strong></strong></h5></li></ul><p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">void</span></span><br><span class="line">map_images(<span class="keyword">unsigned</span> count, <span class="keyword">const</span> <span class="keyword">char</span> * <span class="keyword">const</span> paths[],</span><br><span class="line">           <span class="keyword">const</span> struct mach_header * <span class="keyword">const</span> mhdrs[])</span><br><span class="line">&#123;</span><br><span class="line">    <span class="function"><span class="keyword">mutex_locker_t</span> <span class="title">lock</span><span class="params">(runtimeLock)</span></span>;</span><br><span class="line">    <span class="keyword">return</span> map_images_nolock(count, paths, mhdrs);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p><figure class="highlight objectivec"><table><tr><td class="code"><pre><span class="line"><span class="keyword">void</span> </span><br><span class="line">map_images_nolock(<span class="keyword">unsigned</span> mhCount, <span class="keyword">const</span> <span class="keyword">char</span> * <span class="keyword">const</span> mhPaths[],</span><br><span class="line">                  <span class="keyword">const</span> <span class="keyword">struct</span> mach_header * <span class="keyword">const</span> mhdrs[])</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">//......</span></span><br><span class="line">    <span class="keyword">if</span> (hCount &gt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="comment">//读取镜像信息</span></span><br><span class="line">        _read_images(hList, hCount, totalClasses, unoptimizedTotalClasses);</span><br><span class="line">    &#125;</span><br><span class="line">    firstTime = <span class="literal">NO</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p><figure class="highlight xl"><table><tr><td class="code"><pre><span class="line">void _read_images(header_info **hList, uint32_t hCount, int totalClasses, int unoptimizedTotalClasses)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">//......</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// =================================查找classes=================================</span></span><br><span class="line">    <span class="comment">// Discover classes. Fix up unresolved future classes. Mark bundle classes.</span></span><br><span class="line">    <span class="keyword">for</span> (EACH_HEADER) &#123;</span><br><span class="line">        <span class="comment">//从Mach-O的 __DATA区 __objc_classlist 获取所有类,并加入gdb_objc_realized_classes list中</span></span><br><span class="line">        classref_t *classlist = _getObjc2ClassList(hi, &amp;count);</span><br><span class="line">        <span class="comment">//.......</span></span><br><span class="line">        <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; count; i++) &#123;</span><br><span class="line">            Class cls = (Class)classlist[i];</span><br><span class="line">            Class newCls = readClass(cls, headerIsBundle, headerIsPreoptimized);</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> (newCls != cls  &amp;&amp;  newCls) &#123;</span><br><span class="line">                <span class="comment">// 类被移动了但是没有被删除的情况</span></span><br><span class="line">                <span class="comment">// Class was moved but not deleted. Currently this occurs </span></span><br><span class="line">                <span class="comment">// only when the new class resolved a future class.</span></span><br><span class="line">                <span class="comment">// Non-lazily realize the class below.</span></span><br><span class="line">                resolvedFutureClasses = (Class *)realloc(resolvedFutureClasses, (resolvedFutureClassCount+<span class="number">1</span>) * sizeof(Class));</span><br><span class="line">                resolvedFutureClasses[resolvedFutureClassCount++] = newCls;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    ts.<span class="built_in">log</span>(<span class="string">"IMAGE TIMES: discover classes"</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 修复重新映射的类</span></span><br><span class="line">    <span class="comment">// Fix up remapped classes</span></span><br><span class="line">    <span class="comment">// Class list and nonlazy class list remain unremapped.</span></span><br><span class="line">    <span class="comment">// Class refs and super refs are remapped for message dispatching.</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span> (!noClassesRemapped()) &#123;</span><br><span class="line">        <span class="keyword">for</span> (EACH_HEADER) &#123;</span><br><span class="line">            Class *classrefs = _getObjc2ClassRefs(hi, &amp;count);</span><br><span class="line">            <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; count; i++) &#123;</span><br><span class="line">                remapClassRef(&amp;classrefs[i]);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// fixme why doesn't test future1 catch the absence of this?</span></span><br><span class="line">            classrefs = _getObjc2SuperRefs(hi, &amp;count);</span><br><span class="line">            <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; count; i++) &#123;</span><br><span class="line">                remapClassRef(&amp;classrefs[i]);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 重新映射类</span></span><br><span class="line">    ts.<span class="built_in">log</span>(<span class="string">"IMAGE TIMES: remap classes"</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Fix up @selector references</span></span><br><span class="line">    static size_t UnfixedSelectors;</span><br><span class="line">    &#123;</span><br><span class="line">        mutex_locker_t lock(selLock);</span><br><span class="line">        <span class="keyword">for</span> (EACH_HEADER) &#123;</span><br><span class="line">            <span class="function"><span class="title">if</span> (hi-&gt;</span>isPreoptimized()) continue;</span><br><span class="line">            </span><br><span class="line">            <span class="function"><span class="title">bool</span> isBundle = hi-&gt;</span>isBundle();</span><br><span class="line">            SEL *sels = _getObjc2SelectorRefs(hi, &amp;count);</span><br><span class="line">            UnfixedSelectors += count;</span><br><span class="line">            <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; count; i++) &#123;</span><br><span class="line">                const char *<span class="keyword">name</span> = sel_cname(sels[i]);</span><br><span class="line">                <span class="comment">// 3. 注册Sel,并存储到全局变量namedSelectors的list中</span></span><br><span class="line">                sels[i] = sel_registerNameNoLock(<span class="keyword">name</span>, isBundle);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    ts.<span class="built_in">log</span>(<span class="string">"IMAGE TIMES: fix up selector references"</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">//.....</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// Discover protocols. Fix up protocol refs.</span></span><br><span class="line">    <span class="keyword">for</span> (EACH_HEADER) &#123;</span><br><span class="line">        extern objc_class OBJC_CLASS_$_Protocol;</span><br><span class="line">        Class cls = (Class)&amp;OBJC_CLASS_$_Protocol;</span><br><span class="line">        assert(cls);</span><br><span class="line">        NXMapTable *protocol_map = protocols();</span><br><span class="line">        <span class="function"><span class="title">bool</span> isPreoptimized = hi-&gt;</span>isPreoptimized();</span><br><span class="line">        <span class="function"><span class="title">bool</span> isBundle = hi-&gt;</span>isBundle();</span><br><span class="line">        <span class="comment">//找到所有Protocol并处理引用</span></span><br><span class="line">        protocol_t **protolist = _getObjc2ProtocolList(hi, &amp;count);</span><br><span class="line">        <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; count; i++) &#123;</span><br><span class="line">            readProtocol(protolist[i], cls, protocol_map, </span><br><span class="line">                         isPreoptimized, isBundle);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    ts.<span class="built_in">log</span>(<span class="string">"IMAGE TIMES: discover protocols"</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Fix up @protocol references</span></span><br><span class="line">    <span class="comment">// Preoptimized images may have the right </span></span><br><span class="line">    <span class="comment">// answer already but we don't know for sure.</span></span><br><span class="line">    <span class="keyword">for</span> (EACH_HEADER) &#123;</span><br><span class="line">        protocol_t **protolist = _getObjc2ProtocolRefs(hi, &amp;count);</span><br><span class="line">        <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; count; i++) &#123;</span><br><span class="line">            remapProtocolRef(&amp;protolist[i]);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    ts.<span class="built_in">log</span>(<span class="string">"IMAGE TIMES: fix up @protocol references"</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Realize non-lazy classes (for +load methods and static instances)</span></span><br><span class="line">    <span class="keyword">for</span> (EACH_HEADER) &#123;</span><br><span class="line">        classref_t *classlist = </span><br><span class="line">            _getObjc2NonlazyClassList(hi, &amp;count);</span><br><span class="line">        <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; count; i++) &#123;</span><br><span class="line">            Class cls = remapClass(classlist[i]);</span><br><span class="line">            <span class="keyword">if</span> (!cls) continue;</span><br><span class="line">            addClassTableEntry(cls);</span><br><span class="line">            realizeClass(cls);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    ts.<span class="built_in">log</span>(<span class="string">"IMAGE TIMES: realize non-lazy classes"</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Realize newly-resolved future classes, in case CF manipulates them</span></span><br><span class="line">    <span class="keyword">if</span> (resolvedFutureClasses) &#123;</span><br><span class="line">        <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; resolvedFutureClassCount; i++) &#123;</span><br><span class="line">            realizeClass(resolvedFutureClasses[i]);</span><br><span class="line">            <span class="function"><span class="title">resolvedFutureClasses</span>[i]-&gt;</span>setInstancesRequireRawIsa(<span class="literal">false</span><span class="comment">/*inherited*/</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        free(resolvedFutureClasses);</span><br><span class="line">    &#125;    </span><br><span class="line"></span><br><span class="line">    ts.<span class="built_in">log</span>(<span class="string">"IMAGE TIMES: realize future classes"</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Discover categories. </span></span><br><span class="line">    <span class="keyword">for</span> (EACH_HEADER) &#123;</span><br><span class="line">        category_t **catlist = </span><br><span class="line">            _getObjc2CategoryList(hi, &amp;count);</span><br><span class="line">        <span class="comment">//查看是否包含属性</span></span><br><span class="line">        <span class="function"><span class="title">bool</span> hasClassProperties = hi-&gt;</span><span class="function"><span class="title">info</span>()-&gt;</span>hasCategoryClassProperties();</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; count; i++) &#123;</span><br><span class="line">            category_t *cat = catlist[i];</span><br><span class="line">            <span class="comment">//获取到所属的类</span></span><br><span class="line">            C<span class="function"><span class="title">lass</span> cls = remapClass(cat-&gt;</span>cls);</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> (!cls) &#123;</span><br><span class="line">                <span class="comment">// Category's target class is missing (probably weak-linked).</span></span><br><span class="line">                <span class="comment">// Disavow any knowledge of this category.</span></span><br><span class="line">                catlist[i] = <span class="literal">nil</span>;</span><br><span class="line">                <span class="keyword">if</span> (PrintConnecting) &#123;</span><br><span class="line">                    _objc_inform(<span class="string">"CLASS: IGNORING category \?\?\?(%s) %p with "</span></span><br><span class="line">                                 <span class="string">"missing weak-linked target class"</span>, </span><br><span class="line">                                 <span class="function"><span class="title">cat</span>-&gt;</span><span class="keyword">name</span>, cat);</span><br><span class="line">                &#125;</span><br><span class="line">                continue;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="comment">// Process this category. </span></span><br><span class="line">            <span class="comment">// First, register the category with its target class. </span></span><br><span class="line">            <span class="comment">// Then, rebuild the class's method lists (etc) if </span></span><br><span class="line">            <span class="comment">// the class is realized. </span></span><br><span class="line">            bool classExists = NO;</span><br><span class="line">            <span class="comment">//如果有实例方法，协议或者实例属性</span></span><br><span class="line">            <span class="function"><span class="title">if</span> (cat-&gt;</span><span class="function"><span class="title">instanceMethods</span> ||  cat-&gt;</span>protocols  </span><br><span class="line">                ||  <span class="function"><span class="title">cat</span>-&gt;</span>instanceProperties) </span><br><span class="line">            &#123;</span><br><span class="line">                addUnattachedCategoryForClass(cat, cls, hi);</span><br><span class="line">                <span class="function"><span class="title">if</span> (cls-&gt;</span>isRealized()) &#123;</span><br><span class="line">                    <span class="comment">//将分类添到属性，方法，协议添加到对应的类中</span></span><br><span class="line">                    remethodizeClass(cls);</span><br><span class="line">                    classExists = YES;</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">if</span> (PrintConnecting) &#123;</span><br><span class="line">                    _objc_inform(<span class="string">"CLASS: found category -%s(%s) %s"</span>, </span><br><span class="line">                                 <span class="function"><span class="title">cls</span>-&gt;</span><span class="function"><span class="title">nameForLogging</span>(), cat-&gt;</span><span class="keyword">name</span>, </span><br><span class="line">                                 classExists ? <span class="string">"on existing class"</span> : <span class="string">""</span>);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">//如果有类方法，协议或者类属性</span></span><br><span class="line">            <span class="function"><span class="title">if</span> (cat-&gt;</span><span class="function"><span class="title">classMethods</span>  ||  cat-&gt;</span>protocols  </span><br><span class="line">                ||  (<span class="function"><span class="title">hasClassProperties</span> &amp;&amp; cat-&gt;</span>_classProperties)) </span><br><span class="line">            &#123;</span><br><span class="line">                <span class="function"><span class="title">addUnattachedCategoryForClass</span>(cat, cls-&gt;</span>ISA(), hi);</span><br><span class="line">                <span class="function"><span class="title">if</span> (cls-&gt;</span>ISA()-&gt;isRealized()) &#123;</span><br><span class="line">                    <span class="function"><span class="title">remethodizeClass</span>(cls-&gt;</span>ISA());</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">if</span> (PrintConnecting) &#123;</span><br><span class="line">                    _objc_inform(<span class="string">"CLASS: found category +%s(%s)"</span>, </span><br><span class="line">                                 <span class="function"><span class="title">cls</span>-&gt;</span><span class="function"><span class="title">nameForLogging</span>(), cat-&gt;</span><span class="keyword">name</span>);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    ts.<span class="built_in">log</span>(<span class="string">"IMAGE TIMES: discover categories"</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Category discovery MUST BE LAST to avoid potential races </span></span><br><span class="line">    <span class="comment">// when other threads call the new category code before </span></span><br><span class="line">    <span class="comment">// this thread finishes its fixups.</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// +load handled by prepare_load_methods()</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (DebugNonFragileIvars) &#123;</span><br><span class="line">        realizeAllClasses();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//......</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>map_images 最关键的代码在_read_images方法中，<strong><strong>_read_images</strong></strong>故名思议就是读取镜像，在这个方法中会从镜像的_DATA区域通过****_getObjc2XXXX****将该镜像的类列表，分类列表，协议列表读取出来，对应的方法以及读取的session部分可以查看如下声明，这里需要提一下_DATA区域中的这些session数据是由编译器写入的。</p><p><figure class="highlight lisp"><table><tr><td class="code"><pre><span class="line">//      function name                 content type     section name</span><br><span class="line">GETSECT(<span class="name">_getObjc2SelectorRefs</span>,        SEL,             <span class="string">"__objc_selrefs"</span>)<span class="comment">; </span></span><br><span class="line">GETSECT(<span class="name">_getObjc2MessageRefs</span>,         message_ref_t,   <span class="string">"__objc_msgrefs"</span>)<span class="comment">; </span></span><br><span class="line">GETSECT(<span class="name">_getObjc2ClassRefs</span>,           Class,           <span class="string">"__objc_classrefs"</span>)<span class="comment">;</span></span><br><span class="line">GETSECT(<span class="name">_getObjc2SuperRefs</span>,           Class,           <span class="string">"__objc_superrefs"</span>)<span class="comment">;</span></span><br><span class="line">GETSECT(<span class="name">_getObjc2ClassList</span>,           classref_t,      <span class="string">"__objc_classlist"</span>)<span class="comment">;</span></span><br><span class="line">GETSECT(<span class="name">_getObjc2NonlazyClassList</span>,    classref_t,      <span class="string">"__objc_nlclslist"</span>)<span class="comment">;</span></span><br><span class="line">GETSECT(<span class="name">_getObjc2CategoryList</span>,        category_t *,    "__objc_catlist");</span><br><span class="line">GETSECT(_getObjc2NonlazyCategoryList, category_t *,    <span class="string">"__objc_nlcatlist"</span>)<span class="comment">;</span></span><br><span class="line">GETSECT(<span class="name">_getObjc2ProtocolList</span>,        protocol_t *,    "__objc_protolist");</span><br><span class="line">GETSECT(_getObjc2ProtocolRefs,        protocol_t *,    <span class="string">"__objc_protorefs"</span>)<span class="comment">;</span></span><br><span class="line">GETSECT(<span class="name">getLibobjcInitializers</span>,       UnsignedInitializer, <span class="string">"__objc_init_func"</span>)<span class="comment">;</span></span><br></pre></td></tr></table></figure></p><p>这个方法的每个阶段结束都会调用ts.log(&quot;IMAGE TIMES:XXXXXX)来提示每个阶段结束，在这些阶段中有我们之前分析过的Catogies 加载，以及协议加载。我们重点看下最末尾的realizeAllClasses方法：</p><p><figure class="highlight abnf"><table><tr><td class="code"><pre><span class="line">static void realizeAllClasses(void)</span><br><span class="line">&#123;</span><br><span class="line">    runtimeLock.assertLocked()<span class="comment">;</span></span><br><span class="line">    header_info *hi<span class="comment">;</span></span><br><span class="line">    for (hi = FirstHeader<span class="comment">; hi; hi = hi-&gt;getNext()) &#123;</span></span><br><span class="line">        realizeAllClassesInImage(hi)<span class="comment">;</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p><figure class="highlight lisp"><table><tr><td class="code"><pre><span class="line">static void realizeAllClassesInImage(<span class="name">header_info</span> *hi) &#123;</span><br><span class="line"></span><br><span class="line">    runtimeLock.assertLocked();</span><br><span class="line"></span><br><span class="line">    size_t count, i;</span><br><span class="line">    classref_t *classlist<span class="comment">;</span></span><br><span class="line"></span><br><span class="line">    if (<span class="name">hi-&gt;areAllClassesRealized</span>()) return<span class="comment">;</span></span><br><span class="line"></span><br><span class="line">    classlist = _getObjc2ClassList(<span class="name">hi</span>, <span class="symbol">&amp;count</span>)<span class="comment">;</span></span><br><span class="line"></span><br><span class="line">    for (<span class="name">i</span> = <span class="number">0</span><span class="comment">; i &lt; count; i++) &#123;</span></span><br><span class="line">        realizeClass(<span class="name">remapClass</span>(<span class="name">classlist</span>[i]))<span class="comment">;</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    hi-&gt;setAllClassesRealized(<span class="name">YES</span>)<span class="comment">;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>realizeAllClasses会对镜像中的所有class调用realizeClass方法。</p><p><figure class="highlight xl"><table><tr><td class="code"><pre><span class="line">static Class realizeClass(Class cls) &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//.....</span></span><br><span class="line">    const class_ro_t *ro;</span><br><span class="line">    class_rw_t *rw;</span><br><span class="line">    Class supercls;</span><br><span class="line">    Class metacls;</span><br><span class="line">    bool isMeta;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (!cls) return <span class="literal">nil</span>;               <span class="comment">//cls 不能为空</span></span><br><span class="line">    <span class="function"><span class="title">if</span> (cls-&gt;</span>isRealized()) return cls;  <span class="comment">//cls 如果已经初始化直接返回</span></span><br><span class="line">    assert(cls == remapClass(cls));     <span class="comment">//cls 没有重新分配，remapClass 返回指向cls的实时指针</span></span><br><span class="line">    <span class="comment">// 【✨】获取只读结构体</span></span><br><span class="line">    <span class="function"><span class="title">ro</span> = (const class_ro_t *)cls-&gt;</span><span class="keyword">data</span>();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Normal class. Allocate writeable class data.</span></span><br><span class="line">    rw = (class_rw_t *)calloc(sizeof(class_rw_t), <span class="number">1</span>);           <span class="comment">//分配读写数据</span></span><br><span class="line">    <span class="function"><span class="title">rw</span>-&gt;</span>ro = ro;                                                <span class="comment">//只写数据</span></span><br><span class="line">    <span class="function"><span class="title">rw</span>-&gt;</span>flags = RW_REALIZED|RW_REALIZING;                       <span class="comment">//设置已经初始化标志</span></span><br><span class="line">    <span class="function"><span class="title">cls</span>-&gt;</span>setData(rw);                                           <span class="comment">//为cls设置data数据</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="title">isMeta</span> = ro-&gt;</span>flags &amp; RO_META;                                   <span class="comment">//判断是否是元类</span></span><br><span class="line">    <span class="function"><span class="title">rw</span>-&gt;</span>version = isMeta ? <span class="number">7</span> : <span class="number">0</span>;  <span class="comment">// old runtime went up to 6      //版本信息，旧版本的版本信息为6</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">//.....</span></span><br><span class="line">    <span class="comment">// Realize superclass and metaclass, if they aren't already.</span></span><br><span class="line">    <span class="comment">// This needs to be done after RW_REALIZED is set above, for root classes.</span></span><br><span class="line">    <span class="comment">// This needs to be done after class index is chosen, for root metaclasses.</span></span><br><span class="line">    <span class="comment">// 【✨】为supercls，metacls 分配空间</span></span><br><span class="line">    <span class="function"><span class="title">supercls</span> = realizeClass(remapClass(cls-&gt;</span>superclass));</span><br><span class="line">    <span class="function"><span class="title">metacls</span> = realizeClass(remapClass(cls-&gt;</span>ISA()));</span><br><span class="line"></span><br><span class="line">    <span class="comment">//......</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 【✨】 Update superclass and metaclass in case of remapping</span></span><br><span class="line">    <span class="function"><span class="title">cls</span>-&gt;</span><span class="function"><span class="title">superclass</span> = supercls; //将supercls赋给 cls-&gt;</span>superclass</span><br><span class="line">    <span class="comment">//将上面分配的metacls赋给cls</span></span><br><span class="line">    <span class="function"><span class="title">cls</span>-&gt;</span>initClassIsa(metacls);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 调整实例变量的偏移和布局，这个将会重新分配class_ro_t</span></span><br><span class="line">    <span class="comment">// Reconcile instance variable offsets / layout.</span></span><br><span class="line">    <span class="comment">// This may reallocate class_ro_t, updating our ro variable.</span></span><br><span class="line">    <span class="keyword">if</span> (supercls  &amp;&amp;  !isMeta) reconcileInstanceVariables(cls, supercls, ro);</span><br><span class="line"></span><br><span class="line">    <span class="comment">//设置对象尺寸</span></span><br><span class="line">    <span class="comment">// Set fastInstanceSize if it wasn't set already.</span></span><br><span class="line">    <span class="function"><span class="title">cls</span>-&gt;</span><span class="function"><span class="title">setInstanceSize</span>(ro-&gt;</span>instanceSize);</span><br><span class="line"></span><br><span class="line">    <span class="comment">//【✨】从ro中拷贝部分标志位到rw 字段</span></span><br><span class="line">    <span class="comment">// Copy some flags from ro to rw</span></span><br><span class="line">    <span class="function"><span class="title">if</span> (ro-&gt;</span>flags &amp; RO_HAS_CXX_STRUCTORS) &#123;</span><br><span class="line">        <span class="function"><span class="title">cls</span>-&gt;</span>setHasCxxDtor();</span><br><span class="line">        <span class="function"><span class="title">if</span> (! (ro-&gt;</span>flags &amp; RO_HAS_CXX_DTOR_ONLY)) &#123;</span><br><span class="line">            <span class="function"><span class="title">cls</span>-&gt;</span>setHasCxxCtor();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 【✨】 Connect this class to its superclass's subclass lists</span></span><br><span class="line">    <span class="comment">// 将当前class与父类相关连</span></span><br><span class="line">    <span class="keyword">if</span> (supercls) &#123;</span><br><span class="line">        <span class="comment">//将当前类作为supercls的子类添加到父类的子类列表</span></span><br><span class="line">        addSubclass(supercls, cls);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="comment">//将当前类作为根类</span></span><br><span class="line">        addRootClass(cls);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">//【✨】实例化类结构</span></span><br><span class="line">    <span class="comment">// Attach categories</span></span><br><span class="line">    <span class="comment">// 使得类有条理</span></span><br><span class="line">    methodizeClass(cls);</span><br><span class="line"></span><br><span class="line">    return cls;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>realizeClass 方法实际上是为类的class_rw_t，superclass，metacls等分配空间，并初始化。在最后的时候会调用methodizeClass进行进一步的初始化：</p><p><figure class="highlight xl"><table><tr><td class="code"><pre><span class="line">static void methodizeClass(Class cls)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">//检查锁</span></span><br><span class="line">    runtimeLock.assertLocked();</span><br><span class="line">    <span class="comment">//是否是元类</span></span><br><span class="line">    <span class="function"><span class="title">bool</span> isMeta = cls-&gt;</span>isMetaClass();</span><br><span class="line">    <span class="comment">//获取可读写字段</span></span><br><span class="line">    <span class="function"><span class="title">auto</span> rw = cls-&gt;</span><span class="keyword">data</span>();</span><br><span class="line">    <span class="comment">//获取只读字段</span></span><br><span class="line">    <span class="function"><span class="title">auto</span> ro = rw-&gt;</span>ro;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Install methods and properties that the class implements itself.</span></span><br><span class="line">    <span class="comment">// 加载类自身实现的方法和属性</span></span><br><span class="line">    <span class="function"><span class="title">method_list_t</span> *list = ro-&gt;</span>baseMethods();</span><br><span class="line">    <span class="keyword">if</span> (list) &#123;</span><br><span class="line">        prepareMethodLists(cls, &amp;list<span class="comment">/*自身实现的方法列表*/</span>, <span class="number">1</span>, YES, isBundleClass(cls));</span><br><span class="line">        <span class="comment">//将ro-&gt;baseMethods方法添加到rw-&gt;methods</span></span><br><span class="line">        <span class="function"><span class="title">rw</span>-&gt;</span>methods.attachLists(&amp;list, <span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="title">property_list_t</span> *proplist = ro-&gt;</span>baseProperties;</span><br><span class="line">    <span class="keyword">if</span> (proplist) &#123;</span><br><span class="line">        <span class="comment">//将ro-&gt;baseProperties方法添加到rw-&gt;properties</span></span><br><span class="line">        <span class="function"><span class="title">rw</span>-&gt;</span>properties.attachLists(&amp;proplist, <span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="title">protocol_list_t</span> *protolist = ro-&gt;</span>baseProtocols;</span><br><span class="line">    <span class="keyword">if</span> (protolist) &#123;</span><br><span class="line">        <span class="comment">//将ro-&gt;baseProtocols方法添加到rw-&gt;protocols</span></span><br><span class="line">        <span class="function"><span class="title">rw</span>-&gt;</span>protocols.attachLists(&amp;protolist, <span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Root classes get bonus method implementations if they don't have </span></span><br><span class="line">    <span class="comment">// them already. These apply before category replacements.</span></span><br><span class="line">    <span class="function"><span class="title">if</span> (cls-&gt;</span>isRootMetaclass()) &#123;</span><br><span class="line">        <span class="comment">// root metaclass</span></span><br><span class="line">        addMethod(cls, SEL_initialize, (IMP)&amp;objc_noop_imp, <span class="string">""</span>, NO);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Attach categories.</span></span><br><span class="line">    <span class="comment">// 获取未添加的分类</span></span><br><span class="line">    category_list *cats = unattachedCategoriesForClass(cls, <span class="literal">true</span> <span class="comment">/*realizing*/</span>);</span><br><span class="line">    <span class="comment">// 添加分类</span></span><br><span class="line">    attachCategories(cls, cats, <span class="literal">false</span> <span class="comment">/*don't flush caches*/</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">//.....</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span> (cats) free(cats);</span><br><span class="line">    <span class="comment">//.....</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>methodizeClass方法则进一步初始化rw，这一步将ro中的属性，协议，方法拷贝到rw，以及将分类中对应的属性，协议，方法也拷贝到class中的rw对应字段。</p><p>回顾整个map_image阶段就是在镜像实例话结束后，通过读取_DATA中的指定session数据，来读出整个镜像中各个class的关键数据，并构建objc_class数据结构对象，通过从_DATA中读取的数据来填充objc_class来完成整个类的实例化。</p><ul><li><h5><strong><strong>load_images</strong></strong></h5></li></ul><p>load方法是我们在日常开发中可以接触到的调用时间最靠前的方法，它的调用不是惰性的，在主函数运行之前，load 方法就会调用，并且它只会在程序调用期间调用一次，最重要的是，如果在类与分类中都实现了 load 方法，它们都会被调用，不像其它的在分类中实现的方法会被覆盖，但是在使用load方法的时候需要注意，由于load方法的运行时间过早，所以可能不是一个理想的环境，因为它不能保证某些类可能需要在在其它类之前加载，但是在这个时间点，所有的 framework 都已经加载到了运行时中，所以调用 framework 中的方法都是安全的，同时需要注意的是重载Class 的 +load 方法时不能调父类super，我们来看下这部分逻辑：</p><p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">void</span></span><br><span class="line">load_images(<span class="keyword">const</span> <span class="keyword">char</span> *path __unused, <span class="keyword">const</span> struct mach_header *mh)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">//......</span></span><br><span class="line">    <span class="comment">// Discover load methods</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="function"><span class="keyword">mutex_locker_t</span> <span class="title">lock2</span><span class="params">(runtimeLock)</span></span>;</span><br><span class="line">        <span class="comment">//prepare_load_methods方法中对load方法进行了前期准备</span></span><br><span class="line">        prepare_load_methods((<span class="keyword">const</span> headerType *)mh);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 调用+load方法</span></span><br><span class="line">    <span class="comment">// Call +load methods (without runtimeLock - re-entrant)</span></span><br><span class="line">    call_load_methods();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>load_images 方法中主要调用了prepare_load_methods 以及 call_load_methods</p><p><figure class="highlight lisp"><table><tr><td class="code"><pre><span class="line">void prepare_load_methods(<span class="name">const</span> headerType *mhdr)</span><br><span class="line">&#123;</span><br><span class="line">    //....</span><br><span class="line">    //获取类</span><br><span class="line">    classref_t *classlist = </span><br><span class="line">        _getObjc2NonlazyClassList(<span class="name">mhdr</span>, <span class="symbol">&amp;count</span>)<span class="comment">;</span></span><br><span class="line">    for (<span class="name">i</span> = <span class="number">0</span><span class="comment">; i &lt; count; i++) &#123;</span></span><br><span class="line">        schedule_class_load(<span class="name">remapClass</span>(<span class="name">classlist</span>[i]))<span class="comment">;</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    //添加分类</span><br><span class="line">    category_t **categorylist = _getObjc2NonlazyCategoryList(<span class="name">mhdr</span>, <span class="symbol">&amp;count</span>)<span class="comment">;</span></span><br><span class="line">    for (<span class="name">i</span> = <span class="number">0</span><span class="comment">; i &lt; count; i++) &#123;</span></span><br><span class="line">        category_t *cat = categorylist[i];</span><br><span class="line">        Class cls = remapClass(cat-&gt;cls);</span><br><span class="line">        if (!cls) continue;  // category for ignored weak-linked class</span><br><span class="line">        realizeClass(cls);</span><br><span class="line">        assert(cls-&gt;ISA()-&gt;isRealized());</span><br><span class="line">        add_category_to_loadable_list(cat);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> void schedule_class_load(<span class="class"><span class="keyword">Class</span> <span class="title">cls</span>)</span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (!cls) <span class="keyword">return</span>;</span><br><span class="line">    assert(cls-&gt;isRealized());  <span class="comment">// _read_images should realize</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (cls-&gt;data()-&gt;flags &amp; RW_LOADED) <span class="keyword">return</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//先加载父类</span></span><br><span class="line">    <span class="comment">// Ensure superclass-first ordering</span></span><br><span class="line">    <span class="comment">//分析这段代码，可以知道，在将子类添加到加载列表之前，其父类一定会优先加载到列表中。</span></span><br><span class="line">    <span class="comment">//这也是为何父类的+load方法在子类的+load方法之前调用的根本原因。</span></span><br><span class="line">    schedule_class_load(cls-&gt;superclass);</span><br><span class="line">    <span class="comment">//将类添加到可加载列表</span></span><br><span class="line">    add_class_to_loadable_list(cls);</span><br><span class="line">    cls-&gt;setInfo(RW_LOADED); </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>prepare_load_methods 以及 schedule_class_load方法会按照父类，子类，分类的顺序将需要调用load方法的class添加到loadable_list中去</p><p><figure class="highlight oxygene"><table><tr><td class="code"><pre><span class="line">void add_class_to_loadable_list(<span class="keyword">Class</span> cls)</span><br><span class="line"><span class="comment">&#123;</span></span><br><span class="line"><span class="comment">    IMP method;</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">    loadMethodLock.assertLocked();</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">    //该类是否有load方法</span></span><br><span class="line"><span class="comment">    method = cls-&gt;getLoadMethod();</span></span><br><span class="line"><span class="comment">    if (!method) return;  // Don't bother if cls has no +load method</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">    //........</span></span><br><span class="line"><span class="comment">    if (loadable_classes_used == loadable_classes_allocated) &#123;</span></span><br><span class="line"><span class="comment">        loadable_classes_allocated = loadable_classes_allocated*2 + 16;</span></span><br><span class="line"><span class="comment">        loadable_classes = (struct loadable_class *)</span></span><br><span class="line"><span class="comment">            realloc(loadable_classes,</span></span><br><span class="line"><span class="comment">                              loadable_classes_allocated *</span></span><br><span class="line"><span class="comment">                              sizeof(struct loadable_class));</span></span><br><span class="line"><span class="comment">    &#125;</span></span><br><span class="line"></span><br><span class="line">    loadable_classes[loadable_classes_used].cls = cls;</span><br><span class="line">    loadable_classes[loadable_classes_used]<span class="function">.<span class="keyword">method</span> = <span class="title">method</span>;</span></span><br><span class="line">    loadable_classes_used++;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p><figure class="highlight oxygene"><table><tr><td class="code"><pre><span class="line">void add_category_to_loadable_list(Category cat)</span><br><span class="line"><span class="comment">&#123;</span></span><br><span class="line"><span class="comment">    IMP method;</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">    loadMethodLock.assertLocked();</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">    //该分类是否有load方法</span></span><br><span class="line"><span class="comment">    method = _category_getLoadMethod(cat);</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">    if (!method) return;</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">    if (loadable_categories_used == loadable_categories_allocated) &#123;</span></span><br><span class="line"><span class="comment">        loadable_categories_allocated = loadable_categories_allocated*2 + 16;</span></span><br><span class="line"><span class="comment">        loadable_categories = (struct loadable_category *)</span></span><br><span class="line"><span class="comment">            realloc(loadable_categories,</span></span><br><span class="line"><span class="comment">                              loadable_categories_allocated *</span></span><br><span class="line"><span class="comment">                              sizeof(struct loadable_category));</span></span><br><span class="line"><span class="comment">    &#125;</span></span><br><span class="line"></span><br><span class="line">    loadable_categories[loadable_categories_used].cat = cat;</span><br><span class="line">    loadable_categories[loadable_categories_used]<span class="function">.<span class="keyword">method</span> = <span class="title">method</span>;</span></span><br><span class="line">    loadable_categories_used++;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>调用类以及分类中的load方法</p><p><figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="comment"><span class="markdown">/<span class="strong">*****</span><span class="strong">*****</span><span class="strong">*****</span><span class="strong">*****</span><span class="strong">*****</span><span class="strong">*****</span><span class="strong">*****</span><span class="strong">*****</span><span class="strong">*****</span><span class="strong">*****</span><span class="strong">*****</span><span class="strong">*****</span><span class="strong">*****</span><span class="strong">*****</span>*</span></span></span><br><span class="line"><span class="comment"><span class="markdown"><span class="bullet">* </span>call<span class="emphasis">_load_</span>methods</span></span></span><br><span class="line"><span class="comment"><span class="markdown"><span class="bullet">* </span>Call all pending class and category +load methods.</span></span></span><br><span class="line"><span class="comment"><span class="markdown"><span class="bullet">* </span>Class +load methods </span>are<span class="markdown"> called superclass-first. </span></span></span><br><span class="line"><span class="comment"><span class="markdown"><span class="bullet">* </span>Category +load methods </span>are<span class="markdown"> not called until after </span>the<span class="markdown"> parent class's +load.</span></span></span><br><span class="line"><span class="comment"><span class="markdown"><span class="bullet">* </span></span></span></span><br><span class="line"><span class="comment"><span class="markdown"><span class="bullet">* </span>This method must be RE-ENTRANT, because </span>a<span class="markdown"> +load could trigger </span></span></span><br><span class="line"><span class="comment"><span class="markdown"><span class="bullet">* </span></span>more<span class="markdown"> image mapping. In addition, </span>the<span class="markdown"> superclass-first ordering </span></span></span><br><span class="line"><span class="comment"><span class="markdown"><span class="bullet">* </span>must be preserved in </span>the<span class="markdown"> face of re-entrant calls. Therefore, </span></span></span><br><span class="line"><span class="comment"><span class="markdown"><span class="bullet">* </span>only </span>the<span class="markdown"> OUTERMOST call of this function </span>will<span class="markdown"> do anything, and </span></span></span><br><span class="line"><span class="comment"><span class="markdown"><span class="bullet">* </span>that call </span>will<span class="markdown"> handle all loadable classes, even those generated </span></span></span><br><span class="line"><span class="comment"><span class="markdown"><span class="bullet">* </span>while it was running.</span></span></span><br><span class="line"><span class="comment"><span class="markdown">*</span></span></span><br><span class="line"><span class="comment"><span class="markdown"><span class="bullet">* </span>The sequence below preserves +load ordering in </span>the<span class="markdown"> face of </span></span></span><br><span class="line"><span class="comment"><span class="markdown"><span class="bullet">* </span>image loading during </span>a<span class="markdown"> +load, and make sure that no </span></span></span><br><span class="line"><span class="comment"><span class="markdown"><span class="bullet">* </span>+load method is forgotten because it was added during </span></span></span><br><span class="line"><span class="comment"><span class="markdown"><span class="bullet">* </span></span>a<span class="markdown"> +load call.</span></span></span><br><span class="line"><span class="comment"><span class="markdown"><span class="bullet">* </span>Sequence:</span></span></span><br><span class="line"><span class="comment"><span class="markdown"><span class="bullet">* </span>1. Repeatedly call class +loads until there aren't any </span>more</span></span><br><span class="line"><span class="comment"><span class="markdown"><span class="bullet">* </span>2. Call category +loads ONCE.</span></span></span><br><span class="line"><span class="comment"><span class="markdown"><span class="bullet">* </span>3. Run </span>more<span class="markdown"> +loads if:</span></span></span><br><span class="line"><span class="comment"><span class="markdown"><span class="bullet">*    </span>(</span>a<span class="markdown">) there </span>are<span class="markdown"> </span>more<span class="markdown"> classes to load, OR</span></span></span><br><span class="line"><span class="comment"><span class="markdown"><span class="bullet">*    </span>(b) there </span>are<span class="markdown"> some potential category +loads that have </span></span></span><br><span class="line"><span class="comment"><span class="markdown"><span class="bullet">*        </span>still never been attempted.</span></span></span><br><span class="line"><span class="comment"><span class="markdown"><span class="bullet">* </span>Category +loads </span>are<span class="markdown"> only run once to ensure "parent class first" </span></span></span><br><span class="line"><span class="comment"><span class="markdown"><span class="bullet">* </span>ordering, even if </span>a<span class="markdown"> category +load triggers </span>a<span class="markdown"> new loadable class </span></span></span><br><span class="line"><span class="comment"><span class="markdown"><span class="bullet">* </span>and </span>a<span class="markdown"> new loadable category attached to that class. </span></span></span><br><span class="line"><span class="comment"><span class="markdown">*</span></span></span><br><span class="line"><span class="comment"><span class="markdown"><span class="bullet">* </span>Locking: loadMethodLock must be held by </span>the<span class="markdown"> caller </span></span></span><br><span class="line"><span class="comment"><span class="markdown"><span class="bullet">*   </span>All other locks must not be held.</span></span></span><br><span class="line"><span class="comment"><span class="markdown"><span class="strong">*****</span><span class="strong">*****</span><span class="strong">*****</span><span class="strong">*****</span><span class="strong">*****</span><span class="strong">*****</span><span class="strong">*****</span><span class="strong">*****</span><span class="strong">*****</span><span class="strong">*****</span><span class="strong">*****</span><span class="strong">*****</span><span class="strong">*****</span><span class="strong">*****</span>/</span></span></span><br><span class="line"><span class="keyword">void</span> call_load_methods(<span class="keyword">void</span>)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">static</span> <span class="built_in">bool</span> loading = NO;</span><br><span class="line">    <span class="built_in">bool</span> more_categories;</span><br><span class="line"></span><br><span class="line">    loadMethodLock.assertLocked();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Re-entrant calls do nothing; the outermost call will finish the job.</span></span><br><span class="line">    <span class="keyword">if</span> (loading) <span class="keyword">return</span>;</span><br><span class="line">    loading = YES;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">void</span> *pool = objc_autoreleasePoolPush();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">do</span> &#123;</span><br><span class="line">        <span class="comment">// 1. Repeatedly call class +loads until there aren't any more</span></span><br><span class="line">        <span class="keyword">while</span> (loadable_classes_used &gt; <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="comment">//调用类的load方法</span></span><br><span class="line">            call_class_loads();</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 2. Call category +loads ONCE</span></span><br><span class="line">        <span class="comment">//调用分类的load方法</span></span><br><span class="line">        more_categories = call_category_loads();</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 3. Run more +loads if there are classes OR more untried categories</span></span><br><span class="line">    &#125; <span class="keyword">while</span> (loadable_classes_used &gt; <span class="number">0</span>  ||  more_categories);</span><br><span class="line"></span><br><span class="line">    objc_autoreleasePoolPop(pool);</span><br><span class="line"></span><br><span class="line">    loading = NO;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">call_class_loads</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// Detach current loadable list.</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">loadable_class</span> *<span class="title">classes</span> = <span class="title">loadable_classes</span>;</span><span class="comment">/*这是preppare阶段构造的*/</span></span><br><span class="line">    <span class="keyword">int</span> used = loadable_classes_used;</span><br><span class="line">    loadable_classes = nil;</span><br><span class="line">    loadable_classes_allocated = <span class="number">0</span>;</span><br><span class="line">    loadable_classes_used = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Call all +loads for the detached list.</span></span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; used; i++) &#123;</span><br><span class="line">        Class cls = classes[i].cls;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">load_method_t</span> load_method = (<span class="keyword">load_method_t</span>)classes[i].method;</span><br><span class="line">        <span class="keyword">if</span> (!cls) <span class="keyword">continue</span>; </span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (PrintLoading) &#123;</span><br><span class="line">            _objc_inform(<span class="string">"LOAD: +[%s load]\n"</span>, cls-&gt;nameForLogging());</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">//调用+load方法</span></span><br><span class="line">        (*load_method)(cls, SEL_load);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// Destroy the detached list.</span></span><br><span class="line">    <span class="keyword">if</span> (classes) <span class="built_in">free</span>(classes);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p><figure class="highlight gauss"><table><tr><td class="code"><pre><span class="line">static <span class="keyword">bool</span> <span class="built_in">call_category_loads</span>(void)</span><br><span class="line">&#123;</span><br><span class="line">    int i, shift;</span><br><span class="line">    <span class="keyword">bool</span> new_categories_added = NO;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// Detach current loadable list.</span></span><br><span class="line">    <span class="keyword">struct</span> <span class="type">loadable_category</span> *cats = loadable_categories;<span class="comment">/*数据来源 在prepare阶段构建*/</span></span><br><span class="line">    int used = loadable_categories_used;</span><br><span class="line">    int allocated = loadable_categories_allocated;</span><br><span class="line">    loadable_categories = nil;</span><br><span class="line">    loadable_categories_allocated = <span class="number">0</span>;</span><br><span class="line">    loadable_categories_used = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Call all +loads for the detached list.</span></span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; used; i++) &#123;</span><br><span class="line">        Category cat = cats[i].cat;</span><br><span class="line">        load_method_t load_method = (load_method_t)cats[i].method;</span><br><span class="line">        Class <span class="keyword">cls</span>;</span><br><span class="line">        <span class="keyword">if</span> (!cat) <span class="keyword">continue</span>;</span><br><span class="line"></span><br><span class="line">        cls = <span class="built_in">_category_getClass</span>(cat);</span><br><span class="line">        <span class="keyword">if</span> (<span class="keyword">cls</span>  &amp;&amp;  <span class="keyword">cls</span>-&gt;<span class="built_in">isLoadable</span>()) &#123;</span><br><span class="line">            <span class="keyword">if</span> (PrintLoading) &#123;</span><br><span class="line">                <span class="built_in">_objc_inform</span>(<span class="string">"LOAD: +[%s(%s) load]\n"</span>, </span><br><span class="line">                             cls-&gt;<span class="built_in">nameForLogging</span>(), </span><br><span class="line">                             <span class="built_in">_category_getName</span>(cat));</span><br><span class="line">            &#125;</span><br><span class="line">            (*load_method)(<span class="keyword">cls</span>, SEL_load);</span><br><span class="line">            cats[i].cat = nil;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Compact detached list (order-preserving)</span></span><br><span class="line">    shift = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; used; i++) &#123;</span><br><span class="line">        if (cats[i].cat) &#123;</span><br><span class="line">            cats[i-shift] = cats[i];</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            shift++;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    used -= shift;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Copy any new +load candidates from the new list to the detached list.</span></span><br><span class="line">    new_categories_added = (loadable_categories_used &gt; <span class="number">0</span>);</span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; loadable_categories_used; i++) &#123;</span><br><span class="line">        if (used == allocated) &#123;</span><br><span class="line">            allocated = allocated*2 + 16;</span><br><span class="line">            cats = (<span class="keyword">struct</span> <span class="type">loadable_category</span> *)</span><br><span class="line">                <span class="built_in">realloc</span>(cats, allocated *</span><br><span class="line">                                  <span class="built_in">sizeof</span>(struct loadable_category));</span><br><span class="line">        &#125;</span><br><span class="line">        cats[used++] = loadable_categories[i];</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Destroy the new list.</span></span><br><span class="line">    <span class="keyword">if</span> (loadable_categories) <span class="built_in">free</span>(loadable_categories);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Reattach the (now augmented) detached list. </span></span><br><span class="line">    <span class="comment">// But if there's nothing left to load, destroy the list.</span></span><br><span class="line">    <span class="keyword">if</span> (used) &#123;</span><br><span class="line">        loadable_categories = cats;</span><br><span class="line">        loadable_categories_used = used;</span><br><span class="line">        loadable_categories_allocated = allocated;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (cats) <span class="built_in">free</span>(cats);</span><br><span class="line">        loadable_categories = nil;</span><br><span class="line">        loadable_categories_used = <span class="number">0</span>;</span><br><span class="line">        loadable_categories_allocated = <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (PrintLoading) &#123;</span><br><span class="line">        <span class="keyword">if</span> (loadable_categories_used != <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="built_in">_objc_inform</span>(<span class="string">"LOAD: %d categories still waiting for +load\n"</span>,</span><br><span class="line">                         loadable_categories_used);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> new_categories_added;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>我们在这篇博客的最后对整个过程进行一个简单的总结：</p><p>在应用启动的时候会从镜像中查找dyld的地址，而后将dyld加载进来，找到dyld的入口地址__dyld_start,并将后续工作交给 dyld 负责：</p><ol><li>dyld 开始将程序二进制文件实例化为一个ImageLoader</li><li>交由 ImageLoader 读取 image 其中包含了我们的类、方法等各种符号，以及根据Mach-O的Load Commands段加载所有依赖的动态库并完成链接，初始化工作。在主程序初始化阶段，runtime会向dyld绑定回调。</li><li>当image加载到内存后，dyld 会通知 runtime 进行处理，runtime 接手后调用 map_images 做解析和处理，接下来 load_images 中调用 call_load_methods 方法，遍历所有加载进来的 Class，按继承层级依次调用 Class 的 +load 方法和其 Category 的 +load 方法</li><li>当map_images以及load_images执行完毕之后可执行文件中和动态库所有的符号（Class，Protocol，Selector，IMP，…）都已经按格式成功加载到内存中，被 runtime 所管理。</li><li>所有初始化工作结束后，dyld 调用真正的 main 函数，紧接着dyld 会清理现场，将调用栈回归，只剩下main函数。</li></ol>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;前面我们介绍了Mach O的结构，App的启动，dyld的加载，以及通过dyld将镜像加载进来，经过rebase/Bind处理后，找到main入口，以及Runtime相关的数据结构，有了前面的一系列铺垫这里介绍Runtime初始化就显得相对轻松了，这篇博客我们从《iOS 
      
    
    </summary>
    
      <category term="Objective C 相关" scheme="http://yoursite.com/categories/Objective-C-%E7%9B%B8%E5%85%B3/"/>
    
    
      <category term="Objective C 相关" scheme="http://yoursite.com/tags/Objective-C-%E7%9B%B8%E5%85%B3/"/>
    
  </entry>
  
  <entry>
    <title>iOS Runtime源码解析重要的数据结构介绍</title>
    <link href="http://yoursite.com/2020/02/02/iOS-Runtime%E6%BA%90%E7%A0%81%E8%A7%A3%E6%9E%90%E9%87%8D%E8%A6%81%E7%9A%84%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%BB%8B%E7%BB%8D/"/>
    <id>http://yoursite.com/2020/02/02/iOS-Runtime源码解析重要的数据结构介绍/</id>
    <published>2020-02-01T23:49:22.000Z</published>
    <updated>2020-02-06T14:01:12.000Z</updated>
    
    <content type="html"><![CDATA[<p>很早之前写过图解Runtime系列，那时候比较忙只是画了流程图，当然对自己理解已经够用了，但是对于没有看过源码对同学可能会看不懂，想着一个系列的博客都快完成了，少了这块总觉得缺了什么，并且Runtime是iOS的核心对理解底层有很大帮助，所以找了个空余时间将这块补上了。</p><p>我们知道编程语言有静态语言和动态语言之分，静态语言在编译的时候就已经明确了每行代码最终执行哪些代码，Objective C 作为动态语言它的底层是由编译器和Runtime构成，编译时期只是决定向某个对象发送某个消息，但是最终这个对象怎么处理这个消息取决于这个对象而不是固定的，也就是说在编译之后还可以针对发过来的消息对这个消息进行一系列处理最终决定执行哪些代码，这部分工作都交由Runtime处理。在介绍Runtime细节之前我们先看下相关的数据结构，熟悉这些数据结构对理解Runtime来说是必不可少的。本文将以Objc 2.0 中的数据结构为研究对象。</p><p>我们上层最经常接触的应该算是<strong><strong>Class</strong></strong>和<strong><strong>id</strong></strong>这两个类型了，它实际上分别是****objc_class * ****，****objc_object * ****的重定义类型。</p><p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">objc_class</span> *<span class="title">Class</span>;</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">objc_object</span> *<span class="title">id</span>;</span></span><br></pre></td></tr></table></figure></p><ul><li><h6><strong><strong>objc_object 结构</strong></strong></h6></li></ul><p>objc_object里面有个重要的成员对象<strong><strong>isa_t isa</strong></strong>，因为objc_object是用来存储对象数据用的，所以不宜将无用的数据存储在objc_object上，所以这里只存放了<strong><strong>isa_t isa</strong></strong>，通过<strong><strong>isa_t isa</strong></strong>可以找到objc_class，objc_class中存放的才是这些对象公用的数据。</p><p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">objc_object</span> &#123;</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">isa_t</span> isa;</span><br><span class="line">    <span class="comment">//.....</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p><ul><li><h6><strong><strong>objc_class 结构</strong></strong></h6></li></ul><p>objc_class结构如下所示：</p><p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">objc_class</span> :</span> objc_object &#123;</span><br><span class="line">    <span class="comment">// Class ISA;</span></span><br><span class="line">    Class superclass;</span><br><span class="line">    <span class="keyword">cache_t</span> cache;             <span class="comment">// formerly cache pointer and vtable</span></span><br><span class="line">    <span class="keyword">class_data_bits_t</span> bits;    <span class="comment">// class_rw_t * plus custom rr/alloc flags</span></span><br><span class="line">    <span class="comment">//.....</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p><p>我们从它的定义上看，它其实也是一个objc_object，所以objc_class也会有一个isa，它其实指向的是Meta class，Meta class 也是一个objc_class它和objc_object指向objc_class区别在于它的cache以及bits中存放的是类静态方法等数据。这些会在后面介绍。</p><ul><li><h6><strong><strong>isa_t 结构</strong></strong></h6></li></ul><p><figure class="highlight crystal"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">union</span> <span class="title">isa_t</span> &#123;</span></span><br><span class="line">    isa_t() &#123; &#125;</span><br><span class="line">    isa_t(uintptr_t value) : bits(value) &#123; &#125;</span><br><span class="line"></span><br><span class="line">    Class cls;</span><br><span class="line">    uintptr_t bits;</span><br><span class="line"><span class="comment">#if defined(ISA_BITFIELD)</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> &#123;</span></span><br><span class="line">        ISA_BITFIELD;  <span class="regexp">//</span> defined in isa.h</span><br><span class="line">    &#125;;</span><br><span class="line"><span class="comment">#endif</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p><p><strong><strong>isa_t</strong></strong> 是一个共用体，在64位机器中会启用NONPOINTER_ISA优化，具体见<a href="https://github.com/tbfungeek/iOS-Daily-Interview/issues/2" target="_blank" rel="noopener">谈谈iOS的内存管理方式的理解</a>,这时候isa就不单纯用于存储指向某个类的指针。那么当启用NONPOINTER_ISA优化的时候作用域是怎样的呢？我们这里看到****#if defined(ISA_BITFIELD)****，这个条件编译，ISA_BITFIELD是在isa.h中定义的我们来看下它的具体定义：</p><p><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="comment"># if __arm64__</span></span><br><span class="line"><span class="string">//.........</span></span><br><span class="line"><span class="comment">#   define ISA_BITFIELD                                                      \</span></span><br><span class="line">      <span class="attr">uintptr_t nonpointer        :</span> <span class="number">1</span><span class="string">;</span>                                       <span class="string">\</span></span><br><span class="line">      <span class="attr">uintptr_t has_assoc         :</span> <span class="number">1</span><span class="string">;</span>                                       <span class="string">\</span></span><br><span class="line">      <span class="attr">uintptr_t has_cxx_dtor      :</span> <span class="number">1</span><span class="string">;</span>                                       <span class="string">\</span></span><br><span class="line">      <span class="attr">uintptr_t shiftcls          :</span> <span class="number">33</span><span class="string">;</span> <span class="string">/*MACH_VM_MAX_ADDRESS</span> <span class="number">0x1000000000</span><span class="string">*/</span> <span class="string">\</span></span><br><span class="line">      <span class="attr">uintptr_t magic             :</span> <span class="number">6</span><span class="string">;</span>                                       <span class="string">\</span></span><br><span class="line">      <span class="attr">uintptr_t weakly_referenced :</span> <span class="number">1</span><span class="string">;</span>                                       <span class="string">\</span></span><br><span class="line">      <span class="attr">uintptr_t deallocating      :</span> <span class="number">1</span><span class="string">;</span>                                       <span class="string">\</span></span><br><span class="line">      <span class="attr">uintptr_t has_sidetable_rc  :</span> <span class="number">1</span><span class="string">;</span>                                       <span class="string">\</span></span><br><span class="line">      <span class="attr">uintptr_t extra_rc          :</span> <span class="number">19</span></span><br></pre></td></tr></table></figure></p><p>下面是这些字段的意义：</p><ul><li><p><strong><strong>nonpointer （1位）</strong></strong> 标记是否是纯的ISA指针，还是非指针型的NOPOINTER_ISA指针indexed，0表示普通的isa,1表示NOPOINTER_ISA指针</p></li><li><p><strong><strong>has_assoc （1位）</strong></strong> 表示该对象是否包含 associated object，如果没有，则析构时会更快</p></li><li><p><strong><strong>has_cxx_dtor （1位）</strong></strong> 表示该对象是否有 C++ 或 ARC 的析构函数，如果没有，则析构时更快</p></li><li><p><strong><strong>shiftcls （33位）</strong></strong> 类的指针</p></li><li><p><strong><strong>magic（6位）</strong></strong> 固定值为 0xd2，用于在调试时分辨对象是否未完成初始化</p></li><li><p><strong><strong>weakly_referenced（1位）</strong></strong> 标记对象是否有弱引用指针weakly_referenced</p></li><li><p><strong><strong>deallocating （1位）</strong></strong> 该对象是否正在析构</p></li><li><p><strong><strong>has_sidetable_rc （1位）</strong></strong> 是否使用了引用计数表sideTable</p></li><li><p><strong><strong>extra_rc（19位）</strong></strong> 存储引用计数值减一后的值 (首先会存储在该字段中，当到达上限后，has_sidetable_rc 等于1，对应的引用计数值存入相应的引用计数表中)</p></li><li><h6><strong><strong>cache_t 结构</strong></strong></h6></li></ul><p>cache_t主要用于存储某个类中使用过方法的缓存，一般一个类会有很多方法，这些方法并不都是常用的，如果每次调用都需要从该类的所有方法列表中查询明显会降低效率，所以这里引入了cache_t，通过这种以空间换时间的方式来加快方法查找效率。</p><p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">cache_t</span> &#123;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">bucket_t</span> *_<span class="title">buckets</span>;</span></span><br><span class="line">    <span class="keyword">mask_t</span> _mask;</span><br><span class="line">    <span class="keyword">mask_t</span> _occupied;</span><br><span class="line">    <span class="comment">//......</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p><p>cache_t中包含三个属性：</p><p><strong><strong>_buckets</strong></strong> 指向bucket_t结构体，它实际上是一个可动态扩展的哈希表。在存储的数据超过3/4的时候就会调用expend方法进行扩展。<strong><strong>_mask</strong></strong> 表示整个_buckets链表的大小，<strong><strong>_occupied</strong></strong>表示当前_buckets链表里面缓存的bucket_t节点数。</p><p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">bucket_t</span> &#123;</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    MethodCacheIMP _imp;</span><br><span class="line">    <span class="keyword">cache_key_t</span> _key;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p><strong><strong>bucket_t</strong></strong> 结构也十分简单，就是一个以selector映射而成的cache_key_t以及方法指针_imp。</p><ul><li><h6><strong><strong>class_data_bits_t 结构</strong></strong></h6></li></ul><p>我们再继续看objc_class中的class_data_bits_t类型：</p><p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">class_data_bits_t</span> &#123;</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">uintptr_t</span> bits;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//.....</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">class_rw_t</span>* <span class="title">data</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> (<span class="keyword">class_rw_t</span> *)(bits &amp; FAST_DATA_MASK);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//......</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>这里最关键的数据为class_rw_t，它才是包含类最有用信息的部分，我们从名字上可以看出它是可读写的，为什么要强调它是可读写的，因为它里面还有一个十分重要的class_ro_t类型。这个我们后面再介绍，我们先将重心放在class_rw_t中：</p><p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">class_rw_t</span> &#123;</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">uint32_t</span> flags;</span><br><span class="line">    <span class="keyword">uint32_t</span> version;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">class_ro_t</span> *ro;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">method_array_t</span> methods;</span><br><span class="line">    <span class="keyword">property_array_t</span> properties;</span><br><span class="line">    <span class="keyword">protocol_array_t</span> protocols;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">class_rw_t</span>* <span class="title">data</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> (<span class="keyword">class_rw_t</span> *)(bits &amp; FAST_DATA_MASK);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//........</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p><ul><li><strong><strong>methods</strong></strong>对象或者类的方法，这部分是可读写的，在Runtime阶段，会将分类的中的方法追加到这部分。</li><li><strong><strong>properties</strong></strong>对象或者类的属性</li><li><strong><strong>protocols</strong></strong>对象或者类的协议</li><li><strong><strong>ro</strong></strong>对象或者类中只读的部分，这部分在编译时期就已经确定了。</li></ul><p>我们再来看下<strong><strong>class_ro_t</strong></strong>结构：</p><p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">class_ro_t</span> &#123;</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">char</span> * name;</span><br><span class="line">    <span class="keyword">uint32_t</span> flags;</span><br><span class="line">    <span class="keyword">uint32_t</span> instanceStart;</span><br><span class="line">    <span class="keyword">uint32_t</span> instanceSize;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">ivar_list_t</span> * ivars;</span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">uint8_t</span> * ivarLayout;</span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">uint8_t</span> * weakIvarLayout;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">method_list_t</span> * baseMethodList;</span><br><span class="line">    <span class="keyword">protocol_list_t</span> * baseProtocols;</span><br><span class="line">    <span class="keyword">property_list_t</span> *baseProperties;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//......</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p><p>它包括实例起始位置<strong><strong>instanceStart</strong></strong>，以及实例大小<strong><strong>instanceSize</strong></strong>。成员变量相关：<strong><strong>ivars</strong></strong>，<strong><strong>ivarLayout</strong></strong>，<strong><strong>weakIvarLayout</strong></strong>。基础成分：<strong><strong>baseMethodList</strong></strong>，<strong><strong>baseProtocols</strong></strong>，<strong><strong>baseProperties</strong></strong></p><ul><li><h6><strong><strong>SEL 定义</strong></strong></h6></li></ul><p>SEL和id Class 一样都是objc对应类型的结构体指针的便捷定义。</p><p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">objc_selector</span> *<span class="title">SEL</span>;</span></span><br></pre></td></tr></table></figure></p><p>objc_selector是一个映射到方法的C字符串，不同类中相同名字的方法所对应的selector是相同的，即使方法名字相同而变量类型不同也会导致它们具有相同的selector。由于这点特性，导致了OC不支持函数重载。简单说SEL不包含返回值类型，参数类型，以及所属类的信息。只包含方法名对应的信息，实际上它在Runtime中作为消息发送给对象，对象根据它来查找对应的 IMP进行执行。</p><ul><li><h6><strong><strong>IMP 定义</strong></strong></h6></li></ul><p>IMP是函数指针，即函数执行的入口。该函数使用标准的C调用。</p><p><figure class="highlight elm"><table><tr><td class="code"><pre><span class="line">typedef id _Nullable (*<span class="type">IMP</span>)(id _Nonnull, <span class="type">SEL</span> _Nonnull, ...);</span><br></pre></td></tr></table></figure></p><p>它包含两个参数：</p><ul><li><p>第一个参数指向 self（它代表当前类实例的地址，如果是类则指向的是它的元类），作为消息的接受者；</p></li><li><p>第二个参数代表方法的选择子；</p></li><li><p>... 代表可选参数，前面的 id 代表返回值。</p></li><li><h6><strong><strong>Method 定义</strong></strong></h6></li></ul><p><figure class="highlight oxygene"><table><tr><td class="code"><pre><span class="line">typedef struct method_t *<span class="function"><span class="keyword">Method</span>;</span></span><br></pre></td></tr></table></figure></p><p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">method_t</span> &#123;</span></span><br><span class="line">    SEL name;</span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">char</span> *types;</span><br><span class="line">    MethodListIMP imp;</span><br><span class="line">    <span class="comment">//.....</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p><p>我们知道一个方法的组成包括方法返回值，方法参数，方法名，方法实现体，对应于method_t分别是<strong><strong>types</strong></strong>，<strong><strong>name</strong></strong>，<strong><strong>imp</strong></strong>，关于types涉及到类型编码，如果大家不了解可以通过如下链接进行扩展阅读：</p><ul><li><a href="https://nshipster.cn/type-encodings/" target="_blank" rel="noopener">Type Encodings</a></li><li><a href="https://developer.apple.com/library/archive/documentation/Cocoa/Conceptual/ObjCRuntimeGuide/Articles/ocrtTypeEncodings.html" target="_blank" rel="noopener">Apple Develper Type Encodings</a></li><li><a href="https://blog.csdn.net/liumengdi666/article/details/52402296" target="_blank" rel="noopener">iOS类型编码Type Encodings与属性类型Property Type</a></li></ul><p>在实际编程中可以通过method_getTypeEncoding方法来获得：</p><p><figure class="highlight oxygene"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> char * _Nullable method_getTypeEncoding(<span class="function"><span class="keyword">Method</span> _<span class="title">Nonnull</span> <span class="title">m</span>)</span></span><br></pre></td></tr></table></figure></p><p><img src="/2020/02/02/iOS-Runtime源码解析重要的数据结构介绍/000000001.png" alt></p><ul><li><h6><strong><strong>protocol_t 定义</strong></strong></h6></li></ul><p>protocol_t 主要用于存放协议相关的数据（注意它其实也是一个objc_object），由于协议也可以遵循协议，所以协议内部有协议列表用于存放当前协议所遵循的协议。而实例方法和类方法都包含必须实现和可选实现的部分，这两部是分开存放的，同时需要注意的是协议内部也为实例方法中提供了属性的存储，实际上协议也可以声明属性的，在介绍协议的使用的时候会给予介绍。</p><p><figure class="highlight arduino"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">protocol_t</span> :</span> objc_object &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//....</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">protocol_list_t</span> *<span class="title">protocols</span>;</span></span><br><span class="line">    <span class="keyword">method_list_t</span> *instanceMethods;</span><br><span class="line">    <span class="keyword">method_list_t</span> *classMethods;</span><br><span class="line">    <span class="keyword">method_list_t</span> *optionalInstanceMethods;</span><br><span class="line">    <span class="keyword">method_list_t</span> *optionalClassMethods;</span><br><span class="line">    <span class="keyword">property_list_t</span> *instanceProperties;</span><br><span class="line">    <span class="keyword">uint32_t</span> <span class="built_in">size</span>;   <span class="comment">// sizeof(protocol_t)</span></span><br><span class="line">    <span class="keyword">uint32_t</span> flags;</span><br><span class="line">    <span class="comment">//.......</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p><ul><li><h6><strong><strong>property_t 定义</strong></strong></h6></li></ul><p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">property_t</span> &#123;</span></span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">char</span> *name;</span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">char</span> *attributes;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p><p>对于属性结构比较简单就只有属性名字，以及对应的属性字符串，至于属性至于属性字符串的介绍可以看下面官方的文档介绍：</p><ul><li><p><a href="https://developer.apple.com/library/archive/documentation/Cocoa/Conceptual/ObjCRuntimeGuide/Articles/ocrtPropertyIntrospection.html" target="_blank" rel="noopener">Property Type and Functions</a></p></li><li><h6><strong><strong>ivar_t 定义</strong></strong></h6></li></ul><p><figure class="highlight arduino"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">ivar_t</span> &#123;</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">int32_t</span> *offset;</span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">char</span> *name;</span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">char</span> *type;</span><br><span class="line">    <span class="comment">// alignment is sometimes -1; use alignment() instead</span></span><br><span class="line">    <span class="keyword">uint32_t</span> alignment_raw;</span><br><span class="line">    <span class="keyword">uint32_t</span> <span class="built_in">size</span>;</span><br><span class="line">    <span class="comment">//.....</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p><p>如果我们使用下面方式声明实例变量，那么这些实例变量就会转换为ivar_t</p><p><figure class="highlight less"><table><tr><td class="code"><pre><span class="line"><span class="variable">@interface</span> <span class="attribute">TestClass </span>: NSObject &#123;</span><br><span class="line">    <span class="variable">@public</span></span><br><span class="line">        NSString *_name;</span><br><span class="line">    <span class="variable">@private</span></span><br><span class="line">        NSString *_ID;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>但是实际开发中大多数使用属性而不使用这种方法，使用这种写法，对象布局在编译器就已经固定了。只要碰到访问_name变量的代码，编译器就把其替换为偏移量ivar_t 中的（offset），这个偏移量是硬编码，表示该变量距离存放对象的内存区域的起始地址有多远。但是如果在运行过程中，又新增了一个实例变量，硬编码于其中的变量就会读到错误的值，这也是为什么OC无法动态添加成员变量的原因。</p><p>最后使用一个图来总结下全文的内容：</p><p><img src="/2020/02/02/iOS-Runtime源码解析重要的数据结构介绍/000000002.png" alt></p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;很早之前写过图解Runtime系列，那时候比较忙只是画了流程图，当然对自己理解已经够用了，但是对于没有看过源码对同学可能会看不懂，想着一个系列的博客都快完成了，少了这块总觉得缺了什么，并且Runtime是iOS的核心对理解底层有很大帮助，所以找了个空余时间将这块补上了。&lt;/
      
    
    </summary>
    
      <category term="Objective C 相关" scheme="http://yoursite.com/categories/Objective-C-%E7%9B%B8%E5%85%B3/"/>
    
    
      <category term="Objective C 相关" scheme="http://yoursite.com/tags/Objective-C-%E7%9B%B8%E5%85%B3/"/>
    
  </entry>
  
  <entry>
    <title>iOS-Runtime源码解析之Runtime较好的文章推荐</title>
    <link href="http://yoursite.com/2020/01/18/iOS-Runtime%E6%BA%90%E7%A0%81%E8%A7%A3%E6%9E%90%E4%B9%8BRuntime%E8%BE%83%E5%A5%BD%E7%9A%84%E6%96%87%E7%AB%A0%E6%8E%A8%E8%8D%90-md/"/>
    <id>http://yoursite.com/2020/01/18/iOS-Runtime源码解析之Runtime较好的文章推荐-md/</id>
    <published>2020-01-18T15:33:27.000Z</published>
    <updated>2020-02-04T17:52:10.000Z</updated>
    
    <content type="html"><![CDATA[<h5>1. <a href="https://github.com/draveness/analyze" target="_blank" rel="noopener">draveness</a></h5><ul><li><a href="https://github.com/draveness/analyze/blob/master/contents/objc/%E4%BB%8E%20NSObject%20%E7%9A%84%E5%88%9D%E5%A7%8B%E5%8C%96%E4%BA%86%E8%A7%A3%20isa.md" target="_blank" rel="noopener">从 NSObject 的初始化了解 isa</a></li><li><a href="https://github.com/draveness/analyze/blob/master/contents/objc/%E6%B7%B1%E5%85%A5%E8%A7%A3%E6%9E%90%20ObjC%20%E4%B8%AD%E6%96%B9%E6%B3%95%E7%9A%84%E7%BB%93%E6%9E%84.md" target="_blank" rel="noopener">深入解析 ObjC 中方法的结构</a></li><li><a href="https://github.com/draveness/analyze/blob/master/contents/objc/%E4%BB%8E%E6%BA%90%E4%BB%A3%E7%A0%81%E7%9C%8B%20ObjC%20%E4%B8%AD%E6%B6%88%E6%81%AF%E7%9A%84%E5%8F%91%E9%80%81.md" target="_blank" rel="noopener">从源代码看 ObjC 中消息的发送</a></li><li><a href="https://github.com/draveness/analyze/blob/master/contents/objc/%E4%BD%A0%E7%9C%9F%E7%9A%84%E4%BA%86%E8%A7%A3%20load%20%E6%96%B9%E6%B3%95%E4%B9%88%EF%BC%9F.md" target="_blank" rel="noopener">你真的了解 load 方法么？</a></li><li><a href="https://github.com/draveness/analyze/blob/master/contents/objc/%E8%87%AA%E5%8A%A8%E9%87%8A%E6%94%BE%E6%B1%A0%E7%9A%84%E5%89%8D%E4%B8%96%E4%BB%8A%E7%94%9F.md" target="_blank" rel="noopener">自动释放池的前世今生</a></li><li><a href="https://github.com/draveness/analyze/blob/master/contents/objc/%E9%BB%91%E7%AE%B1%E4%B8%AD%E7%9A%84%20retain%20%E5%92%8C%20release.md" target="_blank" rel="noopener">黑箱中的 retain 和 release</a></li><li><a href="https://github.com/draveness/analyze/blob/master/contents/objc/%E5%85%B3%E8%81%94%E5%AF%B9%E8%B1%A1%20AssociatedObject%20%E5%AE%8C%E5%85%A8%E8%A7%A3%E6%9E%90.md" target="_blank" rel="noopener">关联对象 AssociatedObject 完全解析</a></li><li><a href="https://github.com/draveness/analyze/blob/master/contents/objc/%E6%87%92%E6%83%B0%E7%9A%84%20initialize%20%E6%96%B9%E6%B3%95.md" target="_blank" rel="noopener">懒惰的 initialize 方法</a></li><li><a href="https://github.com/draveness/analyze/blob/master/contents/objc/%E5%AF%B9%E8%B1%A1%E6%98%AF%E5%A6%82%E4%BD%95%E5%88%9D%E5%A7%8B%E5%8C%96%E7%9A%84%EF%BC%88iOS%EF%BC%89.md" target="_blank" rel="noopener">对象是如何初始化的（iOS）</a></li></ul><h5>2. <a href="https://southpeak.github.io/" target="_blank" rel="noopener">南峰子</a></h5><ul><li><a href="http://southpeak.github.io/2014/10/25/objective-c-runtime-1/" target="_blank" rel="noopener">Objective-C Runtime 运行时之一：类与对象</a></li><li><a href="http://southpeak.github.io/2014/10/30/objective-c-runtime-2/" target="_blank" rel="noopener">Objective-C Runtime 运行时之二：成员变量与属性</a></li><li><a href="http://southpeak.github.io/2014/11/03/objective-c-runtime-3/" target="_blank" rel="noopener">Objective-C Runtime 运行时之三：方法与消息</a></li><li><a href="http://southpeak.github.io/2014/11/06/objective-c-runtime-4/" target="_blank" rel="noopener">Objective-C Runtime 运行时之四：Method Swizzling</a></li><li><a href="http://southpeak.github.io/2014/11/08/objective-c-runtime-5/" target="_blank" rel="noopener">Objective-C Runtime 运行时之五：协议与分类</a></li><li><a href="http://southpeak.github.io/2014/11/09/objective-c-runtime-6/" target="_blank" rel="noopener">Objective-C Runtime 运行时之六：拾遗</a></li></ul><h5>3. <a href="https://halfrost.com/" target="_blank" rel="noopener">halfrost</a></h5><ul><li><a href="http://halfrost.com/objc_life/" target="_blank" rel="noopener">Objc 对象的今生今世</a></li><li><a href="http://halfrost.com/objc_runtime_isa_class/" target="_blank" rel="noopener">神经病院 Objective-C Runtime 入院第一天 —— isa 和 Class</a></li><li><a href="http://halfrost.com/objc_runtime_objc_msgsend/" target="_blank" rel="noopener">神经病院 Objective-C Runtime 住院第二天 —— 消息发送与转发</a></li><li><a href="http://halfrost.com/how_to_use_runtime/" target="_blank" rel="noopener">神经病院 Objective-C Runtime 出院第三天 —— 如何正确使用 Runtime</a></li></ul><h5>4. <a href="http://blog.leichunfeng.com/" target="_blank" rel="noopener">雷纯锋</a></h5><ul><li><a href="http://blog.leichunfeng.com/blog/2015/04/25/objective-c-object-model/" target="_blank" rel="noopener">Objective-C 对象模型</a></li><li><a href="http://blog.leichunfeng.com/blog/2015/05/02/objective-c-plus-load-vs-plus-initialize/" target="_blank" rel="noopener">Objective-C +load vs +initialize</a></li><li><a href="http://blog.leichunfeng.com/blog/2015/05/18/objective-c-category-implementation-principle/" target="_blank" rel="noopener">Objective-C Category 的实现原理</a></li><li><a href="http://blog.leichunfeng.com/blog/2015/05/31/objective-c-autorelease-pool-implementation-principle/" target="_blank" rel="noopener">Objective-C Autorelease Pool 的实现原理</a></li><li><a href="http://blog.leichunfeng.com/blog/2015/06/14/objective-c-method-swizzling-best-practice/" target="_blank" rel="noopener">Objective-C Method Swizzling 的最佳实践</a></li><li><a href="http://blog.leichunfeng.com/blog/2015/06/26/objective-c-associated-objects-implementation-principle/" target="_blank" rel="noopener">Objective-C Associated Objects 的实现原理</a></li></ul><h5>5. <a href="http://blog.sunnyxx.com/" target="_blank" rel="noopener">sunnyxx</a></h5><ul><li><a href="http://blog.sunnyxx.com/2014/08/24/objc-duck/" target="_blank" rel="noopener">objc 与鸭子对象（上）</a></li><li><a href="http://blog.sunnyxx.com/2014/08/26/objc-duck-advanced/" target="_blank" rel="noopener">objc 与鸭子对象（下）</a></li><li><a href="http://blog.sunnyxx.com/2014/11/06/runtime-nuts/" target="_blank" rel="noopener">神经病院 objc runtime 入院考试</a></li><li><a href="http://blog.sunnyxx.com/2016/08/13/reunderstanding-runtime-0/" target="_blank" rel="noopener">重识 Objective-C Runtime - Smalltalk 与 C 的融合</a></li><li><a href="http://blog.sunnyxx.com/2016/08/13/reunderstanding-runtime-1/" target="_blank" rel="noopener">重识 Objective-C Runtime - 看透 Type 与 Value</a></li></ul><h5>6. <a href="http://blog.sunnyxx.com/" target="_blank" rel="noopener">ibireme</a></h5><ul><li><a href="https://blog.ibireme.com/2013/11/25/objc-object/" target="_blank" rel="noopener">Objective-C 中的类和对象</a></li></ul><h5>7. <a href="https://blog.devtang.com/" target="_blank" rel="noopener">唐巧</a></h5><ul><li><a href="https://blog.devtang.com/2013/10/15/objective-c-object-model/" target="_blank" rel="noopener">Objective-C对象模型及应用</a></li></ul><h5>8. <a href="https://www.jianshu.com/u/a9008a597529" target="_blank" rel="noopener">CornBallast</a></h5><ul><li><a href="https://www.jianshu.com/p/88d11bb12ba1" target="_blank" rel="noopener">Runtime 深度解析以及实用技巧</a></li><li><a href="https://www.jianshu.com/p/37e1b71ad03a" target="_blank" rel="noopener">Runtime 奇技淫巧__带你深入理解 Runtime 特有概念</a></li><li><a href="https://www.jianshu.com/p/38bbde2c5632" target="_blank" rel="noopener">Runtime 奇技淫巧__不受限制的消息发送</a></li><li><a href="https://www.jianshu.com/p/cb55dbb72c84" target="_blank" rel="noopener">Runtime 奇技淫巧__获取类的实例变量以及属性并强势 与KVC、msgSend 混用</a></li><li><a href="https://www.jianshu.com/p/13ab44c46904" target="_blank" rel="noopener">Runtime 奇技淫巧__方法交换</a></li><li><a href="https://www.jianshu.com/p/48220940118f" target="_blank" rel="noopener">Runtime 奇技淫巧__动态链接对象以及给分类添加属性</a></li><li><a href="https://www.jianshu.com/p/c5bdd6f7a68c" target="_blank" rel="noopener">Runtime 奇技淫巧__动态添加方法，消息转发改善异常</a></li></ul><h5>9. <a href="http://yulingtianxia.com/" target="_blank" rel="noopener">杨萧玉</a></h5><ul><li><a href="http://yulingtianxia.com/blog/2014/11/05/objective-c-runtime/" target="_blank" rel="noopener">Objective-C Runtime</a></li><li><a href="http://yulingtianxia.com/blog/2016/06/15/Objective-C-Message-Sending-and-Forwarding/" target="_blank" rel="noopener">Objective-C 消息发送与转发机制原理</a></li></ul>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h5&gt;1. &lt;a href=&quot;https://github.com/draveness/analyze&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;draveness&lt;/a&gt;&lt;/h5&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&quot;https://github.com
      
    
    </summary>
    
      <category term="Objective C 相关" scheme="http://yoursite.com/categories/Objective-C-%E7%9B%B8%E5%85%B3/"/>
    
    
      <category term="Objective C 相关" scheme="http://yoursite.com/tags/Objective-C-%E7%9B%B8%E5%85%B3/"/>
    
  </entry>
  
  <entry>
    <title>iOS Runtime源码解析之dyld</title>
    <link href="http://yoursite.com/2020/01/14/iOS-Runtime%E6%BA%90%E7%A0%81%E8%A7%A3%E6%9E%90%E4%B9%8Bdyld/"/>
    <id>http://yoursite.com/2020/01/14/iOS-Runtime源码解析之dyld/</id>
    <published>2020-01-13T16:19:28.000Z</published>
    <updated>2020-02-08T03:33:33.000Z</updated>
    
    <content type="html"><![CDATA[<h4>源码地址</h4><ul><li><a href="https://github.com/opensource-apple/dyld" target="_blank" rel="noopener">dyld</a></li></ul><h4>源码分析</h4><p>上一篇博客已经介绍到dyld被加载，以及设置dyld的入口点为__dyld_start,这篇博客就从__dyld_start开始看下应用执行main之前都经历了哪些过程，在开始之前我们需要先对iOS中对动态库有一定对了解，如果你之前接触过其他平台你可能会听说过Windows系统下的DLL文件，Linux 系统下的so文件，iOS系统则是使用dylib作为动态库，至于iOS动态库与静态库的对比我们会专门用一篇博客进行介绍，dylib和普通的可执行文件在文件结构上大同小异，都是Mach O格式文件，只不过在文件类型上，一个是MH_DYLIB,一个是MH_EXECUTE，但是动态库不能像可执行文件那样直接运行，而是需要通过dyld加载到内存与主程序链接后才可以执行，在主程序启动后会通过dyld将它所依赖的全部动态链接库链接起来，最终组成可运行的应用。</p><p>如果想查看某个可执行文件所依赖的动态链接库可以通过命令：<strong><strong>jtool -L IDLFundation</strong></strong> 来查看，我们接下来就来看下整个过程：</p><p><figure class="highlight perl"><table><tr><td class="code"><pre><span class="line">__dyld_start:</span><br><span class="line">popq%rdi<span class="comment"># param1 = mh of app</span></span><br><span class="line">pushq$0<span class="comment"># push a zero for debugger end of frames marker</span></span><br><span class="line">movq%rsp,%rbp<span class="comment"># pointer to base of kernel frame</span></span><br><span class="line">andq    $-<span class="number">16</span>,%rsp       <span class="comment"># force SSE alignment</span></span><br><span class="line">subq$16,%rsp<span class="comment"># room for local variables</span></span><br><span class="line"></span><br><span class="line">    //调用dyldbootstrap::start</span><br><span class="line"><span class="comment"># call dyldbootstrap::start(app_mh, argc, argv, slide, dyld_mh, &amp;startGlue)</span></span><br><span class="line">movl<span class="number">8</span>(%rbp),%esi<span class="comment"># param2 = argc into %esi</span></span><br><span class="line">leaq<span class="number">16</span>(%rbp),%rdx<span class="comment"># param3 = &amp;argv[0] into %rdx</span></span><br><span class="line">movq__dyld_start_static(%rip), %r8</span><br><span class="line">leaq__dyld_start(%rip), %rcx</span><br><span class="line">subq %r8, %rcx<span class="comment"># param4 = slide into %rcx</span></span><br><span class="line">leaq___dso_handle(%rip),%r8 <span class="comment"># param5 = dyldsMachHeader</span></span><br><span class="line">leaq-<span class="number">8</span>(%rbp),%r9</span><br><span class="line">call__ZN13dyldbootstrap5startEPK12macho_headeriPPKclS2_Pm</span><br><span class="line">movq-<span class="number">8</span>(%rbp),%rdi</span><br><span class="line">cmpq$0,%rdi</span><br><span class="line">jneLnew</span><br></pre></td></tr></table></figure></p><p>在__dyld_start会通过汇编调用dyldbootstrap::start方法</p><p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">uintptr_t</span> <span class="title">start</span><span class="params">(<span class="keyword">const</span> struct macho_header* appsMachHeader, <span class="keyword">int</span> argc, <span class="keyword">const</span> <span class="keyword">char</span>* argv[], </span></span></span><br><span class="line"><span class="function"><span class="params">                <span class="keyword">intptr_t</span> slide, <span class="keyword">const</span> struct macho_header* dyldsMachHeader,</span></span></span><br><span class="line"><span class="function"><span class="params">                <span class="keyword">uintptr_t</span>* startGlue)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">//.........</span></span><br><span class="line">    <span class="keyword">return</span> dyld::_main(appsMachHeader, appsSlide, argc, argv, envp, apple, startGlue);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>dyldbootstrap::start方法中会进入dyld的入口main函数。</p><p><figure class="highlight arduino"><table><tr><td class="code"><pre><span class="line">_main(<span class="keyword">const</span> macho_header* mainExecutableMH, <span class="keyword">uintptr_t</span> mainExecutableSlide, </span><br><span class="line">        <span class="keyword">int</span> argc, <span class="keyword">const</span> <span class="keyword">char</span>* argv[], <span class="keyword">const</span> <span class="keyword">char</span>* envp[], <span class="keyword">const</span> <span class="keyword">char</span>* apple[], </span><br><span class="line">        <span class="keyword">uintptr_t</span>* startGlue)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">uintptr_t</span> result = <span class="number">0</span>;</span><br><span class="line">    sMainExecutableMachHeader = mainExecutableMH;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 设置ImageLoader的context</span></span><br><span class="line">    setContext(mainExecutableMH, argc, argv, envp, apple);</span><br><span class="line">    <span class="comment">//......</span></span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="comment">//...</span></span><br><span class="line">        <span class="comment">// instantiate ImageLoader for main executable</span></span><br><span class="line">        <span class="comment">// 加载Image(代表镜像)</span></span><br><span class="line">        <span class="comment">// 为主程序初始化imageLoader,用于后续的链接等过程</span></span><br><span class="line">        sMainExecutable = instantiateFromLoadedImage(mainExecutableMH, mainExecutableSlide, sExecPath);</span><br><span class="line">        gLinkContext.mainExecutable = sMainExecutable;</span><br><span class="line">        gLinkContext.processIsRestricted = sProcessIsRestricted;</span><br><span class="line">        gLinkContext.processRequiresLibraryValidation = sProcessRequiresLibraryValidation;</span><br><span class="line">        gLinkContext.mainExecutableCodeSigned = hasCodeSignatureLoadCommand(mainExecutableMH);</span><br><span class="line"></span><br><span class="line">        <span class="comment">//.....</span></span><br><span class="line">        <span class="keyword">if</span> ( gLinkContext.sharedRegionMode != ImageLoader::kDontUseSharedRegion )</span><br><span class="line">            <span class="comment">// 将共享库加载到内存中</span></span><br><span class="line">            mapSharedCache();</span><br><span class="line">        <span class="comment">// .......</span></span><br><span class="line">        <span class="comment">// 如果有插入的库,加载</span></span><br><span class="line">        <span class="comment">// load any inserted libraries</span></span><br><span class="line">        <span class="keyword">if</span>( sEnv.DYLD_INSERT_LIBRARIES != <span class="literal">NULL</span> ) &#123;</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">const</span> <span class="keyword">char</span>* <span class="keyword">const</span>* lib = sEnv.DYLD_INSERT_LIBRARIES; *lib != <span class="literal">NULL</span>; ++lib) </span><br><span class="line">                <span class="comment">//// 加载环境变量DYLD_INSERT_LIBRARIES中的动态库，使用loadInsertedDylib进行加载</span></span><br><span class="line">                loadInsertedDylib(*lib);<span class="comment">//把环境变量DYLD_INSERT_LIBRARIES 中的动态库调用loadInsertedDylib进行加载</span></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// record count of inserted libraries so that a flat search will look at </span></span><br><span class="line">        <span class="comment">// inserted libraries, then main, then others.</span></span><br><span class="line">        sInsertedDylibCount = sAllImages.<span class="built_in">size</span>()<span class="number">-1</span>;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// link main executable</span></span><br><span class="line">        <span class="comment">// 链接主程序</span></span><br><span class="line">        gLinkContext.linkingMainExecutable = <span class="literal">true</span>;</span><br><span class="line">        <span class="comment">//继续回到dyld的_main函数中来,继续加载根目录Framework目录下的其他动态库. 加载完所有的dylibs之后,每个dylib之间还是没有关联,</span></span><br><span class="line">        <span class="comment">//不知道怎么调用,这时候就该进行link操作了,link操作分成rebase和binding辆部分.</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">//ASLR（Address space layout randomization）是一种针对缓冲区溢出的安全保护技术，</span></span><br><span class="line">        <span class="comment">//通过对堆、栈、共享库映射等线性区布局的随机化，通过增加攻击者预测目的地址的难度，防止攻击者直接定位攻击代码位置，达到阻止溢出攻击的目的。</span></span><br><span class="line">        <span class="comment">//据研究表明ASLR可以有效的降低缓冲区溢出攻击的成功率，如今Linux、FreeBSD、Windows等主流操作系统都已采用了该技术。</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">//rebase: 由于ASLR访问地址被随机化,所以rebase在动态库内部进行修正访问地址,并创建访问地址存储在__DATA段,这个期间可能会产生缺页并进行IO操作</span></span><br><span class="line">        <span class="comment">//binding: 主要负责动态库之间的调用地址的修正和创建</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">//initialize</span></span><br><span class="line">        <span class="comment">//这里就比较简单了, 这个时候各个库都已经load完成,访问地址指针也已经修正过,就可以初始化所有dylib了, 会调用C++的初始化构造方法,</span></span><br><span class="line">        <span class="comment">//也就是用__attribute__((constructor))修饰的方法,被修饰的方法都会在main()方法之前调用</span></span><br><span class="line">        link(sMainExecutable, sEnv.DYLD_BIND_AT_LAUNCH, <span class="literal">true</span>, ImageLoader::RPathChain(<span class="literal">NULL</span>, <span class="literal">NULL</span>));</span><br><span class="line">        sMainExecutable-&gt;setNeverUnloadRecursive();</span><br><span class="line">        <span class="keyword">if</span> ( sMainExecutable-&gt;forceFlat() ) &#123;</span><br><span class="line">            gLinkContext.bindFlat = <span class="literal">true</span>;</span><br><span class="line">            gLinkContext.prebindUsage = ImageLoader::kUseNoPrebinding;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// link any inserted libraries</span></span><br><span class="line">        <span class="comment">// 链接任何插入的库</span></span><br><span class="line">        <span class="comment">// do this after linking main executable so that any dylibs pulled in by inserted </span></span><br><span class="line">        <span class="comment">// dylibs (e.g. libSystem) will not be in front of dylibs the program uses</span></span><br><span class="line">        <span class="keyword">if</span> ( sInsertedDylibCount &gt; <span class="number">0</span> ) &#123;</span><br><span class="line">            <span class="keyword">for</span>(<span class="keyword">unsigned</span> <span class="keyword">int</span> i=<span class="number">0</span>; i &lt; sInsertedDylibCount; ++i) &#123;</span><br><span class="line">                ImageLoader* <span class="built_in">image</span> = sAllImages[i+<span class="number">1</span>];</span><br><span class="line">                link(<span class="built_in">image</span>, sEnv.DYLD_BIND_AT_LAUNCH, <span class="literal">true</span>, ImageLoader::RPathChain(<span class="literal">NULL</span>, <span class="literal">NULL</span>));</span><br><span class="line">                <span class="built_in">image</span>-&gt;setNeverUnloadRecursive();</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// only INSERTED libraries can interpose</span></span><br><span class="line">            <span class="comment">// register interposing info after all inserted libraries are bound so chaining works</span></span><br><span class="line">            <span class="keyword">for</span>(<span class="keyword">unsigned</span> <span class="keyword">int</span> i=<span class="number">0</span>; i &lt; sInsertedDylibCount; ++i) &#123;</span><br><span class="line">                ImageLoader* <span class="built_in">image</span> = sAllImages[i+<span class="number">1</span>];</span><br><span class="line">                <span class="built_in">image</span>-&gt;registerInterposing();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// &lt;rdar://problem/19315404&gt; dyld should support interposition even without DYLD_INSERT_LIBRARIES</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i=sInsertedDylibCount+<span class="number">1</span>; i &lt; sAllImages.<span class="built_in">size</span>(); ++i) &#123;</span><br><span class="line">            ImageLoader* <span class="built_in">image</span> = sAllImages[i];</span><br><span class="line">            <span class="keyword">if</span> ( <span class="built_in">image</span>-&gt;inSharedCache() )</span><br><span class="line">                <span class="keyword">continue</span>;</span><br><span class="line">            <span class="built_in">image</span>-&gt;registerInterposing();</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// apply interposing to initial set of images</span></span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>; i &lt; sImageRoots.<span class="built_in">size</span>(); ++i) &#123;</span><br><span class="line">            sImageRoots[i]-&gt;applyInterposing(gLinkContext);</span><br><span class="line">        &#125;</span><br><span class="line">        gLinkContext.linkingMainExecutable = <span class="literal">false</span>;</span><br><span class="line"></span><br><span class="line">        sMainExecutable-&gt;weakBind(gLinkContext);</span><br><span class="line"></span><br><span class="line">        initializeMainExecutable(); </span><br><span class="line"></span><br><span class="line">        <span class="comment">// 寻找主程序的入口</span></span><br><span class="line">        <span class="comment">// find entry point for main executable</span></span><br><span class="line">        result = (<span class="keyword">uintptr_t</span>)sMainExecutable-&gt;getThreadPC();</span><br><span class="line">        <span class="keyword">if</span> ( result != <span class="number">0</span> ) &#123;</span><br><span class="line">            <span class="comment">// 主可执行文件使用lc_main，需要返回libdyld.dylib中的glue</span></span><br><span class="line">            <span class="comment">//调用main()</span></span><br><span class="line">            <span class="comment">//当执行完dyld::_main方法之后,返回了main()函数地址,这个时候所有初始化工作都已经完成了,正式进入Objc声明周期</span></span><br><span class="line">            <span class="comment">// main executable uses LC_MAIN, needs to return to glue in libdyld.dylib</span></span><br><span class="line">            <span class="keyword">if</span> ( (gLibSystemHelpers != <span class="literal">NULL</span>) &amp;&amp; (gLibSystemHelpers-&gt;version &gt;= <span class="number">9</span>) )</span><br><span class="line">                *startGlue = (<span class="keyword">uintptr_t</span>)gLibSystemHelpers-&gt;startGlueToCallExit;</span><br><span class="line">            <span class="keyword">else</span></span><br><span class="line">                halt(<span class="string">"libdyld.dylib support not present for LC_MAIN"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="comment">// main executable uses LC_UNIXTHREAD, dyld needs to let "start" in program set up for main()</span></span><br><span class="line">            result = (<span class="keyword">uintptr_t</span>)sMainExecutable-&gt;getMain();</span><br><span class="line">            *startGlue = <span class="number">0</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">catch</span>(<span class="keyword">const</span> <span class="keyword">char</span>* message) &#123;</span><br><span class="line">        syncAllImages();</span><br><span class="line">        halt(message);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">catch</span>(...) &#123;</span><br><span class="line">        dyld::<span class="built_in">log</span>(<span class="string">"dyld: launch failed\n"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> result;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>在main函数中会执行如下操作：</p><ol><li>将主程序初始化为imageLoader,用于后续的链接等操作</li><li>加载共享库到内存</li><li>加载插入的动态库</li><li>链接主程序,链接插入库</li><li>初始化主程序</li><li>寻找主程序入口点</li></ol><p>接下来我们将按照上面的顺序进行展开：</p><p><strong><strong>将主程序初始化为imageLoader</strong></strong></p><p>在dyld main方法中会通过instantiateFromLoadedImage创建ImageLoader，其中第一个参数传入的是mainExecutableMH为主程序的Mach O Header,有了mainExecutableMH，dyld就可以从头开始遍历整个Mach O文件信息了。在开始创建ImageLoader之前会先调用isCompatibleMachO来查看mainExecutableMH中的cputype与cpusubtype是否与当前设备兼容，只有兼容的情况下才会继续创建ImageLoader,创建后的ImageLoader会通过addImage添加到sAllImages，然后调用addMappedRange()申请内存，更新主程序镜像映射的内存区。</p><p><figure class="highlight arduino"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> ImageLoader* <span class="title">instantiateFromLoadedImage</span><span class="params">(<span class="keyword">const</span> macho_header* mh, <span class="keyword">uintptr_t</span> slide, <span class="keyword">const</span> <span class="keyword">char</span>* path)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 检测mach-o header的cputype与cpusubtype是否与当前系统兼容</span></span><br><span class="line">    <span class="keyword">if</span> ( isCompatibleMachO((<span class="keyword">const</span> <span class="keyword">uint8_t</span>*)mh, path) ) &#123;</span><br><span class="line">        <span class="comment">//初始化镜像加载器</span></span><br><span class="line">        ImageLoader* <span class="built_in">image</span> = ImageLoaderMachO::instantiateMainExecutable(mh, slide, path, gLinkContext);</span><br><span class="line">        addImage(<span class="built_in">image</span>);</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">image</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">throw</span> <span class="string">"main executable not a known format"</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>我们看下ImageLoader的创建方法：</p><p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function">ImageLoader* <span class="title">ImageLoaderMachO::instantiateMainExecutable</span><span class="params">(<span class="keyword">const</span> macho_header* mh, <span class="keyword">uintptr_t</span> slide, <span class="keyword">const</span> <span class="keyword">char</span>* path, <span class="keyword">const</span> LinkContext&amp; context)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">bool</span> compressed;</span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">int</span> segCount;</span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">int</span> libCount;</span><br><span class="line">    <span class="keyword">const</span> linkedit_data_command* codeSigCmd;</span><br><span class="line">    <span class="keyword">const</span> encryption_info_command* encryptCmd;</span><br><span class="line">    <span class="comment">// sniffLoadCommands主要获取加载命令中compressed的值（压缩还是传统）以及segment的数量、libCount(需要加载的动态库的数量)</span></span><br><span class="line">    sniffLoadCommands(mh, path, <span class="literal">false</span>, &amp;compressed, &amp;segCount, &amp;libCount, context, &amp;codeSigCmd, &amp;encryptCmd);</span><br><span class="line">    <span class="comment">// instantiate concrete class based on content of load commands</span></span><br><span class="line">    <span class="keyword">if</span> ( compressed ) </span><br><span class="line">        <span class="keyword">return</span> ImageLoaderMachOCompressed::instantiateMainExecutable(mh, slide, path, segCount, libCount, context);</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">#<span class="keyword">if</span> SUPPORT_CLASSIC_MACHO</span><br><span class="line">        <span class="keyword">return</span> ImageLoaderMachOClassic::instantiateMainExecutable(mh, slide, path, segCount, libCount, context);</span><br><span class="line"><span class="meta">#<span class="meta-keyword">else</span></span></span><br><span class="line">        <span class="keyword">throw</span> <span class="string">"missing LC_DYLD_INFO load command"</span>;</span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>instantiateMainExecutable会首先调用sniffLoadCommands从加载信息中获取到当前的主程序是Compressed还是Classic类型的，以及segment的数量，需要加载的动态库的数量。然后根据主程序的类型来创建ImageLoaderMachOCompressed或者ImageLoaderMachOClassic类型的ImageLoader.</p><p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">ImageLoaderMachO::sniffLoadCommands</span><span class="params">(<span class="keyword">const</span> macho_header* mh, <span class="keyword">const</span> <span class="keyword">char</span>* path, <span class="keyword">bool</span> inCache, <span class="keyword">bool</span>* compressed,</span></span></span><br><span class="line"><span class="function"><span class="params">                                            <span class="keyword">unsigned</span> <span class="keyword">int</span>* segCount, <span class="keyword">unsigned</span> <span class="keyword">int</span>* libCount, <span class="keyword">const</span> LinkContext&amp; context,</span></span></span><br><span class="line"><span class="function"><span class="params">                                            <span class="keyword">const</span> linkedit_data_command** codeSigCmd,</span></span></span><br><span class="line"><span class="function"><span class="params">                                            <span class="keyword">const</span> encryption_info_command** encryptCmd)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">//......</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">uint32_t</span> i = <span class="number">0</span>; i &lt; cmd_count; ++i) &#123;</span><br><span class="line">        <span class="keyword">uint32_t</span> cmdLength = cmd-&gt;cmdsize;</span><br><span class="line">        <span class="class"><span class="keyword">struct</span> <span class="title">macho_segment_command</span>* <span class="title">segCmd</span>;</span></span><br><span class="line">        <span class="comment">//.....</span></span><br><span class="line">        <span class="keyword">switch</span> (cmd-&gt;cmd) &#123;</span><br><span class="line">            <span class="keyword">case</span> LC_DYLD_INFO:</span><br><span class="line">            <span class="keyword">case</span> LC_DYLD_INFO_ONLY:</span><br><span class="line">                *compressed = <span class="literal">true</span>;</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            <span class="keyword">case</span> LC_SEGMENT_COMMAND:</span><br><span class="line">                segCmd = (struct macho_segment_command*)cmd;</span><br><span class="line">                <span class="comment">//.........</span></span><br><span class="line">                <span class="comment">// ignore zero-sized segments</span></span><br><span class="line">                <span class="keyword">if</span> ( segCmd-&gt;vmsize != <span class="number">0</span> )</span><br><span class="line">                    *segCount += <span class="number">1</span>;</span><br><span class="line">                <span class="comment">//......</span></span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            <span class="comment">//.....</span></span><br><span class="line">            <span class="keyword">case</span> LC_LOAD_DYLIB:</span><br><span class="line">            <span class="keyword">case</span> LC_LOAD_WEAK_DYLIB:</span><br><span class="line">            <span class="keyword">case</span> LC_REEXPORT_DYLIB:</span><br><span class="line">            <span class="keyword">case</span> LC_LOAD_UPWARD_DYLIB:</span><br><span class="line">                *libCount += <span class="number">1</span>;</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            <span class="keyword">case</span> LC_CODE_SIGNATURE:</span><br><span class="line">                *codeSigCmd = (struct linkedit_data_command*)cmd; <span class="comment">// only support one LC_CODE_SIGNATURE per image</span></span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            <span class="keyword">case</span> LC_ENCRYPTION_INFO:</span><br><span class="line">            <span class="keyword">case</span> LC_ENCRYPTION_INFO_64:</span><br><span class="line">                *encryptCmd = (struct encryption_info_command*)cmd; <span class="comment">// only support one LC_ENCRYPTION_INFO[_64] per image</span></span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        cmd = nextCmd;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//.......</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// fSegmentsArrayCount is only 8-bits</span></span><br><span class="line">    <span class="keyword">if</span> ( *segCount &gt; <span class="number">255</span> )</span><br><span class="line">        dyld::throwf(<span class="string">"malformed mach-o image: more than 255 segments in %s"</span>, path);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// fSegmentsArrayCount is only 8-bits</span></span><br><span class="line">    <span class="keyword">if</span> ( *libCount &gt; <span class="number">4095</span> )</span><br><span class="line">        dyld::throwf(<span class="string">"malformed mach-o image: more than 4095 dependent libraries in %s"</span>, path);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> ( needsAddedLibSystemDepency(*libCount, mh) )</span><br><span class="line">        *libCount = <span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>sniffLoadCommands 会根据Mach 文件中是否有<strong><strong>LC_DYLD_INFO</strong></strong>，<strong><strong>LC_DYLD_INFO_ONLY</strong></strong>来判断当前Mach O文件是否是compressed的，并且获取segment 数量以及所依赖的动态库数量libCount，还有代码签名命令codeSigCmd以及加密命令encryptCmd。我们这里以compressed类型作为例子。那么上面返回的就是<strong><strong>ImageLoaderMachOCompressed</strong></strong>,</p><p><figure class="highlight arduino"><table><tr><td class="code"><pre><span class="line"><span class="function">ImageLoaderMachOCompressed* <span class="title">ImageLoaderMachOCompressed::instantiateMainExecutable</span><span class="params">(<span class="keyword">const</span> macho_header* mh, <span class="keyword">uintptr_t</span> slide, <span class="keyword">const</span> <span class="keyword">char</span>* path, </span></span></span><br><span class="line"><span class="function"><span class="params">                                                        <span class="keyword">unsigned</span> <span class="keyword">int</span> segCount, <span class="keyword">unsigned</span> <span class="keyword">int</span> libCount, <span class="keyword">const</span> LinkContext&amp; context)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    ImageLoaderMachOCompressed* <span class="built_in">image</span> = ImageLoaderMachOCompressed::instantiateStart(mh, path, segCount, libCount);</span><br><span class="line">    <span class="comment">//.....</span></span><br><span class="line">    <span class="built_in">image</span>-&gt;disableCoverageCheck();</span><br><span class="line">    <span class="built_in">image</span>-&gt;instantiateFinish(context);</span><br><span class="line">    <span class="built_in">image</span>-&gt;setMapped(context);</span><br><span class="line"></span><br><span class="line">    <span class="comment">//....</span></span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">image</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p><strong><strong>加载共享库到内存</strong></strong></p><p>dyld加载的时候会启动共享缓存技术对这个过程进行优化，共享缓存会在进程启动的时候被dyld映射到内存中，不同App间访问的共享库最终都会映射到同一块物理内存，之后在加载任何Mach O镜像的时候，都会先去检查该Mach O镜像以及所需的动态库文件是否已经存在在共享缓存中，如果存在则直接将它在共享内存中的内存地址映射到进程的内存地址空间，从而加快动态库的加载的速度，对启动性能会有较大的提高。在Mac OS系统中，动态库共享缓存以文件的形式存放在/var/db/dyld目录下，我们需要根据当前设备的CPU架构类型去匹配要打开的缓存文件，接着读取缓存文件的前8192字节，解析并加载缓存头dyld_cache_header信息到内存，我们可以通过/usr/bin/update_dyld_shared_cache来更新共享库。</p><p><figure class="highlight arduino"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">mapSharedCache</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">uint64_t</span> cacheBaseAddress = <span class="number">0</span>;</span><br><span class="line">    <span class="comment">// 检查当前共享缓存是否已经映射到共享区域</span></span><br><span class="line">    <span class="keyword">if</span> ( _shared_region_check_np(&amp;cacheBaseAddress) == <span class="number">0</span> ) &#123;</span><br><span class="line">        <span class="comment">/// 共享库已经被映射到内存中</span></span><br><span class="line">        sSharedCache = (dyld_cache_header*)cacheBaseAddress;</span><br><span class="line">        <span class="comment">// 检查共享库的兼容性，如果已经映射到内存中的共享库但是不兼容，这时候我们将忽略它</span></span><br><span class="line">        <span class="keyword">if</span> ( <span class="built_in">strcmp</span>(sSharedCache-&gt;magic, magic) != <span class="number">0</span> ) &#123;</span><br><span class="line">            sSharedCache = <span class="literal">NULL</span>;</span><br><span class="line">            <span class="comment">//.....</span></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">//.........</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="comment">// 如果共享库没有加载到内存中，进行加载</span></span><br><span class="line">        <span class="keyword">int</span> fd = openSharedCacheFile();</span><br><span class="line">        <span class="keyword">if</span> ( fd != <span class="number">-1</span> ) &#123;</span><br><span class="line">            <span class="keyword">uint8_t</span> firstPages[<span class="number">8192</span>];</span><br><span class="line">            <span class="comment">// 获取共享库文件的句柄，然后进行读取解析</span></span><br><span class="line">            <span class="keyword">if</span> ( ::<span class="built_in">read</span>(fd, firstPages, <span class="number">8192</span>) == <span class="number">8192</span> ) &#123;</span><br><span class="line">                dyld_cache_header* header = (dyld_cache_header*)firstPages;</span><br><span class="line">                <span class="comment">//共享缓存合法性检查</span></span><br><span class="line">                <span class="keyword">if</span> ( <span class="built_in">strcmp</span>(header-&gt;magic, magic) == <span class="number">0</span> ) &#123;</span><br><span class="line">                    <span class="comment">//.......</span></span><br><span class="line">                    <span class="comment">// 校验缓存文件的完整性</span></span><br><span class="line">                    <span class="comment">//.......</span></span><br><span class="line">                    <span class="keyword">if</span> ( goodCache ) &#123;</span><br><span class="line">                        <span class="comment">//.......</span></span><br><span class="line">                        <span class="comment">// 将共享缓存映射到共享区域</span></span><br><span class="line">                        <span class="keyword">if</span> (_shared_region_map_and_slide_np(fd, mappingCount, mappings, codeSignatureMappingIndex, cacheSlide, slideInfo, slideInfoSize) == <span class="number">0</span>) &#123;</span><br><span class="line">                            <span class="comment">// successfully mapped cache into shared region</span></span><br><span class="line">                            sSharedCache = (dyld_cache_header*)mappings[<span class="number">0</span>].sfm_address;</span><br><span class="line">                            sSharedCacheSlide = cacheSlide;</span><br><span class="line">                            dyld::gProcessInfo-&gt;sharedCacheSlide = cacheSlide;</span><br><span class="line">                            <span class="keyword">if</span> ( header-&gt;mappingOffset &gt;= <span class="number">0x68</span> ) &#123;</span><br><span class="line">                                <span class="built_in">memcpy</span>(dyld::gProcessInfo-&gt;sharedCacheUUID, header-&gt;uuid, <span class="number">16</span>);</span><br><span class="line">                            &#125;</span><br><span class="line">                        &#125;</span><br><span class="line">                        <span class="keyword">else</span> &#123;</span><br><span class="line">                            <span class="comment">//........</span></span><br><span class="line">                        &#125;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">else</span> &#123;</span><br><span class="line">                    <span class="keyword">if</span> ( gLinkContext.verboseMapping ) </span><br><span class="line">                        dyld::<span class="built_in">log</span>(<span class="string">"dyld: shared cached file is invalid\n"</span>);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="keyword">if</span> ( gLinkContext.verboseMapping ) </span><br><span class="line">                    dyld::<span class="built_in">log</span>(<span class="string">"dyld: shared cached file cannot be read\n"</span>);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="built_in">close</span>(fd);  </span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="keyword">if</span> ( gLinkContext.verboseMapping ) </span><br><span class="line">                dyld::<span class="built_in">log</span>(<span class="string">"dyld: shared cached file cannot be opened\n"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//......</span></span><br><span class="line">    <span class="comment">// tell gdb where the shared cache is</span></span><br><span class="line">    <span class="comment">//........</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p><strong><strong>加载插入的动态库</strong></strong></p><p>插入的动态库会存储在DYLD_INSERT_LIBRARIES，在这时候会遍历DYLD_INSERT_LIBRARIES环境变量中指定的动态库列表，并调用loadInsertedDylib()将其加载。</p><p><figure class="highlight arduino"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span>( sEnv.DYLD_INSERT_LIBRARIES != <span class="literal">NULL</span> ) &#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">const</span> <span class="keyword">char</span>* <span class="keyword">const</span>* lib = sEnv.DYLD_INSERT_LIBRARIES; *lib != <span class="literal">NULL</span>; ++lib) </span><br><span class="line">        <span class="comment">// 加载环境变量DYLD_INSERT_LIBRARIES中的动态库，使用loadInsertedDylib进行加载</span></span><br><span class="line">        loadInsertedDylib(*lib);<span class="comment">//把环境变量DYLD_INSERT_LIBRARIES 中的动态库调用loadInsertedDylib进行加载</span></span><br><span class="line">&#125;</span><br><span class="line">sInsertedDylibCount = sAllImages.<span class="built_in">size</span>()<span class="number">-1</span>;</span><br></pre></td></tr></table></figure></p><p>loadInsertedDylib会调用load方法完成加载工作。load方法会先调用loadPhase0尝试从文件加载，然后一直从loadPhase0一直到loadPhase6，查找动态库的路径，每个阶段都会为下一阶段生成搜索的路径。整个查找顺序会顺着: ****DYLD_ROOT_PATH -&gt; LD_LIBRARY_PATH -&gt; DYLD_FRAMEWORK_PATH -&gt; raw path - &gt;DYLD_FALLBACK_LIBRARY_PATH **** 如果找到则调用ImageLoaderMachO::instantiateFromFile来实例化一个ImageLoader，之后调用checkandAddImage验证映像并将其加入到全局映像列表中。如果loadPhase0返回空，表示在路径中没有找到动态库，就尝试从共享缓存中查找，找到就调用ImageLoaderMachO::instantiateFromCache()从缓存中加载，否则就抛出没找到映像的异常。</p><p><figure class="highlight arduino"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="function">ImageLoader* <span class="title">load</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span>* path, <span class="keyword">const</span> LoadContext&amp; context)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">//.....</span></span><br><span class="line">    <span class="comment">// 尝试所有路径排列并检查现有加载的镜像</span></span><br><span class="line">    <span class="comment">// try all path permutations and check against existing loaded images</span></span><br><span class="line">    ImageLoader* <span class="built_in">image</span> = loadPhase0(path, orgPath, context, <span class="literal">NULL</span>);</span><br><span class="line">    <span class="keyword">if</span> ( <span class="built_in">image</span> != <span class="literal">NULL</span> ) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">image</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//......</span></span><br><span class="line">    <span class="built_in">image</span> = loadPhase0(path, orgPath, context, &amp;exceptions);</span><br><span class="line">    <span class="keyword">if</span> ( (<span class="built_in">image</span> == <span class="literal">NULL</span>) &amp;&amp; cacheablePath(path) &amp;&amp; !context.dontLoad ) &#123;</span><br><span class="line">        <span class="comment">//......    </span></span><br><span class="line">        <span class="keyword">if</span> ( (myerr == ENOENT) || (myerr == <span class="number">0</span>) ) &#123;</span><br><span class="line">            <span class="comment">// see if this image is in shared cache</span></span><br><span class="line">            <span class="comment">//......</span></span><br><span class="line">            <span class="keyword">if</span> ( findInSharedCacheImage(resolvedPath, <span class="literal">false</span>, <span class="literal">NULL</span>, &amp;mhInCache, &amp;pathInCache, &amp;slideInCache) ) &#123;</span><br><span class="line">                <span class="comment">//......</span></span><br><span class="line">                <span class="built_in">image</span> = ImageLoaderMachO::instantiateFromCache(mhInCache, pathInCache, slideInCache, stat_buf, gLinkContext);</span><br><span class="line">                    <span class="built_in">image</span> = checkandAddImage(<span class="built_in">image</span>, context);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p><figure class="highlight arduino"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> ImageLoader* <span class="title">loadPhase6</span><span class="params">(<span class="keyword">int</span> fd, <span class="keyword">const</span> struct stat&amp; stat_buf, <span class="keyword">const</span> <span class="keyword">char</span>* path, <span class="keyword">const</span> LoadContext&amp; context)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">//.......</span></span><br><span class="line">    <span class="comment">// try mach-o loader</span></span><br><span class="line">    <span class="keyword">if</span> ( shortPage ) </span><br><span class="line">        <span class="keyword">throw</span> <span class="string">"file too short"</span>;</span><br><span class="line">    <span class="keyword">if</span> ( isCompatibleMachO(firstPage, path) ) &#123;</span><br><span class="line">        <span class="comment">// 只有MH_BUNDLE，MH_DYLIB，以及一些MH_EXECUTE 才能被动态加载</span></span><br><span class="line">        <span class="keyword">switch</span> ( ((mach_header*)firstPage)-&gt;filetype ) &#123;</span><br><span class="line">            <span class="keyword">case</span> MH_EXECUTE:</span><br><span class="line">            <span class="keyword">case</span> MH_DYLIB:</span><br><span class="line">            <span class="keyword">case</span> MH_BUNDLE:</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            <span class="keyword">default</span>:</span><br><span class="line">                <span class="keyword">throw</span> <span class="string">"mach-o, but wrong filetype"</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">//实例化镜像</span></span><br><span class="line">        ImageLoader* <span class="built_in">image</span> = ImageLoaderMachO::instantiateFromFile(path, fd, firstPage, fileOffset, fileLength, stat_buf, gLinkContext);</span><br><span class="line">        <span class="comment">// validate</span></span><br><span class="line">        <span class="keyword">return</span> checkandAddImage(<span class="built_in">image</span>, context);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//.....</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p><strong><strong>链接主程序</strong></strong></p><p><figure class="highlight arduino"><table><tr><td class="code"><pre><span class="line">link(sMainExecutable, sEnv.DYLD_BIND_AT_LAUNCH, <span class="literal">true</span>, ImageLoader::RPathChain(<span class="literal">NULL</span>, <span class="literal">NULL</span>));</span><br><span class="line"><span class="comment">//......</span></span><br><span class="line"><span class="comment">// 4.link any inserted libraries</span></span><br><span class="line"><span class="comment">// 链接任何插入的库</span></span><br><span class="line"><span class="comment">// do this after linking main executable so that any dylibs pulled in by inserted </span></span><br><span class="line"><span class="comment">// dylibs (e.g. libSystem) will not be in front of dylibs the program uses</span></span><br><span class="line"><span class="keyword">if</span> ( sInsertedDylibCount &gt; <span class="number">0</span> ) &#123;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">unsigned</span> <span class="keyword">int</span> i=<span class="number">0</span>; i &lt; sInsertedDylibCount; ++i) &#123;</span><br><span class="line">        ImageLoader* <span class="built_in">image</span> = sAllImages[i+<span class="number">1</span>];</span><br><span class="line">        link(<span class="built_in">image</span>, sEnv.DYLD_BIND_AT_LAUNCH, <span class="literal">true</span>, ImageLoader::RPathChain(<span class="literal">NULL</span>, <span class="literal">NULL</span>));</span><br><span class="line">        <span class="built_in">image</span>-&gt;setNeverUnloadRecursive();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//......</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//......</span></span><br><span class="line">gLinkContext.linkingMainExecutable = <span class="literal">false</span>;</span><br><span class="line">sMainExecutable-&gt;weakBind(gLinkContext);</span><br></pre></td></tr></table></figure></p><p><figure class="highlight verilog"><table><tr><td class="code"><pre><span class="line"><span class="keyword">void</span> ImageLoader::link(<span class="keyword">const</span> LinkContext&amp; <span class="keyword">context</span>, bool forceLazysBound, bool preflightOnly, bool neverUnload, <span class="keyword">const</span> RPathChain&amp; loaderRPaths)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">//......</span></span><br><span class="line">    <span class="comment">//递归加载所有依赖库进内存。</span></span><br><span class="line">    <span class="keyword">this</span>-&gt;recursiveLoadLibraries(<span class="keyword">context</span>, preflightOnly, loaderRPaths);</span><br><span class="line">    <span class="keyword">context</span><span class="variable">.notifyBatch</span>(dyld_image_state_dependents_mapped);</span><br><span class="line"></span><br><span class="line">    <span class="comment">//.....</span></span><br><span class="line">    uint64_t t1 = mach_absolute_time();</span><br><span class="line">    <span class="keyword">context</span><span class="variable">.clearAllDepths</span>();</span><br><span class="line">    <span class="keyword">this</span>-&gt;recursiveUpdateDepth(<span class="keyword">context</span><span class="variable">.imageCount</span>());</span><br><span class="line"></span><br><span class="line">    uint64_t t2 = mach_absolute_time();</span><br><span class="line">    <span class="comment">//递归对自己以及依赖库进行复基位操作。在以前，程序每次加载其在内存中的堆栈基地址都是一样的，这意味着你的方法，变量等地址每次都一样的，这使得程序很不安全，后面就出现ASLR（Address space layout randomization,地址空间配置随机加载），</span></span><br><span class="line">    <span class="comment">//程序每次启动后地址都会随机变化，这样程序里所有的代码地址都是错的，需要重新对代码地址进行计算修复才能正常访问</span></span><br><span class="line">    <span class="comment">// 递归修正自己和依赖库的基地址，因为ASLR的原因，需要根据随机slide修正基地址</span></span><br><span class="line">    <span class="keyword">this</span>-&gt;recursiveRebase(<span class="keyword">context</span>);</span><br><span class="line">    <span class="keyword">context</span><span class="variable">.notifyBatch</span>(dyld_image_state_rebased);</span><br><span class="line"></span><br><span class="line">    uint64_t t3 = mach_absolute_time();</span><br><span class="line">    <span class="comment">//对库中所有nolazy的符号进行bind,一般的情况下多数符号都是lazybind的，他们在第一次使用的时候才进行bind。</span></span><br><span class="line">    <span class="comment">// recursiveBind对于noLazy的符号进行绑定，lazy的符号会在运行时动态绑定</span></span><br><span class="line">    <span class="keyword">this</span>-&gt;recursiveBind(<span class="keyword">context</span>, forceLazysBound, neverUnload);</span><br><span class="line"></span><br><span class="line">    uint64_t t4 = mach_absolute_time();</span><br><span class="line">    <span class="keyword">if</span> ( !<span class="keyword">context</span><span class="variable">.linkingMainExecutable</span> )</span><br><span class="line">        <span class="keyword">this</span>-&gt;weakBind(<span class="keyword">context</span>);</span><br><span class="line">    uint64_t t5 = mach_absolute_time();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">context</span><span class="variable">.notifyBatch</span>(dyld_image_state_bound);</span><br><span class="line">    <span class="comment">//........</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p><strong><strong>recursiveLoadLibraries</strong></strong>会先获取当前镜像所需要的库列表，然后会先从缓存库中加载所需要的库，如果没在缓存库中找到则调用loadLibrary进行加载，loadLibrary 实际上调用的是之前介绍的load方法。将依赖的库加载进来并装载。<strong><strong>recursiveUpdateDepth</strong></strong> 会对镜像及其依赖库进行排序。</p><p>接下来我们看下<strong><strong>recursiveRebase</strong></strong><figure class="highlight verilog"><table><tr><td class="code"><pre><span class="line"><span class="keyword">void</span> ImageLoader::recursiveRebase(<span class="keyword">const</span> LinkContext&amp; <span class="keyword">context</span>)</span><br><span class="line">&#123; </span><br><span class="line">    <span class="keyword">if</span> ( fState &lt; dyld_image_state_rebased ) &#123;</span><br><span class="line">        <span class="comment">// break cycles</span></span><br><span class="line">        fState = dyld_image_state_rebased;</span><br><span class="line">        try &#123;</span><br><span class="line">            <span class="comment">// rebase lower level libraries first</span></span><br><span class="line">            <span class="keyword">for</span>(<span class="keyword">unsigned</span> <span class="keyword">int</span> i=<span class="number">0</span>; i &lt; libraryCount(); ++i) &#123;</span><br><span class="line">                ImageLoader* dependentImage = libImage(i);</span><br><span class="line">                <span class="keyword">if</span> ( dependentImage != NULL )</span><br><span class="line">                    dependentImage-&gt;recursiveRebase(<span class="keyword">context</span>);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// rebase this image</span></span><br><span class="line">            doRebase(<span class="keyword">context</span>);</span><br><span class="line">            <span class="comment">// notify</span></span><br><span class="line">            <span class="keyword">context</span><span class="variable">.notifySingle</span>(dyld_image_state_rebased, <span class="keyword">this</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">//......</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>在之前的步骤中我们都在完成库的加载，但是这些dylibs之间是没有关联的，需要rebase，binding对地址修正，不知道大家看过高达之类的动画片没有，在变身过程中装备会从四面八方飞过来，然后会一个个安装到身上，嗯，link就是这个过程，动态库中的地址是相对的，因为它需要保证它内部逻辑的独立性，同时为了降低缓冲区溢出攻击的成功率主流的操作系统都会采用ASLR（Address space layout randomization）技术，它通过对堆、栈、共享库映射等线性区布局的随机化来增加攻击者预测目的地址的难度，防止攻击者直接定位攻击代码位置，达到阻止溢出攻击的目的。</p><p>****Rebase：在镜像内部调整指针的指向 ****</p><p>就像上面提到的未加载到内存的动态库镜像里面的地址都是相对的，是距第一个Segement的偏移量，而当加载到内存中的时候，起始地址就是申请的内存的起始地址，就不再是0，那么如何再能够找到这些引用的正确内存位置呢？这个就是rebase需要解决的问题，rebase这一步就是将相对地址调整为绝对地址，以及修正ASLR带来的地址不确定性：在没有使用ASLR技术之前，动态库加载的时候会把 dylib 加载到指定地址，所有指针和数据对于代码来说都是确定的，使用了ASLR后每次加载都会将dylib 加载到新的随机地址，这个随机的地址跟代码和数据指向的旧地址会有偏差，dyld 在rebase这个阶段需要修正这个偏差，做法就是将 dylib 内部的指针地址都加上这个偏移量，然后重复不断地对 __DATA 段中需要 rebase 的指针加上这个偏移量。也就是说经过rebase处理后镜像内部在内存中的映射已经确定下来了，不再是相对于动态库内部的相对地址，并创建访问地址存储在__DATA段。总的来说经过rebase处理后我们会在内存中获得一个有明确入口的，并且内部地址明确的动态库，但是对其他外部依赖库调用的地址在这步还没修正，需要在binding阶段进行处理。我们先继续往下看：</p><p><strong><strong>recursiveRebase</strong></strong> 里面调用的是<strong><strong>doRebase</strong></strong>，而<strong><strong>doRebase</strong></strong>会调用ImageLoaderMachOCompressed::rebase</p><p><figure class="highlight verilog"><table><tr><td class="code"><pre><span class="line"><span class="keyword">void</span> ImageLoaderMachO::doRebase(<span class="keyword">const</span> LinkContext&amp; <span class="keyword">context</span>)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">//.......</span></span><br><span class="line">    <span class="comment">// do actual rebasing</span></span><br><span class="line">    <span class="keyword">this</span>-&gt;rebase(<span class="keyword">context</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p><figure class="highlight arduino"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">ImageLoaderMachOCompressed::rebase</span><span class="params">(<span class="keyword">const</span> LinkContext&amp; context)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">//......</span></span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="comment">//.....</span></span><br><span class="line">        <span class="keyword">bool</span> done = <span class="literal">false</span>;</span><br><span class="line">        <span class="keyword">while</span> ( !done &amp;&amp; (p &lt; <span class="built_in">end</span>) ) &#123;</span><br><span class="line">            <span class="keyword">uint8_t</span> immediate = *p &amp; REBASE_IMMEDIATE_MASK;</span><br><span class="line">            <span class="keyword">uint8_t</span> opcode = *p &amp; REBASE_OPCODE_MASK;</span><br><span class="line">            ++p;</span><br><span class="line">            <span class="keyword">switch</span> (opcode) &#123;</span><br><span class="line">                <span class="keyword">case</span> REBASE_OPCODE_DONE:</span><br><span class="line">                    done = <span class="literal">true</span>;</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                <span class="keyword">case</span> REBASE_OPCODE_SET_TYPE_IMM:</span><br><span class="line">                    type = immediate;</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                <span class="keyword">case</span> REBASE_OPCODE_SET_SEGMENT_AND_OFFSET_ULEB:</span><br><span class="line">                    segmentIndex = immediate;</span><br><span class="line">                    <span class="comment">//.....</span></span><br><span class="line">                    address = segActualLoadAddress(segmentIndex) + read_uleb128(p, <span class="built_in">end</span>);</span><br><span class="line">                    segmentEndAddress = segActualEndAddress(segmentIndex);</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                <span class="keyword">case</span> REBASE_OPCODE_ADD_ADDR_ULEB:</span><br><span class="line">                    address += read_uleb128(p, <span class="built_in">end</span>);</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                <span class="keyword">case</span> REBASE_OPCODE_ADD_ADDR_IMM_SCALED:</span><br><span class="line">                    address += immediate*<span class="keyword">sizeof</span>(<span class="keyword">uintptr_t</span>);</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                <span class="keyword">case</span> REBASE_OPCODE_DO_REBASE_IMM_TIMES:</span><br><span class="line">                    <span class="keyword">for</span> (<span class="keyword">int</span> i=<span class="number">0</span>; i &lt; immediate; ++i) &#123;</span><br><span class="line">                        <span class="keyword">if</span> ( address &gt;= segmentEndAddress ) </span><br><span class="line">                            throwBadRebaseAddress(address, segmentEndAddress, segmentIndex, start, <span class="built_in">end</span>, p);</span><br><span class="line">                        rebaseAt(context, address, slide, type);</span><br><span class="line">                        address += <span class="keyword">sizeof</span>(<span class="keyword">uintptr_t</span>);</span><br><span class="line">                    &#125;</span><br><span class="line">                    fgTotalRebaseFixups += immediate;</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                <span class="keyword">case</span> REBASE_OPCODE_DO_REBASE_ULEB_TIMES:</span><br><span class="line">                    count = read_uleb128(p, <span class="built_in">end</span>);</span><br><span class="line">                    <span class="keyword">for</span> (<span class="keyword">uint32_t</span> i=<span class="number">0</span>; i &lt; count; ++i) &#123;</span><br><span class="line">                        rebaseAt(context, address, slide, type);</span><br><span class="line">                        address += <span class="keyword">sizeof</span>(<span class="keyword">uintptr_t</span>);</span><br><span class="line">                    &#125;</span><br><span class="line">                    fgTotalRebaseFixups += count;</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                <span class="keyword">case</span> REBASE_OPCODE_DO_REBASE_ADD_ADDR_ULEB:</span><br><span class="line">                    rebaseAt(context, address, slide, type);</span><br><span class="line">                    address += read_uleb128(p, <span class="built_in">end</span>) + <span class="keyword">sizeof</span>(<span class="keyword">uintptr_t</span>);</span><br><span class="line">                    ++fgTotalRebaseFixups;</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                <span class="keyword">case</span> REBASE_OPCODE_DO_REBASE_ULEB_TIMES_SKIPPING_ULEB:</span><br><span class="line">                    count = read_uleb128(p, <span class="built_in">end</span>);</span><br><span class="line">                    skip = read_uleb128(p, <span class="built_in">end</span>);</span><br><span class="line">                    <span class="keyword">for</span> (<span class="keyword">uint32_t</span> i=<span class="number">0</span>; i &lt; count; ++i) &#123;</span><br><span class="line">                        rebaseAt(context, address, slide, type);</span><br><span class="line">                        address += skip + <span class="keyword">sizeof</span>(<span class="keyword">uintptr_t</span>);</span><br><span class="line">                    &#125;</span><br><span class="line">                    fgTotalRebaseFixups += count;</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                <span class="keyword">default</span>:</span><br><span class="line">                    dyld::throwf(<span class="string">"bad rebase opcode %d"</span>, *p);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">catch</span> (<span class="keyword">const</span> <span class="keyword">char</span>* msg) &#123;</span><br><span class="line">        <span class="comment">//......</span></span><br><span class="line">    &#125;</span><br><span class="line">    CRSetCrashLogMessage2(<span class="literal">NULL</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>在ImageLoaderMachOCompressed::rebase方法中就是对这些地址进行修正，从而获得明确的绝对地址。</p><p>接下来我们看下Bind 过程的代码<strong><strong>recursiveBind</strong></strong>：</p><p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">ImageLoader::recursiveBind</span><span class="params">(<span class="keyword">const</span> LinkContext&amp; context, <span class="keyword">bool</span> forceLazysBound, <span class="keyword">bool</span> neverUnload)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// Normally just non-lazy pointers are bound immediately.</span></span><br><span class="line">    <span class="comment">// The exceptions are:</span></span><br><span class="line">    <span class="comment">//   1) DYLD_BIND_AT_LAUNCH will cause lazy pointers to be bound immediately</span></span><br><span class="line">    <span class="comment">//   2) some API's (e.g. RTLD_NOW) can cause lazy pointers to be bound immediately</span></span><br><span class="line">    <span class="keyword">if</span> ( fState &lt; dyld_image_state_bound ) &#123;</span><br><span class="line">        <span class="comment">// break cycles</span></span><br><span class="line">        fState = dyld_image_state_bound;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="comment">// bind lower level libraries first</span></span><br><span class="line">            <span class="keyword">for</span>(<span class="keyword">unsigned</span> <span class="keyword">int</span> i=<span class="number">0</span>; i &lt; libraryCount(); ++i) &#123;</span><br><span class="line">                ImageLoader* dependentImage = libImage(i);</span><br><span class="line">                <span class="keyword">if</span> ( dependentImage != <span class="literal">NULL</span> )</span><br><span class="line">                    dependentImage-&gt;recursiveBind(context, forceLazysBound, neverUnload);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// bind this image</span></span><br><span class="line">            <span class="keyword">this</span>-&gt;doBind(context, forceLazysBound);</span><br><span class="line">            <span class="comment">// ......</span></span><br><span class="line">            context.notifySingle(dyld_image_state_bound, <span class="keyword">this</span>); </span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">catch</span> (<span class="keyword">const</span> <span class="keyword">char</span>* msg) &#123;</span><br><span class="line">            <span class="comment">//......</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">ImageLoaderMachOCompressed::doBind</span><span class="params">(<span class="keyword">const</span> LinkContext&amp; context, <span class="keyword">bool</span> forceLazysBound)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    CRSetCrashLogMessage2(<span class="keyword">this</span>-&gt;getPath());</span><br><span class="line"></span><br><span class="line">    <span class="comment">// if prebound and loaded at prebound address, and all libraries are same as when this was prebound, then no need to bind</span></span><br><span class="line">    <span class="comment">// note: flat-namespace binaries need to have imports rebound (even if correctly prebound)</span></span><br><span class="line">    <span class="keyword">if</span> ( <span class="keyword">this</span>-&gt;usablePrebinding(context) ) &#123;</span><br><span class="line">        <span class="comment">// don't need to bind</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> &#123;</span><br><span class="line">    #<span class="keyword">if</span> TEXT_RELOC_SUPPORT</span><br><span class="line">        <span class="comment">// if there are __TEXT fixups, temporarily make __TEXT writable</span></span><br><span class="line">        <span class="keyword">if</span> ( fTextSegmentBinds )</span><br><span class="line">            <span class="keyword">this</span>-&gt;makeTextSegmentWritable(context, <span class="literal">true</span>);</span><br><span class="line">    <span class="meta">#<span class="meta-keyword">endif</span></span></span><br><span class="line"></span><br><span class="line">        <span class="comment">// run through all binding opcodes</span></span><br><span class="line">        eachBind(context, &amp;ImageLoaderMachOCompressed::bindAt);</span><br><span class="line">    <span class="meta">#<span class="meta-keyword">if</span> TEXT_RELOC_SUPPORT</span></span><br><span class="line">        <span class="comment">// if there were __TEXT fixups, restore write protection</span></span><br><span class="line">        <span class="keyword">if</span> ( fTextSegmentBinds )    </span><br><span class="line">        <span class="keyword">this</span>-&gt;makeTextSegmentWritable(context, <span class="literal">false</span>);</span><br><span class="line">    <span class="meta">#<span class="meta-keyword">endif</span>  </span></span><br><span class="line">        <span class="comment">// if this image is in the shared cache, but depends on something no longer in the shared cache,</span></span><br><span class="line">        <span class="comment">// there is no way to reset the lazy pointers, so force bind them now</span></span><br><span class="line">        <span class="keyword">if</span> ( forceLazysBound || fInSharedCache ) </span><br><span class="line">            <span class="keyword">this</span>-&gt;doBindJustLazies(context);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// this image is in cache, but something below it is not.  If</span></span><br><span class="line">        <span class="comment">// this image has lazy pointer to a resolver function, then</span></span><br><span class="line">        <span class="comment">// the stub may have been altered to point to a shared lazy pointer.</span></span><br><span class="line">        <span class="keyword">if</span> ( fInSharedCache ) </span><br><span class="line">            <span class="keyword">this</span>-&gt;updateOptimizedLazyPointers(context);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// tell kernel we are done with chunks of LINKEDIT</span></span><br><span class="line">        <span class="keyword">if</span> ( !context.preFetchDisabled ) </span><br><span class="line">            <span class="keyword">this</span>-&gt;markFreeLINKEDIT(context);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// set up dyld entry points in image</span></span><br><span class="line">    <span class="comment">// do last so flat main executables will have __dyld or __program_vars set up</span></span><br><span class="line">    <span class="keyword">this</span>-&gt;setupLazyPointerHandler(context);</span><br><span class="line">    CRSetCrashLogMessage2(<span class="literal">NULL</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>****Bind：将指针指向镜像外部的内容 ****</p><p>我们前面提到了经过rebase处理后我们将会在内存中获取该库的一个明确的入口地址，并且库内部的相对地址会被确定为一个明确的地址，但是对其他库引用的地方还是没明确，这部分就需要Binding阶段进行处理了，Binding阶段主要的任务就是根据被导出符号重定位表项，去符号表中找出符号的基本信息，再去其他库符号表中resolve符号，将resolve结果bind到需要重定位地址上。说句人话就是，在其他库的符号表中去查找明确地址，然后在当前动态库中使用这个地址。一般我们使用另一个动态库的某个方法的时候会有一个间接引用，这个地址中存放的就是我们需要跳转的地址，在binding阶段会将其他库中查找的地址这需要很多计算，去符号表里查找。找到后会将内容存储到 __DATA 段中的那个指针中，下次调用的时候就会从这个地方去拿这个地址去跳转到要调用其他动态库的函数地址。</p><p>这里还有两种特殊的绑定：</p><p><strong><strong>Lazy Bind:</strong></strong></p><p>我们启动优化中常常会考虑将某些动态依赖库改为静态依赖，虽然静态依赖会增大包的体积，但是会改善启动缓慢的问题，上面看到Bind的过程，发现Bind的过程需要查到对应的符号再进行bind. 如果在启动的时候，所有的符号都立即Bind成功，那么势必拖慢启动速度，那么我们可以将某些不必要的符号绑定延后吗？嗯，肯定可以的，Lazy Bind就是第一次调用到才会进行真正的Bind.这里其实用到dyld_stub_binder，在需要绑定的时候会将调用信息传递给它，由它来找到最终要跳转的地址，从而完成绑定。</p><p><strong><strong>Weak Bind:</strong></strong></p><p>Weak Bind这一步是将相同的弱符号统一化，如果有强符号则统一成强符号，否则统一成按装载顺序的首个弱符号，在这个过程中会去寻找，找到一个相同的符号后，如果是强符号则不需要再查找，否则从装载顺序找到首个弱符号地址，将所有库中的相同符号都覆盖为该地址。</p><p><strong><strong>初始化主程序:</strong></strong></p><p><figure class="highlight less"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">void</span> <span class="selector-tag">initializeMainExecutable</span>()</span><br><span class="line">&#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 初始化任意插入的dylibs</span></span><br><span class="line">    <span class="comment">// run initialzers for any inserted dylibs</span></span><br><span class="line">    <span class="selector-tag">if</span> ( rootCount &gt; <span class="number">1</span> ) &#123;</span><br><span class="line">        <span class="comment">//这里需要注意的是sImageRoots 中的第一个变量是主程序镜像, 因此这里初始化的时候需要跳过第一个数据, 对其他后面插入的dylib进行调用ImageLoader::runInitializers进行初始化</span></span><br><span class="line">        <span class="selector-tag">for</span>(size_t i=<span class="number">1</span>; i &lt; rootCount; ++i) &#123;</span><br><span class="line">            <span class="selector-tag">sImageRoots</span><span class="selector-attr">[i]</span><span class="selector-tag">-</span>&gt;<span class="selector-tag">runInitializers</span>(gLinkContext, initializerTimes[<span class="number">0</span>]);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 最后运行主程序的初始化器</span></span><br><span class="line">    <span class="comment">// run initializers for main executable and everything it brings up </span></span><br><span class="line">    <span class="selector-tag">sMainExecutable-</span>&gt;<span class="selector-tag">runInitializers</span>(gLinkContext, initializerTimes[<span class="number">0</span>]);</span><br><span class="line"></span><br><span class="line">    <span class="comment">//.........</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>我们看到<strong><strong>initializeMainExecutable</strong></strong>方法中会先对插入的动态链接库进行初始化，然后再对主程序进行初始化，初始化都是调用<strong><strong>runInitializers</strong></strong></p><p><figure class="highlight verilog"><table><tr><td class="code"><pre><span class="line"><span class="keyword">void</span> ImageLoader::runInitializers(<span class="keyword">const</span> LinkContext&amp; <span class="keyword">context</span>, InitializerTimingList&amp; timingInfo)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">//......</span></span><br><span class="line">    <span class="comment">//处理初始化器</span></span><br><span class="line">    processInitializers(<span class="keyword">context</span>, thisThread, timingInfo, up);</span><br><span class="line">    <span class="keyword">context</span><span class="variable">.notifyBatch</span>(dyld_image_state_initialized);</span><br><span class="line">    <span class="comment">//......</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>runInitializers方法中就做两件事情，一个就是调用processInitializers，以及在初始化结束发出通知，告诉监听者，当前已经初始化完毕。</p><p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">ImageLoader::processInitializers</span><span class="params">(<span class="keyword">const</span> LinkContext&amp; context, <span class="keyword">mach_port_t</span> thisThread,</span></span></span><br><span class="line"><span class="function"><span class="params">                                    InitializerTimingList&amp; timingInfo, ImageLoader::UninitedUpwards&amp; images)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">//在镜像列表中的所有镜像进行递归初始化</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">uintptr_t</span> i=<span class="number">0</span>; i &lt; images.count; ++i) &#123;</span><br><span class="line">        images.images[i]-&gt;recursiveInitialization(context, thisThread, timingInfo, ups);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//......</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">ImageLoader::recursiveInitialization</span><span class="params">(<span class="keyword">const</span> LinkContext&amp; context, <span class="keyword">mach_port_t</span> this_thread,</span></span></span><br><span class="line"><span class="function"><span class="params">                                        InitializerTimingList&amp; timingInfo, UninitedUpwards&amp; uninitUps)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="function">recursive_lock <span class="title">lock_info</span><span class="params">(this_thread)</span></span>;</span><br><span class="line">    recursiveSpinLock(lock_info);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> ( fState &lt; dyld_image_state_dependents_initialized<span class="number">-1</span> ) &#123;</span><br><span class="line">        <span class="comment">//......</span></span><br><span class="line">        <span class="comment">// let objc know we are about to initialize this image</span></span><br><span class="line">        <span class="keyword">uint64_t</span> t1 = mach_absolute_time();</span><br><span class="line">        fState = dyld_image_state_dependents_initialized;</span><br><span class="line">        oldState = fState;  </span><br><span class="line">        <span class="comment">// 通知 runtime, 当前状态发生变化 -- image的依赖已经完全加载. 如果在runtime中注册了状态监听，当状态发送变化时, 会触发回调函数.</span></span><br><span class="line">        context.notifySingle(dyld_image_state_dependents_initialized, <span class="keyword">this</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">//递归的调用当前image的依赖的dylib动态库的初始化函数进行初始化</span></span><br><span class="line">        <span class="comment">// initialize this image    </span></span><br><span class="line">        <span class="keyword">bool</span> hasInitializers = <span class="keyword">this</span>-&gt;doInitialization(context);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// let anyone know we finished initializing this image</span></span><br><span class="line">        fState = dyld_image_state_initialized;</span><br><span class="line">        oldState = fState;</span><br><span class="line">        <span class="comment">// 通知runtime, 当前镜像初始化完成</span></span><br><span class="line">        context.notifySingle(dyld_image_state_initialized, <span class="keyword">this</span>);</span><br><span class="line">        <span class="comment">//.....</span></span><br><span class="line">    &#125;</span><br><span class="line">    recursiveSpinUnLock();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p><figure class="highlight verilog"><table><tr><td class="code"><pre><span class="line">bool ImageLoaderMachO::doInitialization(<span class="keyword">const</span> LinkContext&amp; <span class="keyword">context</span>)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// 初始化镜像</span></span><br><span class="line">    <span class="comment">// mach-o has -init and static initializers</span></span><br><span class="line">    doImageInit(<span class="keyword">context</span>);</span><br><span class="line">    doModInitFunctions(<span class="keyword">context</span>);</span><br><span class="line">    <span class="keyword">return</span> (fHasDashInit || fHasInitializers);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p><figure class="highlight livescript"><table><tr><td class="code"><pre><span class="line"><span class="literal">void</span> ImageLoaderMachO::doImageInit(<span class="keyword">const</span> LinkContext&amp; context)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">if</span> ( fHasDashInit ) &#123;</span><br><span class="line">        <span class="keyword">const</span> uint32_t <span class="function"><span class="title">cmd_count</span> = <span class="params">((macho_header*)fMachOData)</span>-&gt;</span>ncmds;</span><br><span class="line">        <span class="keyword">const</span> struct load_command* <span class="keyword">const</span> cmds = (struct load_command*)&amp;fMachOData[sizeof(macho_header)];</span><br><span class="line">        <span class="keyword">const</span> struct load_command* cmd = cmds;</span><br><span class="line">        <span class="keyword">for</span> (uint32_t i = <span class="number">0</span>; i &lt; cmd_count; ++i) &#123;</span><br><span class="line">            <span class="keyword">switch</span> (cmd-&gt;cmd) &#123;</span><br><span class="line">                <span class="keyword">case</span> LC_ROUTINES_COMMAND:</span><br><span class="line">                    Initializer <span class="function"><span class="title">func</span> = <span class="params">(Initializer)(((struct macho_routines_command*)cmd)-&gt;init_address + fSlide)</span>;</span></span><br><span class="line"><span class="function">                    //.......</span></span><br><span class="line"><span class="function">                    <span class="title">func</span><span class="params">(context.argc, context.argv, context.envp, context.apple, &amp;context.programVars)</span>;</span></span><br><span class="line"><span class="function">                    <span class="title">break</span>;</span></span><br><span class="line"><span class="function">            &#125;</span></span><br><span class="line"><span class="function">            <span class="title">cmd</span> = <span class="params">(<span class="keyword">const</span> struct load_command*)(((char*)cmd)+cmd-&gt;cmdsize)</span>;</span></span><br><span class="line"><span class="function">        &#125;</span></span><br><span class="line"><span class="function">    &#125;</span></span><br><span class="line"><span class="function">&#125;</span></span><br></pre></td></tr></table></figure></p><p>doImageInit方法主要获取Mach O的init方法的地址并调用它，完成初始化操作。</p><p><figure class="highlight verilog"><table><tr><td class="code"><pre><span class="line"><span class="keyword">void</span> ImageLoaderMachO::doModInitFunctions(<span class="keyword">const</span> LinkContext&amp; <span class="keyword">context</span>)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">if</span> ( fHasInitializers ) &#123;</span><br><span class="line">        <span class="keyword">const</span> uint32_t cmd_count = ((macho_header*)fMachOData)-&gt;ncmds;</span><br><span class="line">        <span class="keyword">const</span> <span class="keyword">struct</span> load_command* <span class="keyword">const</span> cmds = (<span class="keyword">struct</span> load_command*)&amp;fMachOData[sizeof(macho_header)];<span class="keyword">const</span> <span class="keyword">struct</span> load_command* cmd = cmds;</span><br><span class="line">        <span class="keyword">for</span> (uint32_t i = <span class="number">0</span>; i &lt; cmd_count; ++i) &#123;</span><br><span class="line">            <span class="keyword">if</span> ( cmd-&gt;cmd == LC_SEGMENT_COMMAND ) &#123;</span><br><span class="line">                <span class="keyword">const</span> <span class="keyword">struct</span> macho_segment_command* seg = (<span class="keyword">struct</span> macho_segment_command*)cmd;</span><br><span class="line">                <span class="keyword">const</span> <span class="keyword">struct</span> macho_section* <span class="keyword">const</span> sectionsStart = (<span class="keyword">struct</span> macho_section*)((char*)seg + sizeof(<span class="keyword">struct</span> macho_segment_command));</span><br><span class="line">                <span class="keyword">const</span> <span class="keyword">struct</span> macho_section* <span class="keyword">const</span> sectionsEnd = &amp;sectionsStart[seg-&gt;nsects];</span><br><span class="line">                <span class="keyword">for</span> (<span class="keyword">const</span> <span class="keyword">struct</span> macho_section* sect=sectionsStart; sect &lt; sectionsEnd; ++sect) &#123;</span><br><span class="line">                    <span class="keyword">const</span> uint8_t <span class="keyword">type</span> = sect-&gt;flags &amp; SECTION_TYPE;</span><br><span class="line">                    <span class="keyword">if</span> ( <span class="keyword">type</span> == S_MOD_INIT_FUNC_POINTERS ) &#123;</span><br><span class="line">                        <span class="comment">//初始化器</span></span><br><span class="line">                        Initializer* inits = (Initializer*)(sect-&gt;addr + fSlide);</span><br><span class="line">                        <span class="keyword">const</span> size_t count = sect-&gt;size / sizeof(uintptr_t);</span><br><span class="line">                        <span class="keyword">for</span> (size_t i=<span class="number">0</span>; i &lt; count; ++i) &#123;</span><br><span class="line">                            <span class="comment">//......</span></span><br><span class="line">                            func(<span class="keyword">context</span><span class="variable">.argc</span>, <span class="keyword">context</span><span class="variable">.argv</span>, <span class="keyword">context</span><span class="variable">.envp</span>, <span class="keyword">context</span><span class="variable">.apple</span>, &amp;<span class="keyword">context</span><span class="variable">.programVars</span>);</span><br><span class="line">                        &#125;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            cmd = (<span class="keyword">const</span> <span class="keyword">struct</span> load_command*)(((char*)cmd)+cmd-&gt;cmdsize);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>doModInitFunctions方法主要获取Mach O的static initializer的地址并调用</p><p>这里需要注意的是这里的initalizer并非指的是名为initalizer的方法, 而是使用 attribute((constructor) 进行修饰的方法, 在ImageLoader类中initializer函数指针所指向该初始化方法的地址。</p><p>这里有一个十分重要的动态库初始化方法，<strong><strong>libSystem_initializer</strong></strong>就是在这个阶段被调用的。<figure class="highlight delphi"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> __attribute__((<span class="function"><span class="keyword">constructor</span>)) </span></span><br><span class="line"><span class="function"><span class="title">void</span> <span class="title">libSystem_initializer</span><span class="params">(int argc, <span class="keyword">const</span> char* argv[], <span class="keyword">const</span> char* envp[], <span class="keyword">const</span> char* apple[], <span class="keyword">const</span> struct ProgramVars* vars)</span></span></span><br><span class="line"><span class="function"><span class="comment">&#123;</span></span></span><br><span class="line"><span class="function"><span class="comment">    //......</span></span></span><br><span class="line"><span class="function"><span class="comment">    _libkernel_init(libkernel_funcs);</span></span></span><br><span class="line"><span class="function"><span class="comment"></span></span></span><br><span class="line"><span class="function"><span class="comment">    bootstrap_init();</span></span></span><br><span class="line"><span class="function"><span class="comment">    mach_init();</span></span></span><br><span class="line"><span class="function"><span class="comment">    pthread_init();</span></span></span><br><span class="line"><span class="function"><span class="comment">    __libc_init(vars, libSystem_atfork_prepare, libSystem_atfork_parent, libSystem_atfork_child, apple);</span></span></span><br><span class="line"><span class="function"><span class="comment">    __keymgr_initializer();</span></span></span><br><span class="line"><span class="function"><span class="comment">    _dyld_initializer();</span></span></span><br><span class="line"><span class="function"><span class="comment"></span></span></span><br><span class="line"><span class="function"><span class="comment">    libdispatch_init();</span></span></span><br><span class="line"><span class="function"><span class="comment">    </span></span></span><br><span class="line"><span class="function"><span class="comment">    _libxpc_initializer();</span></span></span><br><span class="line"><span class="function"><span class="comment">    //......</span></span></span><br><span class="line"><span class="function"><span class="comment">errno = 0;</span></span></span><br><span class="line"><span class="function"><span class="comment">&#125;</span></span></span><br></pre></td></tr></table></figure></p><p>libSystem_initializer会调用libdispatch_init再到_objc_init初始化runtime在_objc_init中通过注册了几个关键通知, 从dyld这里接手了关键时机的处理，这个方法会在下一篇介绍runtime的博客中进行介绍，我们先继续往下看。对于Libsystem代码地址可以在<a href="https://opensource.apple.com/tarballs/Libsystem/" target="_blank" rel="noopener">该地址</a>进行下载.</p><p><strong><strong>获取主程序入口</strong></strong></p><p><figure class="highlight xl"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="title">result</span> = (uintptr_t)sMainExecutable-&gt;</span>getThreadPC();</span><br><span class="line"><span class="keyword">if</span> ( result != <span class="number">0</span> ) &#123;</span><br><span class="line">    <span class="comment">// 主可执行文件使用lc_main，需要返回libdyld.dylib中的glue</span></span><br><span class="line">    <span class="comment">//调用main()</span></span><br><span class="line">    <span class="comment">//当执行完dyld::_main方法之后,返回了main()函数地址,这个时候所有初始化工作都已经完成了,正式进入Objc声明周期</span></span><br><span class="line">    <span class="comment">// main executable uses LC_MAIN, needs to return to glue in libdyld.dylib</span></span><br><span class="line">    <span class="function"><span class="title">if</span> ( (gLibSystemHelpers != NULL) &amp;&amp; (gLibSystemHelpers-&gt;</span>version &gt;= <span class="number">9</span>) )</span><br><span class="line">        *<span class="function"><span class="title">startGlue</span> = (uintptr_t)gLibSystemHelpers-&gt;</span>startGlueToCallExit;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        halt(<span class="string">"libdyld.dylib support not present for LC_MAIN"</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="comment">// main executable uses LC_UNIXTHREAD, dyld needs to let "start" in program set up for main()</span></span><br><span class="line">    <span class="function"><span class="title">result</span> = (uintptr_t)sMainExecutable-&gt;</span>getMain();</span><br><span class="line">    *startGlue = <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>main 地址有两种方式获取，一种是存放在LC_MAIN命令中，这时候需要调用<strong><strong>getThreadPC</strong></strong></p><p><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line">void* ImageLoaderMachO::getThreadPC() <span class="keyword">const</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">const</span> uint32_t cmd_count = ((macho_header*)fMachOData)-&gt;ncmds;</span><br><span class="line">    <span class="keyword">const</span> <span class="class"><span class="keyword">struct</span> <span class="title">load_command</span></span>* <span class="keyword">const</span> cmds = (<span class="class"><span class="keyword">struct</span> <span class="title">load_command</span></span>*)&amp;fMachOData[sizeof(macho_header)];</span><br><span class="line">    <span class="keyword">const</span> <span class="class"><span class="keyword">struct</span> <span class="title">load_command</span></span>* cmd = cmds;</span><br><span class="line">    <span class="keyword">for</span> (uint32_t i = <span class="number">0</span>; i &lt; cmd_count; ++i) &#123;</span><br><span class="line">        <span class="comment">// 遍历loadCommand,加载loadCommand中的'LC_MAIN'所指向的偏移地址</span></span><br><span class="line">        <span class="keyword">if</span> ( cmd-&gt;cmd == LC_MAIN ) &#123;</span><br><span class="line">            entry_point_command* mainCmd = (entry_point_command*)cmd;</span><br><span class="line">            <span class="comment">// 偏移量 + header所占的字节数，就是main的入口</span></span><br><span class="line">            void* entry = (void*)(mainCmd-&gt;entryoff + (<span class="built_in">char</span>*)fMachOData);</span><br><span class="line">            <span class="keyword">if</span> ( this-&gt;containsAddress(entry) )</span><br><span class="line">                <span class="keyword">return</span> entry;</span><br><span class="line">            <span class="keyword">else</span></span><br><span class="line">                throw <span class="string">"LC_MAIN entryoff is out of range"</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        cmd = (<span class="keyword">const</span> <span class="class"><span class="keyword">struct</span> <span class="title">load_command</span></span>*)(((<span class="built_in">char</span>*)cmd)+cmd-&gt;cmdsize);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> NULL;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>getThreadPC主要逻辑就是遍历loadCommand，找到’LC_MAIN’指令，得到该指令所指向的偏移地址，经过处理后，就得到了main函数的地址，将此地址返回给__dyld_start。__dyld_start中将main函数地址保存在寄存器后，跳转到对应的地址，开始执行main函数，另一种Mach O不支持LC_MAIN仅支持LC_UNIXTHREAD，这时候就需要调用<strong><strong>getMain</strong></strong>方法。</p><p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span>* <span class="title">ImageLoaderMachO::getMain</span><span class="params">()</span> <span class="keyword">const</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">uint32_t</span> cmd_count = ((macho_header*)fMachOData)-&gt;ncmds;</span><br><span class="line">    <span class="keyword">const</span> <span class="class"><span class="keyword">struct</span> <span class="title">load_command</span>* <span class="title">const</span> <span class="title">cmds</span> = (<span class="title">struct</span> <span class="title">load_command</span>*)&amp;<span class="title">fMachOData</span>[<span class="title">sizeof</span>(<span class="title">macho_header</span>)];</span></span><br><span class="line">    <span class="keyword">const</span> <span class="class"><span class="keyword">struct</span> <span class="title">load_command</span>* <span class="title">cmd</span> = <span class="title">cmds</span>;</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">uint32_t</span> i = <span class="number">0</span>; i &lt; cmd_count; ++i) &#123;</span><br><span class="line">        <span class="keyword">switch</span> (cmd-&gt;cmd) &#123;</span><br><span class="line">            <span class="keyword">case</span> LC_UNIXTHREAD: </span><br><span class="line">            &#123;</span><br><span class="line">            <span class="meta">#<span class="meta-keyword">if</span> __i386__</span></span><br><span class="line">                <span class="keyword">const</span> <span class="keyword">i386_thread_state_t</span>* registers = (<span class="keyword">i386_thread_state_t</span>*)(((<span class="keyword">char</span>*)cmd) + <span class="number">16</span>);</span><br><span class="line">                <span class="keyword">void</span>* entry = (<span class="keyword">void</span>*)(registers-&gt;eip + fSlide);</span><br><span class="line">            <span class="meta">#<span class="meta-keyword">elif</span> __x86_64__</span></span><br><span class="line">                <span class="keyword">const</span> <span class="keyword">x86_thread_state64_t</span>* registers = (<span class="keyword">x86_thread_state64_t</span>*)(((<span class="keyword">char</span>*)cmd) + <span class="number">16</span>);</span><br><span class="line">                <span class="keyword">void</span>* entry = (<span class="keyword">void</span>*)(registers-&gt;rip + fSlide);</span><br><span class="line">            <span class="comment">//.....</span></span><br><span class="line">            &#125;   </span><br><span class="line">            <span class="keyword">break</span>;      </span><br><span class="line">        &#125;</span><br><span class="line">        cmd = (<span class="keyword">const</span> struct load_command*)(((<span class="keyword">char</span>*)cmd)+cmd-&gt;cmdsize);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">throw</span> <span class="string">"no valid entry point"</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><h4>总结</h4><p>上一篇博客我们主要介绍了从点击应用到加载dyld，再将地址设置到dyld到入口地址，这篇博客就从dyld入口地址__dyld_start作为起点，在这个阶段中主要做了如下工作：</p><p><figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line"><span class="bullet">1. </span>将主程序初始化为imageLoader</span><br><span class="line"><span class="bullet">2. </span>加载共享库到内存</span><br><span class="line"><span class="bullet">3. </span>加载插入的动态库</span><br><span class="line"><span class="bullet">4. </span>链接主程序,链接插入库</span><br><span class="line"><span class="bullet">5. </span>初始化主程序，插入库</span><br><span class="line"><span class="bullet">6. </span>寻找主程序入口点</span><br></pre></td></tr></table></figure></p><p>而下一篇博客要给大家介绍的是十分关键的runtime，它的初始化就发生在初始化主程序，插入库这个阶段，这个阶段会调用使用 attribute((constructor) 进行修饰的方法, 其中这里有一个十分重要的动态库初始化方法，<strong><strong>libSystem_initializer</strong></strong>就是在这个阶段被调用的libSystem_initializer会调用libdispatch_init再到_objc_init初始化runtime在_objc_init中通过注册了几个关键通知, 从dyld这里接手了关键时机的处理，包括镜像映射，镜像加载，镜像卸载，这些都是下一篇博客将要介绍的。从下一篇开始就要正式介入介绍Runtime了。</p><ul><li><a href="http://yulingtianxia.com/blog/2016/10/30/Optimizing-App-Startup-Time/" target="_blank" rel="noopener">优化 App 的启动时间</a></li><li><a href="https://blog.cnbluebox.com/blog/2017/06/20/dyldyu-objc/" target="_blank" rel="noopener">dyld与ObjC</a></li><li><a href="https://blog.cnbluebox.com/blog/2017/10/12/dyld2/" target="_blank" rel="noopener">Dyld之二: 动态链接过程</a></li><li><a href="https://www.jianshu.com/p/d2eeb4085af5" target="_blank" rel="noopener">iOS App启动时发生了什么?</a></li><li><a href="https://zhiyongzou.github.io/2018/03/26/App-%E5%90%AF%E5%8A%A8%E4%BC%98%E5%8C%96%E6%8E%A2%E7%B4%A2/" target="_blank" rel="noopener">App 启动优化探索</a></li></ul>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h4&gt;源码地址&lt;/h4&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&quot;https://github.com/opensource-apple/dyld&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;dyld&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;h4&gt;源码分析&lt;/h4&gt;
&lt;
      
    
    </summary>
    
      <category term="Objective C 相关" scheme="http://yoursite.com/categories/Objective-C-%E7%9B%B8%E5%85%B3/"/>
    
    
      <category term="Objective C 相关" scheme="http://yoursite.com/tags/Objective-C-%E7%9B%B8%E5%85%B3/"/>
    
  </entry>
  
  <entry>
    <title>iOS Runtime源码解析之App启动</title>
    <link href="http://yoursite.com/2020/01/09/iOS-Runtime%E6%BA%90%E7%A0%81%E8%A7%A3%E6%9E%90%E4%B9%8BApp%E5%90%AF%E5%8A%A8/"/>
    <id>http://yoursite.com/2020/01/09/iOS-Runtime源码解析之App启动/</id>
    <published>2020-01-09T13:34:38.000Z</published>
    <updated>2020-02-07T09:41:50.000Z</updated>
    
    <content type="html"><![CDATA[<p>上一篇博客介绍了Mach O文件的格式，以及每个部分的作用，这一部分开始将要介绍的是在上一个基础上如何将Mach O文件加载并运行起来。我们会从点击执行应用开始，到加载dyld，初始化dyld，再到dyld将镜像加载到内存，Rebase/Bind，Runtime初始化，其它的初始化代码，最后运行main函数为止。整个过程如下图所示：</p><p><img src="/2020/01/09/iOS-Runtime源码解析之App启动/000001.png" alt></p><p>在开始这篇博客内容规划之前，最难的是从哪里开头讲，后面查看了XNU代码后发现个人能hold住的起点应该是load_init_program，当然后面随着技术的提高肯定能够从更深层次给大家讲明白这个问题，都是时间问题，对自己有信心，好了不说太多无关的内容了，直接切入主题：</p><p>我们知道XNU内核启动后，启动的第一个进程是launchd。launchd启动之后会启动其他守护进程。我们可以看XNU源码中有如下一句注释：</p><p><figure class="highlight 1c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Description:Load the "init" program; in most cases, this will be "launchd"</span></span><br></pre></td></tr></table></figure></p><p>我们先来看下<strong><strong>load_init_program</strong></strong>：</p><p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">void</span></span><br><span class="line">load_init_program(<span class="keyword">proc_t</span> p)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">//......</span></span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">     * Copy out program name.</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    init_addr = VM_MIN_ADDRESS;</span><br><span class="line">    (<span class="keyword">void</span>) vm_allocate(current_map(), &amp;init_addr, PAGE_SIZE,VM_FLAGS_ANYWHERE);</span><br><span class="line">    <span class="keyword">if</span> (init_addr == <span class="number">0</span>)</span><br><span class="line">        init_addr++;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//将/sbin/launchd拷贝到 init_addr = 1 </span></span><br><span class="line">    (<span class="keyword">void</span>) copyout((<span class="keyword">caddr_t</span>) init_program_name, CAST_USER_ADDR_T(init_addr),(<span class="keyword">unsigned</span>) <span class="keyword">sizeof</span>(init_program_name)+<span class="number">1</span>);</span><br><span class="line"></span><br><span class="line">    argv[argc++] = (<span class="keyword">char</span> *) init_addr; <span class="comment">//argv[0] = "/sbin/launchd"</span></span><br><span class="line">    init_addr += <span class="keyword">sizeof</span>(init_program_name);</span><br><span class="line">    init_addr = (<span class="keyword">vm_offset_t</span>)ROUND_PTR(<span class="keyword">char</span>, init_addr);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (boothowto &amp; RB_SINGLE) &#123;</span><br><span class="line">        <span class="keyword">const</span> <span class="keyword">char</span> *init_args = <span class="string">"-s"</span>;</span><br><span class="line">        copyout(init_args, CAST_USER_ADDR_T(init_addr),<span class="built_in">strlen</span>(init_args));</span><br><span class="line">        argv[argc++] = (<span class="keyword">char</span> *)init_addr; <span class="comment">//argv[1] = "-s"</span></span><br><span class="line">        init_addr += <span class="built_in">strlen</span>(init_args);</span><br><span class="line">        init_addr = (<span class="keyword">vm_offset_t</span>)ROUND_PTR(<span class="keyword">char</span>, init_addr);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">     * Null-end the argument list</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    argv[argc] = <span class="literal">NULL</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">     * Copy out the argument list.</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="comment">//参数拷贝到指定到位置</span></span><br><span class="line">    (<span class="keyword">void</span>) copyout((<span class="keyword">caddr_t</span>) argv, CAST_USER_ADDR_T(init_addr),(<span class="keyword">unsigned</span>) <span class="keyword">sizeof</span>(argv));</span><br><span class="line"></span><br><span class="line">    <span class="comment">//到此位置 argv[0] = "/sbin/launchd"</span></span><br><span class="line">    <span class="comment">//        argv[1] = "-s"</span></span><br><span class="line">    <span class="comment">//        argv[2] = 参数列表</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">     * Set up argument block for fake call to execve.</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    init_exec_args.fname = CAST_USER_ADDR_T(argv[<span class="number">0</span>]);  <span class="comment">//argv[0] = "/sbin/launchd"</span></span><br><span class="line">    init_exec_args.argp = CAST_USER_ADDR_T((<span class="keyword">char</span> **)init_addr);</span><br><span class="line">    init_exec_args.envp = CAST_USER_ADDR_T(<span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">//将init_exec_args作为参数传递给execve</span></span><br><span class="line">    error = execve(p,&amp;init_exec_args,retval);</span><br><span class="line">    <span class="keyword">if</span> (error)</span><br><span class="line">        panic(<span class="string">"Process 1 exec of %s failed, errno %d\n"</span>,</span><br><span class="line">              init_program_name, error);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>我们接下来看下<strong><strong>execve</strong></strong></p><p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">execve</span><span class="params">(<span class="keyword">proc_t</span> p, struct execve_args *uap, <span class="keyword">register_t</span> *retval)</span> </span>&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> __<span class="title">mac_execve_args</span> <span class="title">muap</span>;</span></span><br><span class="line">    <span class="keyword">int</span> err;</span><br><span class="line">    muap.fname = uap-&gt;fname;   <span class="comment">//程序执行路径</span></span><br><span class="line">    muap.argp = uap-&gt;argp;     <span class="comment">//参数列表</span></span><br><span class="line">    muap.envp = uap-&gt;envp;     <span class="comment">//环境变量</span></span><br><span class="line">    muap.mac_p = USER_ADDR_NULL;</span><br><span class="line">    <span class="comment">//这里只是调用了__mac_execve将执行文件名，参数列表，环境变量传入</span></span><br><span class="line">    err = __mac_execve(p, &amp;muap, retval);</span><br><span class="line">    <span class="keyword">return</span>(err);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>在execve主要将execve_args中的程序执行文件路径，参数列表，环境变量通过__mac_execve传入到后续流程：</p><p><figure class="highlight aspectj"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">__mac_execve</span><span class="params">(proc_t p, struct __mac_execve_args *uap, register_t *retval)</span> </span>&#123;</span><br><span class="line">    <span class="comment">//.....</span></span><br><span class="line">    task = current_task();   <span class="comment">//获取当前的任务</span></span><br><span class="line">    <span class="comment">//.......</span></span><br><span class="line">    <span class="comment">//激活镜像:主要是为加载镜像进行数据的初始化，以及资源相关的操作</span></span><br><span class="line">    <span class="keyword">error</span> = exec_activate_image(imgp);</span><br><span class="line">    <span class="comment">//.......</span></span><br><span class="line">    <span class="keyword">return</span>(<span class="keyword">error</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>__mac_execve 主要是调用exec_activate_image来激活镜像</p><p><figure class="highlight mel"><table><tr><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * exec_activate_image</span></span><br><span class="line"><span class="comment"> * 遍历可用镜像激活器，并激活与imgp 结构相关的镜像</span></span><br><span class="line"><span class="comment"> * 主要是拷贝可执行文件到内存中，并根据不同的可执行文件类型选择不同的加载函数，</span></span><br><span class="line"><span class="comment"> * 所有的镜像的加载要么终止在一个错误上，要么最终完成加载镜像。</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line">static <span class="keyword">int</span> exec_activate_image(struct image_params *imgp) &#123;</span><br><span class="line">    <span class="comment">//.......</span></span><br><span class="line">again:</span><br><span class="line">    <span class="comment">// 检查镜像是否是普通文件，是否可执行，是否可读</span></span><br><span class="line">    <span class="keyword">error</span> = exec_check_permissions(imgp);</span><br><span class="line">    <span class="comment">//......</span></span><br><span class="line">encapsulated_binary:</span><br><span class="line">    <span class="comment">//根据不同的可执行文件类型选择不同的加载函数</span></span><br><span class="line">    <span class="comment">//OS X有三种可执行文件，mach-o由exec_mach_imgact处理，fat binary由exec_fat_imgact处理，</span></span><br><span class="line">    <span class="comment">//interpreter（解释器）由exec_shell_imgact处理</span></span><br><span class="line">    <span class="keyword">for</span>(i = <span class="number">0</span>; <span class="keyword">error</span> == <span class="number">-1</span> &amp;&amp; execsw[i].ex_imgact != NULL; i++) &#123;</span><br><span class="line">        <span class="comment">//遍历镜像激活函数，寻找可以激活当前镜像的激活器</span></span><br><span class="line">        <span class="keyword">error</span> = (*execsw[i].ex_imgact)(imgp);</span><br><span class="line">        <span class="comment">//...........</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//........</span></span><br><span class="line">    <span class="keyword">return</span> (<span class="keyword">error</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>exec_activate_image主要任务是遍历可用镜像激活器，按照可执行文件的格式，执行不同的函数。目前有三种格式，单指令集可执行文件（Mach-o Binary），多指令集可执行文件（Fat Binary），shell 脚本（Interpreter Script）。exec_activate_image首先会对镜像文件进行一次校验，检查镜像是否是普通文件，是否可执行，是否可读，然后遍历镜像激活函数，寻找可以激活当前镜像的激活器。<strong><strong>execsw</strong></strong>是当前能够支持的镜像激活表。它是一个结构体指针，每项结构体的结构如下所示：</p><p><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">execsw</span></span> &#123;</span><br><span class="line">    int (*ex_imgact)(<span class="class"><span class="keyword">struct</span> <span class="title">image_params</span></span> *); <span class="comment">//激活镜像的方法指针</span></span><br><span class="line">    <span class="keyword">const</span> <span class="built_in">char</span> *ex_name;                     <span class="comment">//镜像名</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>包含激活镜像的方法，以及镜像名。</p><p><figure class="highlight objectivec"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> execsw &#123;</span><br><span class="line">    <span class="keyword">int</span> (*ex_imgact)(<span class="keyword">struct</span> image_params *); <span class="comment">//激活镜像的方法指针</span></span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">char</span> *ex_name;                     <span class="comment">//镜像名</span></span><br><span class="line">&#125; execsw[] = &#123;</span><br><span class="line">    &#123; exec_mach_imgact, <span class="string">"Mach-o Binary"</span> &#125;,</span><br><span class="line">    &#123; exec_fat_imgact, <span class="string">"Fat Binary"</span> &#125;,</span><br><span class="line"><span class="meta">#<span class="meta-keyword">ifdef</span> IMGPF_POWERPC</span></span><br><span class="line">    &#123; exec_powerpc32_imgact, <span class="string">"PowerPC binary"</span> &#125;,</span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span>  <span class="comment">/* IMGPF_POWERPC */</span></span></span><br><span class="line">    &#123; exec_shell_imgact, <span class="string">"Interpreter Script"</span> &#125;,</span><br><span class="line">    &#123; <span class="literal">NULL</span>, <span class="literal">NULL</span>&#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p><p>我们这里只以Mach-o Binary的激活器为例子来看下是怎么激活的。<figure class="highlight xl"><table><tr><td class="code"><pre><span class="line">static int exec_mach_imgact(struct image_params *imgp) &#123;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="title">struct</span> mach_header *mach_header = (struct mach_header *)imgp-&gt;</span>ip_vdata;</span><br><span class="line">    <span class="comment">//........</span></span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">     * 首先确保这是个Mach-O 1.0 或者Mach-O 2.0二进制文件</span></span><br><span class="line"><span class="comment">     * make sure it's a Mach-O 1.0 or Mach-O 2.0 binary; the difference</span></span><br><span class="line"><span class="comment">     * is a reserved field on the end, so for the most part, we can</span></span><br><span class="line"><span class="comment">     * treat them as if they were identical.</span></span><br><span class="line"><span class="comment">     * magic检查</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="title">if</span> ((mach_header-&gt;</span>magic != MH_MAGIC<span class="comment">/*32位架构*/</span>) &amp;&amp;</span><br><span class="line">        (<span class="function"><span class="title">mach_header</span>-&gt;</span>magic != MH_MAGIC_64<span class="comment">/*64位架构*/</span>)) &#123;</span><br><span class="line">        error = -<span class="number">1</span>;</span><br><span class="line">        goto bad;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 如果文件类型为MH_DYLIB 或者MH_BUNDLE 返回错误</span></span><br><span class="line">    <span class="comment">//为什么MH_DYLIB，MH_BUNDLE 要认定为error</span></span><br><span class="line">    <span class="function"><span class="title">switch</span> (mach_header-&gt;</span>filetype) &#123;</span><br><span class="line">    case MH_DYLIB:</span><br><span class="line">    case MH_BUNDLE:</span><br><span class="line">        error = -<span class="number">1</span>;</span><br><span class="line">        goto bad;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//cpu 类型检查</span></span><br><span class="line">    <span class="function"><span class="title">if</span> (!imgp-&gt;</span>ip_origcputype) &#123;</span><br><span class="line">        <span class="function"><span class="title">imgp</span>-&gt;</span><span class="function"><span class="title">ip_origcputype</span> = mach_header-&gt;</span>cputype;</span><br><span class="line">        <span class="function"><span class="title">imgp</span>-&gt;</span><span class="function"><span class="title">ip_origcpusubtype</span> = mach_header-&gt;</span>cpusubtype;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    task = current_task();</span><br><span class="line">    thread = current_thread();</span><br><span class="line">    uthread = get_bsdthread_info(thread);</span><br><span class="line">    <span class="comment">//.....</span></span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">     *  加载 Mach-O 文件 </span></span><br><span class="line"><span class="comment">     *  Load the Mach-O file.</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">     * <span class="doctag">NOTE:</span> An error after this point  indicates we have potentially</span></span><br><span class="line"><span class="comment">     * destroyed or overwrote some process state while attempting an</span></span><br><span class="line"><span class="comment">     * execve() following a vfork(), which is an unrecoverable condition.</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">     * We reset the task to 64-bit (or not) here.  It may have picked up</span></span><br><span class="line"><span class="comment">     * a new map, and we need that to reflect its true 64-bit nature.</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line"></span><br><span class="line">    task_set_64bit(task, </span><br><span class="line">               ((<span class="function"><span class="title">imgp</span>-&gt;</span>ip_flags &amp; IMGPF_IS_64BIT) == IMGPF_IS_64BIT));</span><br><span class="line"></span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">     * 实际加载我们之前想要加载的镜像</span></span><br><span class="line"><span class="comment">     * Actually load the image file we previously decided to load.</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    lret = load_machfile(imgp, mach_header, thread, map, &amp;load_result);</span><br><span class="line"></span><br><span class="line">    <span class="comment">//.....</span></span><br><span class="line">    <span class="comment">/* load_machfile() maps the vnode */</span></span><br><span class="line">    (<span class="function"><span class="title">void</span>)ubc_map(imgp-&gt;</span>ip_vp, PROT_READ | PROT_EXEC);</span><br><span class="line"></span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">     * Set up the system reserved areas in the new address space.</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    vm_map_exec(get_task_map(task),</span><br><span class="line">            task,</span><br><span class="line">            (<span class="function"><span class="title">void</span> *) p-&gt;</span><span class="function"><span class="title">p_fd</span>-&gt;</span>fd_rdir,</span><br><span class="line">            cpu_type());</span><br><span class="line">    <span class="comment">//.......</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>exec_mach_imgact 其实会对Mach O文件的header信息校验，确保当前的文件是Mach O文件，并且校验它的cpu类型是否符合当前设备平台。当这些都校验通过后就通过load_machfile加载Mach O文件。</p><p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">load_return_t</span> <span class="title">load_machfile</span><span class="params">(</span></span></span><br><span class="line"><span class="function"><span class="params">    struct image_params *imgp,</span></span></span><br><span class="line"><span class="function"><span class="params">    struct mach_header  *header,</span></span></span><br><span class="line"><span class="function"><span class="params">    <span class="keyword">thread_t</span>    thread,</span></span></span><br><span class="line"><span class="function"><span class="params">    <span class="keyword">vm_map_t</span>    new_map,</span></span></span><br><span class="line"><span class="function"><span class="params">    <span class="keyword">load_result_t</span>   *result</span></span></span><br><span class="line"><span class="function"><span class="params">)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">//......</span></span><br><span class="line">    <span class="comment">//解析mach文件</span></span><br><span class="line">    lret = parse_machfile(vp, <span class="built_in">map</span>, thread, header, file_offset, macho_size,<span class="number">0</span>, result);</span><br><span class="line">    <span class="comment">//.....</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>load_machfile会加载Mach-O中的各种load monmand命令。在其内部会禁止数据段执行防止溢出漏洞攻击，还会设置地址空间布局随机化（ASLR），还有一些映射的调整。load_machfile内部直接调用了parse_machfile方法。<figure class="highlight gauss"><table><tr><td class="code"><pre><span class="line">static load_return_t <span class="built_in">parse_machfile</span>(</span><br><span class="line">    struct vnode         *vp,</span><br><span class="line">    vm_map_t             map,</span><br><span class="line">    thread_t             thread,</span><br><span class="line">    struct mach_header   *<span class="built_in">header</span>,</span><br><span class="line">    off_t                file_offset,</span><br><span class="line">    off_t                macho_size,</span><br><span class="line">    int                  depth,</span><br><span class="line">    load_result_t       *result</span><br><span class="line">)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">//.....</span></span><br><span class="line">    <span class="comment">//类型校验</span></span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">header</span>-&gt;magic == MH_MAGIC_64 || <span class="built_in">header</span>-&gt;magic == MH_CIGAM_64) &#123;</span><br><span class="line">        mach_header_sz = <span class="built_in">sizeof</span>(struct mach_header_64);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">     *Break infinite recursion</span></span><br><span class="line"><span class="comment">     *  打破因为层级较深导致的无限递归</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">if</span> (depth &gt; <span class="number">6</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span>(LOAD_FAILURE);</span><br><span class="line">    &#125;</span><br><span class="line">    task = (task_t)<span class="built_in">get_threadtask</span>(thread);</span><br><span class="line"></span><br><span class="line">    <span class="comment">//深度计数值递增</span></span><br><span class="line">    <span class="comment">// depth负责parse_machfile 遍历次数，第一次是解析mach-o, 第二次'load_dylinker'会调用此函数来进行dyld的解析</span></span><br><span class="line">    depth++;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">     *  检查是否是正确的计算机类型</span></span><br><span class="line"><span class="comment">     *  Check to see if right machine type.</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">if</span> (((cpu_type_t)(<span class="built_in">header</span>-&gt;cputype &amp; ~CPU_ARCH_MASK) != <span class="built_in">cpu_type</span>()) ||</span><br><span class="line">        !<span class="built_in">grade_binary</span>(<span class="built_in">header</span>-&gt;cputype, </span><br><span class="line">            <span class="built_in">header</span>-&gt;cpusubtype &amp; ~CPU_SUBTYPE_MASK))</span><br><span class="line">        <span class="keyword">return</span>(LOAD_BADARCH);</span><br><span class="line"></span><br><span class="line">    abi64 = ((<span class="built_in">header</span>-&gt;cputype &amp; CPU_ARCH_ABI64) == CPU_ARCH_ABI64);</span><br><span class="line"></span><br><span class="line">    <span class="comment">//主要是用来对Mach-O做检测，会检测Mach-O头部，解析其架构、检查imgp等内容，</span></span><br><span class="line">    <span class="comment">//并拒绝接受Dylib和Bundle这样的文件，这些文件会由dyld负责加载</span></span><br><span class="line">    <span class="built_in">switch</span> (<span class="built_in">header</span>-&gt;filetype) &#123;</span><br><span class="line">    case MH_OBJECT:</span><br><span class="line">    case MH_EXECUTE:</span><br><span class="line">    case MH_PRELOAD:</span><br><span class="line">        <span class="comment">//第一次的时候这里会走通</span></span><br><span class="line">        <span class="keyword">if</span> (depth != <span class="number">1</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> (LOAD_FAILURE);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">    case MH_FVMLIB:</span><br><span class="line">    case MH_DYLIB:</span><br><span class="line">        <span class="keyword">if</span> (depth == <span class="number">1</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> (LOAD_FAILURE);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">    case MH_DYLINKER:</span><br><span class="line">        <span class="comment">//第二次的时候这里会走通</span></span><br><span class="line">        <span class="keyword">if</span> (depth != <span class="number">2</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> (LOAD_FAILURE);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">    default:</span><br><span class="line">        <span class="keyword">return</span> (LOAD_FAILURE);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//.....</span></span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">     *  Round size of Mach-O commands up to page boundry.</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">    size = <span class="built_in">round_page</span>(mach_header_sz + <span class="built_in">header</span>-&gt;sizeofcmds);</span><br><span class="line">    <span class="keyword">if</span> (size &lt;= <span class="number">0</span>)</span><br><span class="line">        <span class="keyword">return</span>(LOAD_BADMACHO);</span><br><span class="line"></span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">     * 将加载命令映射到内核地址</span></span><br><span class="line"><span class="comment">     * Map the load commands into kernel memory.</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">//.......</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">     *  扫描每个命令，处理每个命令</span></span><br><span class="line"><span class="comment">     *  Scan through the commands, processing each one as necessary.</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">    <span class="keyword">for</span> (pass = <span class="number">1</span>; pass &lt;= <span class="number">2</span>; pass++) &#123;</span><br><span class="line">        <span class="comment">/*</span></span><br><span class="line"><span class="comment">         * Loop through each of the load_commands indicated by the</span></span><br><span class="line"><span class="comment">         * Mach-O header; if an absurd value is provided, we just</span></span><br><span class="line"><span class="comment">         * run off the end of the reserved section by incrementing</span></span><br><span class="line"><span class="comment">         * the offset too far, so we are implicitly fail-safe.</span></span><br><span class="line"><span class="comment">         */</span></span><br><span class="line">        offset = mach_header_sz;</span><br><span class="line">        <span class="comment">//加载命令数目</span></span><br><span class="line">        ncmds = <span class="built_in">header</span>-&gt;ncmds;</span><br><span class="line">        <span class="keyword">while</span> (ncmds--) &#123;</span><br><span class="line">            <span class="comment">/*</span></span><br><span class="line"><span class="comment">             *  获取指向命令的地址</span></span><br><span class="line"><span class="comment">             *  Get a pointer to the command.</span></span><br><span class="line"><span class="comment">            */</span></span><br><span class="line">            lcp = (<span class="keyword">struct</span> <span class="type">load_command</span> *)(addr + offset);</span><br><span class="line">            oldoffset = offset;</span><br><span class="line">            offset += lcp-&gt;cmdsize;</span><br><span class="line">            <span class="comment">/*</span></span><br><span class="line"><span class="comment">             * Perform prevalidation of the struct load_command</span></span><br><span class="line"><span class="comment">             * before we attempt to use its contents.  Invalid</span></span><br><span class="line"><span class="comment">             * values are ones which result in an overflow, or</span></span><br><span class="line"><span class="comment">             * which can not possibly be valid commands, or which</span></span><br><span class="line"><span class="comment">             * straddle or exist past the reserved section at the</span></span><br><span class="line"><span class="comment">             * start of the image.</span></span><br><span class="line"><span class="comment">             */</span></span><br><span class="line">            <span class="keyword">if</span> (oldoffset &gt; offset ||</span><br><span class="line">                lcp-&gt;cmdsize &lt; <span class="built_in">sizeof</span>(struct load_command) ||</span><br><span class="line">                offset &gt; <span class="built_in">header</span>-&gt;sizeofcmds + mach_header_sz) &#123;</span><br><span class="line">                ret = LOAD_BADMACHO;</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">/*</span></span><br><span class="line"><span class="comment">             * Act on struct load_command's for which kernel</span></span><br><span class="line"><span class="comment">             * intervention is required.</span></span><br><span class="line"><span class="comment">             */</span></span><br><span class="line">            <span class="built_in">switch</span>(lcp-&gt;cmd) &#123;</span><br><span class="line">            <span class="comment">/*加载64位segment*/</span></span><br><span class="line">            case LC_SEGMENT_64:</span><br><span class="line">                <span class="keyword">if</span> (pass != <span class="number">1</span>)</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                ret = <span class="built_in">load_segment_64</span>(</span><br><span class="line">                        (struct segment_command_64 *)lcp,</span><br><span class="line">                           pager,</span><br><span class="line">                           file_offset,</span><br><span class="line">                           macho_size,</span><br><span class="line">                           <span class="built_in">ubc_getsize</span>(vp),</span><br><span class="line">                           map,</span><br><span class="line">                           result);</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            <span class="comment">/*加载32位segment*/</span></span><br><span class="line">            case LC_SEGMENT:</span><br><span class="line">                <span class="keyword">if</span> (pass != <span class="number">1</span>)</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                ret = <span class="built_in">load_segment</span>(</span><br><span class="line">                           (struct segment_command *) lcp,</span><br><span class="line">                           pager,</span><br><span class="line">                           file_offset,</span><br><span class="line">                           macho_size,</span><br><span class="line">                           <span class="built_in">ubc_getsize</span>(vp),</span><br><span class="line">                           map,</span><br><span class="line">                           result);</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            <span class="comment">/*加载线程数据*/</span></span><br><span class="line">            case LC_THREAD:</span><br><span class="line">                <span class="keyword">if</span> (pass != <span class="number">2</span>)</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">                ret = <span class="built_in">load_thread</span>((struct thread_command *)lcp,</span><br><span class="line">                           thread,</span><br><span class="line">                           result);</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            <span class="comment">/*加载unix线程数据*/</span></span><br><span class="line">            case LC_UNIXTHREAD:</span><br><span class="line">                <span class="keyword">if</span> (pass != <span class="number">2</span>)</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                ret = <span class="built_in">load_unixthread</span>(</span><br><span class="line">                        (struct thread_command *) lcp,</span><br><span class="line">                         thread,</span><br><span class="line">                         result);</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            <span class="comment">/*加载动态加载器 程序需要的dyld的路径*/</span></span><br><span class="line">            case LC_LOAD_DYLINKER:</span><br><span class="line">                <span class="keyword">if</span> (pass != <span class="number">2</span>)</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                <span class="keyword">if</span> ((depth == <span class="number">1</span>) &amp;&amp; (dlp == <span class="number">0</span>)) &#123;</span><br><span class="line">                    dlp = (<span class="keyword">struct</span> <span class="type">dylinker_command</span> *)lcp;</span><br><span class="line">                    dlarchbits = (<span class="built_in">header</span>-&gt;cputype &amp; CPU_ARCH_MASK);</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    ret = LOAD_FAILURE;</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            <span class="comment">/*加载代码签名加载器*/</span></span><br><span class="line">            case LC_CODE_SIGNATURE:</span><br><span class="line">                <span class="comment">/* CODE SIGNING */</span></span><br><span class="line">                <span class="keyword">if</span> (pass != <span class="number">2</span>)</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                <span class="comment">/* pager -&gt; uip -&gt;</span></span><br><span class="line"><span class="comment">                   load signatures &amp; store in uip</span></span><br><span class="line"><span class="comment">                    set VM object "signed_pages"</span></span><br><span class="line"><span class="comment">                */</span></span><br><span class="line">                ret = <span class="built_in">load_code_signature</span>(</span><br><span class="line">                    (struct linkedit_data_command *) lcp,</span><br><span class="line">                    vp,</span><br><span class="line">                    file_offset,</span><br><span class="line">                    macho_size,</span><br><span class="line">                    <span class="built_in">header</span>-&gt;cputype,</span><br><span class="line">                    (depth == <span class="number">1</span>) ? result : <span class="built_in">NULL</span>);</span><br><span class="line">                <span class="keyword">if</span> (ret != LOAD_SUCCESS) &#123;</span><br><span class="line">                        <span class="built_in">printf</span>(<span class="string">"proc %d: load code signature error %d "</span></span><br><span class="line">                           <span class="string">"for file \"%s\"\n"</span>,</span><br><span class="line">                           p-&gt;p_pid, ret, vp-&gt;v_name);</span><br><span class="line">                    ret = LOAD_SUCCESS; <span class="comment">/* ignore error */</span></span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    got_code_signatures = TRUE;</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            default:</span><br><span class="line">                <span class="comment">/* Other commands are ignored by the kernel */</span></span><br><span class="line">                ret = LOAD_SUCCESS;</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (ret != LOAD_SUCCESS)</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (ret != LOAD_SUCCESS)</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//加载成功</span></span><br><span class="line">    <span class="keyword">if</span> (ret == LOAD_SUCCESS) &#123; </span><br><span class="line">        <span class="keyword">if</span> (! got_code_signatures) &#123;</span><br><span class="line">            <span class="keyword">struct</span> <span class="type">cs_blob</span> *blob;</span><br><span class="line">            <span class="comment">/* no embedded signatures: look for detached ones */</span></span><br><span class="line">            blob = <span class="built_in">ubc_cs_blob_get</span>(vp, <span class="number">-1</span>, file_offset);</span><br><span class="line">            <span class="keyword">if</span> (blob != <span class="built_in">NULL</span>) &#123;</span><br><span class="line">                <span class="comment">/* get flags to be applied to the process */</span></span><br><span class="line">                result-&gt;csflags |= blob-&gt;csb_flags;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">//加载动态链接器dlp为从上面获取到的动态dyliner的路径</span></span><br><span class="line">        <span class="keyword">if</span> (dlp != <span class="number">0</span>)</span><br><span class="line">            <span class="comment">//加载dylinker</span></span><br><span class="line">            ret = <span class="built_in">load_dylinker</span>(dlp, dlarchbits, map, thread, depth, result, abi64);</span><br><span class="line">            <span class="comment">//......</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//.....</span></span><br><span class="line">    <span class="keyword">return</span>(ret);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>在parse_machfile中会将Mach O文件中loadCommand部分的命令加载到内存执行。这些command中最重要的命令是<strong><strong>LC_SEGMENT/LC_SEGMENT_64</strong></strong> 以及 <strong><strong>LC_UNIXTHREAD/LC_MAIN</strong></strong>，<strong><strong>LC_SEGMENT/LC_SEGMENT_64</strong></strong>用于告诉加载器某些可执行的部分代码需要映射到指定的内存区域，<strong><strong>LC_UNIXTHREAD/LC_MAIN</strong></strong> 告诉加载器在可执行代码加载后的入口点，有了它动态加载器知道在加载结束后跳到哪个位置，这些入口点要么是main方法，要么是在编译时期编译器添加的启动代码。如果没有动态库，这些命令已经足够了，但是如果有依赖动态库，在将所有的segments映射到内存中的时候，加载器还需要处理可执行代码的全部依赖，这些在后面的时候会详细介绍。由于parse_machfile会在后续递归调用，所以这里用了一个depth来控制递归深度。</p><p>当depth等于1的时候：</p><p><figure class="highlight groovy"><table><tr><td class="code"><pre><span class="line"><span class="keyword">switch</span> (header-&gt;filetype) &#123;</span><br><span class="line"><span class="keyword">case</span> <span class="string">MH_OBJECT:</span></span><br><span class="line"><span class="keyword">case</span> <span class="string">MH_EXECUTE:</span></span><br><span class="line"><span class="keyword">case</span> <span class="string">MH_PRELOAD:</span></span><br><span class="line">    <span class="comment">//depth=1 时候这里会走通</span></span><br><span class="line">    <span class="keyword">if</span> (depth != <span class="number">1</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> (LOAD_FAILURE);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line"><span class="keyword">case</span> <span class="string">MH_FVMLIB:</span></span><br><span class="line"><span class="keyword">case</span> <span class="string">MH_DYLIB:</span></span><br><span class="line">    <span class="comment">//depth=1 时候这里会失败</span></span><br><span class="line">    <span class="keyword">if</span> (depth == <span class="number">1</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> (LOAD_FAILURE);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line"><span class="keyword">case</span> <span class="string">MH_DYLINKER:</span></span><br><span class="line">    <span class="comment">//depth=1 时候这里会失败</span></span><br><span class="line">    <span class="keyword">if</span> (depth != <span class="number">2</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> (LOAD_FAILURE);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line"><span class="string">default:</span></span><br><span class="line">    <span class="keyword">return</span> (LOAD_FAILURE);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>这时候如果Mach O 文件类型为 <strong><strong>MH_OBJECT</strong></strong>，<strong><strong>MH_EXECUTE</strong></strong>，<strong><strong>MH_PRELOAD</strong></strong> 就会走到下面的流程，下面的流程会先执行LC_SEGMENT_64，LC_SEGMENT将某些可执行的代码映射到指定的内存区域。然后再执行LC_THREAD，LC_UNIXTHREAD，LC_LOAD_DYLINKER，LC_CODE_SIGNATURE。这里最为关键的命令是LC_LOAD_DYLINKER，它会对dyld进行赋值。在最后会调用：</p><p><figure class="highlight ini"><table><tr><td class="code"><pre><span class="line"><span class="attr">ret</span> = load_dylinker(dlp, dlarchbits, map, thread, depth, result, abi64)<span class="comment">;</span></span><br></pre></td></tr></table></figure></p><p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span></span><br><span class="line"><span class="keyword">load_return_t</span></span><br><span class="line">load_dylinker(</span><br><span class="line">    struct dylinker_command *lcp,</span><br><span class="line">    <span class="keyword">integer_t</span>   archbits,</span><br><span class="line">    <span class="keyword">vm_map_t</span>    <span class="built_in">map</span>,</span><br><span class="line">    <span class="keyword">thread_t</span>    thread,</span><br><span class="line">    <span class="keyword">int</span>         depth,</span><br><span class="line">    <span class="keyword">load_result_t</span>   *result,</span><br><span class="line">    <span class="keyword">boolean_t</span>       is_64bit</span><br><span class="line">)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">//..........</span></span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">     * 首先直接映射dyld </span></span><br><span class="line"><span class="comment">     *  First try to map dyld in directly.  This should work most of</span></span><br><span class="line"><span class="comment">     *  the time since there shouldn't normally be something already</span></span><br><span class="line"><span class="comment">     *  mapped to its address.</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="comment">// 解析dyld</span></span><br><span class="line">    ret = parse_machfile(vp, <span class="built_in">map</span>, thread, &amp;header, file_offset, macho_size, depth, &amp;myresult);</span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">     *  If it turned out something was in the way, then we'll take</span></span><br><span class="line"><span class="comment">     *  take this longer path to map dyld into a temporary map and</span></span><br><span class="line"><span class="comment">     *  copy it into destination map at a different address.</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="comment">//如果加载成功设置会返回entry_point</span></span><br><span class="line">    <span class="keyword">if</span> (ret == LOAD_SUCCESS) &#123;</span><br><span class="line">        result-&gt;dynlinker = TRUE;</span><br><span class="line">        result-&gt;entry_point = myresult.entry_point;</span><br><span class="line">        (<span class="keyword">void</span>)ubc_map(vp, PROT_READ | PROT_EXEC);</span><br><span class="line">    &#125;</span><br><span class="line">out:</span><br><span class="line">    vnode_put(vp);</span><br><span class="line">    <span class="keyword">return</span> (ret);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>load_dylinker函数主要负责加载dyld,以及调用parse_machfile函数对dyld解析。这时候header-&gt;filetype = MH_DYLINKER 并且 depth = 2.</p><p><figure class="highlight groovy"><table><tr><td class="code"><pre><span class="line"><span class="keyword">switch</span> (header-&gt;filetype) &#123;</span><br><span class="line"><span class="keyword">case</span> <span class="string">MH_OBJECT:</span></span><br><span class="line"><span class="keyword">case</span> <span class="string">MH_EXECUTE:</span></span><br><span class="line"><span class="keyword">case</span> <span class="string">MH_PRELOAD:</span></span><br><span class="line">    <span class="comment">//depth=2 时候这里走不通</span></span><br><span class="line">    <span class="keyword">if</span> (depth != <span class="number">1</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> (LOAD_FAILURE);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line"><span class="keyword">case</span> <span class="string">MH_FVMLIB:</span></span><br><span class="line"><span class="keyword">case</span> <span class="string">MH_DYLIB:</span></span><br><span class="line">    <span class="comment">//depth=2 时候这里会走通</span></span><br><span class="line">    <span class="keyword">if</span> (depth == <span class="number">1</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> (LOAD_FAILURE);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line"><span class="keyword">case</span> <span class="string">MH_DYLINKER:</span></span><br><span class="line">    <span class="comment">//depth=2 时候这里会走通</span></span><br><span class="line">    <span class="keyword">if</span> (depth != <span class="number">2</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> (LOAD_FAILURE);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line"><span class="string">default:</span></span><br><span class="line">    <span class="keyword">return</span> (LOAD_FAILURE);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>下面的流程会先执行LC_SEGMENT_64，LC_SEGMENT将某些可执行的代码映射到指定的内存区域。然后再执行LC_THREAD，LC_UNIXTHREAD，LC_CODE_SIGNATURE。这时候dyld就被加载进来了，在LC_UNIXTHREAD中会设置dyld的entry point。</p><p>我们来看下LC_UNIXTHREAD</p><p><figure class="highlight crystal"><table><tr><td class="code"><pre><span class="line"><span class="keyword">case</span> <span class="symbol">LC_UNIXTHREAD:</span></span><br><span class="line">    <span class="keyword">if</span> (pass != <span class="number">2</span>)</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">    ret = load_unixthread(</span><br><span class="line">                (<span class="class"><span class="keyword">struct</span> <span class="title">thread_command</span> *) <span class="title">lcp</span>,</span></span><br><span class="line">                thread,</span><br><span class="line">                result);</span><br><span class="line">    <span class="keyword">break</span>;</span><br></pre></td></tr></table></figure></p><p><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span></span><br><span class="line">load_return_t</span><br><span class="line">load_thread(</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">thread_command</span></span>*tcp,</span><br><span class="line">thread_tthread,</span><br><span class="line">load_result_t*result</span><br><span class="line">)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">//.....</span></span><br><span class="line">task = get_threadtask(thread);</span><br><span class="line">    <span class="comment">//.....</span></span><br><span class="line"></span><br><span class="line">lret = load_threadstate(thread,</span><br><span class="line">       (unsigned long *)(((vm_offset_t)tcp) + </span><br><span class="line">       sizeof(<span class="class"><span class="keyword">struct</span> <span class="title">thread_command</span></span>)),</span><br><span class="line">       tcp-&gt;cmdsize - sizeof(<span class="class"><span class="keyword">struct</span> <span class="title">thread_command</span></span>));</span><br><span class="line"><span class="keyword">if</span> (lret != LOAD_SUCCESS)</span><br><span class="line"><span class="keyword">return</span> (lret);</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (result-&gt;thread_count == <span class="number">0</span>) &#123;</span><br><span class="line">lret = load_threadstack(thread,</span><br><span class="line">(unsigned long *)(((vm_offset_t)tcp) + </span><br><span class="line">sizeof(<span class="class"><span class="keyword">struct</span> <span class="title">thread_command</span></span>)),</span><br><span class="line">tcp-&gt;cmdsize - sizeof(<span class="class"><span class="keyword">struct</span> <span class="title">thread_command</span></span>),</span><br><span class="line">&amp;result-&gt;user_stack,</span><br><span class="line">&amp;customstack);</span><br><span class="line"><span class="keyword">if</span> (customstack)</span><br><span class="line">result-&gt;customstack = <span class="number">1</span>;</span><br><span class="line"><span class="keyword">else</span></span><br><span class="line">result-&gt;customstack = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (lret != LOAD_SUCCESS)</span><br><span class="line"><span class="keyword">return</span>(lret);</span><br><span class="line"></span><br><span class="line">lret = load_threadentry(thread,</span><br><span class="line">(unsigned long *)(((vm_offset_t)tcp) + </span><br><span class="line">sizeof(<span class="class"><span class="keyword">struct</span> <span class="title">thread_command</span></span>)),</span><br><span class="line">tcp-&gt;cmdsize - sizeof(<span class="class"><span class="keyword">struct</span> <span class="title">thread_command</span></span>),</span><br><span class="line">&amp;result-&gt;entry_point);</span><br><span class="line"><span class="keyword">if</span> (lret != LOAD_SUCCESS)</span><br><span class="line"><span class="keyword">return</span>(lret);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span></span><br><span class="line">thread_resume(thread);</span><br><span class="line"></span><br><span class="line">result-&gt;thread_count++;</span><br><span class="line"></span><br><span class="line"><span class="keyword">return</span>(LOAD_SUCCESS);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>这里最关键的部分是<strong><strong>load_threadentry</strong></strong></p><p><figure class="highlight arduino"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span></span><br><span class="line"><span class="keyword">load_return_t</span></span><br><span class="line">load_threadentry(</span><br><span class="line"><span class="keyword">thread_t</span>thread,</span><br><span class="line"><span class="keyword">unsigned</span> <span class="keyword">long</span>*ts,</span><br><span class="line"><span class="keyword">unsigned</span> <span class="keyword">long</span>total_size,</span><br><span class="line"><span class="keyword">mach_vm_offset_t</span>*entry_point</span><br><span class="line">)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">//......</span></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> *Set the thread state.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line">*entry_point = MACH_VM_MIN_ADDRESS;</span><br><span class="line"><span class="keyword">while</span> (total_size &gt; <span class="number">0</span>) &#123;</span><br><span class="line">flavor = *ts++;</span><br><span class="line"><span class="built_in">size</span> = *ts++;</span><br><span class="line">entry_size = (<span class="built_in">size</span>+<span class="number">2</span>)*<span class="keyword">sizeof</span>(<span class="keyword">unsigned</span> <span class="keyword">long</span>);</span><br><span class="line"><span class="keyword">if</span> (entry_size &gt; total_size)</span><br><span class="line"><span class="keyword">return</span>(LOAD_BADMACHO);</span><br><span class="line">total_size -= entry_size;</span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * Third argument is a kernel space pointer; it gets cast</span></span><br><span class="line"><span class="comment"> * to the appropriate type in thread_entrypoint() based on</span></span><br><span class="line"><span class="comment"> * the value of flavor.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line">ret = thread_entrypoint(thread, flavor, (<span class="keyword">thread_state_t</span>)ts, <span class="built_in">size</span>, entry_point);</span><br><span class="line"><span class="keyword">if</span> (ret != KERN_SUCCESS) &#123;</span><br><span class="line"><span class="keyword">return</span>(LOAD_FAILURE);</span><br><span class="line">&#125;</span><br><span class="line">ts += <span class="built_in">size</span>;<span class="comment">/* ts is a (unsigned long *) */</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">return</span>(LOAD_SUCCESS);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">kern_return_t</span></span><br><span class="line">thread_entrypoint(</span><br><span class="line">    __unused <span class="keyword">thread_t</span>   thread,</span><br><span class="line">    <span class="keyword">int</span>                 flavor,</span><br><span class="line">    <span class="keyword">thread_state_t</span>      tstate,</span><br><span class="line">    __unused <span class="keyword">unsigned</span> <span class="keyword">int</span>        count,</span><br><span class="line">    <span class="keyword">mach_vm_offset_t</span>    *entry_point</span><br><span class="line">)</span><br><span class="line">&#123; </span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * Set a default.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">if</span> (*entry_point == <span class="number">0</span>)</span><br><span class="line">*entry_point = VM_MIN_ADDRESS;</span><br><span class="line"></span><br><span class="line"><span class="keyword">switch</span> (flavor) &#123;</span><br><span class="line"><span class="keyword">case</span> x86_THREAD_STATE32:</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">x86_thread_state32_t</span> *state25;</span><br><span class="line"></span><br><span class="line">state25 = (<span class="keyword">i386_thread_state_t</span> *) tstate;</span><br><span class="line">*entry_point = state25-&gt;eip ? state25-&gt;eip: VM_MIN_ADDRESS;</span><br><span class="line"><span class="keyword">break</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">case</span> x86_THREAD_STATE64:</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">x86_thread_state64_t</span> *state25;</span><br><span class="line"></span><br><span class="line">state25 = (<span class="keyword">x86_thread_state64_t</span> *) tstate;</span><br><span class="line">*entry_point = state25-&gt;rip ? state25-&gt;rip: VM_MIN_ADDRESS64;</span><br><span class="line"><span class="keyword">break</span>;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">return</span> (KERN_SUCCESS);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>thread_entrypoint 中会对entry_point进行设置。那么state25-&gt;rip又是什么呢？它就是dyld的入口地址****_dyld_start****。也就是说在dyld加载到内存结束后，会将入口指向****_dyld_start****。如果想对dyld有比较直观的了解，其实在deviceSupport/xx.x.x/Symbols/usr/lib/dyld.到此为止就是从launchd开始到dyld加载结束到指定dyld的入口地址的全部过程。下篇博客将会对dyld进行详细的介绍。</p><ul><li><a href="https://justinyan.me/post/4057" target="_blank" rel="noopener">macOS 内核之一个 App 如何运行起来</a></li><li><a href="https://stackoverflow.com/questions/39863112/what-is-required-for-a-mach-o-executable-to-load" target="_blank" rel="noopener">What is required for a Mach-O executable to load?</a></li><li><a href="https://zhangbuhuai.com/post/dyld-bootstrap.html" target="_blank" rel="noopener">分析 dyld 的启动过程</a></li><li><a href="https://blog.cnbluebox.com/blog/2017/06/30/dyld2/" target="_blank" rel="noopener">Dyld系列之一：_dyld_start之前</a></li><li><a href="https://github.com/apple/darwin-xnu" target="_blank" rel="noopener">darwin-xnu</a></li><li><a href="http://darlinghq.org/developer-zone/mach-o-dynamic-loader/" target="_blank" rel="noopener">Darling</a></li><li><a href="https://mp.weixin.qq.com/s/I60p2M-IHDmeUanDUkFdVw" target="_blank" rel="noopener">XNU、dyld源码分析Mach-O和动态库的加载过程(上)</a></li><li><a href="https://mp.weixin.qq.com/s/fdDPyjRkVf9AdWiikBagHg" target="_blank" rel="noopener">XNU、dyld 源码分析，Mach-O 和动态库的加载过程 (下)</a></li><li><a href="https://juejin.im/post/5a0c5c3451882554bd509a46" target="_blank" rel="noopener">趣探 Mach-O：加载过程</a></li></ul>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;上一篇博客介绍了Mach O文件的格式，以及每个部分的作用，这一部分开始将要介绍的是在上一个基础上如何将Mach O文件加载并运行起来。我们会从点击执行应用开始，到加载dyld，初始化dyld，再到dyld将镜像加载到内存，Rebase/Bind，Runtime初始化，其
      
    
    </summary>
    
      <category term="Objective C 相关" scheme="http://yoursite.com/categories/Objective-C-%E7%9B%B8%E5%85%B3/"/>
    
    
      <category term="Objective C 相关" scheme="http://yoursite.com/tags/Objective-C-%E7%9B%B8%E5%85%B3/"/>
    
  </entry>
  
  <entry>
    <title>iOS Runtime源码解析Mach O 结构</title>
    <link href="http://yoursite.com/2020/01/08/iOS-Runtime%E6%BA%90%E7%A0%81%E8%A7%A3%E6%9E%90%E4%B9%8BMach-O-%E7%BB%93%E6%9E%84/"/>
    <id>http://yoursite.com/2020/01/08/iOS-Runtime源码解析之Mach-O-结构/</id>
    <published>2020-01-08T13:51:12.000Z</published>
    <updated>2020-02-07T10:30:08.000Z</updated>
    
    <content type="html"><![CDATA[<p>我们编译出的.app 文件其实也是Mach O的一种，我们在对Runtime进行分析之前总是绕不开Mach O 和 dyld 这两大块内容，所以在这个系列开始之前对带大家了解下Mach O 和 dyld。</p><p>下图是Mach O文件的组成，主要分成三大部分,我们接下来以otool命令行工具来输出这些数据的内容：</p><p><strong><strong>Header</strong></strong>: 头部区域；<strong><strong>LoadCommands</strong></strong>: 加载命令，由多个Segments command组成；<strong><strong>Data</strong></strong>:Segments的具体数据；</p><p>我们一一看下这些部分的组成数据结构以及功能：</p><h5>Header</h5><p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">mach_header_64</span> &#123;</span></span><br><span class="line"><span class="keyword">uint32_t</span>magic;<span class="comment">/* mach magic number identifier */</span></span><br><span class="line"><span class="keyword">cpu_type_t</span>cputype;<span class="comment">/* cpu specifier */</span></span><br><span class="line"><span class="keyword">cpu_subtype_t</span>cpusubtype;<span class="comment">/* machine specifier */</span></span><br><span class="line"><span class="keyword">uint32_t</span>filetype;<span class="comment">/* type of file */</span></span><br><span class="line"><span class="keyword">uint32_t</span>ncmds;<span class="comment">/* number of load commands */</span></span><br><span class="line"><span class="keyword">uint32_t</span>sizeofcmds;<span class="comment">/* the size of all the load commands */</span></span><br><span class="line"><span class="keyword">uint32_t</span>flags;<span class="comment">/* flags */</span></span><br><span class="line"><span class="keyword">uint32_t</span>reserved;<span class="comment">/* reserved */</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p><ul><li>magic: 从这个字段上可以看出当前镜像的平台是64位还是32位的：由于这里只研究64位的，所以这个值为<strong><strong>MH_MAGIC_64</strong></strong></li><li>cputype: cpu类型          （具体可查看<strong><strong>mach/machine.h</strong></strong>）</li><li>cpusubtype: cpu子类型     （具体可查看<strong><strong>mach/machine.h</strong></strong>）</li><li>filetype : 文件类型，是可执行文件还是动态链接文件</li><li>ncmds : load command 的数量</li><li>sizeofcmds : load command 占用的空间大小</li><li>flags : dyld 加载的标志</li></ul><p>下面是otool工具打印出的某个应用的Header部分：</p><p><figure class="highlight lsl"><table><tr><td class="code"><pre><span class="line">$ otool -hV IDLFundation</span><br><span class="line">Mach header</span><br><span class="line">      magic cputype cpusubtype  caps    filetype ncmds sizeofcmds      flags</span><br><span class="line">MH_MAGIC_64  X86_64        ALL  <span class="number">0x00</span>     EXECUTE    <span class="number">85</span>       <span class="number">8400</span>   NOUNDEFS DYLDLINK TWOLEVEL WEAK_DEFINES BINDS_TO_WEAK PIE</span><br></pre></td></tr></table></figure></p><p>这里简单提下几种常见的Mach-O类型，也就是上面提到的filetype：</p><ul><li><p><strong><strong>MH_EXECUTE</strong></strong>可单独执行的可执行文件，这种文件一般有main方法作为入口。</p></li><li><p><strong><strong>MH_DYLINKER</strong></strong>动态链接器，用于加载动态库 在iOS 中就是/usr/lib/dyld，/usr/lib/dyld仅会处理MH_BUNDLE, MH_DYLIB，MH_EXECUTE类型文件。</p></li><li><p><strong><strong>MH_OBJECT</strong></strong>.m，.c等文件编译出来的目标文件，文件后缀是.o。Static Library类型产出是MH_OBJECT类型文件的archieve。</p></li><li><p><strong><strong>MH_FVMLIB</strong></strong> <strong><strong>MH_CORE</strong></strong> <strong><strong>MH_PRELOAD</strong></strong> <strong><strong>MH_DYLIB</strong></strong>动态库文件，包括.dylib文件，动态framework；对应Dynamic Library类型产出。</p></li><li><p><strong><strong>MH_BUNDLE</strong></strong>独立的二进制文件，不支持在项目中添加Link Binary使用。可以在Copy Bundle Resources中作为资源添加。 通过NSBundle load的方式加载；对应Bundle类型产出。典型的例子就是/System/Library/AccessibilityBundles目录的.axbundle后缀的文件。</p></li><li><p><strong><strong>MH_DSYM</strong></strong>存储二进制文件符号信息的文件，用于Debug分析；</p></li></ul><h5>Load Commands</h5><p>load commands 紧随着mach_header，它的总大小以及命令数都存储在mach_header中。<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">load_command</span> &#123;</span></span><br><span class="line">    <span class="keyword">uint32_t</span> cmd;       <span class="comment">/* type of load command */</span></span><br><span class="line">    <span class="keyword">uint32_t</span> cmdsize;   <span class="comment">/* total size of command in bytes */</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p><p>load_command结构比较简单就两个字段cmd和cmdsize，cmd存放的是命令的类型，每个类型有一个专门的结构体。</p><p>这些加载命令在Mach-O文件加载解析时，用于指导如何加载对应的二进制数据段，下面列出了一些加载命令类型的常量，更多的加载命令类型可以查看<strong><strong>loader.h</strong></strong></p><p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span>LC_SEGMENT     0x1<span class="comment">/* segment of this file to be mapped */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span>LC_SEGMENT_640x19<span class="comment">/* 64-bit segment of this file to be mapped */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span>LC_SYMTAB0x2<span class="comment">/* link-edit stab symbol table info */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span>LC_SYMSEG0x3<span class="comment">/* link-edit gdb symbol table info (obsolete) */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span>LC_THREAD0x4<span class="comment">/* thread */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span>LC_UNIXTHREAD0x5<span class="comment">/* unix thread (includes a stack) */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span>LC_LOADFVMLIB0x6<span class="comment">/* load a specified fixed VM shared library */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span>LC_IDFVMLIB0x7<span class="comment">/* fixed VM shared library identification */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span>LC_IDENT0x8<span class="comment">/* object identification info (obsolete) */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LC_FVMFILE0x9<span class="comment">/* fixed VM file inclusion (internal use) */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LC_PREPAGE      0xa     <span class="comment">/* prepage command (internal use) */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span>LC_DYSYMTAB0xb<span class="comment">/* dynamic link-edit symbol table info */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span>LC_LOAD_DYLIB0xc<span class="comment">/* load a dynamically linked shared library */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span>LC_ID_DYLIB0xd<span class="comment">/* dynamically linked shared lib ident */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LC_LOAD_DYLINKER 0xe<span class="comment">/* load a dynamic linker */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LC_ID_DYLINKER0xf<span class="comment">/* dynamic linker identification */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span>LC_PREBOUND_DYLIB 0x10<span class="comment">/* modules prebound for a dynamically */</span></span></span><br><span class="line"><span class="comment">/*  linked shared library */</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span>LC_ROUTINES0x11<span class="comment">/* image routines */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span>LC_SUB_FRAMEWORK 0x12<span class="comment">/* sub framework */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span>LC_SUB_UMBRELLA 0x13<span class="comment">/* sub umbrella */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span>LC_SUB_CLIENT0x14<span class="comment">/* sub client */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span>LC_SUB_LIBRARY  0x15<span class="comment">/* sub library */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span>LC_TWOLEVEL_HINTS 0x16<span class="comment">/* two-level namespace lookup hints */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span>LC_PREBIND_CKSUM  0x17<span class="comment">/* prebind checksum */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span>LC_LOAD_WEAK_DYLIB (0x18 | LC_REQ_DYLD)</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span>LC_ROUTINES_640x1a<span class="comment">/* 64-bit image routines */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LC_UUID0x1b<span class="comment">/* the uuid */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LC_RPATH       (0x1c | LC_REQ_DYLD)    <span class="comment">/* runpath additions */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LC_CODE_SIGNATURE 0x1d<span class="comment">/* local of code signature */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LC_SEGMENT_SPLIT_INFO 0x1e <span class="comment">/* local of info to split segments */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LC_REEXPORT_DYLIB (0x1f | LC_REQ_DYLD) <span class="comment">/* load and re-export dylib */</span></span></span><br></pre></td></tr></table></figure></p><p>下面是通过otool命令来输出的load commands：</p><p><figure class="highlight ebnf"><table><tr><td class="code"><pre><span class="line"><span class="attribute">otool -l IDLFundation</span></span><br></pre></td></tr></table></figure></p><p><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="string">Load</span> <span class="string">command</span> <span class="number">20</span></span><br><span class="line">          <span class="string">cmd</span> <span class="string">LC_LOAD_DYLIB</span></span><br><span class="line">      <span class="string">cmdsize</span> <span class="number">80</span></span><br><span class="line">         <span class="string">name</span> <span class="string">@rpath/FLAnimatedImage.framework/FLAnimatedImage</span> <span class="string">(offset</span> <span class="number">24</span><span class="string">)</span></span><br><span class="line">   <span class="string">time</span> <span class="string">stamp</span> <span class="number">2</span> <span class="string">Thu</span> <span class="string">Jan</span>  <span class="number">1</span> <span class="number">08</span><span class="string">:00:02</span> <span class="number">1970</span></span><br><span class="line">      <span class="string">current</span> <span class="string">version</span> <span class="number">1.0</span><span class="number">.0</span></span><br><span class="line"><span class="string">compatibility</span> <span class="string">version</span> <span class="number">1.0</span><span class="number">.0</span></span><br></pre></td></tr></table></figure></p><p>这里比较重要的是加载segments的命令：</p><p>它用于表示当前文件的一部分将会被映射到64位任务的地址空间</p><p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">segment_command_64</span> &#123;</span> <span class="comment">/* for 64-bit architectures */</span></span><br><span class="line"><span class="keyword">uint32_t</span>cmd;    <span class="comment">/* 对于64位系统来说这个值恒为 LC_SEGMENT_64 */</span></span><br><span class="line"><span class="keyword">uint32_t</span>cmdsize;    <span class="comment">/* 用于表示这个命令的大小 */</span></span><br><span class="line"><span class="keyword">char</span>segname[<span class="number">16</span>];<span class="comment">/* 该segment的名字 */</span></span><br><span class="line"><span class="keyword">uint64_t</span>vmaddr;    <span class="comment">/* 这个segment的虚拟内存地址空间 */</span></span><br><span class="line"><span class="keyword">uint64_t</span>vmsize;    <span class="comment">/* 这个segment的虚拟内存大小 */</span></span><br><span class="line"><span class="keyword">uint64_t</span>fileoff;    <span class="comment">/* 这个segment位于这个文件的偏移 */</span></span><br><span class="line"><span class="keyword">uint64_t</span>filesize;    <span class="comment">/* 文件的大小 */</span></span><br><span class="line"><span class="keyword">vm_prot_t</span>maxprot;    <span class="comment">/* 最大虚拟内存保护 */</span></span><br><span class="line"><span class="keyword">vm_prot_t</span>initprot;    <span class="comment">/* 初始的内存保护 */</span></span><br><span class="line"><span class="keyword">uint32_t</span>nsects;    <span class="comment">/* segments的session数 */</span></span><br><span class="line"><span class="keyword">uint32_t</span>flags;    <span class="comment">/* flags */</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p><p>通过这个命令会将该段对应的文件从offset处加载 file size大小到虚拟内存 vmaddr处。</p><h5>Segments &amp; Session</h5><p>Mach O文件中有一系列的Segment，每个segment又是由一系列的session构成。我们看下Mach O文件中都有哪些Segment:</p><ul><li><strong><strong>__TEXT</strong></strong> :代码段/只读数据段</li><li><strong><strong>__DATA</strong></strong> :数据段</li><li><strong><strong>__PAGEZERO</strong></strong>: 空指针陷阱段，用于捕获对NULL指针的引用</li><li><strong><strong>__LINKEDIT</strong></strong>: 动态链接库使用的原始数据，比如符号，字符串，重定位表条目等等， 该段可读写。</li><li><strong><strong>__OBJC</strong></strong>: 包括会被Runtime使用到的一些数据</li></ul><p>可以用如下命令打印出各个段的数据</p><p><figure class="highlight sqf"><table><tr><td class="code"><pre><span class="line">otool -v -s <span class="variable">__DATA</span> <span class="variable">__objc_selrefs</span> IDLFundation</span><br></pre></td></tr></table></figure></p><p>下面是****__TEXT<strong><strong>以及</strong></strong>__DATA****segment下的重要section：</p><p><strong><strong>__TEXT 部分</strong></strong></p><p><figure class="highlight sqf"><table><tr><td class="code"><pre><span class="line"><span class="variable">__text</span>程序代码</span><br><span class="line"><span class="variable">__stubs</span>用于动态链接的存根</span><br><span class="line"><span class="variable">__stub_helper</span>用于动态链接的存根</span><br><span class="line"><span class="variable">__const</span>程序中const关键字修饰的常量变量</span><br><span class="line"><span class="variable">__objc_methname</span>objc方法名</span><br><span class="line"><span class="variable">__cstring</span>程序中硬编码的ANSI的字符串</span><br><span class="line"><span class="variable">__objc_classname</span>objc类名</span><br><span class="line"><span class="variable">__objc_methtype</span>objc方法类型</span><br><span class="line"><span class="variable">__gcc_except_tab</span>异常处理相关</span><br><span class="line"><span class="variable">__ustring</span>unicode字符串</span><br><span class="line"><span class="variable">__unwind_info</span>异常处理</span><br><span class="line"><span class="variable">__eh_frame</span>异常处理</span><br></pre></td></tr></table></figure></p><p><strong><strong>__DATA 部分</strong></strong></p><p><figure class="highlight sqf"><table><tr><td class="code"><pre><span class="line"><span class="variable">__nl_symbol_ptr</span>动态符号链接相关,指针数组</span><br><span class="line"><span class="variable">__got</span>全局偏移表, Global Offset Table</span><br><span class="line"><span class="variable">__la_symbol_ptr</span>动态符号链接相关，也是指针数组，通过dyld_stub_binder辅助链接</span><br><span class="line"><span class="variable">__mod_init_func</span>初始化的全局函数地址，会在main之前被调用</span><br><span class="line"><span class="variable">__const</span>const修饰的常量</span><br><span class="line"><span class="variable">__cstring</span>程序中硬编码的ANSI的字符串</span><br><span class="line"><span class="variable">__cfstring</span>CF用到的字符串</span><br><span class="line"><span class="variable">__objc_classlist</span>objc类列表</span><br><span class="line"><span class="variable">__objc_nlclslist</span>objcload方法列表</span><br><span class="line"><span class="variable">__objc_catlist</span>objc category列表</span><br><span class="line"><span class="variable">__objc_protolist</span>objc protocol列表</span><br><span class="line"><span class="variable">__objc_imageinfo</span>镜像信息</span><br><span class="line"><span class="variable">__objc_const</span>objc的常量</span><br><span class="line"><span class="variable">__objc_selrefs</span>objc引用的SEL列表</span><br><span class="line"><span class="variable">__objc_protorefs</span>objc引用的protocol列表</span><br><span class="line"><span class="variable">__objc_classrefs</span>objc引用的class列表</span><br><span class="line"><span class="variable">__objc_superrefs</span>objc父类的引用列表</span><br><span class="line"><span class="variable">__objc_ivar</span>objcivar信息</span><br><span class="line"><span class="variable">__objc_data</span>class信息</span><br><span class="line"><span class="variable">__bss</span>未初始化的静态变量区</span><br><span class="line"><span class="variable">__data</span>初始化的可变变量</span><br></pre></td></tr></table></figure></p><p>关于Mach O结构部分先介绍到这，在博客的末尾简单列下：</p><p><figure class="highlight routeros"><table><tr><td class="code"><pre><span class="line">-f <span class="builtin-name">print</span> the fat headers</span><br><span class="line">-a <span class="builtin-name">print</span> the archive header</span><br><span class="line">-h <span class="builtin-name">print</span> the mach header</span><br><span class="line">-l <span class="builtin-name">print</span> the load commands</span><br><span class="line">-L <span class="builtin-name">print</span> shared libraries used</span><br><span class="line">-D <span class="builtin-name">print</span> shared library id name</span><br><span class="line">-t <span class="builtin-name">print</span> the text section (disassemble with -v)</span><br><span class="line">-x <span class="builtin-name">print</span> all text sections (disassemble with -v)</span><br><span class="line">-p &lt;routine name&gt;  start dissassemble <span class="keyword">from</span> routine name</span><br><span class="line">-s &lt;segname&gt; &lt;sectname&gt; <span class="builtin-name">print</span> contents of section</span><br><span class="line">-d <span class="builtin-name">print</span> the data section</span><br><span class="line">-o <span class="builtin-name">print</span> the Objective-C segment</span><br><span class="line">-r <span class="builtin-name">print</span> the relocation entries</span><br><span class="line">-S <span class="builtin-name">print</span> the table of contents of a library (obsolete)</span><br><span class="line">-T <span class="builtin-name">print</span> the table of contents of a dynamic shared library (obsolete)</span><br><span class="line">-M <span class="builtin-name">print</span> the module table of a dynamic shared library (obsolete)</span><br><span class="line">-R <span class="builtin-name">print</span> the reference table of a dynamic shared library (obsolete)</span><br><span class="line">-I <span class="builtin-name">print</span> the indirect symbol table</span><br><span class="line">-H <span class="builtin-name">print</span> the two-level hints table (obsolete)</span><br><span class="line">-G <span class="builtin-name">print</span> the data <span class="keyword">in</span> code table</span><br><span class="line">-v <span class="builtin-name">print</span> verbosely (symbolically) when possible</span><br><span class="line">-V <span class="builtin-name">print</span> disassembled operands symbolically</span><br><span class="line">-c <span class="builtin-name">print</span> argument strings of a core file</span><br><span class="line">-X <span class="builtin-name">print</span> <span class="literal">no</span> leading addresses <span class="keyword">or</span> headers</span><br><span class="line">-m don<span class="string">'t use archive(member) syntax</span></span><br><span class="line"><span class="string">-B force Thumb disassembly (ARM objects only)</span></span><br><span class="line"><span class="string">-q use llvm'</span>s disassembler (the default)</span><br><span class="line">-Q use otool(1)<span class="string">'s disassembler</span></span><br><span class="line"><span class="string">-mcpu=arg use `arg'</span> as the cpu <span class="keyword">for</span> disassembly</span><br><span class="line">-j <span class="builtin-name">print</span> opcode bytes</span><br><span class="line">-P <span class="builtin-name">print</span> the <span class="builtin-name">info</span> plist section as strings</span><br><span class="line">-C <span class="builtin-name">print</span> linker optimization hints</span><br><span class="line">--version <span class="builtin-name">print</span> the version of /Applications/Xcode.app/Contents/Developer/Toolchains/XcodeDefault.xctoolchain/usr/bin/otool</span><br></pre></td></tr></table></figure></p><p>大家可以通过任何途径获取到你想研究应用到ipa包，然后把后缀名改为zip，解压缩得到Payload文件夹，里面就是你的APP，打开终端，直接cd到你的xxxx.app目录下。具体做法，输入cd，然后把xxxx.app直接拖到终端里打个回车。然后就可以通过otool命令来进行研究它了。</p><p>还有一个otool的扩展版本<a href="http://www.newosxbook.com/tools/jtool.html" target="_blank" rel="noopener">jtool</a>大家有兴趣可以看下</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;我们编译出的.app 文件其实也是Mach O的一种，我们在对Runtime进行分析之前总是绕不开Mach O 和 dyld 这两大块内容，所以在这个系列开始之前对带大家了解下Mach O 和 dyld。&lt;/p&gt;
&lt;p&gt;下图是Mach O文件的组成，主要分成三大部分,我们接
      
    
    </summary>
    
      <category term="Objective C 相关" scheme="http://yoursite.com/categories/Objective-C-%E7%9B%B8%E5%85%B3/"/>
    
    
      <category term="Objective C 相关" scheme="http://yoursite.com/tags/Objective-C-%E7%9B%B8%E5%85%B3/"/>
    
  </entry>
  
  <entry>
    <title>iOS Category 源码解析</title>
    <link href="http://yoursite.com/2020/01/06/iOS-Category-%E6%BA%90%E7%A0%81%E8%A7%A3%E6%9E%90/"/>
    <id>http://yoursite.com/2020/01/06/iOS-Category-源码解析/</id>
    <published>2020-01-06T15:21:05.000Z</published>
    <updated>2020-01-06T17:38:46.000Z</updated>
    
    <content type="html"><![CDATA[<p>在之前的博客《iOS 分类扩展与协议总结》中介绍了分类的用法以及性质，该博客将会从分类的源码对分类进一步分析。</p><p>首先我们先看下分类的数据结构：</p><p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">category_t</span> &#123;</span></span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">char</span> *name;                           <span class="comment">//类的名字</span></span><br><span class="line">    <span class="keyword">classref_t</span> cls;                             <span class="comment">//类</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">method_list_t</span> *<span class="title">instanceMethods</span>;</span>      <span class="comment">//catogies 中所有给类添加的实例方法列表</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">method_list_t</span> *<span class="title">classMethods</span>;</span>         <span class="comment">//catogies 中所有给类添加的类方法列表</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">protocol_list_t</span> *<span class="title">protocols</span>;</span>          <span class="comment">//catogies 中所有给类添加的协议列表</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">property_list_t</span> *<span class="title">instanceProperties</span>;</span> <span class="comment">//catogies 中所有给类添加的属性</span></span><br><span class="line">    <span class="comment">// Fields below this point are not always present on disk.</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">property_list_t</span> *_<span class="title">classProperties</span>;</span>   <span class="comment">//catogies 中所有给类添加的类属性</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">method_list_t</span> *<span class="title">methodsForMeta</span><span class="params">(<span class="keyword">bool</span> isMeta)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (isMeta) <span class="keyword">return</span> classMethods;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">return</span> instanceMethods;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">property_list_t</span> *<span class="title">propertiesForMeta</span><span class="params">(<span class="keyword">bool</span> isMeta, struct header_info *hi)</span></span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p><p>从上述对象中可以看出通过category可以添加实例方法，类方法，协议，属性，但是不能为对象添加实例变量。因为category是在运行时添加到类中的，这时候类的内存布局已经确定，如果添加实例变量就会破坏类的内部布局。</p><p>接下来我们就从runtime最开始介绍分类是如何添加到类中的。<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">void</span> _objc_init(<span class="keyword">void</span>)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">bool</span> initialized = <span class="literal">false</span>;</span><br><span class="line">    <span class="keyword">if</span> (initialized) <span class="keyword">return</span>;</span><br><span class="line">    initialized = <span class="literal">true</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// fixme defer initialization until an objc-using image is found?</span></span><br><span class="line">    environ_init();</span><br><span class="line">    tls_init();</span><br><span class="line">    static_init();</span><br><span class="line">    lock_init();</span><br><span class="line">    exception_init();</span><br><span class="line"></span><br><span class="line">    <span class="comment">//向dyld注册了回调函数,当image map到内存中,当初始化完成image时和卸载image的时候都会回调注册者</span></span><br><span class="line">    _dyld_objc_notify_register(&amp;map_images, load_images, unmap_image);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>在_objc_init方法中会向dyld注册三个十分关键的回调函数：map_images，load_images，unmap_image</p><p>在镜像被加载后会回调map_images，分类的添加就是在这个阶段完成的，这以后会调用<strong><strong>map_images_nolock</strong></strong>方法来处理被dyld映射进来的镜像。map_images_nolock方法很长，但是和我们今天代码相关的只有</p><p><figure class="highlight lisp"><table><tr><td class="code"><pre><span class="line">_read_images(<span class="name">hList</span>, hCount, totalClasses, unoptimizedTotalClasses)<span class="comment">;</span></span><br></pre></td></tr></table></figure></p><p><figure class="highlight xl"><table><tr><td class="code"><pre><span class="line">void _read_images(header_info **hList, uint32_t hCount, int totalClasses, int unoptimizedTotalClasses) &#123;</span><br><span class="line">    <span class="comment">//......</span></span><br><span class="line">    <span class="comment">// Discover categories. </span></span><br><span class="line">    <span class="keyword">for</span> (EACH_HEADER) &#123;</span><br><span class="line">        category_t **catlist = _getObjc2CategoryList(hi, &amp;count);</span><br><span class="line">        <span class="comment">//查看是否包含类属性</span></span><br><span class="line">        <span class="function"><span class="title">bool</span> hasClassProperties = hi-&gt;</span><span class="function"><span class="title">info</span>()-&gt;</span>hasCategoryClassProperties();</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; count; i++) &#123;</span><br><span class="line">            category_t *cat = catlist[i];</span><br><span class="line">            <span class="comment">//获取到所属的类</span></span><br><span class="line">            C<span class="function"><span class="title">lass</span> cls = remapClass(cat-&gt;</span>cls);</span><br><span class="line">            <span class="comment">// ....</span></span><br><span class="line">            <span class="comment">// Process this category. </span></span><br><span class="line">            bool classExists = NO;</span><br><span class="line">            <span class="comment">//如果有实例方法，协议或者实例属性</span></span><br><span class="line">            <span class="function"><span class="title">if</span> (cat-&gt;</span><span class="function"><span class="title">instanceMethods</span> ||  cat-&gt;</span><span class="function"><span class="title">protocols</span> ||  cat-&gt;</span>instanceProperties) &#123;</span><br><span class="line">                addUnattachedCategoryForClass(cat, cls, hi);</span><br><span class="line">                <span class="function"><span class="title">if</span> (cls-&gt;</span>isRealized()) &#123;</span><br><span class="line">                    <span class="comment">//将分类添到属性，方法，协议添加到对应的类中</span></span><br><span class="line">                    remethodizeClass(cls);</span><br><span class="line">                    classExists = YES;</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="comment">//......</span></span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">//如果有类方法，协议或者类属性</span></span><br><span class="line">            <span class="function"><span class="title">if</span> (cat-&gt;</span><span class="function"><span class="title">classMethods</span>  ||  cat-&gt;</span><span class="function"><span class="title">protocols</span>  ||  (hasClassProperties &amp;&amp; cat-&gt;</span>_classProperties))  &#123;</span><br><span class="line">                <span class="function"><span class="title">addUnattachedCategoryForClass</span>(cat, cls-&gt;</span>ISA(), hi);</span><br><span class="line">                <span class="function"><span class="title">if</span> (cls-&gt;</span>ISA()-&gt;isRealized()) &#123;</span><br><span class="line">                    <span class="function"><span class="title">remethodizeClass</span>(cls-&gt;</span>ISA());</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="comment">//......</span></span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    ts.<span class="built_in">log</span>(<span class="string">"IMAGE TIMES: discover categories"</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">//.......</span></span><br><span class="line">    <span class="comment">// +load handled by prepare_load_methods()</span></span><br><span class="line">    <span class="keyword">if</span> (DebugNonFragileIvars) &#123;</span><br><span class="line">        realizeAllClasses();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//.......</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>_read_images 会从镜像中读取很多数据，这里只关注分类的部分。</p><p>首先通过****_getObjc2CategoryList****从_DATA_部分的__objc_catlist获取到当前镜像中全部的分类。__objc_catlist这个数据是哪里来的呢？其实是编译时候生成的。</p><p>那么编译阶段，编译器做了哪些事情呢？</p><ul><li>编译器生成了实例方法列表</li><li>编译器生成了category本身</li><li>编译器在DATA段下的objc_catlist section里保存了一个大小为1的category_t的数组</li></ul><p>objc_catlist section就是在加载镜像的时候被加载进来的。</p><p>我们继续，然后针对每个分类调用<strong><strong>addUnattachedCategoryForClass</strong></strong></p><p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">addUnattachedCategoryForClass</span><span class="params">(<span class="keyword">category_t</span> *cat, Class cls, header_info *catHeader)</span> </span>&#123;</span><br><span class="line">    runtimeLock.assertLocked();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// DO NOT use cat-&gt;cls! cls may be cat-&gt;cls-&gt;isa instead</span></span><br><span class="line">    NXMapTable *cats = unattachedCategories();</span><br><span class="line">    category_list *<span class="built_in">list</span>;</span><br><span class="line">    <span class="built_in">list</span> = (category_list *)NXMapGet(cats, cls);</span><br><span class="line">    <span class="keyword">if</span> (!<span class="built_in">list</span>) &#123;</span><br><span class="line">        <span class="built_in">list</span> = (category_list *)</span><br><span class="line">            <span class="built_in">calloc</span>(<span class="keyword">sizeof</span>(*<span class="built_in">list</span>) + <span class="keyword">sizeof</span>(<span class="built_in">list</span>-&gt;<span class="built_in">list</span>[<span class="number">0</span>]), <span class="number">1</span>);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="built_in">list</span> = (category_list *)</span><br><span class="line">            <span class="built_in">realloc</span>(<span class="built_in">list</span>, <span class="keyword">sizeof</span>(*<span class="built_in">list</span>) + <span class="keyword">sizeof</span>(<span class="built_in">list</span>-&gt;<span class="built_in">list</span>[<span class="number">0</span>]) * (<span class="built_in">list</span>-&gt;count + <span class="number">1</span>));</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">list</span>-&gt;<span class="built_in">list</span>[<span class="built_in">list</span>-&gt;count++] = (<span class="keyword">locstamped_category_t</span>)&#123;cat, catHeader&#125;;</span><br><span class="line">    NXMapInsert(cats, cls, <span class="built_in">list</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>在分析addUnattachedCategoryForClass之前我们先看下unattachedCategories方法:</p><p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> NXMapTable *<span class="title">unattachedCategories</span><span class="params">(<span class="keyword">void</span>)</span> </span>&#123;</span><br><span class="line">    runtimeLock.assertLocked();</span><br><span class="line">    <span class="keyword">static</span> NXMapTable *category_map = nil;</span><br><span class="line">    <span class="keyword">if</span> (category_map) <span class="keyword">return</span> category_map;</span><br><span class="line">    <span class="comment">// fixme initial map size</span></span><br><span class="line">    category_map = NXCreateMapTable(NXPtrValueMapPrototype, <span class="number">16</span>);</span><br><span class="line">    <span class="keyword">return</span> category_map;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>注意category_map是静态的，首次的时候会通过NXCreateMapTable(NXPtrValueMapPrototype, 16)创建一个MapTable，后续会将同一个category_map返回。category_map存放的是什么呢？它存放的是 class =&gt; categories 映射关系，这些分类是未添加到对应类上的所以叫做<strong><strong>unattachedCategories</strong></strong>。我们再回到addUnattachedCategoryForClass：</p><p>在调用unattachedCategories获取到全部类以及它的分类映射map之后，通过<strong><strong>NXMapGet(cats, cls)</strong></strong> 获取到的是cats map中以cls类作为key的，为添加到cls的分类列表，然后开辟一个list-&gt;count + 1 再加sizeof(*list)大小的空间，用于存放cat以及catHeader。然后再将这个列表添加到category_map中。所以经过addUnattachedCategoryForClass处理后。我们获得到的数据将会以如下的形式组织起来：</p><p><img src="/2020/01/06/iOS-Category-源码解析/0001.png" alt></p><p>接下来我们来看下分类的添加过程：</p><p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">remethodizeClass</span><span class="params">(Class cls)</span> </span>&#123;</span><br><span class="line">    category_list *cats;</span><br><span class="line">    <span class="keyword">bool</span> isMeta;</span><br><span class="line">    runtimeLock.assertLocked();</span><br><span class="line">    isMeta = cls-&gt;isMetaClass();</span><br><span class="line">    <span class="comment">// Re-methodizing: check for more categories</span></span><br><span class="line">    <span class="keyword">if</span> ((cats = unattachedCategoriesForClass(cls, <span class="literal">false</span><span class="comment">/*not realizing*/</span>))) &#123;    </span><br><span class="line">        attachCategories(cls, cats, <span class="literal">true</span> <span class="comment">/*flush caches*/</span>);        </span><br><span class="line">        <span class="built_in">free</span>(cats);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>remethodizeClass方法中先通过unattachedCategoriesForClass获取到当前类的未添加的分类cats：</p><p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> category_list *</span><br><span class="line">unattachedCategoriesForClass(Class cls, <span class="keyword">bool</span> realizing)</span><br><span class="line">&#123;</span><br><span class="line">    runtimeLock.assertLocked();</span><br><span class="line">    <span class="keyword">return</span> (category_list *)NXMapRemove(unattachedCategories(), cls);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>注意这里返回分类列表后会调用NXMapRemove从category_map中移除。</p><p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="keyword">void</span> </span><br><span class="line">attachCategories(Class cls, category_list *cats, <span class="keyword">bool</span> flush_caches) &#123;</span><br><span class="line">    <span class="keyword">if</span> (!cats) <span class="keyword">return</span>;</span><br><span class="line">    <span class="keyword">bool</span> isMeta = cls-&gt;isMetaClass();</span><br><span class="line">    <span class="comment">// 方法列表</span></span><br><span class="line">    <span class="keyword">method_list_t</span> **mlists = (<span class="keyword">method_list_t</span> **)<span class="built_in">malloc</span>(cats-&gt;count * <span class="keyword">sizeof</span>(*mlists));</span><br><span class="line">    <span class="comment">// 属性列表</span></span><br><span class="line">    <span class="keyword">property_list_t</span> **proplists = (<span class="keyword">property_list_t</span> **)<span class="built_in">malloc</span>(cats-&gt;count * <span class="keyword">sizeof</span>(*proplists));</span><br><span class="line">    <span class="comment">// 协议列表</span></span><br><span class="line">    <span class="keyword">protocol_list_t</span> **protolists = (<span class="keyword">protocol_list_t</span> **)<span class="built_in">malloc</span>(cats-&gt;count * <span class="keyword">sizeof</span>(*protolists));</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Count backwards through cats to get newest categories first</span></span><br><span class="line">    <span class="keyword">int</span> mcount = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">int</span> propcount = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">int</span> protocount = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">int</span> i = cats-&gt;count;</span><br><span class="line">    <span class="keyword">bool</span> fromBundle = NO;</span><br><span class="line">    <span class="comment">//i 表示分类列表序号</span></span><br><span class="line">    <span class="keyword">while</span> (i--) &#123; <span class="comment">//这里注意是逆序遍历，所以最后加载的最先添加到类中</span></span><br><span class="line">        <span class="keyword">auto</span>&amp; entry = cats-&gt;<span class="built_in">list</span>[i];</span><br><span class="line">        <span class="keyword">method_list_t</span> *mlist = entry.cat-&gt;methodsForMeta(isMeta);</span><br><span class="line">        <span class="keyword">if</span> (mlist) &#123;</span><br><span class="line">            mlists[mcount++] = mlist;</span><br><span class="line">            fromBundle |= entry.hi-&gt;isBundle();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">property_list_t</span> *proplist = </span><br><span class="line">            entry.cat-&gt;propertiesForMeta(isMeta, entry.hi);</span><br><span class="line">        <span class="keyword">if</span> (proplist) &#123;</span><br><span class="line">            proplists[propcount++] = proplist;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">protocol_list_t</span> *protolist = entry.cat-&gt;protocols;</span><br><span class="line">        <span class="keyword">if</span> (protolist) &#123;</span><br><span class="line">            protolists[protocount++] = protolist;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//将catories中的方法属性以及协议添加到rw中</span></span><br><span class="line">    <span class="keyword">auto</span> rw = cls-&gt;data();</span><br><span class="line"></span><br><span class="line">    prepareMethodLists(cls, mlists, mcount, NO, fromBundle);</span><br><span class="line">    rw-&gt;methods.attachLists(mlists, mcount);</span><br><span class="line">    <span class="built_in">free</span>(mlists);</span><br><span class="line">    <span class="keyword">if</span> (flush_caches  &amp;&amp;  mcount &gt; <span class="number">0</span>) flushCaches(cls);</span><br><span class="line"></span><br><span class="line">    rw-&gt;properties.attachLists(proplists, propcount);</span><br><span class="line">    <span class="built_in">free</span>(proplists);</span><br><span class="line"></span><br><span class="line">    rw-&gt;protocols.attachLists(protolists, protocount);</span><br><span class="line">    <span class="built_in">free</span>(protolists);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>分类的添加的逻辑位于attachCategories方法中。这里要特别注意理解数据结构的组织，为了帮助大家理解，我这里画了一张图：</p><p><img src="/2020/01/06/iOS-Category-源码解析/0002.png" alt></p><p>mlists，proplists，protolists如上图所示，从横向看，mlists，proplists，protolists是当前类所有分类的方法列表，属性列表以及协议列表。从纵向看，分别是某个分类的方法列表，协议列表，以及属性列表。这里需要十分注意的是上面的遍历是逆序遍历，所以最后加载的分类最先添加到类中，也就是说后添加的分类的方法，协议，以及属性会覆盖先添加的同名的方法，协议，以及属性，这就导致查找某个方法的时候会逆着分类加载链寻找，一旦找到立刻停止。</p><p>紧接着将当前类的所有方法列表，属性列表和分类列表追加到类的可写区域中。这里我们再重点看下attachLists方法。</p><p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">attachLists</span><span class="params">(List* <span class="keyword">const</span> * addedLists, <span class="keyword">uint32_t</span> addedCount)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (addedCount == <span class="number">0</span>) <span class="keyword">return</span>;</span><br><span class="line">    <span class="keyword">if</span> (hasArray()) &#123;</span><br><span class="line">        <span class="comment">// many lists -&gt; many lists</span></span><br><span class="line">        <span class="comment">// 原有数组的元素个数</span></span><br><span class="line">        <span class="keyword">uint32_t</span> oldCount = <span class="built_in">array</span>()-&gt;count;</span><br><span class="line">        <span class="comment">// 待添加数组元素个数</span></span><br><span class="line">        <span class="keyword">uint32_t</span> newCount = oldCount + addedCount;</span><br><span class="line">        <span class="comment">// 为新数组开辟对应的空间</span></span><br><span class="line">        setArray((<span class="keyword">array_t</span> *)<span class="built_in">realloc</span>(<span class="built_in">array</span>(), <span class="keyword">array_t</span>::byteSize(newCount)));</span><br><span class="line">        <span class="built_in">array</span>()-&gt;count = newCount;</span><br><span class="line">        <span class="comment">//原有的数据向后腾出addedCount大小的空间，给addedLists。</span></span><br><span class="line">        memmove(<span class="built_in">array</span>()-&gt;lists + addedCount<span class="comment">/*指向用于存储复制内容的目标数组的起始地址*/</span>, <span class="built_in">array</span>()-&gt;lists<span class="comment">/*指向要复制的数据源*/</span>, oldCount * <span class="keyword">sizeof</span>(<span class="built_in">array</span>()-&gt;lists[<span class="number">0</span>]<span class="comment">/*要被复制的字节数*/</span>));</span><br><span class="line">        <span class="comment">//将addedLists添加到原有数据的前面。</span></span><br><span class="line">        <span class="built_in">memcpy</span>(<span class="built_in">array</span>()-&gt;lists, addedLists, addedCount * <span class="keyword">sizeof</span>(<span class="built_in">array</span>()-&gt;lists[<span class="number">0</span>]));</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (!<span class="built_in">list</span>  &amp;&amp;  addedCount == <span class="number">1</span>) &#123;</span><br><span class="line">        <span class="comment">// 0 lists -&gt; 1 list</span></span><br><span class="line">        <span class="built_in">list</span> = addedLists[<span class="number">0</span>];</span><br><span class="line">    &#125; </span><br><span class="line">    <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="comment">// 1 list -&gt; many lists</span></span><br><span class="line">        List* oldList = <span class="built_in">list</span>;</span><br><span class="line">        <span class="keyword">uint32_t</span> oldCount = oldList ? <span class="number">1</span> : <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">uint32_t</span> newCount = oldCount + addedCount;</span><br><span class="line">        setArray((<span class="keyword">array_t</span> *)<span class="built_in">malloc</span>(<span class="keyword">array_t</span>::byteSize(newCount)));</span><br><span class="line">        <span class="built_in">array</span>()-&gt;count = newCount;</span><br><span class="line">        <span class="keyword">if</span> (oldList) <span class="built_in">array</span>()-&gt;lists[addedCount] = oldList;</span><br><span class="line">        <span class="built_in">memcpy</span>(<span class="built_in">array</span>()-&gt;lists, addedLists, </span><br><span class="line">                addedCount * <span class="keyword">sizeof</span>(<span class="built_in">array</span>()-&gt;lists[<span class="number">0</span>]));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>attachLists其实是将该类所有的分类对应的方法列表，属性列表，协议列表，添加到类的可读写区域中，attachLists方法会重新开辟一个内存空间，将原有的数据向后腾出空间，给分类对应的元素。这意味着什么？意味着分类的方法，协议，以及属性，会被添加到类原有的方法，协议，以及属性之前。</p><p>通过上面的源码解析不难理解下面的一些细节点：</p><ul><li><p>category的方法没有“完全替换掉”原来类已经有的方法，也就是说如果category和原来类都有methodA，那么category附加完成之后，类的方法列表里会有两个methodA。只不过category的方法被放到了新方法列表的前面，而原来类的方法被放到了新方法列表的后面，这也就是我们平常所说的category的方法会“覆盖”掉原来类的同名方法，这是因为运行时在查找方法的时候是顺着方法列表的顺序查找的，它只要一找到对应名字的方法，就会停止查找。</p></li><li><p>在编译路径中位于后面的分类会先添加到列表，所以最后编译的分类最终生效。</p></li></ul>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;在之前的博客《iOS 分类扩展与协议总结》中介绍了分类的用法以及性质，该博客将会从分类的源码对分类进一步分析。&lt;/p&gt;
&lt;p&gt;首先我们先看下分类的数据结构：&lt;/p&gt;
&lt;p&gt;&lt;figure class=&quot;highlight cpp&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=
      
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>iOS 内存管理之对象创建，销毁，持有，弱引用源码剖析</title>
    <link href="http://yoursite.com/2020/01/04/iOS-%E5%86%85%E5%AD%98%E7%AE%A1%E7%90%86%E4%B9%8B%E5%AF%B9%E8%B1%A1%E5%88%9B%E5%BB%BA%EF%BC%8C%E9%94%80%E6%AF%81%EF%BC%8C%E6%8C%81%E6%9C%89%EF%BC%8C%E5%BC%B1%E5%BC%95%E7%94%A8%E6%BA%90%E7%A0%81%E5%89%96%E6%9E%90/"/>
    <id>http://yoursite.com/2020/01/04/iOS-内存管理之对象创建，销毁，持有，弱引用源码剖析/</id>
    <published>2020-01-04T07:56:07.000Z</published>
    <updated>2020-01-05T03:18:15.000Z</updated>
    
    <content type="html"><![CDATA[<p>iOS 这部分代码位于 <strong><strong>objc-runtime/runtime/NSObject.mm</strong></strong>:</p><h5>1 对象的创建 alloc/init：</h5><p><strong><em>调用栈</em></strong></p><p><figure class="highlight ada"><table><tr><td class="code"><pre><span class="line"><span class="comment">--- alloc </span></span><br><span class="line">    <span class="comment">--- _objc_rootAlloc</span></span><br><span class="line">        <span class="comment">--- callAlloc</span></span><br><span class="line">            <span class="comment">--- allocWithZone</span></span><br><span class="line">                <span class="comment">--- class_createInstance</span></span><br><span class="line">                    <span class="comment">--- _class_createInstanceFromZone</span></span><br><span class="line">                        <span class="comment">--- calloc</span></span><br><span class="line">                        <span class="comment">--- initIsa</span></span><br><span class="line"></span><br><span class="line"><span class="comment">--- init</span></span><br><span class="line">    <span class="comment">--- _objc_rootInit</span></span><br></pre></td></tr></table></figure></p><p>我们在创建对象的时候会调用 alloc 方法为要创建的对象分配内存空间：</p><p><figure class="highlight objectivec"><table><tr><td class="code"><pre><span class="line">+ (<span class="keyword">id</span>)alloc &#123;</span><br><span class="line">    <span class="keyword">return</span> _objc_rootAlloc(<span class="keyword">self</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>在alloc中会调用_objc_rootAlloc</p><p><figure class="highlight php"><table><tr><td class="code"><pre><span class="line">id</span><br><span class="line">_objc_rootAlloc(<span class="class"><span class="keyword">Class</span> <span class="title">cls</span>)</span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> callAlloc(cls, <span class="keyword">false</span><span class="comment">/*checkNil*/</span>, <span class="keyword">true</span><span class="comment">/*allocWithZone*/</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>在_objc_rootAlloc中会调用callAlloc，它有两个参数第一个参数表示是否检查cls参数是否为空，第二个参数表示是否在zone中分配这个内存。</p><p><figure class="highlight hsp"><table><tr><td class="code"><pre><span class="line">static ALWAYS_INLINE id</span><br><span class="line">callAlloc(Class <span class="keyword">cls</span>, bool checkNil<span class="comment">/*false*/</span>, bool allocWithZone=false<span class="comment">/*true*/</span>)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">if</span> (slowpath(checkNil &amp;&amp; !<span class="keyword">cls</span>)) <span class="keyword">return</span> nil<span class="comment">;</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">if</span> __OBJC2__</span></span><br><span class="line">    <span class="comment">//hasCustomAWZ( )方法是用来判断当前class是否有自定义的allocWithZone。</span></span><br><span class="line">    <span class="keyword">if</span> (fastpath(!<span class="keyword">cls</span>-&gt;ISA()-&gt;hasCustomAWZ())) &#123;</span><br><span class="line">        <span class="comment">// 没有自定义的alloc/allocWithZone实现的时候</span></span><br><span class="line">        <span class="comment">// No alloc/allocWithZone implementation. Go straight to the allocator.</span></span><br><span class="line">        <span class="comment">// fixme store hasCustomAWZ in the non-meta class and </span></span><br><span class="line">        <span class="comment">// add it to canAllocFast's summary</span></span><br><span class="line">        <span class="keyword">if</span> (fastpath(<span class="keyword">cls</span>-&gt;canAllocFast())) &#123;</span><br><span class="line">            <span class="comment">// No ctors, raw isa, etc. Go straight to the metal.</span></span><br><span class="line">            bool dtor = <span class="keyword">cls</span>-&gt;hasCxxDtor()<span class="comment">;</span></span><br><span class="line">            id obj = (id)calloc(<span class="number">1</span>, <span class="keyword">cls</span>-&gt;bits.fastInstanceSize())<span class="comment">;</span></span><br><span class="line">            <span class="keyword">if</span> (slowpath(!obj)) <span class="keyword">return</span> callBadAllocHandler(<span class="keyword">cls</span>)<span class="comment">;</span></span><br><span class="line">            obj-&gt;initInstanceIsa(<span class="keyword">cls</span>, dtor)<span class="comment">;</span></span><br><span class="line">            <span class="keyword">return</span> obj<span class="comment">;</span></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="comment">// Has ctor or raw isa or something. Use the slower path.</span></span><br><span class="line">            id obj = class_createInstance(<span class="keyword">cls</span>, <span class="number">0</span>)<span class="comment">;</span></span><br><span class="line">            <span class="keyword">if</span> (slowpath(!obj)) <span class="keyword">return</span> callBadAllocHandler(<span class="keyword">cls</span>)<span class="comment">;</span></span><br><span class="line">            <span class="keyword">return</span> obj<span class="comment">;</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// No shortcuts available.</span></span><br><span class="line">    <span class="keyword">if</span> (allocWithZone) <span class="keyword">return</span> [<span class="keyword">cls</span> allocWithZone:nil]<span class="comment">;</span></span><br><span class="line">    <span class="keyword">return</span> [<span class="keyword">cls</span> <span class="keyword">alloc</span>]<span class="comment">;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>首先看下hasCustomAWZ</p><p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">hasCustomAWZ</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> ! bits.hasDefaultAWZ();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">hasDefaultAWZ</span><span class="params">( )</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> data()-&gt;flags &amp; RW_HAS_DEFAULT_AWZ;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> RW_HAS_DEFAULT_AWZ    (1&lt;&lt;16)</span></span><br></pre></td></tr></table></figure></p><p>它实际上是查看metaclass 中的对应标识位flags的RW_HAS_DEFAULT_AWZ来查看当前的class或者是superclass是否有默认的alloc/allocWithZone:如果这个标志位被标记了那么hasCustomAWZ就为NO.</p><p>cls-&gt;canAllocFast() 这个一般情况下会返回false. 代码中有注解<strong><strong>No ctors, raw isa, etc</strong></strong>的情况下cls-&gt;canAllocFast() 返回YES.具体还不是很清楚这是什么意思。待弄明白后补上，这里记得返回false就好。</p><p>我们以最常见的：没有自定义alloc/allocWithZone:的情况作为简化后的分析对象：这时候代码可以简化为：</p><p><figure class="highlight objectivec"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> ALWAYS_INLINE <span class="keyword">id</span></span><br><span class="line">callAlloc(Class cls, <span class="keyword">bool</span> checkNil<span class="comment">/*false*/</span>, <span class="keyword">bool</span> allocWithZone=<span class="literal">false</span><span class="comment">/*true*/</span>)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// No shortcuts available.</span></span><br><span class="line">    <span class="keyword">if</span> (allocWithZone) <span class="keyword">return</span> [cls allocWithZone:<span class="literal">nil</span>];</span><br><span class="line">    <span class="keyword">return</span> [cls alloc];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>我们继续往下看：</p><p><figure class="highlight crystal"><table><tr><td class="code"><pre><span class="line">+ (id)<span class="symbol">allocWithZone:</span>(<span class="class"><span class="keyword">struct</span> <span class="title">_NSZone</span> *)<span class="title">zone</span> &#123;</span></span><br><span class="line">    <span class="keyword">return</span> _objc_rootAllocWithZone(<span class="keyword">self</span>, (malloc_zone_t *)zone);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p><figure class="highlight hsp"><table><tr><td class="code"><pre><span class="line">id</span><br><span class="line">_objc_rootAllocWithZone(Class <span class="keyword">cls</span>, malloc_zone_t *zone)</span><br><span class="line">&#123;</span><br><span class="line">    id obj<span class="comment">;</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">if</span> __OBJC2__</span></span><br><span class="line">    <span class="comment">// allocWithZone under __OBJC2__ ignores the zone parameter</span></span><br><span class="line">    (void)zone<span class="comment">;</span></span><br><span class="line">    obj = class_createInstance(<span class="keyword">cls</span>, <span class="number">0</span>)<span class="comment">;</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">else</span></span></span><br><span class="line">    <span class="keyword">if</span> (!zone) &#123;</span><br><span class="line">        obj = class_createInstance(<span class="keyword">cls</span>, <span class="number">0</span>)<span class="comment">;</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> &#123;</span><br><span class="line">        obj = class_createInstanceFromZone(<span class="keyword">cls</span>, <span class="number">0</span>, zone)<span class="comment">;</span></span><br><span class="line">    &#125;</span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (slowpath(!obj)) obj = callBadAllocHandler(<span class="keyword">cls</span>)<span class="comment">;</span></span><br><span class="line">    <span class="keyword">return</span> obj<span class="comment">;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>我们记住这里zone传进去的是nil，所以_objc_rootAllocWithZone代码简化后如下：</p><p><figure class="highlight hsp"><table><tr><td class="code"><pre><span class="line">id</span><br><span class="line">_objc_rootAllocWithZone(Class <span class="keyword">cls</span>, malloc_zone_t *zone)</span><br><span class="line">&#123;</span><br><span class="line">    id obj<span class="comment">;</span></span><br><span class="line"></span><br><span class="line">    (void)zone<span class="comment">;</span></span><br><span class="line">    obj = class_createInstance(<span class="keyword">cls</span>, <span class="number">0</span>)<span class="comment">;</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (slowpath(!obj)) obj = callBadAllocHandler(<span class="keyword">cls</span>)<span class="comment">;</span></span><br><span class="line">    <span class="keyword">return</span> obj<span class="comment">;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">id </span><br><span class="line">class_createInstance(Class cls, <span class="keyword">size_t</span> extraBytes)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">return</span> _class_createInstanceFromZone(cls, extraBytes, nil);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p><figure class="highlight arduino"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> __attribute__((always_inline)) </span><br><span class="line">id</span><br><span class="line">_class_createInstanceFromZone(Class cls, <span class="keyword">size_t</span> extraBytes, <span class="keyword">void</span> *zone, </span><br><span class="line">                              <span class="keyword">bool</span> cxxConstruct = <span class="literal">true</span>, </span><br><span class="line">                              <span class="keyword">size_t</span> *outAllocatedSize = nil)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// Read class's info bits all at once for performance</span></span><br><span class="line">    <span class="comment">// 当前class或者superclass 是否有构造方法的实现</span></span><br><span class="line">    <span class="keyword">bool</span> hasCxxCtor = cls-&gt;hasCxxCtor();</span><br><span class="line">    <span class="comment">// 判断当前class或者superclass 是否有析构方法的实现。</span></span><br><span class="line">    <span class="keyword">bool</span> hasCxxDtor = cls-&gt;hasCxxDtor();</span><br><span class="line">    <span class="comment">// 获取对象的大小</span></span><br><span class="line">    <span class="keyword">size_t</span> <span class="built_in">size</span> = cls-&gt;instanceSize(extraBytes);</span><br><span class="line">    <span class="comment">//....</span></span><br><span class="line">    <span class="comment">//分配size内存空间</span></span><br><span class="line">    obj = (id)<span class="built_in">calloc</span>(<span class="number">1</span>, <span class="built_in">size</span>);</span><br><span class="line">    <span class="comment">//初始化isa</span></span><br><span class="line">    obj-&gt;initIsa(cls);</span><br><span class="line">    <span class="comment">//是否有构造函数</span></span><br><span class="line">    <span class="keyword">if</span> (cxxConstruct &amp;&amp; hasCxxCtor) &#123;</span><br><span class="line">        obj = _objc_constructOrFree(obj, cls);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> obj;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>_class_createInstanceFromZone 方法中首先判断当前class或者superclass 是否有构造方法和析构方法。然后从类对象中获取要创建当前对象所需要的空间大小，紧接着就调用calloc分配内存，然后初始化Isa结构体。如果有构造函数的情况下还需要调用_objc_constructOrFree。_objc_constructOrFree最终会找到该类的C++构造方法，向它发送消息。</p><p><figure class="highlight objectivec"><table><tr><td class="code"><pre><span class="line">- (<span class="keyword">id</span>)init &#123;</span><br><span class="line">    <span class="keyword">return</span> _objc_rootInit(<span class="keyword">self</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">id</span></span><br><span class="line">_objc_rootInit(<span class="keyword">id</span> obj)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// In practice, it will be hard to rely on this function.</span></span><br><span class="line">    <span class="comment">// Many classes do not properly chain -init calls.</span></span><br><span class="line">    <span class="keyword">return</span> obj;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><h5>2 对象的持有 retain</h5><p><strong><strong>调用栈</strong></strong></p><p>对象的持有实际上是在对应的SideTable散列表中通过当前对象的地址作为散列值，找到存储当前对象引用计数的哈希表将引用计数增加1.我们下面看下整个调用过程：</p><p><figure class="highlight ada"><table><tr><td class="code"><pre><span class="line"><span class="comment">--- retain</span></span><br><span class="line">    <span class="comment">--- rootRetain</span></span><br><span class="line">        <span class="comment">--- sidetable_retain</span></span><br></pre></td></tr></table></figure></p><p><figure class="highlight livescript"><table><tr><td class="code"><pre><span class="line">- (id)retain &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="function"><span class="params">((id)self)</span>-&gt;</span>rootRetain();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p><figure class="highlight applescript"><table><tr><td class="code"><pre><span class="line">ALWAYS_INLINE <span class="built_in">id</span> </span><br><span class="line">objc_object::rootRetain()</span><br><span class="line">&#123;</span><br><span class="line"><span class="built_in">    return</span> rootRetain(<span class="literal">false</span>, <span class="literal">false</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p><figure class="highlight haxe"><table><tr><td class="code"><pre><span class="line">ALWAYS_INLINE id </span><br><span class="line">objc_object:<span class="type"></span>:rootRetain(bool tryRetain, bool handleOverflow)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">if</span> (isTaggedPointer()) <span class="keyword">return</span> (id)<span class="built_in">this</span>;</span><br><span class="line"></span><br><span class="line">    bool sideTableLocked = <span class="literal">false</span>;</span><br><span class="line">    bool transcribeToSideTable = <span class="literal">false</span>;</span><br><span class="line"></span><br><span class="line">    isa_t oldisa;</span><br><span class="line">    isa_t <span class="keyword">new</span><span class="type">isa</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">do</span> &#123;</span><br><span class="line">        transcribeToSideTable = <span class="literal">false</span>;</span><br><span class="line">        oldisa = LoadExclusive(&amp;isa.bits);</span><br><span class="line">        <span class="keyword">new</span><span class="type">isa</span> = oldisa;</span><br><span class="line">        <span class="keyword">if</span> (slowpath(!<span class="keyword">new</span><span class="type">isa</span>.nonpointer)) &#123;</span><br><span class="line">            ClearExclusive(&amp;isa.bits);</span><br><span class="line">            <span class="keyword">if</span> (!tryRetain &amp;&amp; sideTableLocked) sidetable_unlock();</span><br><span class="line">            <span class="keyword">if</span> (tryRetain) <span class="keyword">return</span> sidetable_tryRetain() ? (id)<span class="built_in">this</span> : <span class="type">nil</span>;</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">return</span> sidetable_retain();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// don't check newisa.fast_rr; we already called any RR overrides</span></span><br><span class="line">        <span class="keyword">if</span> (slowpath(tryRetain &amp;&amp; <span class="keyword">new</span><span class="type">isa</span>.deallocating)) &#123;</span><br><span class="line">            ClearExclusive(&amp;isa.bits);</span><br><span class="line">            <span class="keyword">if</span> (!tryRetain &amp;&amp; sideTableLocked) sidetable_unlock();</span><br><span class="line">            <span class="keyword">return</span> nil;</span><br><span class="line">        &#125;</span><br><span class="line">        uintptr_t carry;</span><br><span class="line">        <span class="keyword">new</span><span class="type">isa</span>.bits = addc(<span class="keyword">new</span><span class="type">isa</span>.bits, RC_ONE, <span class="number">0</span>, &amp;carry);  <span class="comment">// extra_rc++</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (slowpath(carry)) &#123;</span><br><span class="line">            <span class="comment">// newisa.extra_rc++ overflowed</span></span><br><span class="line">            <span class="keyword">if</span> (!handleOverflow) &#123;</span><br><span class="line">                ClearExclusive(&amp;isa.bits);</span><br><span class="line">                <span class="keyword">return</span> rootRetain_overflow(tryRetain);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// Leave half of the retain counts inline and </span></span><br><span class="line">            <span class="comment">// prepare to copy the other half to the side table.</span></span><br><span class="line">            <span class="keyword">if</span> (!tryRetain &amp;&amp; !sideTableLocked) sidetable_lock();</span><br><span class="line">            sideTableLocked = <span class="literal">true</span>;</span><br><span class="line">            transcribeToSideTable = <span class="literal">true</span>;</span><br><span class="line">            <span class="keyword">new</span><span class="type">isa</span>.extra_rc = RC_HALF;</span><br><span class="line">            <span class="keyword">new</span><span class="type">isa</span>.has_sidetable_rc = <span class="literal">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">while</span> (slowpath(!StoreExclusive(&amp;isa.bits, oldisa.bits, <span class="keyword">new</span><span class="type">isa</span>.bits)));</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (slowpath(transcribeToSideTable)) &#123;</span><br><span class="line">        <span class="comment">// Copy the other half of the retain counts to the side table.</span></span><br><span class="line">        sidetable_addExtraRC_nolock(RC_HALF);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (slowpath(!tryRetain &amp;&amp; sideTableLocked)) sidetable_unlock();</span><br><span class="line">    <span class="keyword">return</span> (id)<span class="built_in">this</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>这里最关键的是sidetable_retain，至于什么是SiteTable,以及对象引用计数的存储介绍可以查看 我准备的一个面试题：</p><p><a href="https://github.com/tbfungeek/iOS-Daily-Interview/issues/2" target="_blank" rel="noopener">谈谈iOS的内存管理方式的理解</a>来理解如何查找散列表的过程。</p><p><figure class="highlight gams"><table><tr><td class="code"><pre><span class="line">id</span><br><span class="line">objc_object::sidetable_retain()</span><br><span class="line">&#123;</span><br><span class="line">#<span class="keyword">if</span> SUPPORT_NONPOINTER_ISA</span><br><span class="line">    assert(!isa.nonpointer);</span><br><span class="line">#endif</span><br><span class="line">    SideTable&amp; <span class="keyword">table</span> = SideTables()[this];</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">table</span>.lock();</span><br><span class="line">    size_t&amp; refcntStorage = <span class="keyword">table</span>.refcnts[this];</span><br><span class="line">    if (! (refcntStorage &amp; SIDE_TABLE_RC_PINNED)) &#123;</span><br><span class="line">        refcntStorage += SIDE_TABLE_RC_ONE;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">table</span>.unlock();</span><br><span class="line"></span><br><span class="line">    return (id)this;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><h5>3 对象的释放 release</h5><p><strong><strong>调用栈</strong></strong></p><p>对象的释放实际上是在对应的SideTable散列表中通过当前对象的地址作为散列值，找到存储当前对象引用计数的哈希表将引用计数减1.如果减去1后为0 那么就会调用dealloc释放对象：</p><p><figure class="highlight ada"><table><tr><td class="code"><pre><span class="line"><span class="comment">--- release</span></span><br><span class="line">    <span class="comment">--- rootRelease</span></span><br><span class="line">        <span class="comment">--- sidetable_release</span></span><br></pre></td></tr></table></figure></p><p><figure class="highlight livescript"><table><tr><td class="code"><pre><span class="line">- (oneway <span class="literal">void</span>)release &#123;</span><br><span class="line">    <span class="function"><span class="params">((id)self)</span>-&gt;</span>rootRelease();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">objc_object::rootRelease()</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">return</span> rootRelease(<span class="literal">true</span>, <span class="literal">false</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p><figure class="highlight haxe"><table><tr><td class="code"><pre><span class="line">ALWAYS_INLINE bool </span><br><span class="line">objc_object:<span class="type"></span>:rootRelease(bool performDealloc, bool handleUnderflow)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">if</span> (isTaggedPointer()) <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line"></span><br><span class="line">    bool sideTableLocked = <span class="literal">false</span>;</span><br><span class="line"></span><br><span class="line">    isa_t oldisa;</span><br><span class="line">    isa_t <span class="keyword">new</span><span class="type">isa</span>;</span><br><span class="line"></span><br><span class="line"> retry:<span class="type"></span></span><br><span class="line"><span class="type">    do </span>&#123;</span><br><span class="line">        oldisa = LoadExclusive(&amp;isa.bits);</span><br><span class="line">        <span class="keyword">new</span><span class="type">isa</span> = oldisa;</span><br><span class="line">        <span class="keyword">if</span> (slowpath(!<span class="keyword">new</span><span class="type">isa</span>.nonpointer)) &#123;</span><br><span class="line">            ClearExclusive(&amp;isa.bits);</span><br><span class="line">            <span class="keyword">if</span> (sideTableLocked) sidetable_unlock();</span><br><span class="line">            <span class="keyword">return</span> sidetable_release(performDealloc);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// don't check newisa.fast_rr; we already called any RR overrides</span></span><br><span class="line">        uintptr_t carry;</span><br><span class="line">        <span class="keyword">new</span><span class="type">isa</span>.bits = subc(<span class="keyword">new</span><span class="type">isa</span>.bits, RC_ONE, <span class="number">0</span>, &amp;carry);  <span class="comment">// extra_rc--</span></span><br><span class="line">        <span class="keyword">if</span> (slowpath(carry)) &#123;</span><br><span class="line">            <span class="comment">// don't ClearExclusive()</span></span><br><span class="line">            goto underflow;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">while</span> (slowpath(!StoreReleaseExclusive(&amp;isa.bits, </span><br><span class="line">                                             oldisa.bits, <span class="keyword">new</span><span class="type">isa</span>.bits)));</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (slowpath(sideTableLocked)) sidetable_unlock();</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line"></span><br><span class="line"> underflow:<span class="type"></span></span><br><span class="line"><span class="type">    </span>// <span class="keyword">new</span><span class="type">isa</span>.extra_rc-- underflowed: <span class="type">borrow from side table or deallocate</span></span><br><span class="line"><span class="type"></span></span><br><span class="line"><span class="type">    </span>// abandon <span class="keyword">new</span><span class="type">isa</span> to undo the decrement</span><br><span class="line">    <span class="keyword">new</span><span class="type">isa</span> = oldisa;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (slowpath(<span class="keyword">new</span><span class="type">isa</span>.has_sidetable_rc)) &#123;</span><br><span class="line">        <span class="keyword">if</span> (!handleUnderflow) &#123;</span><br><span class="line">            ClearExclusive(&amp;isa.bits);</span><br><span class="line">            <span class="keyword">return</span> rootRelease_underflow(performDealloc);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// Transfer retain count from side table to inline storage.</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (!sideTableLocked) &#123;</span><br><span class="line">            ClearExclusive(&amp;isa.bits);</span><br><span class="line">            sidetable_lock();</span><br><span class="line">            sideTableLocked = <span class="literal">true</span>;</span><br><span class="line">            <span class="comment">// Need to start over to avoid a race against </span></span><br><span class="line">            <span class="comment">// the nonpointer -&gt; raw pointer transition.</span></span><br><span class="line">            goto retry;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// Try to remove some retain counts from the side table.        </span></span><br><span class="line">        size_t borrowed = sidetable_subExtraRC_nolock(RC_HALF);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// To avoid races, has_sidetable_rc must remain set </span></span><br><span class="line">        <span class="comment">// even if the side table count is now zero.</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (borrowed &gt; <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="comment">// Side table retain count decreased.</span></span><br><span class="line">            <span class="comment">// Try to add them to the inline count.</span></span><br><span class="line">            <span class="keyword">new</span><span class="type">isa</span>.extra_rc = borrowed - <span class="number">1</span>;  <span class="comment">// redo the original decrement too</span></span><br><span class="line">            bool stored = StoreReleaseExclusive(&amp;isa.bits, </span><br><span class="line">                                                oldisa.bits, <span class="keyword">new</span><span class="type">isa</span>.bits);</span><br><span class="line">            <span class="keyword">if</span> (!stored) &#123;</span><br><span class="line">                <span class="comment">// Inline update failed. </span></span><br><span class="line">                <span class="comment">// Try it again right now. This prevents livelock on LL/SC </span></span><br><span class="line">                <span class="comment">// architectures where the side table access itself may have </span></span><br><span class="line">                <span class="comment">// dropped the reservation.</span></span><br><span class="line">                isa_t oldisa2 = LoadExclusive(&amp;isa.bits);</span><br><span class="line">                isa_t <span class="keyword">new</span><span class="type">isa2</span> = oldisa2;</span><br><span class="line">                <span class="keyword">if</span> (<span class="keyword">new</span><span class="type">isa2</span>.nonpointer) &#123;</span><br><span class="line">                    uintptr_t overflow;</span><br><span class="line">                    <span class="keyword">new</span><span class="type">isa2</span>.bits = </span><br><span class="line">                        addc(<span class="keyword">new</span><span class="type">isa2</span>.bits, RC_ONE * (borrowed<span class="number">-1</span>), <span class="number">0</span>, &amp;overflow);</span><br><span class="line">                    <span class="keyword">if</span> (!overflow) &#123;</span><br><span class="line">                        stored = StoreReleaseExclusive(&amp;isa.bits, oldisa2.bits, </span><br><span class="line">                                                       <span class="keyword">new</span><span class="type">isa2</span>.bits);</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> (!stored) &#123;</span><br><span class="line">                <span class="comment">// Inline update failed.</span></span><br><span class="line">                <span class="comment">// Put the retains back in the side table.</span></span><br><span class="line">                sidetable_addExtraRC_nolock(borrowed);</span><br><span class="line">                goto retry;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="comment">// Decrement successful after borrowing from side table.</span></span><br><span class="line">            <span class="comment">// This decrement cannot be the deallocating decrement - the side </span></span><br><span class="line">            <span class="comment">// table lock and has_sidetable_rc bit ensure that if everyone </span></span><br><span class="line">            <span class="comment">// else tried to -release while we worked, the last one would block.</span></span><br><span class="line">            sidetable_unlock();</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="comment">// Side table is empty after all. Fall-through to the dealloc path.</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Really deallocate.</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (slowpath(<span class="keyword">new</span><span class="type">isa</span>.deallocating)) &#123;</span><br><span class="line">        ClearExclusive(&amp;isa.bits);</span><br><span class="line">        <span class="keyword">if</span> (sideTableLocked) sidetable_unlock();</span><br><span class="line">        <span class="keyword">return</span> overrelease_error();</span><br><span class="line">        <span class="comment">// does not actually return</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">new</span><span class="type">isa</span>.deallocating = <span class="literal">true</span>;</span><br><span class="line">    <span class="keyword">if</span> (!StoreExclusive(&amp;isa.bits, oldisa.bits, <span class="keyword">new</span><span class="type">isa</span>.bits)) goto retry;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (slowpath(sideTableLocked)) sidetable_unlock();</span><br><span class="line"></span><br><span class="line">    __sync_synchronize();</span><br><span class="line">    <span class="keyword">if</span> (performDealloc) &#123;</span><br><span class="line">        ((void(*)(objc_object *, SEL))objc_msgSend)(<span class="built_in">this</span>, SEL_dealloc);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p><figure class="highlight arduino"><table><tr><td class="code"><pre><span class="line"><span class="keyword">uintptr_t</span></span><br><span class="line">objc_object::sidetable_release(<span class="keyword">bool</span> performDealloc)</span><br><span class="line">&#123;</span><br><span class="line"><span class="meta">#<span class="meta-keyword">if</span> SUPPORT_NONPOINTER_ISA</span></span><br><span class="line">    assert(!isa.nonpointer);</span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br><span class="line">    SideTable&amp; table = SideTables()[<span class="keyword">this</span>];</span><br><span class="line"></span><br><span class="line">    <span class="keyword">bool</span> do_dealloc = <span class="literal">false</span>;</span><br><span class="line"></span><br><span class="line">    table.lock();</span><br><span class="line">    <span class="comment">//在散列表中找到当前对象对应的引用计数</span></span><br><span class="line">    RefcountMap::iterator it = table.refcnts.<span class="built_in">find</span>(<span class="keyword">this</span>);</span><br><span class="line">    <span class="keyword">if</span> (it == table.refcnts.<span class="built_in">end</span>()) &#123;</span><br><span class="line">        <span class="comment">//没找到，标记为已经销毁</span></span><br><span class="line">        do_dealloc = <span class="literal">true</span>;</span><br><span class="line">        table.refcnts[<span class="keyword">this</span>] = SIDE_TABLE_DEALLOCATING;</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (it-&gt;second &lt; SIDE_TABLE_DEALLOCATING) &#123;</span><br><span class="line">        <span class="comment">// SIDE_TABLE_WEAKLY_REFERENCED may be set. Don't change it.</span></span><br><span class="line">        do_dealloc = <span class="literal">true</span>;</span><br><span class="line">        it-&gt;second |= SIDE_TABLE_DEALLOCATING;</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (! (it-&gt;second &amp; SIDE_TABLE_RC_PINNED)) &#123;</span><br><span class="line">        <span class="comment">//引用计数减去1</span></span><br><span class="line">        it-&gt;second -= SIDE_TABLE_RC_ONE;</span><br><span class="line">    &#125;</span><br><span class="line">    table.unlock();</span><br><span class="line">    <span class="comment">//释放已经释放</span></span><br><span class="line">    <span class="keyword">if</span> (do_dealloc  &amp;&amp;  performDealloc) &#123;</span><br><span class="line">        <span class="comment">//调用dealloc</span></span><br><span class="line">        ((<span class="keyword">void</span>(*)(objc_object *, SEL))objc_msgSend)(<span class="keyword">this</span>, SEL_dealloc);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> do_dealloc;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><h5>4 对象的销毁 dealloc</h5><p>在介绍对象释放的最后有提到在某个对象的引用计数减为0的时候会调用对象的dealloc. 下面是相关的方法调用：</p><p><figure class="highlight erlang"><table><tr><td class="code"><pre><span class="line">- <span class="params">(void)</span>dealloc &#123;</span><br><span class="line">    _objc_rootDealloc<span class="params">(self)</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p><figure class="highlight gcode"><table><tr><td class="code"><pre><span class="line">void</span><br><span class="line">_objc_rootDealloc<span class="comment">(id obj)</span></span><br><span class="line">&#123;</span><br><span class="line">    assert<span class="comment">(obj)</span>;</span><br><span class="line"></span><br><span class="line">    obj-&gt;rootDealloc<span class="comment">()</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">inline</span> <span class="keyword">void</span></span><br><span class="line">objc_object::rootDealloc()</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">if</span> (isTaggedPointer()) <span class="keyword">return</span>;  <span class="comment">// fixme necessary?</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (fastpath(isa.nonpointer  &amp;&amp;  </span><br><span class="line">                 !isa.weakly_referenced  &amp;&amp;  </span><br><span class="line">                 !isa.has_assoc  &amp;&amp;  </span><br><span class="line">                 !isa.has_cxx_dtor  &amp;&amp;  </span><br><span class="line">                 !isa.has_sidetable_rc))</span><br><span class="line">    &#123;</span><br><span class="line">        assert(!sidetable_present());</span><br><span class="line">        <span class="built_in">free</span>(<span class="keyword">this</span>);</span><br><span class="line">    &#125; </span><br><span class="line">    <span class="keyword">else</span> &#123;</span><br><span class="line">        object_dispose((id)<span class="keyword">this</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p><figure class="highlight objectivec"><table><tr><td class="code"><pre><span class="line"><span class="keyword">id</span> </span><br><span class="line">object_dispose(<span class="keyword">id</span> obj)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">if</span> (!obj) <span class="keyword">return</span> <span class="literal">nil</span>;</span><br><span class="line"></span><br><span class="line">    objc_destructInstance(obj);    </span><br><span class="line">    free(obj);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="literal">nil</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p><figure class="highlight gauss"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">void *<span class="built_in">objc_destructInstance</span>(id obj) </span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">if</span> (obj) &#123;</span><br><span class="line">        <span class="comment">// Read all of the flags at once for performance.</span></span><br><span class="line">        <span class="keyword">bool</span> cxx = obj-&gt;<span class="built_in">hasCxxDtor</span>();</span><br><span class="line">        <span class="keyword">bool</span> assoc = obj-&gt;<span class="built_in">hasAssociatedObjects</span>();</span><br><span class="line"></span><br><span class="line">        <span class="comment">// This order is important.</span></span><br><span class="line">        <span class="keyword">if</span> (cxx) <span class="built_in">object_cxxDestruct</span>(obj);</span><br><span class="line">        <span class="keyword">if</span> (assoc) <span class="built_in">_object_remove_assocations</span>(obj);</span><br><span class="line">        obj-&gt;<span class="built_in">clearDeallocating</span>();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> obj;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>这里需要注意的是rootDealloc方法，它会先判断是否是taggpointer，如果是的话就直接返回，因为这种对象不需要释放内存，它的值就存放在指针中，这部分相关的内容可以查看<a href="https://github.com/tbfungeek/iOS-Daily-Interview/issues/2" target="_blank" rel="noopener">谈谈iOS的内存管理方式的理解</a>，紧接会判断当前对象的具体情况，如果当前对象是Nonpointer_ISA类型，那么要看相应的标识位，判断是否是弱引用，是否有关联对象，是否有C++析构函数，是否用到了散列表？如果都没有的话就直接free对象。否则通过object_dispose进行后续处理：</p><p>相关的具体处理最终在objc_destructInstance中实现。大家可以看下该方法的注释：</p><p><figure class="highlight dart"><table><tr><td class="code"><pre><span class="line"><span class="comment"><span class="markdown">/<span class="strong">*****</span><span class="strong">*****</span><span class="strong">*****</span><span class="strong">*****</span><span class="strong">*****</span><span class="strong">*****</span><span class="strong">*****</span><span class="strong">*****</span><span class="strong">*****</span><span class="strong">*****</span><span class="strong">*****</span><span class="strong">*****</span><span class="strong">*****</span><span class="strong">*****</span>*</span></span></span><br><span class="line"><span class="comment"><span class="markdown"><span class="bullet">* </span>objc_destructInstance</span></span></span><br><span class="line"><span class="comment"><span class="markdown"><span class="bullet">* </span>Destroys </span>an<span class="markdown"> instance without freeing memory. </span></span></span><br><span class="line"><span class="comment"><span class="markdown"><span class="bullet">* </span>Calls C++ destructors.</span></span></span><br><span class="line"><span class="comment"><span class="markdown"><span class="bullet">* </span>Calls ARC ivar cleanup.</span></span></span><br><span class="line"><span class="comment"><span class="markdown"><span class="bullet">* </span>Removes associative references.</span></span></span><br><span class="line"><span class="comment"><span class="markdown"><span class="bullet">* </span>Returns <span class="code">`obj`</span>. Does nothing if <span class="code">`obj`</span> is nil.</span></span></span><br><span class="line"><span class="comment"><span class="markdown"><span class="strong">*****</span><span class="strong">*****</span><span class="strong">*****</span><span class="strong">*****</span><span class="strong">*****</span><span class="strong">*****</span><span class="strong">*****</span><span class="strong">*****</span><span class="strong">*****</span><span class="strong">*****</span><span class="strong">*****</span><span class="strong">*****</span><span class="strong">*****</span><span class="strong">*****</span>/</span></span></span><br></pre></td></tr></table></figure></p><p>这里面重点针对是否有C++析构函数，是否有关联对象，是否有弱引用进行了对应的处理，我们接下来重点对这部分进行介绍：</p><p><strong><strong>调用析构函数是否对象</strong></strong></p><p><figure class="highlight ceylon"><table><tr><td class="code"><pre><span class="line"><span class="keyword">void</span> <span class="keyword">object</span><span class="number">_</span>cxxDestruct(id obj)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">if</span> (!obj) <span class="keyword">return</span>;</span><br><span class="line">    <span class="keyword">if</span> (obj-&gt;isTaggedPointer()) <span class="keyword">return</span>;</span><br><span class="line">    <span class="keyword">object</span><span class="number">_</span>cxxDestructFromClass(obj, obj-&gt;ISA());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p><figure class="highlight objectivec"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">void</span> object_cxxDestructFromClass(<span class="keyword">id</span> obj, Class cls)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">void</span> (*dtor)(<span class="keyword">id</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Call cls's dtor first, then superclasses's dtors.</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> ( ; cls; cls = cls-&gt;superclass) &#123;</span><br><span class="line">        <span class="keyword">if</span> (!cls-&gt;hasCxxDtor()) <span class="keyword">return</span>; </span><br><span class="line">        dtor = (<span class="keyword">void</span>(*)(<span class="keyword">id</span>))</span><br><span class="line">            lookupMethodInClassAndLoadCache(cls, SEL_cxx_destruct);</span><br><span class="line">        <span class="keyword">if</span> (dtor != (<span class="keyword">void</span>(*)(<span class="keyword">id</span>))_objc_msgForward_impcache) &#123;</span><br><span class="line">            <span class="keyword">if</span> (PrintCxxCtors) &#123;</span><br><span class="line">                _objc_inform(<span class="string">"CXX: calling C++ destructors for class %s"</span>, </span><br><span class="line">                             cls-&gt;nameForLogging());</span><br><span class="line">            &#125;</span><br><span class="line">            (*dtor)(obj);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>实际上就是在类的方法列表中查找对应的析构函数，然后对其进行调用。如果针对这部分想要细致了解大家可以查看<a href="http://blog.sunnyxx.com/2014/04/02/objc_dig_arc_dealloc/" target="_blank" rel="noopener">ARC下dealloc过程及.cxx_destruct的探究</a>这篇博客。</p><p>ARC下在编译器插入的.cxx_desctruct方法中会完成成员变量的自动释放。而对象的实例变量的销毁发生在根类[NSObject dealloc]方法中。</p><p><strong><strong>关联属性移除</strong></strong></p><p>关于关联对象大家可以查看对应的博客，它其实是由全局AssociationsManager进行管理的，在这里会找到对应的关联对象，然后添加到elements，然后对elements里面的对象调用ReleaseValue进行移除。这样和这个对象相关的关联对象就从AssociationsManager中移除了。</p><p><figure class="highlight arduino"><table><tr><td class="code"><pre><span class="line"><span class="keyword">void</span> _object_remove_assocations(id object) &#123;</span><br><span class="line">    <span class="built_in">vector</span>&lt; ObjcAssociation,ObjcAllocator&lt;ObjcAssociation&gt; &gt; elements;</span><br><span class="line">    &#123;</span><br><span class="line">        AssociationsManager manager;</span><br><span class="line">        <span class="function">AssociationsHashMap &amp;<span class="title">associations</span><span class="params">(manager.associations())</span></span>;</span><br><span class="line">        <span class="keyword">if</span> (associations.<span class="built_in">size</span>() == <span class="number">0</span>) <span class="keyword">return</span>;</span><br><span class="line">        <span class="keyword">disguised_ptr_t</span> disguised_object = DISGUISE(object);</span><br><span class="line">        AssociationsHashMap::iterator i = associations.<span class="built_in">find</span>(disguised_object);</span><br><span class="line">        <span class="keyword">if</span> (i != associations.<span class="built_in">end</span>()) &#123;</span><br><span class="line">            <span class="comment">// copy all of the associations that need to be removed.</span></span><br><span class="line">            ObjectAssociationMap *refs = i-&gt;second;</span><br><span class="line">            <span class="keyword">for</span> (ObjectAssociationMap::iterator j = refs-&gt;<span class="built_in">begin</span>(), <span class="built_in">end</span> = refs-&gt;<span class="built_in">end</span>(); j != <span class="built_in">end</span>; ++j) &#123;</span><br><span class="line">                elements.push_back(j-&gt;second);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// remove the secondary table.</span></span><br><span class="line">            <span class="keyword">delete</span> refs;</span><br><span class="line">            associations.erase(i);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// the calls to releaseValue() happen outside of the lock.</span></span><br><span class="line">    for_each(elements.<span class="built_in">begin</span>(), elements.<span class="built_in">end</span>(), ReleaseValue());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p><strong><strong>清除剩余标识位</strong></strong></p><p><figure class="highlight smali"><table><tr><td class="code"><pre><span class="line">inline void </span><br><span class="line">objc_object::clearDeallocating()</span><br><span class="line">&#123;</span><br><span class="line">   <span class="built_in"> if </span>(slowpath(!isa.nonpointer)) &#123;</span><br><span class="line">        // Slow path for raw pointer isa.</span><br><span class="line">        // 普通类型的指针指向的对象</span><br><span class="line">        sidetable_clearDeallocating();</span><br><span class="line">    &#125;</span><br><span class="line">    else<span class="built_in"> if </span>(slowpath(isa.weakly_referenced  ||  isa.has_sidetable_rc)) &#123;</span><br><span class="line">        // Slow path for non-pointer isa with weak refs<span class="built_in"> and/or </span>side table data.</span><br><span class="line">        // non-pointer isa 类型的对象。这种对象之所以要和普通对象区分开是因为它的弱引用标记以及是否使用散列表信息都存在“指针”中，所以要分开处理，但是处理的流程都是一样的，都是查看是否被弱引用指针指向，如果有则将这些设置为nil。紧接着从散列表中清除引用计数。最后将弱引用项从弱引用表中移除。</span><br><span class="line">        clearDeallocating_slow();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    assert(!sidetable_present());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>clearDeallocating 中实际上是用于处理剩余的标识位，这里分成是否是non-pointer isa：non-pointer isa 对象之所以要和普通对象区分开是因为它的弱引用标记以及是否使用散列表信息都存在“指针”中，所以要分开处理，但是处理的流程都是一样的，都是查看是否被弱引用指针指向，如果有则将这些设置为nil。紧接着从散列表中清除引用计数。最后将弱引用项从弱引用表中移除。</p><p><figure class="highlight gams"><table><tr><td class="code"><pre><span class="line">void </span><br><span class="line">objc_object::sidetable_clearDeallocating()</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">//找到对应的散列表</span></span><br><span class="line">    SideTable&amp; <span class="keyword">table</span> = SideTables()[this];</span><br><span class="line"></span><br><span class="line">    <span class="comment">// clear any weak table items</span></span><br><span class="line">    <span class="comment">// clear extra retain count and deallocating bit</span></span><br><span class="line">    <span class="comment">// (fixme warn or abort if extra retain count == 0 ?)</span></span><br><span class="line">    <span class="keyword">table</span>.lock();</span><br><span class="line">    RefcountMap::iterator it = <span class="keyword">table</span>.refcnts.find(this);</span><br><span class="line">    if (it != <span class="keyword">table</span>.refcnts.end()) &#123;</span><br><span class="line">        <span class="comment">//当前对象是否是弱引用对象</span></span><br><span class="line">        if (it-&gt;second &amp; SIDE_TABLE_WEAKLY_REFERENCED) &#123;</span><br><span class="line">            <span class="comment">//如果是的话则将对应对象对它的引用设置为nil</span></span><br><span class="line">            weak_clear_no_lock(&amp;<span class="keyword">table</span>.weak_table, (id)this);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">//清除引用计数</span></span><br><span class="line">        <span class="keyword">table</span>.refcnts.erase(it);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">table</span>.unlock();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>sidetable_clearDeallocating 就做了三件事情：</p><ol><li>找到对应的散列表</li><li>查看弱引用标记，是否是弱引用，如果是的话则将其他对象对它的引用置为nil，并将弱引用标志从SideTable的弱引用表中移除。</li><li>将该对象对应的引用计数表从SideTables中移除</li></ol><p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">void</span> </span><br><span class="line">weak_clear_no_lock(<span class="keyword">weak_table_t</span> *weak_table, id referent_id) </span><br><span class="line">&#123;</span><br><span class="line">    objc_object *referent = (objc_object *)referent_id;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//找到对应的弱引用项入口</span></span><br><span class="line">    <span class="keyword">weak_entry_t</span> *entry = weak_entry_for_referent(weak_table, referent);</span><br><span class="line">    <span class="keyword">if</span> (entry == nil) &#123;</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// zero out references</span></span><br><span class="line">    <span class="keyword">weak_referrer_t</span> *referrers;</span><br><span class="line">    <span class="keyword">size_t</span> count;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span> (entry-&gt;out_of_line()) &#123;</span><br><span class="line">        referrers = entry-&gt;referrers;</span><br><span class="line">        count = TABLE_SIZE(entry);</span><br><span class="line">    &#125; </span><br><span class="line">    <span class="keyword">else</span> &#123;</span><br><span class="line">        referrers = entry-&gt;inline_referrers;</span><br><span class="line">        count = WEAK_INLINE_COUNT;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">size_t</span> i = <span class="number">0</span>; i &lt; count; ++i) &#123;</span><br><span class="line">        objc_object **referrer = referrers[i];</span><br><span class="line">        <span class="keyword">if</span> (referrer) &#123;</span><br><span class="line">            <span class="keyword">if</span> (*referrer == referent) &#123;</span><br><span class="line">                <span class="comment">//将其他对象对他的引用设置为nil</span></span><br><span class="line">                *referrer = nil;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (*referrer) &#123;</span><br><span class="line">                _objc_inform(<span class="string">"__weak variable at %p holds %p instead of %p. "</span></span><br><span class="line">                             <span class="string">"This is probably incorrect use of "</span></span><br><span class="line">                             <span class="string">"objc_storeWeak() and objc_loadWeak(). "</span></span><br><span class="line">                             <span class="string">"Break on objc_weak_error to debug.\n"</span>, </span><br><span class="line">                             referrer, (<span class="keyword">void</span>*)*referrer, (<span class="keyword">void</span>*)referent);</span><br><span class="line">                objc_weak_error();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//移除该项</span></span><br><span class="line">    weak_entry_remove(weak_table, entry);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><h5>5 Weak指针原理</h5><p>__weak 修饰的变量有两大特点：</p><ul><li>不会增加指向对象的引用计数 （规避循环引用）</li><li>指向对象释放后，变量会自动置 nil （规避野指针访问错误）</li></ul><p>大体的实现思路如下：</p><p><figure class="highlight armasm"><table><tr><td class="code"><pre><span class="line">使用__weak 修饰的变量时，runtime 会生成对应的 weak_entry 结构放入 weak_table 中，以赋值对象地址生成的 hash 值为 key，以包装 __weak 修饰的指针变量地址的 <span class="meta">entry</span> 为 value，当赋值对象释放时，runtime 会在目标对象的 dealloc 处理过程中，以对象地址（<span class="keyword">self）为 </span>key 去 weak_table 查找 <span class="meta">entry</span> ，置空 <span class="meta">entry</span> 指向的的所有对象指针。weak_entry 使用数组保存指针变量地址，当地址数量小于<span class="number">4</span>的时候，这个数组就是个普通的内置数组，在地址数量大于<span class="number">4</span>的时候，这个数组就会扩充成一个 hash table。</span><br></pre></td></tr></table></figure></p><p>我们接下来看下这部分源码：</p><p>当我们声明一个指针变量的时候就会调用<strong><strong>objc_initWeak</strong></strong>方法：</p><p><figure class="highlight haxe"><table><tr><td class="code"><pre><span class="line">id</span><br><span class="line">objc_initWeak(id *location, id <span class="keyword">new</span><span class="type">Obj</span>)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">if</span> (!<span class="keyword">new</span><span class="type">Obj</span>) &#123;</span><br><span class="line">        *location = nil;</span><br><span class="line">        <span class="keyword">return</span> nil;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> storeWeak&lt;DontHaveOld, DoHaveNew, DoCrashIfDeallocating&gt;</span><br><span class="line">        (location, (objc_object*)<span class="keyword">new</span><span class="type">Obj</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>weak 对象的存储是通过storeWeak完成的，在看storeWeak代码之前我们先来看下<strong><strong>weak_table_t</strong></strong></p><p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">weak_table_t</span> &#123;</span></span><br><span class="line">    <span class="keyword">weak_entry_t</span> *weak_entries;</span><br><span class="line">    <span class="keyword">size_t</span>    num_entries;</span><br><span class="line">    <span class="keyword">uintptr_t</span> mask;</span><br><span class="line">    <span class="keyword">uintptr_t</span> max_hash_displacement;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p><p>weak_table_t 中存储的每个weak_entry_t是以被引用对象地址为hash Code 进行存储的，这个没啥好说的，我们来看下weak_entry_t。</p><p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">weak_entry_t</span> &#123;</span></span><br><span class="line">    DisguisedPtr&lt;objc_object&gt; referent;</span><br><span class="line">    <span class="keyword">union</span> &#123;</span><br><span class="line">        <span class="class"><span class="keyword">struct</span> &#123;</span></span><br><span class="line">            <span class="keyword">weak_referrer_t</span> *referrers;</span><br><span class="line">            <span class="keyword">uintptr_t</span>        out_of_line_ness : <span class="number">2</span>;</span><br><span class="line">            <span class="keyword">uintptr_t</span>        num_refs : PTR_MINUS_2;</span><br><span class="line">            <span class="keyword">uintptr_t</span>        mask;</span><br><span class="line">            <span class="keyword">uintptr_t</span>        max_hash_displacement;</span><br><span class="line">        &#125;;</span><br><span class="line">        <span class="class"><span class="keyword">struct</span> &#123;</span></span><br><span class="line">            <span class="comment">// out_of_line_ness field is low bits of inline_referrers[1]</span></span><br><span class="line">            <span class="keyword">weak_referrer_t</span>  inline_referrers[WEAK_INLINE_COUNT];</span><br><span class="line">        &#125;;</span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">bool</span> <span class="title">out_of_line</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> (out_of_line_ness == REFERRERS_OUT_OF_LINE);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">weak_entry_t</span>&amp; <span class="keyword">operator</span>=(<span class="keyword">const</span> <span class="keyword">weak_entry_t</span>&amp; other) &#123;</span><br><span class="line">        <span class="built_in">memcpy</span>(<span class="keyword">this</span>, &amp;other, <span class="keyword">sizeof</span>(other));</span><br><span class="line">        <span class="keyword">return</span> *<span class="keyword">this</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">weak_entry_t</span>(objc_object *newReferent, objc_object **newReferrer)</span><br><span class="line">        : referent(newReferent)</span><br><span class="line">    &#123;</span><br><span class="line">        inline_referrers[<span class="number">0</span>] = newReferrer;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt; WEAK_INLINE_COUNT; i++) &#123;</span><br><span class="line">            inline_referrers[i] = nil;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p><p>weak_entry_t 里面包含了一个union，里面有两个结构体，其中 WEAK_INLINE_COUNT 宏为 4 ，也就是说在初始状态下，这个 union 的空间有 weak_referrer_t inline_referrers[4] 这么大，当 entry 保存指针变量地址的个数不大于 4 个时，我们就可以直接使用 inline_referrers 数组，这样写的话，访问更加快速便捷。但是如果超过4个的话则使用哈希表来存储，这样会加速查找的速度。</p><p>下面是weak相关数据结构的大体示意图：</p><p><img src="/2020/01/04/iOS-内存管理之对象创建，销毁，持有，弱引用源码剖析/00001.png" alt></p><p>referent: 是内存上的weak对象referrers: 是指向weak对象的所有变量</p><p>有了上面的存储结构的介绍对storeWeak会有更清晰的理解</p><p>在设置新的关联前，如果 __weak 修饰的指针变量已经关联了其他对象，那么此函数会先解除旧关联，再设置新的。如果 newObjc 是 nil，那么只会进行解除关联以及指针置 nil 操作，objc_destroyWeak 就以这种方式调用 storeWeak 来执行销毁动作，这个后面会介绍。</p><p><figure class="highlight haxe"><table><tr><td class="code"><pre><span class="line">template &lt;HaveOld haveOld, HaveNew haveNew,</span><br><span class="line">          CrashIfDeallocating crashIfDeallocating&gt;</span><br><span class="line"><span class="keyword">static</span> id </span><br><span class="line">storeWeak(id *location, objc_object *<span class="keyword">new</span><span class="type">Obj</span>)</span><br><span class="line">&#123;</span><br><span class="line">    assert(haveOld  ||  haveNew);</span><br><span class="line">    <span class="keyword">if</span> (!haveNew) assert(<span class="keyword">new</span><span class="type">Obj</span> == nil);</span><br><span class="line"></span><br><span class="line">    Class previouslyInitializedClass = nil;</span><br><span class="line">    id oldObj;</span><br><span class="line">    SideTable *oldTable;</span><br><span class="line">    SideTable *<span class="keyword">new</span><span class="type">Table</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//..........</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// Clean up old value, if any.</span></span><br><span class="line">    <span class="keyword">if</span> (haveOld) &#123;</span><br><span class="line">        <span class="comment">//__weak 修饰的指针变量已经指向过某对象</span></span><br><span class="line">        <span class="comment">// 需要把这个对象和此指针变量的关联断开</span></span><br><span class="line">        weak_unregister_no_lock(&amp;oldTable-&gt;weak_table, oldObj, location);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Assign new value, if any.</span></span><br><span class="line">    <span class="keyword">if</span> (haveNew) &#123;</span><br><span class="line">        <span class="comment">// 关联新对象和 __weak 修饰的指针变量</span></span><br><span class="line">        <span class="keyword">new</span><span class="type">Obj</span> = (objc_object *)</span><br><span class="line">            weak_register_no_lock(&amp;<span class="keyword">new</span><span class="type">Table</span>-&gt;weak_table, (id)<span class="keyword">new</span><span class="type">Obj</span>, location, </span><br><span class="line">                                  crashIfDeallocating);</span><br><span class="line">        <span class="comment">// weak_register_no_lock returns nil if weak store should be rejected</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">// Set is-weakly-referenced bit in refcount table.</span></span><br><span class="line">        <span class="keyword">if</span> (<span class="keyword">new</span><span class="type">Obj</span>  &amp;&amp;  !<span class="keyword">new</span><span class="type">Obj</span>-&gt;isTaggedPointer()) &#123;</span><br><span class="line">            <span class="comment">// 设置 isa 指针的 weakly_referenced 位 / sidetable 中的 SIDE_TABLE_WEAKLY_REFERENCED 位</span></span><br><span class="line">            <span class="comment">// 标记此对象被 __weak 修饰的指针变量指向了，dealloc 时可以加速置 nil 处理</span></span><br><span class="line">            <span class="keyword">new</span><span class="type">Obj</span>-&gt;setWeaklyReferenced_nolock();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 设置 __weak 修饰的指针变量的值为 newObj</span></span><br><span class="line">        <span class="comment">// Do not set *location anywhere else. That would introduce a race.</span></span><br><span class="line">        *location = (id)<span class="keyword">new</span><span class="type">Obj</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="comment">// No new value. The storage is not changed.</span></span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    SideTable:<span class="type"></span>:unlockTwo&lt;haveOld, haveNew&gt;(oldTable, <span class="keyword">new</span><span class="type">Table</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> (id)<span class="keyword">new</span><span class="type">Obj</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p><figure class="highlight objectivec"><table><tr><td class="code"><pre><span class="line"><span class="keyword">id</span> </span><br><span class="line">weak_register_no_lock(weak_table_t *weak_table, <span class="keyword">id</span> referent_id, </span><br><span class="line">                      <span class="keyword">id</span> *referrer_id, <span class="keyword">bool</span> crashIfDeallocating)</span><br><span class="line">&#123;</span><br><span class="line">    objc_object *referent = (objc_object *)referent_id;</span><br><span class="line">    objc_object **referrer = (objc_object **)referrer_id;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (!referent  ||  referent-&gt;isTaggedPointer()) <span class="keyword">return</span> referent_id;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// ensure that the referenced object is viable</span></span><br><span class="line">    <span class="keyword">bool</span> deallocating;</span><br><span class="line">    <span class="keyword">if</span> (!referent-&gt;ISA()-&gt;hasCustomRR()) &#123;</span><br><span class="line">        deallocating = referent-&gt;rootIsDeallocating();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="built_in">BOOL</span> (*allowsWeakReference)(objc_object *, SEL) = </span><br><span class="line">            (<span class="built_in">BOOL</span>(*)(objc_object *, SEL))</span><br><span class="line">            object_getMethodImplementation((<span class="keyword">id</span>)referent, </span><br><span class="line">                                           SEL_allowsWeakReference);</span><br><span class="line">        <span class="keyword">if</span> ((IMP)allowsWeakReference == _objc_msgForward) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">nil</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        deallocating =</span><br><span class="line">            ! (*allowsWeakReference)(referent, SEL_allowsWeakReference);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (deallocating) &#123;</span><br><span class="line">        <span class="keyword">if</span> (crashIfDeallocating) &#123;</span><br><span class="line">            _objc_fatal(<span class="string">"Cannot form weak reference to instance (%p) of "</span></span><br><span class="line">                        <span class="string">"class %s. It is possible that this object was "</span></span><br><span class="line">                        <span class="string">"over-released, or is in the process of deallocation."</span>,</span><br><span class="line">                        (<span class="keyword">void</span>*)referent, object_getClassName((<span class="keyword">id</span>)referent));</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">nil</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// now remember it and where it is being stored</span></span><br><span class="line">    weak_entry_t *entry;</span><br><span class="line">    <span class="keyword">if</span> ((entry = weak_entry_for_referent(weak_table, referent))) &#123;</span><br><span class="line">        append_referrer(entry, referrer);</span><br><span class="line">    &#125; </span><br><span class="line">    <span class="keyword">else</span> &#123;</span><br><span class="line">        weak_entry_t new_entry(referent, referrer);</span><br><span class="line">        weak_grow_maybe(weak_table);</span><br><span class="line">        weak_entry_insert(weak_table, &amp;new_entry);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Do not set *referrer. objc_storeWeak() requires that the </span></span><br><span class="line">    <span class="comment">// value not change.</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> referent_id;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>最关键的代码在<strong><strong>weak_register_no_lock</strong></strong> 中，它会判断某个referent的weak_entry是否存在，如果存在则直接在这个weak_entry中添加referrer，否则新建一个weak_entry，加到weak_table中。</p><p><figure class="highlight haxe"><table><tr><td class="code"><pre><span class="line">weak_entry_t *entry;</span><br><span class="line"><span class="keyword">if</span> ((entry = weak_entry_for_referent(weak_table, referent))) &#123;</span><br><span class="line">    append_referrer(entry, referrer);</span><br><span class="line">&#125; </span><br><span class="line"><span class="keyword">else</span> &#123;</span><br><span class="line">    weak_entry_t <span class="keyword">new</span><span class="type">_entry</span>(referent, referrer);</span><br><span class="line">    weak_grow_maybe(weak_table);</span><br><span class="line">    weak_entry_insert(weak_table, &amp;<span class="keyword">new</span><span class="type">_entry</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p><strong><strong>销毁弱引用对象</strong></strong></p><p><figure class="highlight glsl"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span></span><br><span class="line">objc_destroyWeak(id *<span class="keyword">location</span>)</span><br><span class="line">&#123;</span><br><span class="line">    (<span class="type">void</span>)storeWeak&lt;DoHaveOld, DontHaveNew, DontCrashIfDeallocating&gt;</span><br><span class="line">        (<span class="keyword">location</span>, nil);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>销毁弱引用对象和创建弱引用对象入口实际上是一样的，都是storeWeak，只不过这里传入的值为nil。由于传入的是非 nil 新值，storeWeak会删除而不会新建关联信息。</p><p><figure class="highlight zephir"><table><tr><td class="code"><pre><span class="line">void weak_unregister_no_lock(weak_table_t *weak_table, id referent_id, </span><br><span class="line">                        id *referrer_id)</span><br><span class="line">&#123;</span><br><span class="line">    objc_object *referent = (objc_object *)referent_id;</span><br><span class="line">    objc_object **referrer = (objc_object **)referrer_id;</span><br><span class="line"></span><br><span class="line">    weak_entry_t *entry;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (!referent) <span class="keyword">return</span>;</span><br><span class="line">    <span class="comment">// 根据对象地址获取 entry</span></span><br><span class="line">    <span class="keyword">if</span> ((entry = weak_entry_for_referent(weak_table, referent))) &#123;</span><br><span class="line">        <span class="comment">// 移除 entry 中值为 referrer 的指针变量地址</span></span><br><span class="line">        remove_referrer(entry, referrer);</span><br><span class="line">        <span class="keyword">bool</span> <span class="keyword">empty</span> = <span class="keyword">true</span>;</span><br><span class="line">        <span class="comment">// entry 中是否有关联的指针变量地址</span></span><br><span class="line">        <span class="keyword">if</span> (entry-&gt;out_of_line()  &amp;&amp;  entry-&gt;num_refs != <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">empty</span> = <span class="keyword">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="keyword">for</span> (size_t i = <span class="number">0</span>; i &lt; WEAK_INLINE_COUNT; i++) &#123;</span><br><span class="line">                <span class="keyword">if</span> (entry-&gt;inline_referrers[i]) &#123;</span><br><span class="line">                    <span class="keyword">empty</span> = <span class="keyword">false</span>; </span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (<span class="keyword">empty</span>) &#123;</span><br><span class="line">            <span class="comment">// 如果 entry 是空的话，就从 weak_table 中移除掉</span></span><br><span class="line">            weak_entry_remove(weak_table, entry);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;iOS 这部分代码位于 &lt;strong&gt;&lt;strong&gt;objc-runtime/runtime/NSObject.mm&lt;/strong&gt;&lt;/strong&gt;:&lt;/p&gt;
&lt;h5&gt;1 对象的创建 alloc/init：&lt;/h5&gt;
&lt;p&gt;&lt;strong&gt;&lt;em&gt;调用栈&lt;/em&gt;&lt;
      
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>iOS开源库之coobjc使用</title>
    <link href="http://yoursite.com/2019/12/24/iOS%E5%BC%80%E6%BA%90%E5%BA%93%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90%E4%B9%8Bcoobjc/"/>
    <id>http://yoursite.com/2019/12/24/iOS开源库源码分析之coobjc/</id>
    <published>2019-12-24T10:33:42.000Z</published>
    <updated>2019-12-19T14:54:42.000Z</updated>
    
    <content type="html"><![CDATA[<h4>开源信息</h4><ul><li><a href="https://github.com/alibaba/coobjc" target="_blank" rel="noopener">coobjc 源码地址</a></li><li><a href="https://github.com/wangjufan/coobjc/blob/master/README_cn.md" target="_blank" rel="noopener">coobjc 文档地址</a></li><li><a href="https://dkandalov.github.io/async-await" target="_blank" rel="noopener">协程介绍</a></li></ul><p><img src="/2019/12/24/iOS开源库源码分析之coobjc/coobjc_icon.png" alt></p><p>coobjc 是由手淘架构团队推出的能在 iOS 上使用的协程开发框架，所以大家在了解coobjc之前必须先了解什么是协程，说到协程必定让人想起和它相关的两个概念<strong><strong>进程</strong></strong>和<strong><strong>线程</strong></strong>,这里不会对这两个概念进行长篇大论，概括地讲对操作系统来说，线程是最小的执行单元，进程是最小的资源管理单元。进程负责开拓各个资源，线程共享这些资源，每个线程有单独的堆栈空间。并且无论进程还是线程，都是由操作系统所管理的。而协程是用户态的微线程，协程不是被操作系统内核所管理，而完全是由程序所控制。</p><p>它的好处有哪些呢？</p><p>首先它可以以同步的方式写异步逻辑，可以避免“回调地域”现象，在性能方面调度性能更快，协程本身不需要进行内核级线程的切换，调度性能快，即使创建上万个协程也毫无压力。协程的使用以帮助开发减少锁、信号量的滥用。对于经常进行高并发处理的服务端协程是很实用的。但是这不意味着移动端就不需要协程了。就iOS开发而言Objective C中基于Block 的异步编程回调也会照成比较常见的“回调地域”。如果具备同步方式编写异步代码可以极大地改善我们代码的逻辑结构，并且由于协程的引入也可以减少因为锁的使用带来的性能损耗。</p><h4>整体架构</h4><p>下面是coobjc的整体架构图：</p><p><img src="/2019/12/24/iOS开源库源码分析之coobjc/framework_arch.png" alt></p><p><figure class="highlight cs"><table><tr><td class="code"><pre><span class="line">底层是协程的核心，包括堆栈切换管理，协程调度，协程之间channel通信实现。</span><br><span class="line">中间层是协程的封装，提供了 <span class="keyword">async</span>/<span class="keyword">await</span>, generator 和 Actor的支持。</span><br><span class="line">顶层是对系统库的一个扩展，是对Foundation 和 UIKit IO及其他耗时操作的封装</span><br></pre></td></tr></table></figure></p><p>coobjec中每个协程都是可以暂停和恢复的，并且每个协程都分配一个单独的内存区域用于存储它的调用栈，它有四个状态分别是：<strong><strong>READY</strong></strong>，<strong><strong>RUNNING</strong></strong>，<strong><strong>SUSPEND</strong></strong>，<strong><strong>DEAD</strong></strong>，并且在运行过程中可以多次在<strong><strong>RUNNING</strong></strong>，<strong><strong>SUSPEND</strong></strong>状态之间进行切换。下面是协程进行yield和resume操作的示意图：<img src="/2019/12/24/iOS开源库源码分析之coobjc/context.png" alt></p><p>coobjc内部使用一个调度器来负责用户所有协程的调度，它实际上是通过一个协程队列来进行管理，调度器会不断从队列中取出协程去执行。一旦队列中没有协程可以执行的时候，会切换到线程执行。所以当我们需要执行某个协程的时候，我们只需要将协程添加到某个线程的调度器队列中就可以了。调度器负责执行它。</p><p><img src="/2019/12/24/iOS开源库源码分析之coobjc/scheduler.png" alt></p><p>话不多说我们来看下coobjc的具体用法：</p><h4>coobjc 的使用</h4><p><strong><strong>1 创建协程</strong></strong></p><p>coobjc可以在当前线程或者指定的queue创建协程，在哪里创建就会在哪个线程进行调度，当然还可以在指定的队列创建和运行协程。</p><p><figure class="highlight less"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">co_launch</span>(^&#123;</span><br><span class="line">    <span class="comment">//默认在当前线程进行调度</span></span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="selector-tag">co_launch_onqueue</span>(q, ^&#123;</span><br><span class="line">    <span class="comment">//在指定的队列中运行的协程</span></span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure></p><p><strong><strong>2 取消协程</strong></strong></p><p>在coobjc中协程是可取消的，ObjC不建议使用异常，所以coobjc中不使用异常来取消协程。要取消协程可以调用CCOCoroutine的cancel方法。通过co_isCancelled宏来查询当前协程是否被取消了。</p><p><figure class="highlight kotlin"><table><tr><td class="code"><pre><span class="line">CCOCoroutine *co = co_launch(^&#123;</span><br><span class="line">    <span class="keyword">val</span>++;</span><br><span class="line">    co_delay(<span class="number">1.0</span>);</span><br><span class="line">    <span class="keyword">if</span>(co_isCancelled())&#123;</span><br><span class="line">        <span class="comment">//Do some cleaning operations</span></span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">val</span>++;</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line">[co cancel];</span><br></pre></td></tr></table></figure></p><p>在调用cancel后，协程的内部代码将不再继续执行，协程的内存将会被释放。</p><p><strong><strong>3. COPromise用法</strong></strong></p><p>await主要用于避免回调地狱现象，有时候我们会遇到请求两个不相关的数据接口后，将最终的数据组合后作为最终数据返回。如果不借助await的话，只能分别串行请求后最后将数据组合，这样会显得很低效。借助协程可以同时并行执行然后将数据合并。</p><ol><li>定义一个COPromise，在COPromise中通过fulfill 返回成功的结果，通过reject 返回失败的结果。返回的错误信息可以通过co_getError来获取。</li></ol><p><strong><strong>fulfill</strong></strong></p><p><figure class="highlight lisp"><table><tr><td class="code"><pre><span class="line">- (<span class="name">COPromise</span> *)testPromiseFullfill &#123;</span><br><span class="line">    COPromise *promise = [COPromise promise]<span class="comment">;</span></span><br><span class="line">    dispatch_async(<span class="name">dispatch_get_main_queue</span>(), ^&#123;</span><br><span class="line">        [promise fulfill:@<span class="string">"Hello coobjc!"</span>]<span class="comment">;</span></span><br><span class="line">    &#125;)<span class="comment">;</span></span><br><span class="line">    return promise<span class="comment">;</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">co_launch(^&#123;</span><br><span class="line">    id result = await([self testPromiseFullfill])<span class="comment">;</span></span><br><span class="line">    NSLog(@<span class="string">"result = %@"</span>,result)<span class="comment">;</span></span><br><span class="line">&#125;)<span class="comment">;</span></span><br></pre></td></tr></table></figure></p><p><strong><strong>reject</strong></strong></p><p><figure class="highlight objectivec"><table><tr><td class="code"><pre><span class="line">- (COPromise *)testPromiseReject &#123;</span><br><span class="line">    COPromise *promise = [COPromise promise];</span><br><span class="line">    <span class="built_in">dispatch_async</span>(dispatch_get_main_queue(), ^&#123;</span><br><span class="line">        [promise reject:[<span class="built_in">NSError</span> errorWithDomain:<span class="string">@"COPromise"</span> code:<span class="number">-100</span> userInfo:<span class="literal">nil</span>]];</span><br><span class="line">    &#125;);</span><br><span class="line">    <span class="keyword">return</span> promise;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">co_launch(^&#123;</span><br><span class="line">    <span class="keyword">id</span> result = await([<span class="keyword">self</span> testPromiseReject]);</span><br><span class="line">    <span class="built_in">NSError</span> *error = co_getError();</span><br><span class="line">    <span class="keyword">if</span>(error) &#123;</span><br><span class="line">        <span class="built_in">NSLog</span>(<span class="string">@"Error = %@"</span>,error);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="built_in">NSLog</span>(<span class="string">@"result = %@"</span>,result);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure></p><p><strong><strong>fullfill &amp;&amp; reject</strong></strong></p><p><figure class="highlight erlang"><table><tr><td class="code"><pre><span class="line">- <span class="params">(COPromise *)</span>testPromiseRejectFullfile &#123;</span><br><span class="line">    return [COPromise promise:^<span class="params">(COPromiseFulfill  _Nonnull fullfill, COPromiseReject  _Nonnull reject)</span> &#123;</span><br><span class="line">        dispatch_after<span class="params">(dispatch_time(DISPATCH_TIME_NOW, (int64_t)(<span class="number">2</span> * NSEC_PER_SEC)</span>), dispatch_get_main_queue<span class="params">()</span>, ^&#123;</span><br><span class="line">            fullfill<span class="params">(@<span class="string">"Say Hello To Coobjc"</span>)</span>;</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">co_launch<span class="params">(^&#123;</span></span><br><span class="line"><span class="params">    id res = await([self testPromiseRejectFullfile]);</span></span><br><span class="line"><span class="params">    NSLog(@<span class="string">"result = %@"</span>,res);</span></span><br><span class="line"><span class="params">&#125;)</span>;</span><br></pre></td></tr></table></figure></p><p><strong><strong>cancel</strong></strong></p><p><figure class="highlight lisp"><table><tr><td class="code"><pre><span class="line">- (<span class="name">COPromise</span> *)testPromiseCancel &#123;</span><br><span class="line">    COPromise *promise = [COPromise promise]<span class="comment">;</span></span><br><span class="line">    dispatch_async(<span class="name">dispatch_get_main_queue</span>(), ^&#123;</span><br><span class="line">        [promise cancel]<span class="comment">;</span></span><br><span class="line">    &#125;)<span class="comment">;</span></span><br><span class="line">    return promise<span class="comment">;</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">co_launch(^&#123;</span><br><span class="line">    COPromise *promise = [self testPromiseCancel];</span><br><span class="line">    [promise onCancel:^(COPromise * _Nonnull promise) &#123;</span><br><span class="line">        NSLog(@<span class="string">"isCancel"</span>)<span class="comment">;</span></span><br><span class="line">    &#125;]<span class="comment">;</span></span><br><span class="line">    </span><br><span class="line">    [promise catch:^(<span class="name">NSError</span> * _Nonnull error) &#123;</span><br><span class="line">        NSLog(@"NSError");</span><br><span class="line">    &#125;];</span><br><span class="line">    id res = await(promise);</span><br><span class="line">    if(!res) &#123;</span><br><span class="line">        NSError *error = co_getError()<span class="comment">;</span></span><br><span class="line">        BOOL isPromiseCancel = [COPromise isPromiseCancelled<span class="symbol">:error</span>]<span class="comment">;</span></span><br><span class="line">        NSLog(@<span class="string">"isPromiseCancel %ld"</span>,isPromiseCancel)<span class="comment">;</span></span><br><span class="line">        return<span class="comment">;</span></span><br><span class="line">    &#125;</span><br><span class="line">    NSLog(@<span class="string">"result = %@"</span>,res)<span class="comment">;</span></span><br><span class="line">&#125;)<span class="comment">;</span></span><br></pre></td></tr></table></figure></p><p><strong><strong>then</strong></strong></p><p><figure class="highlight objectivec"><table><tr><td class="code"><pre><span class="line">co_launch(^&#123;</span><br><span class="line">    COPromise *promise = [<span class="keyword">self</span> testPromiseFullfill];</span><br><span class="line">    COPromise *thenPromise = [promise then:^<span class="keyword">id</span> _Nullable(<span class="keyword">id</span>  _Nullable value) &#123;</span><br><span class="line">        <span class="keyword">return</span> [<span class="keyword">self</span> testPromiseThen];</span><br><span class="line">    &#125;];</span><br><span class="line"></span><br><span class="line">    <span class="keyword">id</span> res = await(thenPromise);</span><br><span class="line">    <span class="keyword">if</span>(!res) &#123;</span><br><span class="line">        <span class="built_in">NSError</span> *error = co_getError();</span><br><span class="line">        <span class="built_in">BOOL</span> isPromiseCancel = [COPromise isPromiseCancelled:error];</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@"result = %@"</span>,res);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure></p><p><strong><strong>批量等待</strong></strong></p><p><figure class="highlight armasm"><table><tr><td class="code"><pre><span class="line"><span class="symbol">co_launch</span>(^&#123;</span><br><span class="line">    NSArray *<span class="keyword">batchFullfile </span>=</span><br><span class="line">    <span class="keyword">batch_await(@[</span></span><br><span class="line"><span class="keyword"> </span>       [<span class="keyword">self </span>testPromiseFullfill01],</span><br><span class="line">        [<span class="keyword">self </span>testPromiseFullfill02],</span><br><span class="line">        [<span class="keyword">self </span>testPromiseFullfill03],</span><br><span class="line">    ])<span class="comment">;</span></span><br><span class="line">    </span><br><span class="line">    <span class="meta">if</span>(<span class="keyword">batchFullfile </span>&amp;&amp; <span class="keyword">batchFullfile.count) </span>&#123;</span><br><span class="line">        NSLog(<span class="comment">@"%@",batchFullfile);</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;)<span class="comment">;</span></span><br></pre></td></tr></table></figure></p><p>我们来看下COPromise的一些关键属性和方法：</p><p>关键属性：</p><p><figure class="highlight objectivec"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> Tell the promise is pending or not.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">@property</span>(<span class="keyword">nonatomic</span>, <span class="keyword">readonly</span>) <span class="built_in">BOOL</span> isPending;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> Tell the promise is fulfilled or not.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">@property</span>(<span class="keyword">nonatomic</span>, <span class="keyword">readonly</span>) <span class="built_in">BOOL</span> isFulfilled;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> Tell the promise is rejected or not.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">@property</span>(<span class="keyword">nonatomic</span>, <span class="keyword">readonly</span>) <span class="built_in">BOOL</span> isRejected;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> If fulfilled, value store into this property.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">@property</span>(<span class="keyword">nonatomic</span>, <span class="keyword">readonly</span>, <span class="keyword">nullable</span>) Value value;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> If reject, error store into this property</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">@property</span>(<span class="keyword">nonatomic</span>, <span class="keyword">readonly</span>, <span class="keyword">nullable</span>) <span class="built_in">NSError</span> *error;</span><br></pre></td></tr></table></figure></p><p>关键构造方法：</p><p><figure class="highlight smali"><table><tr><td class="code"><pre><span class="line">/**</span><br><span class="line"> Create a promise without<span class="keyword"> constructor</span>. Which means, you should control when the job begins.</span><br><span class="line"></span><br><span class="line"> @return The `COPromise` instance</span><br><span class="line"> */</span><br><span class="line">+ (instancetype)promise;</span><br><span class="line">/**</span><br><span class="line"> Create a promise with<span class="keyword"> constructor</span>. the job begans when someone observing on it.</span><br><span class="line"></span><br><span class="line"> @param<span class="keyword"> constructor</span> the<span class="keyword"> constructor</span> block.</span><br><span class="line"> @return The `COPromise` instance</span><br><span class="line"> */</span><br><span class="line">+ (instancetype)promise:(COPromiseConstructor)constructor;</span><br><span class="line">/**</span><br><span class="line"> Create a promise with<span class="keyword"> constructor</span>. the job begans when someone observing on it.</span><br><span class="line"> </span><br><span class="line"> @param<span class="keyword"> constructor</span> the<span class="keyword"> constructor</span> block.</span><br><span class="line"> @param queue the dispatch_queue_t that the job run.</span><br><span class="line"> @return The `COPromise` instance</span><br><span class="line"> */</span><br><span class="line">+ (instancetype)promise:(COPromiseConstructor)constructor onQueue:(dispatch_queue_t _Nullable )queue;</span><br></pre></td></tr></table></figure></p><p>关键方法：</p><p><figure class="highlight groovy"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> Fulfill the promise with a return value.</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment"> <span class="doctag">@param</span> value the value fulfilled.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line">- (<span class="keyword">void</span>)<span class="string">fulfill:</span>(nullable Value)value;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> Reject the promise with a error</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment"> <span class="doctag">@param</span> error the error.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line">- (<span class="keyword">void</span>)<span class="string">reject:</span>(NSError * _Nullable)error;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> Cancel the job.</span></span><br><span class="line"><span class="comment"> </span></span><br><span class="line"><span class="comment"> <span class="doctag">@discussion</span> If you want a `COPromise` be cancellable, you must make the job cancel in `onCancel:`.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line">- (<span class="keyword">void</span>)cancel;</span><br></pre></td></tr></table></figure></p><p><figure class="highlight zephir"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> Set the onCancelBlock.</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment"> <span class="doctag">@param</span> onCancelBlock will execute on the promise cancelled.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line">- (void)onCancel:(COPromiseOnCancelBlock _Nullable )onCancelBlock;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> Chained observe the promise fulfilled.</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment"> <span class="doctag">@param</span> work the observer worker.</span></span><br><span class="line"><span class="comment"> <span class="doctag">@return</span> The chained promise instance.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line">- (COPromise *)then:(COPromiseThenWorkBlock)work;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> Observe the promises rejected.</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment"> <span class="doctag">@param</span> reject the reject dealing worker.</span></span><br><span class="line"><span class="comment"> <span class="doctag">@return</span> The chained promise instance.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line">- (COPromise *)<span class="keyword">catch</span>:(COPromiseCatchWorkBlock)reject;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> Tell if the error is promise cancelled error</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment"> <span class="doctag">@param</span> error the error object</span></span><br><span class="line"><span class="comment"> <span class="doctag">@return</span> is cancellled error.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line">+ (<span class="keyword">BOOL</span>)isPromiseCancelled:(NSError *)error;</span><br></pre></td></tr></table></figure></p><p><strong><strong>4.COProgressPromise用法</strong></strong></p><p><figure class="highlight objectivec"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> COProgressPromise* progressDownloadFileFromUrl(<span class="built_in">NSString</span> *url)&#123;</span><br><span class="line">    COProgressPromise *promise = [COProgressPromise promise];</span><br><span class="line">    [<span class="built_in">NSURLSession</span> sharedSession].configuration.requestCachePolicy = <span class="built_in">NSURLRequestReloadIgnoringCacheData</span>;</span><br><span class="line">    <span class="built_in">NSURLSessionDataTask</span> *task = [[<span class="built_in">NSURLSession</span> sharedSession] dataTaskWithURL:[<span class="built_in">NSURL</span> URLWithString:url] completionHandler:^(<span class="built_in">NSData</span> * _Nullable data, <span class="built_in">NSURLResponse</span> * _Nullable response, <span class="built_in">NSError</span> * _Nullable error) &#123;</span><br><span class="line">        <span class="keyword">if</span> (error) &#123;</span><br><span class="line">            [promise reject:error];</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span>&#123;</span><br><span class="line">            [promise fulfill:data];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;];</span><br><span class="line">    [task resume];</span><br><span class="line">    <span class="keyword">if</span> (@available(iOS <span class="number">11.0</span>, *)) &#123;</span><br><span class="line">        [promise setupWithProgress:task.progress];</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="comment">// Fallback on earlier versions</span></span><br><span class="line">        <span class="built_in">NSProgress</span> *progress = [<span class="built_in">NSProgress</span> progressWithTotalUnitCount:<span class="number">10</span>];</span><br><span class="line">        [promise setupWithProgress:progress];</span><br><span class="line">        dispatch_source_t timer = <span class="literal">nil</span>;</span><br><span class="line">        <span class="built_in">dispatch_queue_t</span> queue = dispatch_get_global_queue(DISPATCH_QUEUE_PRIORITY_DEFAULT, <span class="number">0</span>);</span><br><span class="line">        timer = dispatch_source_create(DISPATCH_SOURCE_TYPE_TIMER, <span class="number">0</span>, <span class="number">0</span>, queue);</span><br><span class="line">        dispatch_source_set_timer(timer, dispatch_walltime(<span class="literal">NULL</span>, <span class="number">0</span>), <span class="number">0.1</span> * <span class="built_in">NSEC_PER_SEC</span>, <span class="number">0</span> * <span class="built_in">NSEC_PER_SEC</span>);</span><br><span class="line">        dispatch_source_set_event_handler(timer, ^&#123;</span><br><span class="line">            <span class="keyword">if</span> (progress.completedUnitCount &lt; progress.totalUnitCount) &#123;</span><br><span class="line">                progress.completedUnitCount += <span class="number">1</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span>&#123;</span><br><span class="line">                dispatch_source_cancel(timer);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">        dispatch_resume(timer);</span><br><span class="line">        [promise onCancel:^(COPromise * _Nonnull promise) &#123;</span><br><span class="line">            dispatch_source_cancel(timer);</span><br><span class="line">        &#125;];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> promise;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">co_launch(^&#123;</span><br><span class="line">    <span class="keyword">int</span> progressCount = <span class="number">0</span>;</span><br><span class="line">    COProgressPromise *promise = progressDownloadFileFromUrl(<span class="string">@"http://img17.3lian.com/d/file/201701/17/9a0d018ba683b9cbdcc5a7267b90891c.jpg"</span>);</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">id</span> p <span class="keyword">in</span> promise)&#123;</span><br><span class="line">        <span class="keyword">double</span> v = [p doubleValue];</span><br><span class="line">        <span class="built_in">NSLog</span>(<span class="string">@"current progress: %f"</span>, (<span class="keyword">float</span>)v);</span><br><span class="line">        progressCount++;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">NSData</span> *data = await(promise);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure></p><p>上述的这些co_launch都可以在任何子线程中运行。</p><p><strong><strong>5. COChan用法</strong></strong></p><p>COChan 主要用于协程之间进行数据传输，它有阻塞和非阻塞两种方式收发数据，针对channel，可以有无缓存类型，有缓存类型，以及无限缓存类型，下面会进行详细介绍。</p><p><img src="/2019/12/24/iOS开源库源码分析之coobjc/channel1.png" alt></p><p>和Promise类似的用法：</p><p><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">- (COChan&lt;id&gt; *)co_fetchSomething &#123;</span><br><span class="line">    COChan *<span class="keyword">chan</span> = [COChan <span class="keyword">chan</span>];</span><br><span class="line">    dispatch_async(_someQueue, ^&#123;</span><br><span class="line">       <span class="comment">// fetch result operations</span></span><br><span class="line">       ...</span><br><span class="line">       [<span class="keyword">chan</span> send_nonblock:result];</span><br><span class="line">    &#125;);</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">chan</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// calling in a coroutine.</span></span><br><span class="line">co_launch(^&#123;</span><br><span class="line">    id ret = await([self co_fetchSomething]);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure></p><p>作为生产者消费者的用法：</p><p><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">COChan *<span class="keyword">chan</span> = [COChan chanWithBuffCount:<span class="number">10</span>];</span><br><span class="line">co_launch(^&#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">10</span>; i++) &#123;</span><br><span class="line">        [<span class="keyword">chan</span> send_nonblock:@(i)];</span><br><span class="line">    &#125;</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line">co_launch(^&#123;</span><br><span class="line">    <span class="comment">//NSArray *data = [chan receiveAll];</span></span><br><span class="line">    <span class="comment">//NSLog(@"%@",data);</span></span><br><span class="line">    </span><br><span class="line">    NSArray *dataWithCount = [<span class="keyword">chan</span> receiveWithCount:<span class="number">3</span>];</span><br><span class="line">    NSLog(@<span class="string">"%@"</span>,dataWithCount);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure></p><p>当COChan容量为0的时候，发送数据是阻塞的，只有在有人接收数据后才会执行send之后的代码<figure class="highlight sqf"><table><tr><td class="code"><pre><span class="line">COChan *chan = [COChan chanWithBuffCount:<span class="number">0</span>];</span><br><span class="line"><span class="variable">__block</span> NSInteger <span class="built_in">step</span> = <span class="number">0</span>;</span><br><span class="line">co_launch(^&#123;</span><br><span class="line">    <span class="built_in">step</span> = <span class="number">1</span>;<span class="comment">//@1</span></span><br><span class="line">    [chan send:@<span class="number">111</span>]; <span class="comment">//代码会停在这里直到receive_nonblock消费数据后才继续往下执行 //@2</span></span><br><span class="line">    <span class="built_in">step</span> = <span class="number">2</span>;<span class="comment">//@6</span></span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line">co_launch(^&#123;</span><br><span class="line">    <span class="built_in">step</span> = <span class="number">3</span>;<span class="comment">//@3</span></span><br><span class="line">    id value = [chan receive_nonblock];<span class="comment">//@4</span></span><br><span class="line">    <span class="built_in">step</span> = <span class="number">4</span>;<span class="comment">//@5</span></span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure></p><p>上面数字代表的是代码的执行顺序。</p><p>当将COChan容量改为非0的时候执行顺序如下：<figure class="highlight sqf"><table><tr><td class="code"><pre><span class="line">COChan *chan = [COChan chanWithBuffCount:<span class="number">2</span>];</span><br><span class="line"><span class="variable">__block</span> NSInteger <span class="built_in">step</span> = <span class="number">0</span>;</span><br><span class="line">co_launch(^&#123;</span><br><span class="line">    <span class="built_in">step</span> = <span class="number">1</span>;<span class="comment">//@1</span></span><br><span class="line">    [chan send:@<span class="number">111</span>]; <span class="comment">//代码不会阻塞 //@2</span></span><br><span class="line">    <span class="built_in">step</span> = <span class="number">2</span>;<span class="comment">//@3</span></span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line">co_launch(^&#123;</span><br><span class="line">    <span class="built_in">step</span> = <span class="number">3</span>;<span class="comment">//@4</span></span><br><span class="line">    id value = [chan receive_nonblock];<span class="comment">//@5</span></span><br><span class="line">    <span class="built_in">step</span> = <span class="number">4</span>;<span class="comment">//@6</span></span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure></p><p>如果COChan中没有数据的时候调用receive将会阻塞：</p><p><figure class="highlight elixir"><table><tr><td class="code"><pre><span class="line">__block NSInteger step = 0;</span><br><span class="line">COChan *chan = [COChan chan];</span><br><span class="line">co_launch(^&#123;</span><br><span class="line">    step = <span class="number">1</span>;<span class="regexp">//</span><span class="variable">@1</span></span><br><span class="line">    id value = [chan receive];<span class="regexp">//</span><span class="variable">@2</span></span><br><span class="line">    step = <span class="number">2</span>;<span class="regexp">//</span><span class="variable">@6</span></span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line">co_launch(^&#123;</span><br><span class="line">    step = <span class="number">3</span>;<span class="regexp">//</span><span class="variable">@3</span></span><br><span class="line">    [chan <span class="symbol">send:</span><span class="variable">@111</span>];<span class="regexp">//</span><span class="variable">@4</span></span><br><span class="line">    step = <span class="number">4</span>;<span class="regexp">//</span><span class="variable">@5</span></span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure></p><p>也就是send方法会往COChan中通道缓存中添加消息，如果满的话则会阻塞，直到数据被receive之后，才会继续执行。<img src="/2019/12/24/iOS开源库源码分析之coobjc/channel2.png" alt>receive会不断从通道缓存中获取数据，如果数据被取完后就阻塞，直到有数据send到缓存的时候，才会继续执行。这是很典型的生产者消费者模型。send_nonblock会完缓存通道送数据后立刻返回，如果缓存满了就会丢弃数据，不会阻塞。看下coobjc给出的两个测试用例：</p><p><figure class="highlight sqf"><table><tr><td class="code"><pre><span class="line">it(@<span class="string">"send non blocking will not block the coroutine."</span>, ^&#123;</span><br><span class="line">    <span class="variable">__block</span> NSInteger <span class="built_in">step</span> = <span class="number">0</span>;</span><br><span class="line">    COChan *chan = [COChan chanWithBuffCount:<span class="number">1</span>];</span><br><span class="line">    co_launch(^&#123;</span><br><span class="line">        <span class="built_in">step</span> = <span class="number">1</span>;</span><br><span class="line">        [chan send_nonblock:@<span class="number">111</span>];</span><br><span class="line">        expect(<span class="built_in">step</span>).<span class="keyword">to</span>.equal(<span class="number">1</span>);</span><br><span class="line">        <span class="built_in">step</span> = <span class="number">2</span>;</span><br><span class="line">    &#125;);</span><br><span class="line">    </span><br><span class="line">    co_launch(^&#123;</span><br><span class="line">        expect(<span class="built_in">step</span>).<span class="keyword">to</span>.equal(<span class="number">2</span>);</span><br><span class="line">        <span class="built_in">step</span> = <span class="number">3</span>;</span><br><span class="line">        id value = [chan receive_nonblock];</span><br><span class="line">        expect(<span class="built_in">step</span>).<span class="keyword">to</span>.equal(<span class="number">3</span>);</span><br><span class="line">        expect(value).<span class="keyword">to</span>.equal(@<span class="number">111</span>);</span><br><span class="line">    &#125;);</span><br><span class="line">    <span class="keyword">waitUntil</span>(^(DoneCallback done) &#123;</span><br><span class="line">        dispatch_async(dispatch_get_main_queue(), ^&#123;</span><br><span class="line">            expect(<span class="built_in">step</span>).<span class="keyword">to</span>.equal(<span class="number">3</span>);</span><br><span class="line">            done();</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line">it(@<span class="string">"Channel buff is full, send non blocking will abandon the value."</span>, ^&#123;</span><br><span class="line">    <span class="variable">__block</span> NSInteger <span class="built_in">step</span> = <span class="number">0</span>;</span><br><span class="line">    </span><br><span class="line">    COChan *chan = [COChan chan];</span><br><span class="line">    co_launch(^&#123;</span><br><span class="line">        <span class="built_in">step</span> = <span class="number">1</span>;</span><br><span class="line">        [chan send_nonblock:@<span class="number">111</span>];</span><br><span class="line">        expect(<span class="built_in">step</span>).<span class="keyword">to</span>.equal(<span class="number">1</span>);</span><br><span class="line">        <span class="built_in">step</span> = <span class="number">2</span>;</span><br><span class="line">    &#125;);</span><br><span class="line">    co_launch(^&#123;</span><br><span class="line">        expect(<span class="built_in">step</span>).<span class="keyword">to</span>.equal(<span class="number">2</span>);</span><br><span class="line">        <span class="built_in">step</span> = <span class="number">3</span>;</span><br><span class="line">        id value = [chan receive_nonblock];</span><br><span class="line">        expect(<span class="built_in">step</span>).<span class="keyword">to</span>.equal(<span class="number">3</span>);</span><br><span class="line">        expect(value).<span class="keyword">to</span>.equal(<span class="literal">nil</span>);</span><br><span class="line">    &#125;);</span><br><span class="line">    <span class="keyword">waitUntil</span>(^(DoneCallback done) &#123;</span><br><span class="line">        dispatch_async(dispatch_get_main_queue(), ^&#123;</span><br><span class="line">            expect(<span class="built_in">step</span>).<span class="keyword">to</span>.equal(<span class="number">3</span>);</span><br><span class="line">            done();</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure></p><p>我们看下接收被阻塞的例子：</p><p><figure class="highlight autoit"><table><tr><td class="code"><pre><span class="line">it(@<span class="string">"receive can block muti coroutine."</span>, ^&#123;</span><br><span class="line">    __block NSInteger <span class="keyword">step</span> = <span class="number">0</span><span class="comment">;</span></span><br><span class="line">    </span><br><span class="line">    COChan *chan = [COChan chanWithBuffCount:<span class="number">1</span>]<span class="comment">;</span></span><br><span class="line">    </span><br><span class="line">    co_launch(^&#123;</span><br><span class="line">        <span class="keyword">step</span> = <span class="number">1</span><span class="comment">;</span></span><br><span class="line">        id value = [chan receive]<span class="comment">;</span></span><br><span class="line">        expect(<span class="keyword">step</span>).<span class="keyword">to</span>.equal(<span class="number">7</span>)<span class="comment">;</span></span><br><span class="line">        expect(value).<span class="keyword">to</span>.equal(<span class="symbol">@111</span>)<span class="comment">;</span></span><br><span class="line">        <span class="keyword">step</span> = <span class="number">2</span><span class="comment">;</span></span><br><span class="line">    &#125;)<span class="comment">;</span></span><br><span class="line">    </span><br><span class="line">    co_launch(^&#123;</span><br><span class="line">        expect(<span class="keyword">step</span>).<span class="keyword">to</span>.equal(<span class="number">1</span>)<span class="comment">;</span></span><br><span class="line">        <span class="keyword">step</span> = <span class="number">3</span><span class="comment">;</span></span><br><span class="line">        id value = [chan receive]<span class="comment">;</span></span><br><span class="line">        expect(<span class="keyword">step</span>).<span class="keyword">to</span>.equal(<span class="number">2</span>)<span class="comment">;</span></span><br><span class="line">        expect(value).<span class="keyword">to</span>.equal(<span class="symbol">@222</span>)<span class="comment">;</span></span><br><span class="line">        <span class="keyword">step</span> = <span class="number">4</span><span class="comment">;</span></span><br><span class="line">    &#125;)<span class="comment">;</span></span><br><span class="line"></span><br><span class="line">    co_launch(^&#123;</span><br><span class="line">        expect(<span class="keyword">step</span>).<span class="keyword">to</span>.equal(<span class="number">3</span>)<span class="comment">;</span></span><br><span class="line">        <span class="keyword">step</span> = <span class="number">5</span><span class="comment">;</span></span><br><span class="line">        [chan <span class="built_in">send</span>:<span class="symbol">@111]</span><span class="comment">;</span></span><br><span class="line">        expect(<span class="keyword">step</span>).<span class="keyword">to</span>.equal(<span class="number">5</span>)<span class="comment">;</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">step</span> = <span class="number">6</span><span class="comment">;</span></span><br><span class="line">        [chan <span class="built_in">send</span>:<span class="symbol">@222]</span><span class="comment">;</span></span><br><span class="line">        expect(<span class="keyword">step</span>).<span class="keyword">to</span>.equal(<span class="number">6</span>)<span class="comment">;</span></span><br><span class="line">        <span class="keyword">step</span> = <span class="number">7</span><span class="comment">;</span></span><br><span class="line">    &#125;)<span class="comment">;</span></span><br><span class="line">    waitUntil(^(DoneCallback done) &#123;</span><br><span class="line">        dispatch_async(dispatch_get_main_queue(), ^&#123;</span><br><span class="line">            expect(<span class="keyword">step</span>).<span class="keyword">to</span>.equal(<span class="number">4</span>)<span class="comment">;</span></span><br><span class="line">            done()<span class="comment">;</span></span><br><span class="line">        &#125;)<span class="comment">;</span></span><br><span class="line">    &#125;)<span class="comment">;</span></span><br><span class="line">&#125;)<span class="comment">;</span></span><br></pre></td></tr></table></figure></p><p>上面的例子中缓存通道容量都是指定的，还可以使用可扩展的通道expandableChan：</p><p><figure class="highlight jboss-cli"><table><tr><td class="code"><pre><span class="line">it<span class="params">(@"expandableChan will not abandon values.", ^&#123;</span></span><br><span class="line"><span class="params">    __block NSInteger <span class="attr">receiveCount</span> = 0;</span></span><br><span class="line"><span class="params">    __block NSInteger <span class="attr">receiveValue</span> = 0;</span></span><br><span class="line"><span class="params">    __block NSInteger <span class="attr">sendCount</span> = 0;</span></span><br><span class="line"><span class="params">    COChan *<span class="attr">chan</span> = [COChan expandableChan];</span></span><br><span class="line"><span class="params">    for (int <span class="attr">i</span> = 0; i &lt; 2000; i++)</span> &#123;</span><br><span class="line">        co_launch<span class="params">(^&#123;</span></span><br><span class="line"><span class="params">            [chan send:@(i)</span>];</span><br><span class="line">            sendCount++;</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    for <span class="params">(int <span class="attr">i</span> = 0; i &lt; 2000; i++)</span> &#123;</span><br><span class="line">        co_launch<span class="params">(^&#123;</span></span><br><span class="line"><span class="params">            id <span class="attr">value</span> = [chan receive];</span></span><br><span class="line"><span class="params">            receiveCount++;</span></span><br><span class="line"><span class="params">            receiveValue+=[value integerValue];</span></span><br><span class="line"><span class="params">        &#125;)</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    waitUntilTimeout<span class="params">(100, ^(DoneCallback done)</span> &#123;</span><br><span class="line">        dispatch_after<span class="params">(dispatch_time(DISPATCH_TIME_NOW, (int64_t)</span><span class="params">(10 * NSEC_PER_SEC)</span>), dispatch_get_main_queue<span class="params">()</span>, ^&#123;</span><br><span class="line">            expect<span class="params">(receiveCount)</span><span class="string">.to.equal</span><span class="params">(2000)</span>;</span><br><span class="line">            expect<span class="params">(receiveValue)</span><span class="string">.to.equal</span><span class="params">(1999000)</span>;</span><br><span class="line">            expect<span class="params">(sendCount)</span><span class="string">.to.equal</span><span class="params">(2000)</span>;</span><br><span class="line">            done<span class="params">()</span>;</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure></p><p>在进入下一个主题之前我们先回顾下OCChannel接口：</p><p>构造方法：</p><p><figure class="highlight elm"><table><tr><td class="code"><pre><span class="line">/**</span><br><span class="line"> 容量为<span class="number">0</span>的消息通道</span><br><span class="line"> */</span><br><span class="line">+ (instance<span class="keyword">type</span>)chan;</span><br><span class="line"></span><br><span class="line">/**</span><br><span class="line">指定容量的消息通道</span><br><span class="line"> */</span><br><span class="line">+ (instance<span class="keyword">type</span>)chanWithBuffCount:(int32_t)buffCount;</span><br><span class="line"></span><br><span class="line">/**</span><br><span class="line"> 创建一个可扩展的通道，通道的缓存大小是可以扩展的，这样send就不会阻塞当前进程，并且发送的数据将不会丢失</span><br><span class="line"> */</span><br><span class="line">+ (instance<span class="keyword">type</span>)expandableChan;</span><br></pre></td></tr></table></figure></p><p>往通道发送数据：</p><p><figure class="highlight less"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> 发送一个数据到通道上，这个方法会在缓存已满，并且没有接收者的情况下阻塞当前协程</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="selector-tag">-</span> (void)<span class="selector-tag">send</span><span class="selector-pseudo">:(Value</span> <span class="selector-tag">_Nullable</span> )<span class="selector-tag">val</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> 和上面一致，只不过会在协程取消的时候调用cancelBlock</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="selector-tag">-</span> (void)<span class="selector-tag">send</span><span class="selector-pseudo">:(Value</span> <span class="selector-tag">_Nullable</span> )<span class="selector-tag">val</span> <span class="selector-tag">onCancel</span><span class="selector-pseudo">:(COChanOnCancelBlock</span> <span class="selector-tag">_Nullable</span>)<span class="selector-tag">cancelBlock</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> 非阻塞式得向通道发送数据：</span></span><br><span class="line"><span class="comment"> 如果有接收者则发送</span></span><br><span class="line"><span class="comment"> 如果没有接收者，但是缓存还没满则存在缓存中</span></span><br><span class="line"><span class="comment"> 如果没有接收者，并且缓存已经满了，则丢弃数据</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="selector-tag">-</span> (void)<span class="selector-tag">send_nonblock</span><span class="selector-pseudo">:(Value</span> <span class="selector-tag">_Nullable</span> )<span class="selector-tag">val</span>;</span><br></pre></td></tr></table></figure></p><p>接收数据</p><p><figure class="highlight less"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> 阻塞式从通道中获取数据，这个方法会一直阻塞，直到有人往通道上发送数据为止</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="selector-tag">-</span> (Value _Nullable )<span class="selector-tag">receive</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> 和上面一致，只不过会在协程取消的时候调用cancelBlock</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="selector-tag">-</span> (Value _Nullable )<span class="selector-tag">receiveWithOnCancel</span><span class="selector-pseudo">:(COChanOnCancelBlock</span> <span class="selector-tag">_Nullable</span>)<span class="selector-tag">cancelBlock</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> 非阻塞式接收数据</span></span><br><span class="line"><span class="comment"> 如果缓存中有数据则从缓存中直接取数据后返回</span></span><br><span class="line"><span class="comment"> 如果缓存是空的，但是刚好有人发送了数据，则直接接收</span></span><br><span class="line"><span class="comment"> 如果缓存是空的，但是没人发送数据，这时候返回nil</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="selector-tag">-</span> (Value _Nullable)<span class="selector-tag">receive_nonblock</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> 阻塞接收通道的所有数据</span></span><br><span class="line"><span class="comment"> 1. 如果通道没有数据，那么会阻塞等待直到有一个数据为止</span></span><br><span class="line"><span class="comment"> 2. 如果通道内有数据则返回所有的值</span></span><br><span class="line"><span class="comment"> 3. 如果发送了nil，那么接送到数据将会是 [NSNull null]，因此需要注意检测返回数组中的类型</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="selector-tag">-</span> (NSArray&lt;Value&gt; * _Nonnull)<span class="selector-tag">receiveAll</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> 阻塞式接收指定数量的数据，如果通道缓存中没有足够的数据那么就会一直阻塞。</span></span><br><span class="line"><span class="comment"> 如果发送了nil，那么接送到数据将会是 [NSNull null]，因此需要注意检测返回数组中的类型</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="selector-tag">-</span> (NSArray&lt;Value&gt; * _Nonnull)<span class="selector-tag">receiveWithCount</span><span class="selector-pseudo">:(NSUInteger)count</span>;</span><br></pre></td></tr></table></figure></p><p><strong><strong>6. COActor用法</strong></strong></p><blockquote><p>Actor 的概念来自于 Erlang ，在 AKKA 中，可以认为一个 Actor 就是一个容器，用以存储状态、行为、Mailbox 以及子 Actor 与 Supervisor 策略。Actor 之间并不直接通信，而是通过 Mail 来互通有无</p></blockquote><p><img src="/2019/12/24/iOS开源库源码分析之coobjc/000001.png" alt></p><p><figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line"><span class="bullet">* </span>Mailbox: 用于存储消息的队列</span><br><span class="line"><span class="bullet">* </span>Isolated State: actor的状态以及内部变量等。</span><br><span class="line"><span class="bullet">* </span>message: 消息，类似于方法调用</span><br><span class="line">Actor模型有两个特点：</span><br><span class="line"><span class="bullet">1. </span>在单个线程中的每个Actor顺序处理发送给它的消息</span><br><span class="line"><span class="bullet">2. </span>不同的Actors同时并行运行</span><br></pre></td></tr></table></figure></p><p>Actor的创建：</p><p><figure class="highlight lisp"><table><tr><td class="code"><pre><span class="line">co_actor(^(<span class="name">COActorChan</span> *chan) &#123;</span><br><span class="line">    val = 1;</span><br><span class="line">    XCTAssert(chan != nil);</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line">co_actor_onqueue(get_test_queue(), ^(COActorChan *chan) &#123;</span><br><span class="line">    val1 = <span class="number">1</span><span class="comment">;</span></span><br><span class="line">    XCTAssert(<span class="name">chan</span> != <span class="literal">nil</span>)<span class="comment">;</span></span><br><span class="line">&#125;)<span class="comment">;</span></span><br></pre></td></tr></table></figure></p><p><figure class="highlight protobuf"><table><tr><td class="code"><pre><span class="line">it(@<span class="string">"next in chan"</span>, ^&#123;</span><br><span class="line">    __block int val = <span class="number">0</span>;</span><br><span class="line">    COActor* actor = co_actor(^(COActorChan *chan) &#123;</span><br><span class="line">        int tmpVal = <span class="number">0</span>;</span><br><span class="line">        COActorMessage *message = nil;</span><br><span class="line">        while((<span class="class"><span class="keyword">message</span> = [<span class="title">chan</span> next]))</span>&#123;</span><br><span class="line">            if([<span class="class"><span class="keyword">message</span> <span class="title">intType</span>] == 1)</span>&#123;</span><br><span class="line">                tmpVal++;</span><br><span class="line">            &#125;</span><br><span class="line">            else if([<span class="class"><span class="keyword">message</span> <span class="title">intType</span>] == -1)</span>&#123;</span><br><span class="line">                tmpVal--;</span><br><span class="line">            &#125;</span><br><span class="line">            else if([<span class="class"><span class="keyword">message</span> <span class="title">intType</span>] == 2)</span>&#123;</span><br><span class="line">                <span class="comment">//返回数据</span></span><br><span class="line">                <span class="class"><span class="keyword">message</span>.<span class="title">complete</span>(@(tmpVal));</span></span><br><span class="line"><span class="class">            &#125;</span></span><br><span class="line"><span class="class">        &#125;</span></span><br><span class="line"><span class="class">    &#125;);</span></span><br><span class="line"><span class="class">    co_launch(^</span>&#123;</span><br><span class="line">        [actor sendMessage:@(<span class="number">1</span>)];</span><br><span class="line">        [actor sendMessage:@(<span class="number">1</span>)];</span><br><span class="line">        [actor sendMessage:@(<span class="number">1</span>)];</span><br><span class="line">        [actor sendMessage:@(<span class="number">1</span>)];</span><br><span class="line">        [actor sendMessage:@(<span class="number">1</span>)];</span><br><span class="line">        [actor sendMessage:@(<span class="number">1</span>)];</span><br><span class="line">        </span><br><span class="line">        [actor sendMessage:@(-<span class="number">1</span>)];</span><br><span class="line">        </span><br><span class="line">        COActorCompletable *completable = [actor sendMessage:@(<span class="number">2</span>)];</span><br><span class="line">        id result = await(completable);</span><br><span class="line">        val = [result intValue];</span><br><span class="line">        [actor cancel];</span><br><span class="line">    &#125;);</span><br><span class="line">    </span><br><span class="line">    waitUntil(^(DoneCallback done) &#123;</span><br><span class="line">        dispatch_after(dispatch_time(DISPATCH_TIME_NOW, (int64_t)(<span class="number">0.5</span> * NSEC_PER_SEC)), dispatch_get_main_queue(), ^&#123;</span><br><span class="line">            XCTAssert(val == <span class="number">5</span>);</span><br><span class="line">            done();</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">it(@<span class="string">"error example"</span>, ^&#123;</span><br><span class="line">    COActor *actor = co_actor_onqueue(get_test_queue(), ^(COActorChan *channel) &#123;</span><br><span class="line">        for(COActorMessage *<span class="class"><span class="keyword">message</span> <span class="title">in</span> channel)</span>&#123;</span><br><span class="line">            <span class="class"><span class="keyword">message</span>.<span class="title">complete</span>(await(test_promise()));</span></span><br><span class="line"><span class="class">        &#125;</span></span><br><span class="line"><span class="class">    &#125;);</span></span><br><span class="line"><span class="class">    co_launch(^</span>&#123;</span><br><span class="line">        id value = await([actor sendMessage:@<span class="string">"test"</span>]);</span><br><span class="line">        NSError *error = co_getError();</span><br><span class="line">        XCTAssert(error.code == <span class="number">100</span>);</span><br><span class="line">    &#125;);</span><br><span class="line">    </span><br><span class="line">    waitUntilTimeout(<span class="number">3</span>, ^(DoneCallback done) &#123;</span><br><span class="line">        dispatch_after(dispatch_time(DISPATCH_TIME_NOW, (int64_t)(<span class="number">3</span> * NSEC_PER_SEC)), dispatch_get_main_queue(), ^&#123;</span><br><span class="line">            done();</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;);</span><br><span class="line">    </span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure></p><p><strong><strong>7. COTuple用法</strong></strong></p><p><figure class="highlight lisp"><table><tr><td class="code"><pre><span class="line">COPromise&lt;COTuple*&gt;*</span><br><span class="line">cotest_loadContentFromFile(<span class="name">NSString</span> *filePath)&#123;</span><br><span class="line">    return [COPromise promise:^(COPromiseFullfill  _Nonnull resolve, COPromiseReject  _Nonnull reject) &#123;</span><br><span class="line">        if ([[NSFileManager defaultManager] fileExistsAtPath:filePath]) &#123;</span><br><span class="line">            NSData *data = [[NSData alloc] initWithContentsOfFile<span class="symbol">:filePath</span>]<span class="comment">;</span></span><br><span class="line">            resolve(<span class="name">co_tuple</span>(<span class="name">filePath</span>, data, <span class="literal">nil</span>))<span class="comment">;</span></span><br><span class="line">        &#125;</span><br><span class="line">        else&#123;</span><br><span class="line">            NSError *error = [NSError errorWithDomain:@"fileNotFound" code:-1 userInfo:nil];</span><br><span class="line">            resolve(co_tuple(filePath, nil, error));</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">co_launch(^&#123;</span><br><span class="line">    NSString *tmpFilePath = <span class="literal">nil</span><span class="comment">;</span></span><br><span class="line">    NSData *data = nil;</span><br><span class="line">    NSError *error = <span class="literal">nil</span><span class="comment">;</span></span><br><span class="line">    co_unpack(<span class="name">&amp;tmpFilePath</span>, <span class="symbol">&amp;data</span>, <span class="symbol">&amp;error</span>) = await(<span class="name">cotest_loadContentFromFile</span>(<span class="name">filePath</span>))<span class="comment">;</span></span><br><span class="line">    XCTAssert([tmpFilePath isEqualToString<span class="symbol">:filePath</span>], @<span class="string">"file path is wrong"</span>)<span class="comment">;</span></span><br><span class="line">    XCTAssert(<span class="name">data</span>.length &gt; <span class="number">0</span>, @<span class="string">"data is wrong"</span>)<span class="comment">;</span></span><br><span class="line">    XCTAssert(<span class="name">error</span> == <span class="literal">nil</span>, @<span class="string">"error is wrong"</span>)<span class="comment">;</span></span><br><span class="line">&#125;)<span class="comment">;</span></span><br></pre></td></tr></table></figure></p><p><strong><strong>8. 协程内延迟</strong></strong></p><p>co_delay 可以暂停所在的协程但是协程所在的线程不会停止运行，线程内部的其他协程也不会暂停，co_delay只能在协程内部运行，如果在外部执行的话将会抛出异常。</p><p><figure class="highlight inform7"><table><tr><td class="code"><pre><span class="line">co_launch(^&#123;</span><br><span class="line">    NSTimeInterval begin = <span class="comment">[<span class="comment">[NSDate date]</span> timeIntervalSince1970]</span>;</span><br><span class="line">    co_delay(3);</span><br><span class="line">    realDuration = <span class="comment">[<span class="comment">[NSDate date]</span> timeIntervalSince1970]</span> - begin;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure></p><p><strong><strong>9. cokit简介</strong></strong></p><p>cokit 为常用的一些原生方法提供了一些常用的分类供我们使用，主要涉及到文件网络的IO</p><ul><li><strong><strong>NSDictionary+Coroutine.h</strong></strong>：主要用于将NSDictionary从文件中读取和写入文件。</li><li><strong><strong>NSArray+Coroutine.h</strong></strong>：主要用于读取NSPropertyList中的数组。</li><li><strong><strong>NSString+Coroutine.h</strong></strong>: 用于字符串从文件中读取和写入文件。</li><li><strong><strong>NSData+Coroutine.h</strong></strong>：主要用于将NSData数据从文件中读取和写入文件。</li><li><strong><strong>NSFileManager+Coroutine.h</strong></strong>w: 用于文件的操作</li><li><strong><strong>NSJSONSerialization+Coroutine.h</strong></strong>w: 用于JSON的序列化操作</li><li><strong><strong>NSKeyedArchiver+Coroutine.h</strong></strong>：用于归档类操作</li><li><strong><strong>NSURLConnection+Coroutine.h,NSURLSession+Coroutine.h</strong></strong>：用于网络请求操作</li><li><strong><strong>NSUserDefaults+Coroutine.h</strong></strong>：用于NSUserDefaults存储操作</li><li><strong><strong>UIImage+Coroutine.h</strong></strong>：用于获取图片操作</li></ul>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h4&gt;开源信息&lt;/h4&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&quot;https://github.com/alibaba/coobjc&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;coobjc 源码地址&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://
      
    
    </summary>
    
      <category term="iOS 开源库分析" scheme="http://yoursite.com/categories/iOS-%E5%BC%80%E6%BA%90%E5%BA%93%E5%88%86%E6%9E%90/"/>
    
    
      <category term="iOS 开源库分析" scheme="http://yoursite.com/tags/iOS-%E5%BC%80%E6%BA%90%E5%BA%93%E5%88%86%E6%9E%90/"/>
    
  </entry>
  
  <entry>
    <title>iOS开源库之fishhook 概述</title>
    <link href="http://yoursite.com/2019/12/20/fishhook-%E6%BA%90%E7%A0%81%E8%A7%A3%E6%9E%90/"/>
    <id>http://yoursite.com/2019/12/20/fishhook-源码解析/</id>
    <published>2019-12-20T09:00:21.000Z</published>
    <updated>2019-12-19T14:44:22.000Z</updated>
    
    <content type="html"><![CDATA[<h4>源码信息</h4><p><a href="https://github.com/facebook/fishhook" target="_blank" rel="noopener">源码地址</a></p><p>fishhook是真正意义上精炼的代码，不管是接口还是总体代码量都十分简单，但是背后的原理以及它所具备的功能却是十分强大的。最早听说fishhook的时候，只知道它是用于hook的一个开源库，以为和Aspect同种类型所以没把它列到开源代码解析的列表中，后面出于好奇看了下它居然是用于hook C函数，所以赶紧把它补上了,由于涉及底层的内容比较多，如果不是很清楚大家可以看后面推荐的文章，这里只做简单介绍。</p><p>那么上面叨叨了那么多，到底什么是fishhook呢,我们看下官方的解释：</p><blockquote><p>fishhook is a very simple library that enables dynamically rebinding symbols in Mach-O binaries running on iOS in the simulator and on device.</p></blockquote><p>上面的意思就是fishhook是一个用于动态修改 Mach-O 二进制文件里面symbols的一个开源库。fishhook 的强大之处在于它可以 hook 系统的静态 C 函数。我们接下来看下它是怎么做到的：</p><h4>原理介绍</h4><p><strong><strong>使用情景</strong></strong></p><p>我们先来看下fishhook对外暴露的内容，就两大类：一个rebinding结构体，用于封装对应的绑定信息，两个接口进行对C函数进行hook：</p><p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">rebinding</span> &#123;</span></span><br><span class="line">  <span class="keyword">const</span> <span class="keyword">char</span> *name;  <span class="comment">// 目标符号名</span></span><br><span class="line">  <span class="keyword">void</span> *replacement; <span class="comment">// 用于替换的函数地址</span></span><br><span class="line">  <span class="keyword">void</span> **replaced;   <span class="comment">// 用来存放原来的地址值</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">FISHHOOK_VISIBILITY</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">rebind_symbols</span><span class="params">(struct rebinding rebindings[], <span class="keyword">size_t</span> rebindings_nel)</span></span>;</span><br><span class="line"></span><br><span class="line">FISHHOOK_VISIBILITY</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">rebind_symbols_image</span><span class="params">(<span class="keyword">void</span> *header,</span></span></span><br><span class="line"><span class="function"><span class="params">                         <span class="keyword">intptr_t</span> slide,</span></span></span><br><span class="line"><span class="function"><span class="params">                         struct rebinding rebindings[],</span></span></span><br><span class="line"><span class="function"><span class="params">                         <span class="keyword">size_t</span> rebindings_nel)</span></span>;</span><br></pre></td></tr></table></figure></p><p>rebind_symbols和rebind_symbols_image都用于符号重绑定，区别在于前者操作的对象是所有镜像，后者操作的对象是某个指定的镜像</p><p>我们先来看一个官方的例子：</p><p><figure class="highlight arduino"><table><tr><td class="code"><pre><span class="line"><span class="meta">#import <span class="meta-string">&lt;dlfcn.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#import <span class="meta-string">&lt;UIKit/UIKit.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#import <span class="meta-string">"AppDelegate.h"</span></span></span><br><span class="line"><span class="comment">//1. 引入 fishhook.h</span></span><br><span class="line"><span class="meta">#import <span class="meta-string">"fishhook.h"</span></span></span><br><span class="line"> </span><br><span class="line"><span class="comment">//2. 声明与原函数签名相同的函数指针：</span></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="title">int</span> <span class="params">(*orig_close)</span><span class="params">(<span class="keyword">int</span>)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="title">int</span> <span class="params">(*orig_open)</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> *, <span class="keyword">int</span>, ...)</span></span>;</span><br><span class="line"> </span><br><span class="line"><span class="comment">//3. 实现用于替换的方法</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">my_close</span><span class="params">(<span class="keyword">int</span> fd)</span> </span>&#123;</span><br><span class="line">  <span class="built_in">printf</span>(<span class="string">"Calling real close(%d)\n"</span>, fd);</span><br><span class="line">  <span class="comment">//4.执行被hook的方法</span></span><br><span class="line">  <span class="keyword">return</span> orig_close(fd);</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="comment">//3. 实现用于替换的方法</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">my_open</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> *path, <span class="keyword">int</span> oflag, ...)</span> </span>&#123;</span><br><span class="line">  va_list ap = &#123;<span class="number">0</span>&#125;;</span><br><span class="line">  <span class="keyword">mode_t</span> mode = <span class="number">0</span>;</span><br><span class="line"> </span><br><span class="line">  <span class="keyword">if</span> ((oflag &amp; O_CREAT) != <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="comment">// mode only applies to O_CREAT</span></span><br><span class="line">    va_start(ap, oflag);</span><br><span class="line">    mode = va_arg(ap, <span class="keyword">int</span>);</span><br><span class="line">    va_end(ap);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"Calling real open('%s', %d, %d)\n"</span>, path, oflag, mode);</span><br><span class="line">    <span class="comment">//4.执行被hook的方法</span></span><br><span class="line">    <span class="keyword">return</span> orig_open(path, oflag, mode);</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"Calling real open('%s', %d)\n"</span>, path, oflag);</span><br><span class="line">    <span class="comment">//4.执行被hook的方法</span></span><br><span class="line">    <span class="keyword">return</span> orig_open(path, oflag, mode);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> * argv[])</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  @autoreleasepool &#123;</span><br><span class="line">    </span><br><span class="line">    rebind_symbols((struct rebinding[<span class="number">2</span>])&#123;&#123;<span class="string">"close"</span>, my_close, (<span class="keyword">void</span> *)&amp;orig_close&#125;<span class="comment">/*初始化rebinding 结构体*/</span>, &#123;<span class="string">"open"</span>, my_open, (<span class="keyword">void</span> *)&amp;orig_open<span class="comment">/*初始化rebinding 结构体*/</span>&#125;&#125;, <span class="number">2</span> <span class="comment">/*rebind个数*/</span>);</span><br><span class="line"> </span><br><span class="line">    <span class="comment">// Open our own binary and print out first 4 bytes (which is the same</span></span><br><span class="line">    <span class="comment">// for all Mach-O binaries on a given architecture)</span></span><br><span class="line">    <span class="keyword">int</span> fd = <span class="built_in">open</span>(argv[<span class="number">0</span>], O_RDONLY);</span><br><span class="line">    <span class="keyword">uint32_t</span> magic_number = <span class="number">0</span>;</span><br><span class="line">    <span class="built_in">read</span>(fd, &amp;magic_number, <span class="number">4</span>);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"Mach-O Magic Number: %x \n"</span>, magic_number);</span><br><span class="line">    <span class="built_in">close</span>(fd);</span><br><span class="line">    <span class="keyword">return</span> UIApplicationMain(argc, argv, nil, NSStringFromClass([AppDelegate class]));</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>经过上面替换后代码中的open，close都会替换为my_open，my_close。所以运行这段代码会输出：<figure class="highlight maxima"><table><tr><td class="code"><pre><span class="line">Calling <span class="built_in">real</span> open('/<span class="built_in">var</span>/mobile/Applications/161DA598-<span class="number">5B83</span>-41F5-8A44-675491AF6A2C/Test.app/Test', <span class="number">0</span>)</span><br><span class="line">Mach-O Magic Number: feedface </span><br><span class="line">Calling <span class="built_in">real</span> <span class="built_in">close</span>(<span class="number">3</span>)</span><br></pre></td></tr></table></figure></p><p><strong><strong>原理介绍</strong></strong></p><p>要完全弄懂fishhook需要知道Mach-O，dyld，编译过程，动态链接，静态链接的过程，这里如果展开讲的话会涉及很大的一块内容，所以这个阶段先不展开细讲，后面会开一个底层系列到时候再回过头来详细得看下fishhood的源码。</p><p>我们这里只简单介绍下fishhook的大致原理：</p><p>我们知道现在很多代码都是由多个模块构成的（比如我们的一个项目都是由多个.m/.h文件对构成，这里的每个.m/.h文件都相当于一个编译模块）这样有利于复用，并且由于这些模块是可以单独编译的，所以如果只是单个模块发生改变，只要单独编译改变的模块，然后在与其他模块进行链接就可以了。而从源代码到可执行文件需要通过对每个模块的源代码进行编译，生成一个个目标文件，然后再通过链接器将这些中间文件链接起来，形成一个最终的可行性文件Mach O,在单个模块中C函数在编译时就确定了函数指针的地址偏移量（Offset），这个偏移量在编译好的可执行文件中是固定的，而可执行文件每次被重新装载到内存中时被系统分配的起始地址是不断变化的。运行中的静态函数指针地址其实就等于上述 Offset + Mach0 文件在内存中的首地址.而在链接阶段主要负责符号决议和重定位。之所以需要链接阶段是由于存在模块间依赖，不论是依赖方法还是依赖变量，本质上都是模块间的符号引用。</p><p>但是C函数的指针地址是相对固定且不可修改的。所以内部非动态链接库中的C函数fishhook是hook不了的，它只能用于Mach-O外部的函数。对于这些动态链接库是不会被编译到Mach O 文件的，而是在动态链接时才去重新绑定。</p><p>苹果采用了PIC（Position-independent code）技术来完成代码的动态加载特性：编译时在 Mach-O 文件 _DATA 段的符号表中为每一个被引用的系统 C 函数建立一个指针（8字节的数据，放的全是0），这个指针用于动态绑定时重定位到共享库中的函数实现。在运行时当系统 C 函数被第一次调用时会动态绑定一次，然后将 Mach-O 中的 _DATA 段符号表中对应的指针，指向其在共享库中的实际内存地址，也就是会将我们需要替换的C函数在这个共享库中实际的内存地址保存到 Mach-O 中的 _DATA 段符号表中。经过上面的一串操作我们在调用这个方法的时候都会读取_DATA中的这个地址。我们上面提到内部非动态链接库中的代码fishhook不能hook是因为这些地址值是存放在TEXT session中，而这部分数据是只读的。而动态链接库里面的最终符号地址存在可读写的 __DATA segment 的某个 section 中，fishhook 的实现原理就是通过修改这些 section 内容，进而实现符号的 rebind。</p><p>嗯，大致就是这样，讲得太细节容易把自己都陷进去，这样刚好能够理解。最近正好正在看底层的原理，打算放在后面整体梳理清楚后，再补一篇。赶紧逃。^ V ^</p><h4>较好的文章</h4><ul><li><a href="https://draveness.me/fishhook" target="_blank" rel="noopener">动态修改 C 语言函数的实现</a></li><li><a href="https://juejin.im/post/5c810294f265da2db91297f1" target="_blank" rel="noopener">fishhook使用场景&amp;源码分析</a></li><li><a href="https://juejin.im/post/5c7b43976fb9a04a05406312" target="_blank" rel="noopener">fishhook的实现原理浅析</a></li><li><a href="https://juejin.im/post/5c727262e51d457139116208" target="_blank" rel="noopener">dyld背后的故事&amp;源码分析</a></li><li><a href="https://juejin.im/post/5c616552f265da2dd53fa4e7#heading-3" target="_blank" rel="noopener">非越狱下 iOS代码注入&amp;HOOK微信登录</a></li><li><a href="https://juejin.im/entry/5a1fceddf265da43310d9985" target="_blank" rel="noopener">iOS 界的毒瘤：Method Swizzle</a></li><li><a href="https://juejin.im/post/5c67e7efe51d45164c75993b" target="_blank" rel="noopener">MachO 文件结构详解</a></li><li><a href="https://www.googleplus.party/2017/10/26/fishhook%E7%9A%84%E5%9F%BA%E6%9C%AC%E4%BD%BF%E7%94%A8/" target="_blank" rel="noopener">fishhook的基本使用，fishhook可以勾住系统函数</a></li><li><a href="https://zhangbuhuai.com/post/macho-structure.html" target="_blank" rel="noopener">Mach-O 简单分析</a></li><li><a href="https://zhangbuhuai.com/post/macho-static-link.html" target="_blank" rel="noopener">Mach-O 与静态链接</a></li><li><a href="https://zhangbuhuai.com/post/macho-dynamic-link.html" target="_blank" rel="noopener">Mach-O 与动态链接</a></li><li><a href="https://zhangbuhuai.com/post/fishhook.html" target="_blank" rel="noopener">分析 fishhook</a></li></ul>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h4&gt;源码信息&lt;/h4&gt;
&lt;p&gt;&lt;a href=&quot;https://github.com/facebook/fishhook&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;源码地址&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;fishhook是真正意义上精炼的代码，不管是接口还是总体
      
    
    </summary>
    
      <category term="iOS 开源库分析" scheme="http://yoursite.com/categories/iOS-%E5%BC%80%E6%BA%90%E5%BA%93%E5%88%86%E6%9E%90/"/>
    
    
      <category term="iOS 开源库分析" scheme="http://yoursite.com/tags/iOS-%E5%BC%80%E6%BA%90%E5%BA%93%E5%88%86%E6%9E%90/"/>
    
  </entry>
  
</feed>
